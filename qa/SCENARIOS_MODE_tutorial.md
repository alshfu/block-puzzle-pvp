# BlockDuel — Каталог сценариев режима «Обучение (5 шагов)»

**Ровно 1001 сценариев** режима «Обучение (5 шагов)» («1000 и 1»).
Доска: 9×9. Маршрут: `/tutorial`. Сгенерировано детерминированно
из `tools/gen_scenarios.py` (ось «режим») — НЕ редактировать вручную.

- **ID** `MODE-TUTORIAL-0001…MODE-TUTORIAL-1001` стабильны между прогонами.
- **[C]** — критический smoke-срез (каждый ~7-й).
- «Механика режима» — уникальная логика; хвост «Property-фаззинг» —
  осмысленная матрица (seed × конфиг × инвариант) для добора до 1001 в
  режимах малой игровой поверхности (это valid property-тесты, не филлер).

## Покрытие по областям

- Запуск и жизненный цикл: 6
- Рендер и темы: 35
- Механика режима: 69
- Прогрессия и рекорды: 4
- Настройки в режиме: 15
- Ошибки и устойчивость: 4
- Property-фаззинг (seed×конфиг): 868

## Сценарии

1. **MODE-TUTORIAL-0001** — [C] [Запуск и жизненный цикл] Запуск «Обучение (5 шагов)» из меню по маршруту /tutorial — экран рисуется без ошибок
2. **MODE-TUTORIAL-0002** — [Рендер и темы] «Обучение (5 шагов)»: поверхность «доска» в теме «neutral» — цвета/контраст по токенам
3. **MODE-TUTORIAL-0003** — [Механика режима] Шаг 1 «постановка фигуры»: инструкция понятна, действие принимается
4. **MODE-TUTORIAL-0004** — [Прогрессия и рекорды] «Обучение (5 шагов)»: завершение партии пишет результат в статистику режима
5. **MODE-TUTORIAL-0005** — [Настройки в режиме] «Обучение (5 шагов)»: настройка «Звук (soundOn)» применяется в режиме и переживает перезапуск
6. **MODE-TUTORIAL-0006** — [Ошибки и устойчивость] «Обучение (5 шагов)»: повреждённая сохранёнка → graceful fallback, без краша
7. **MODE-TUTORIAL-0007** — [Запуск и жизненный цикл] Возврат из «Обучение (5 шагов)» в меню — стек навигации корректен, без утечек
8. **MODE-TUTORIAL-0008** — [C] [Рендер и темы] «Обучение (5 шагов)»: поверхность «доска» в теме «candy» — цвета/контраст по токенам
9. **MODE-TUTORIAL-0009** — [Механика режима] Шаг 1 «постановка фигуры»: подсветка цели корректна
10. **MODE-TUTORIAL-0010** — [Прогрессия и рекорды] «Обучение (5 шагов)»: вклад в composite-score (сводный рейтинг) учитывается
11. **MODE-TUTORIAL-0011** — [Настройки в режиме] «Обучение (5 шагов)»: настройка «Музыка (musicOn)» применяется в режиме и переживает перезапуск
12. **MODE-TUTORIAL-0012** — [Ошибки и устойчивость] «Обучение (5 шагов)»: сворачивание в момент хода → без рассинхрона состояния
13. **MODE-TUTORIAL-0013** — [Запуск и жизненный цикл] Холодный старт «Обучение (5 шагов)» в теме «neutral» — без белой вспышки
14. **MODE-TUTORIAL-0014** — [Рендер и темы] «Обучение (5 шагов)»: поверхность «доска» в теме «night» — цвета/контраст по токенам
15. **MODE-TUTORIAL-0015** — [C] [Механика режима] Шаг 1 «постановка фигуры»: нельзя пропустить обязательное действие
16. **MODE-TUTORIAL-0016** — [Прогрессия и рекорды] «Обучение (5 шагов)»: XP по исходу начисляется один раз (ничья ×0.5, поражение 0)
17. **MODE-TUTORIAL-0017** — [Настройки в режиме] «Обучение (5 шагов)»: настройка «Громкость SFX» применяется в режиме и переживает перезапуск
18. **MODE-TUTORIAL-0018** — [Ошибки и устойчивость] «Обучение (5 шагов)»: быстрый повторный ввод не даёт двойного хода
19. **MODE-TUTORIAL-0019** — [Запуск и жизненный цикл] Холодный старт «Обучение (5 шагов)» в теме «candy» — без белой вспышки
20. **MODE-TUTORIAL-0020** — [Рендер и темы] «Обучение (5 шагов)»: «доска» при reduceMotion=on — статичный декор
21. **MODE-TUTORIAL-0021** — [Механика режима] Шаг 1 «постановка фигуры»: кнопка «Далее» активна только после выполнения
22. **MODE-TUTORIAL-0022** — [C] [Прогрессия и рекорды] «Обучение (5 шагов)»: личный рекорд обновляется только при улучшении
23. **MODE-TUTORIAL-0023** — [Настройки в режиме] «Обучение (5 шагов)»: настройка «Громкость музыки» применяется в режиме и переживает перезапуск
24. **MODE-TUTORIAL-0024** — [Ошибки и устойчивость] «Обучение (5 шагов)»: возврат/пауза в критический момент не ломает партию
25. **MODE-TUTORIAL-0025** — [Запуск и жизненный цикл] Холодный старт «Обучение (5 шагов)» в теме «night» — без белой вспышки
26. **MODE-TUTORIAL-0026** — [Рендер и темы] «Обучение (5 шагов)»: «доска» на узком окне — нет overflow
27. **MODE-TUTORIAL-0027** — [Механика режима] Шаг 1 «постановка фигуры»: кнопка «Назад» возвращает к предыдущему шагу без потери прогресса
28. **MODE-TUTORIAL-0028** — [Настройки в режиме] «Обучение (5 шагов)»: настройка «Вибрация» применяется в режиме и переживает перезапуск
29. **MODE-TUTORIAL-0029** — [C] [Запуск и жизненный цикл] Двойной быстрый тап по старту «Обучение (5 шагов)» не создаёт две партии
30. **MODE-TUTORIAL-0030** — [Рендер и темы] «Обучение (5 шагов)»: «доска» на широком окне — адаптивная боковая раскладка
31. **MODE-TUTORIAL-0031** — [Механика режима] Шаг 1 «постановка фигуры»: прогресс-индикатор шага отражает текущую позицию
32. **MODE-TUTORIAL-0032** — [Настройки в режиме] «Обучение (5 шагов)»: настройка «Уменьшить движение (reduceMotion)» применяется в режиме и переживает перезапуск
33. **MODE-TUTORIAL-0033** — [Рендер и темы] «Обучение (5 шагов)»: «доска» с нейтральными клетками (a11y) — не зависит от цвета владельца
34. **MODE-TUTORIAL-0034** — [Механика режима] Шаг 1 «постановка фигуры»: неверное действие не ломает шаг, подсказка повторяется
35. **MODE-TUTORIAL-0035** — [Настройки в режиме] «Обучение (5 шагов)»: настройка «Конфетти» применяется в режиме и переживает перезапуск
36. **MODE-TUTORIAL-0036** — [C] [Рендер и темы] «Обучение (5 шагов)»: поверхность «HUD (счёт/таймер)» в теме «neutral» — цвета/контраст по токенам
37. **MODE-TUTORIAL-0037** — [Механика режима] Шаг 1 «постановка фигуры» через тап: ввод распознаётся, действие засчитано
38. **MODE-TUTORIAL-0038** — [Настройки в режиме] «Обучение (5 шагов)»: настройка «Маскоты» применяется в режиме и переживает перезапуск
39. **MODE-TUTORIAL-0039** — [Рендер и темы] «Обучение (5 шагов)»: поверхность «HUD (счёт/таймер)» в теме «candy» — цвета/контраст по токенам
40. **MODE-TUTORIAL-0040** — [Механика режима] Шаг 1 «постановка фигуры» через drag-and-drop: ввод распознаётся, действие засчитано
41. **MODE-TUTORIAL-0041** — [Настройки в режиме] «Обучение (5 шагов)»: настройка «Призрак фигуры (ghost)» применяется в режиме и переживает перезапуск
42. **MODE-TUTORIAL-0042** — [Рендер и темы] «Обучение (5 шагов)»: поверхность «HUD (счёт/таймер)» в теме «night» — цвета/контраст по токенам
43. **MODE-TUTORIAL-0043** — [C] [Механика режима] Шаг 1 «постановка фигуры» через клавиатура: ввод распознаётся, действие засчитано
44. **MODE-TUTORIAL-0044** — [Настройки в режиме] «Обучение (5 шагов)»: настройка «Задержка бота» применяется в режиме и переживает перезапуск
45. **MODE-TUTORIAL-0045** — [Рендер и темы] «Обучение (5 шагов)»: «HUD (счёт/таймер)» при reduceMotion=on — статичный декор
46. **MODE-TUTORIAL-0046** — [Механика режима] Шаг 1 «постановка фигуры» в теме «neutral»: контраст подсказок достаточен
47. **MODE-TUTORIAL-0047** — [Настройки в режиме] «Обучение (5 шагов)»: настройка «Тема по умолчанию» применяется в режиме и переживает перезапуск
48. **MODE-TUTORIAL-0048** — [Рендер и темы] «Обучение (5 шагов)»: «HUD (счёт/таймер)» на узком окне — нет overflow
49. **MODE-TUTORIAL-0049** — [Механика режима] Шаг 1 «постановка фигуры» в теме «candy»: контраст подсказок достаточен
50. **MODE-TUTORIAL-0050** — [C] [Настройки в режиме] «Обучение (5 шагов)»: настройка «Правила по умолчанию (handSize/rotation/flip)» применяется в режиме и переживает перезапуск
51. **MODE-TUTORIAL-0051** — [Рендер и темы] «Обучение (5 шагов)»: «HUD (счёт/таймер)» на широком окне — адаптивная боковая раскладка
52. **MODE-TUTORIAL-0052** — [Механика режима] Шаг 1 «постановка фигуры» в теме «night»: контраст подсказок достаточен
53. **MODE-TUTORIAL-0053** — [Настройки в режиме] «Обучение (5 шагов)»: настройка «Пресет блица» применяется в режиме и переживает перезапуск
54. **MODE-TUTORIAL-0054** — [Рендер и темы] «Обучение (5 шагов)»: «HUD (счёт/таймер)» с нейтральными клетками (a11y) — не зависит от цвета владельца
55. **MODE-TUTORIAL-0055** — [Механика режима] Шаг 2 «поворот/отражение»: инструкция понятна, действие принимается
56. **MODE-TUTORIAL-0056** — [Настройки в режиме] «Обучение (5 шагов)»: настройка «Нейтральные клетки (accessibility)» применяется в режиме и переживает перезапуск
57. **MODE-TUTORIAL-0057** — [C] [Рендер и темы] «Обучение (5 шагов)»: поверхность «панель руки» в теме «neutral» — цвета/контраст по токенам
58. **MODE-TUTORIAL-0058** — [Механика режима] Шаг 2 «поворот/отражение»: подсветка цели корректна
59. **MODE-TUTORIAL-0059** — [Настройки в режиме] «Обучение (5 шагов)»: настройка «Язык интерфейса» применяется в режиме и переживает перезапуск
60. **MODE-TUTORIAL-0060** — [Рендер и темы] «Обучение (5 шагов)»: поверхность «панель руки» в теме «candy» — цвета/контраст по токенам
61. **MODE-TUTORIAL-0061** — [Механика режима] Шаг 2 «поворот/отражение»: нельзя пропустить обязательное действие
62. **MODE-TUTORIAL-0062** — [Рендер и темы] «Обучение (5 шагов)»: поверхность «панель руки» в теме «night» — цвета/контраст по токенам
63. **MODE-TUTORIAL-0063** — [Механика режима] Шаг 2 «поворот/отражение»: кнопка «Далее» активна только после выполнения
64. **MODE-TUTORIAL-0064** — [C] [Рендер и темы] «Обучение (5 шагов)»: «панель руки» при reduceMotion=on — статичный декор
65. **MODE-TUTORIAL-0065** — [Механика режима] Шаг 2 «поворот/отражение»: кнопка «Назад» возвращает к предыдущему шагу без потери прогресса
66. **MODE-TUTORIAL-0066** — [Рендер и темы] «Обучение (5 шагов)»: «панель руки» на узком окне — нет overflow
67. **MODE-TUTORIAL-0067** — [Механика режима] Шаг 2 «поворот/отражение»: прогресс-индикатор шага отражает текущую позицию
68. **MODE-TUTORIAL-0068** — [Рендер и темы] «Обучение (5 шагов)»: «панель руки» на широком окне — адаптивная боковая раскладка
69. **MODE-TUTORIAL-0069** — [Механика режима] Шаг 2 «поворот/отражение»: неверное действие не ломает шаг, подсказка повторяется
70. **MODE-TUTORIAL-0070** — [Рендер и темы] «Обучение (5 шагов)»: «панель руки» с нейтральными клетками (a11y) — не зависит от цвета владельца
71. **MODE-TUTORIAL-0071** — [C] [Механика режима] Шаг 2 «поворот/отражение» через тап: ввод распознаётся, действие засчитано
72. **MODE-TUTORIAL-0072** — [Рендер и темы] «Обучение (5 шагов)»: поверхность «экран результата» в теме «neutral» — цвета/контраст по токенам
73. **MODE-TUTORIAL-0073** — [Механика режима] Шаг 2 «поворот/отражение» через drag-and-drop: ввод распознаётся, действие засчитано
74. **MODE-TUTORIAL-0074** — [Рендер и темы] «Обучение (5 шагов)»: поверхность «экран результата» в теме «candy» — цвета/контраст по токенам
75. **MODE-TUTORIAL-0075** — [Механика режима] Шаг 2 «поворот/отражение» через клавиатура: ввод распознаётся, действие засчитано
76. **MODE-TUTORIAL-0076** — [Рендер и темы] «Обучение (5 шагов)»: поверхность «экран результата» в теме «night» — цвета/контраст по токенам
77. **MODE-TUTORIAL-0077** — [Механика режима] Шаг 2 «поворот/отражение» в теме «neutral»: контраст подсказок достаточен
78. **MODE-TUTORIAL-0078** — [C] [Рендер и темы] «Обучение (5 шагов)»: «экран результата» при reduceMotion=on — статичный декор
79. **MODE-TUTORIAL-0079** — [Механика режима] Шаг 2 «поворот/отражение» в теме «candy»: контраст подсказок достаточен
80. **MODE-TUTORIAL-0080** — [Рендер и темы] «Обучение (5 шагов)»: «экран результата» на узком окне — нет overflow
81. **MODE-TUTORIAL-0081** — [Механика режима] Шаг 2 «поворот/отражение» в теме «night»: контраст подсказок достаточен
82. **MODE-TUTORIAL-0082** — [Рендер и темы] «Обучение (5 шагов)»: «экран результата» на широком окне — адаптивная боковая раскладка
83. **MODE-TUTORIAL-0083** — [Механика режима] Шаг 3 «очистка линии»: инструкция понятна, действие принимается
84. **MODE-TUTORIAL-0084** — [Рендер и темы] «Обучение (5 шагов)»: «экран результата» с нейтральными клетками (a11y) — не зависит от цвета владельца
85. **MODE-TUTORIAL-0085** — [C] [Механика режима] Шаг 3 «очистка линии»: подсветка цели корректна
86. **MODE-TUTORIAL-0086** — [Рендер и темы] «Обучение (5 шагов)»: поверхность «пауза/меню выхода» в теме «neutral» — цвета/контраст по токенам
87. **MODE-TUTORIAL-0087** — [Механика режима] Шаг 3 «очистка линии»: нельзя пропустить обязательное действие
88. **MODE-TUTORIAL-0088** — [Рендер и темы] «Обучение (5 шагов)»: поверхность «пауза/меню выхода» в теме «candy» — цвета/контраст по токенам
89. **MODE-TUTORIAL-0089** — [Механика режима] Шаг 3 «очистка линии»: кнопка «Далее» активна только после выполнения
90. **MODE-TUTORIAL-0090** — [Рендер и темы] «Обучение (5 шагов)»: поверхность «пауза/меню выхода» в теме «night» — цвета/контраст по токенам
91. **MODE-TUTORIAL-0091** — [Механика режима] Шаг 3 «очистка линии»: кнопка «Назад» возвращает к предыдущему шагу без потери прогресса
92. **MODE-TUTORIAL-0092** — [C] [Рендер и темы] «Обучение (5 шагов)»: «пауза/меню выхода» при reduceMotion=on — статичный декор
93. **MODE-TUTORIAL-0093** — [Механика режима] Шаг 3 «очистка линии»: прогресс-индикатор шага отражает текущую позицию
94. **MODE-TUTORIAL-0094** — [Рендер и темы] «Обучение (5 шагов)»: «пауза/меню выхода» на узком окне — нет overflow
95. **MODE-TUTORIAL-0095** — [Механика режима] Шаг 3 «очистка линии»: неверное действие не ломает шаг, подсказка повторяется
96. **MODE-TUTORIAL-0096** — [Рендер и темы] «Обучение (5 шагов)»: «пауза/меню выхода» на широком окне — адаптивная боковая раскладка
97. **MODE-TUTORIAL-0097** — [Механика режима] Шаг 3 «очистка линии» через тап: ввод распознаётся, действие засчитано
98. **MODE-TUTORIAL-0098** — [Рендер и темы] «Обучение (5 шагов)»: «пауза/меню выхода» с нейтральными клетками (a11y) — не зависит от цвета владельца
99. **MODE-TUTORIAL-0099** — [C] [Механика режима] Шаг 3 «очистка линии» через drag-and-drop: ввод распознаётся, действие засчитано
100. **MODE-TUTORIAL-0100** — [Механика режима] Шаг 3 «очистка линии» через клавиатура: ввод распознаётся, действие засчитано
101. **MODE-TUTORIAL-0101** — [Механика режима] Шаг 3 «очистка линии» в теме «neutral»: контраст подсказок достаточен
102. **MODE-TUTORIAL-0102** — [Механика режима] Шаг 3 «очистка линии» в теме «candy»: контраст подсказок достаточен
103. **MODE-TUTORIAL-0103** — [Механика режима] Шаг 3 «очистка линии» в теме «night»: контраст подсказок достаточен
104. **MODE-TUTORIAL-0104** — [Механика режима] Шаг 4 «комбо и множитель»: инструкция понятна, действие принимается
105. **MODE-TUTORIAL-0105** — [Механика режима] Шаг 4 «комбо и множитель»: подсветка цели корректна
106. **MODE-TUTORIAL-0106** — [C] [Механика режима] Шаг 4 «комбо и множитель»: нельзя пропустить обязательное действие
107. **MODE-TUTORIAL-0107** — [Механика режима] Шаг 4 «комбо и множитель»: кнопка «Далее» активна только после выполнения
108. **MODE-TUTORIAL-0108** — [Механика режима] Шаг 4 «комбо и множитель»: кнопка «Назад» возвращает к предыдущему шагу без потери прогресса
109. **MODE-TUTORIAL-0109** — [Механика режима] Шаг 4 «комбо и множитель»: прогресс-индикатор шага отражает текущую позицию
110. **MODE-TUTORIAL-0110** — [Механика режима] Шаг 4 «комбо и множитель»: неверное действие не ломает шаг, подсказка повторяется
111. **MODE-TUTORIAL-0111** — [Механика режима] Шаг 4 «комбо и множитель» через тап: ввод распознаётся, действие засчитано
112. **MODE-TUTORIAL-0112** — [Механика режима] Шаг 4 «комбо и множитель» через drag-and-drop: ввод распознаётся, действие засчитано
113. **MODE-TUTORIAL-0113** — [C] [Механика режима] Шаг 4 «комбо и множитель» через клавиатура: ввод распознаётся, действие засчитано
114. **MODE-TUTORIAL-0114** — [Механика режима] Шаг 4 «комбо и множитель» в теме «neutral»: контраст подсказок достаточен
115. **MODE-TUTORIAL-0115** — [Механика режима] Шаг 4 «комбо и множитель» в теме «candy»: контраст подсказок достаточен
116. **MODE-TUTORIAL-0116** — [Механика режима] Шаг 4 «комбо и множитель» в теме «night»: контраст подсказок достаточен
117. **MODE-TUTORIAL-0117** — [Механика режима] Шаг 5 «завершение и награда»: инструкция понятна, действие принимается
118. **MODE-TUTORIAL-0118** — [Механика режима] Шаг 5 «завершение и награда»: подсветка цели корректна
119. **MODE-TUTORIAL-0119** — [Механика режима] Шаг 5 «завершение и награда»: нельзя пропустить обязательное действие
120. **MODE-TUTORIAL-0120** — [C] [Механика режима] Шаг 5 «завершение и награда»: кнопка «Далее» активна только после выполнения
121. **MODE-TUTORIAL-0121** — [Механика режима] Шаг 5 «завершение и награда»: кнопка «Назад» возвращает к предыдущему шагу без потери прогресса
122. **MODE-TUTORIAL-0122** — [Механика режима] Шаг 5 «завершение и награда»: прогресс-индикатор шага отражает текущую позицию
123. **MODE-TUTORIAL-0123** — [Механика режима] Шаг 5 «завершение и награда»: неверное действие не ломает шаг, подсказка повторяется
124. **MODE-TUTORIAL-0124** — [Механика режима] Шаг 5 «завершение и награда» через тап: ввод распознаётся, действие засчитано
125. **MODE-TUTORIAL-0125** — [Механика режима] Шаг 5 «завершение и награда» через drag-and-drop: ввод распознаётся, действие засчитано
126. **MODE-TUTORIAL-0126** — [Механика режима] Шаг 5 «завершение и награда» через клавиатура: ввод распознаётся, действие засчитано
127. **MODE-TUTORIAL-0127** — [C] [Механика режима] Шаг 5 «завершение и награда» в теме «neutral»: контраст подсказок достаточен
128. **MODE-TUTORIAL-0128** — [Механика режима] Шаг 5 «завершение и награда» в теме «candy»: контраст подсказок достаточен
129. **MODE-TUTORIAL-0129** — [Механика режима] Шаг 5 «завершение и награда» в теме «night»: контраст подсказок достаточен
130. **MODE-TUTORIAL-0130** — [Механика режима] Завершение обучения: награда +50 🪙 начисляется один раз
131. **MODE-TUTORIAL-0131** — [Механика режима] Повторное прохождение не начисляет награду повторно
132. **MODE-TUTORIAL-0132** — [Механика режима] Выход посреди обучения не выдаёт награду
133. **MODE-TUTORIAL-0133** — [Механика режима] Обучение доступно из меню и из первого запуска
134. **MODE-TUTORIAL-0134** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · тема=neutral [tutorial]: счёт неотрицателен на всём протяжении
135. **MODE-TUTORIAL-0135** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · тема=neutral [tutorial]: ни одного исключения/краша за партию
136. **MODE-TUTORIAL-0136** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · тема=neutral [tutorial]: resume в середине — бит-в-бит то же продолжение
137. **MODE-TUTORIAL-0137** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · тема=neutral [tutorial]: число ходов конечно (партия сходится)
138. **MODE-TUTORIAL-0138** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · тема=neutral [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
139. **MODE-TUTORIAL-0139** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · тема=neutral [tutorial]: ни одной невалидной постановки не принято
140. **MODE-TUTORIAL-0140** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · тема=neutral [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
141. **MODE-TUTORIAL-0141** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · тема=candy [tutorial]: счёт неотрицателен на всём протяжении
142. **MODE-TUTORIAL-0142** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · тема=candy [tutorial]: ни одного исключения/краша за партию
143. **MODE-TUTORIAL-0143** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · тема=candy [tutorial]: resume в середине — бит-в-бит то же продолжение
144. **MODE-TUTORIAL-0144** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · тема=candy [tutorial]: число ходов конечно (партия сходится)
145. **MODE-TUTORIAL-0145** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · тема=candy [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
146. **MODE-TUTORIAL-0146** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · тема=candy [tutorial]: ни одной невалидной постановки не принято
147. **MODE-TUTORIAL-0147** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · тема=candy [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
148. **MODE-TUTORIAL-0148** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · тема=night [tutorial]: счёт неотрицателен на всём протяжении
149. **MODE-TUTORIAL-0149** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · тема=night [tutorial]: ни одного исключения/краша за партию
150. **MODE-TUTORIAL-0150** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · тема=night [tutorial]: resume в середине — бит-в-бит то же продолжение
151. **MODE-TUTORIAL-0151** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · тема=night [tutorial]: число ходов конечно (партия сходится)
152. **MODE-TUTORIAL-0152** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · тема=night [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
153. **MODE-TUTORIAL-0153** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · тема=night [tutorial]: ни одной невалидной постановки не принято
154. **MODE-TUTORIAL-0154** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · тема=night [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
155. **MODE-TUTORIAL-0155** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · reduceMotion=on [tutorial]: счёт неотрицателен на всём протяжении
156. **MODE-TUTORIAL-0156** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · reduceMotion=on [tutorial]: ни одного исключения/краша за партию
157. **MODE-TUTORIAL-0157** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · reduceMotion=on [tutorial]: resume в середине — бит-в-бит то же продолжение
158. **MODE-TUTORIAL-0158** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · reduceMotion=on [tutorial]: число ходов конечно (партия сходится)
159. **MODE-TUTORIAL-0159** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · reduceMotion=on [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
160. **MODE-TUTORIAL-0160** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · reduceMotion=on [tutorial]: ни одной невалидной постановки не принято
161. **MODE-TUTORIAL-0161** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · reduceMotion=on [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
162. **MODE-TUTORIAL-0162** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · узкое окно [tutorial]: счёт неотрицателен на всём протяжении
163. **MODE-TUTORIAL-0163** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · узкое окно [tutorial]: ни одного исключения/краша за партию
164. **MODE-TUTORIAL-0164** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · узкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
165. **MODE-TUTORIAL-0165** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · узкое окно [tutorial]: число ходов конечно (партия сходится)
166. **MODE-TUTORIAL-0166** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · узкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
167. **MODE-TUTORIAL-0167** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · узкое окно [tutorial]: ни одной невалидной постановки не принято
168. **MODE-TUTORIAL-0168** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · узкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
169. **MODE-TUTORIAL-0169** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · широкое окно [tutorial]: счёт неотрицателен на всём протяжении
170. **MODE-TUTORIAL-0170** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · широкое окно [tutorial]: ни одного исключения/краша за партию
171. **MODE-TUTORIAL-0171** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · широкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
172. **MODE-TUTORIAL-0172** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · широкое окно [tutorial]: число ходов конечно (партия сходится)
173. **MODE-TUTORIAL-0173** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · широкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
174. **MODE-TUTORIAL-0174** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · широкое окно [tutorial]: ни одной невалидной постановки не принято
175. **MODE-TUTORIAL-0175** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · широкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
176. **MODE-TUTORIAL-0176** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · тема=neutral [tutorial]: счёт неотрицателен на всём протяжении
177. **MODE-TUTORIAL-0177** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · тема=neutral [tutorial]: ни одного исключения/краша за партию
178. **MODE-TUTORIAL-0178** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · тема=neutral [tutorial]: resume в середине — бит-в-бит то же продолжение
179. **MODE-TUTORIAL-0179** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · тема=neutral [tutorial]: число ходов конечно (партия сходится)
180. **MODE-TUTORIAL-0180** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · тема=neutral [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
181. **MODE-TUTORIAL-0181** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · тема=neutral [tutorial]: ни одной невалидной постановки не принято
182. **MODE-TUTORIAL-0182** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · тема=neutral [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
183. **MODE-TUTORIAL-0183** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · тема=candy [tutorial]: счёт неотрицателен на всём протяжении
184. **MODE-TUTORIAL-0184** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · тема=candy [tutorial]: ни одного исключения/краша за партию
185. **MODE-TUTORIAL-0185** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · тема=candy [tutorial]: resume в середине — бит-в-бит то же продолжение
186. **MODE-TUTORIAL-0186** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · тема=candy [tutorial]: число ходов конечно (партия сходится)
187. **MODE-TUTORIAL-0187** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · тема=candy [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
188. **MODE-TUTORIAL-0188** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · тема=candy [tutorial]: ни одной невалидной постановки не принято
189. **MODE-TUTORIAL-0189** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · тема=candy [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
190. **MODE-TUTORIAL-0190** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · тема=night [tutorial]: счёт неотрицателен на всём протяжении
191. **MODE-TUTORIAL-0191** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · тема=night [tutorial]: ни одного исключения/краша за партию
192. **MODE-TUTORIAL-0192** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · тема=night [tutorial]: resume в середине — бит-в-бит то же продолжение
193. **MODE-TUTORIAL-0193** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · тема=night [tutorial]: число ходов конечно (партия сходится)
194. **MODE-TUTORIAL-0194** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · тема=night [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
195. **MODE-TUTORIAL-0195** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · тема=night [tutorial]: ни одной невалидной постановки не принято
196. **MODE-TUTORIAL-0196** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · тема=night [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
197. **MODE-TUTORIAL-0197** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · reduceMotion=on [tutorial]: счёт неотрицателен на всём протяжении
198. **MODE-TUTORIAL-0198** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · reduceMotion=on [tutorial]: ни одного исключения/краша за партию
199. **MODE-TUTORIAL-0199** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · reduceMotion=on [tutorial]: resume в середине — бит-в-бит то же продолжение
200. **MODE-TUTORIAL-0200** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · reduceMotion=on [tutorial]: число ходов конечно (партия сходится)
201. **MODE-TUTORIAL-0201** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · reduceMotion=on [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
202. **MODE-TUTORIAL-0202** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · reduceMotion=on [tutorial]: ни одной невалидной постановки не принято
203. **MODE-TUTORIAL-0203** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · reduceMotion=on [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
204. **MODE-TUTORIAL-0204** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · узкое окно [tutorial]: счёт неотрицателен на всём протяжении
205. **MODE-TUTORIAL-0205** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · узкое окно [tutorial]: ни одного исключения/краша за партию
206. **MODE-TUTORIAL-0206** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · узкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
207. **MODE-TUTORIAL-0207** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · узкое окно [tutorial]: число ходов конечно (партия сходится)
208. **MODE-TUTORIAL-0208** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · узкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
209. **MODE-TUTORIAL-0209** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · узкое окно [tutorial]: ни одной невалидной постановки не принято
210. **MODE-TUTORIAL-0210** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · узкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
211. **MODE-TUTORIAL-0211** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · широкое окно [tutorial]: счёт неотрицателен на всём протяжении
212. **MODE-TUTORIAL-0212** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · широкое окно [tutorial]: ни одного исключения/краша за партию
213. **MODE-TUTORIAL-0213** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · широкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
214. **MODE-TUTORIAL-0214** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · широкое окно [tutorial]: число ходов конечно (партия сходится)
215. **MODE-TUTORIAL-0215** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · широкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
216. **MODE-TUTORIAL-0216** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · широкое окно [tutorial]: ни одной невалидной постановки не принято
217. **MODE-TUTORIAL-0217** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · широкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
218. **MODE-TUTORIAL-0218** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · тема=neutral [tutorial]: счёт неотрицателен на всём протяжении
219. **MODE-TUTORIAL-0219** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · тема=neutral [tutorial]: ни одного исключения/краша за партию
220. **MODE-TUTORIAL-0220** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · тема=neutral [tutorial]: resume в середине — бит-в-бит то же продолжение
221. **MODE-TUTORIAL-0221** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · тема=neutral [tutorial]: число ходов конечно (партия сходится)
222. **MODE-TUTORIAL-0222** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · тема=neutral [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
223. **MODE-TUTORIAL-0223** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · тема=neutral [tutorial]: ни одной невалидной постановки не принято
224. **MODE-TUTORIAL-0224** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · тема=neutral [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
225. **MODE-TUTORIAL-0225** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · тема=candy [tutorial]: счёт неотрицателен на всём протяжении
226. **MODE-TUTORIAL-0226** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · тема=candy [tutorial]: ни одного исключения/краша за партию
227. **MODE-TUTORIAL-0227** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · тема=candy [tutorial]: resume в середине — бит-в-бит то же продолжение
228. **MODE-TUTORIAL-0228** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · тема=candy [tutorial]: число ходов конечно (партия сходится)
229. **MODE-TUTORIAL-0229** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · тема=candy [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
230. **MODE-TUTORIAL-0230** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · тема=candy [tutorial]: ни одной невалидной постановки не принято
231. **MODE-TUTORIAL-0231** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · тема=candy [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
232. **MODE-TUTORIAL-0232** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · тема=night [tutorial]: счёт неотрицателен на всём протяжении
233. **MODE-TUTORIAL-0233** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · тема=night [tutorial]: ни одного исключения/краша за партию
234. **MODE-TUTORIAL-0234** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · тема=night [tutorial]: resume в середине — бит-в-бит то же продолжение
235. **MODE-TUTORIAL-0235** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · тема=night [tutorial]: число ходов конечно (партия сходится)
236. **MODE-TUTORIAL-0236** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · тема=night [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
237. **MODE-TUTORIAL-0237** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · тема=night [tutorial]: ни одной невалидной постановки не принято
238. **MODE-TUTORIAL-0238** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · тема=night [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
239. **MODE-TUTORIAL-0239** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · reduceMotion=on [tutorial]: счёт неотрицателен на всём протяжении
240. **MODE-TUTORIAL-0240** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · reduceMotion=on [tutorial]: ни одного исключения/краша за партию
241. **MODE-TUTORIAL-0241** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · reduceMotion=on [tutorial]: resume в середине — бит-в-бит то же продолжение
242. **MODE-TUTORIAL-0242** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · reduceMotion=on [tutorial]: число ходов конечно (партия сходится)
243. **MODE-TUTORIAL-0243** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · reduceMotion=on [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
244. **MODE-TUTORIAL-0244** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · reduceMotion=on [tutorial]: ни одной невалидной постановки не принято
245. **MODE-TUTORIAL-0245** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · reduceMotion=on [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
246. **MODE-TUTORIAL-0246** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · узкое окно [tutorial]: счёт неотрицателен на всём протяжении
247. **MODE-TUTORIAL-0247** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · узкое окно [tutorial]: ни одного исключения/краша за партию
248. **MODE-TUTORIAL-0248** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · узкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
249. **MODE-TUTORIAL-0249** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · узкое окно [tutorial]: число ходов конечно (партия сходится)
250. **MODE-TUTORIAL-0250** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · узкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
251. **MODE-TUTORIAL-0251** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · узкое окно [tutorial]: ни одной невалидной постановки не принято
252. **MODE-TUTORIAL-0252** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · узкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
253. **MODE-TUTORIAL-0253** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · широкое окно [tutorial]: счёт неотрицателен на всём протяжении
254. **MODE-TUTORIAL-0254** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · широкое окно [tutorial]: ни одного исключения/краша за партию
255. **MODE-TUTORIAL-0255** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · широкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
256. **MODE-TUTORIAL-0256** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · широкое окно [tutorial]: число ходов конечно (партия сходится)
257. **MODE-TUTORIAL-0257** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · широкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
258. **MODE-TUTORIAL-0258** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · широкое окно [tutorial]: ни одной невалидной постановки не принято
259. **MODE-TUTORIAL-0259** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · широкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
260. **MODE-TUTORIAL-0260** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · тема=neutral [tutorial]: счёт неотрицателен на всём протяжении
261. **MODE-TUTORIAL-0261** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · тема=neutral [tutorial]: ни одного исключения/краша за партию
262. **MODE-TUTORIAL-0262** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · тема=neutral [tutorial]: resume в середине — бит-в-бит то же продолжение
263. **MODE-TUTORIAL-0263** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · тема=neutral [tutorial]: число ходов конечно (партия сходится)
264. **MODE-TUTORIAL-0264** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · тема=neutral [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
265. **MODE-TUTORIAL-0265** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · тема=neutral [tutorial]: ни одной невалидной постановки не принято
266. **MODE-TUTORIAL-0266** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · тема=neutral [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
267. **MODE-TUTORIAL-0267** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · тема=candy [tutorial]: счёт неотрицателен на всём протяжении
268. **MODE-TUTORIAL-0268** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · тема=candy [tutorial]: ни одного исключения/краша за партию
269. **MODE-TUTORIAL-0269** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · тема=candy [tutorial]: resume в середине — бит-в-бит то же продолжение
270. **MODE-TUTORIAL-0270** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · тема=candy [tutorial]: число ходов конечно (партия сходится)
271. **MODE-TUTORIAL-0271** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · тема=candy [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
272. **MODE-TUTORIAL-0272** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · тема=candy [tutorial]: ни одной невалидной постановки не принято
273. **MODE-TUTORIAL-0273** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · тема=candy [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
274. **MODE-TUTORIAL-0274** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · тема=night [tutorial]: счёт неотрицателен на всём протяжении
275. **MODE-TUTORIAL-0275** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · тема=night [tutorial]: ни одного исключения/краша за партию
276. **MODE-TUTORIAL-0276** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · тема=night [tutorial]: resume в середине — бит-в-бит то же продолжение
277. **MODE-TUTORIAL-0277** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · тема=night [tutorial]: число ходов конечно (партия сходится)
278. **MODE-TUTORIAL-0278** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · тема=night [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
279. **MODE-TUTORIAL-0279** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · тема=night [tutorial]: ни одной невалидной постановки не принято
280. **MODE-TUTORIAL-0280** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · тема=night [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
281. **MODE-TUTORIAL-0281** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · reduceMotion=on [tutorial]: счёт неотрицателен на всём протяжении
282. **MODE-TUTORIAL-0282** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · reduceMotion=on [tutorial]: ни одного исключения/краша за партию
283. **MODE-TUTORIAL-0283** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · reduceMotion=on [tutorial]: resume в середине — бит-в-бит то же продолжение
284. **MODE-TUTORIAL-0284** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · reduceMotion=on [tutorial]: число ходов конечно (партия сходится)
285. **MODE-TUTORIAL-0285** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · reduceMotion=on [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
286. **MODE-TUTORIAL-0286** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · reduceMotion=on [tutorial]: ни одной невалидной постановки не принято
287. **MODE-TUTORIAL-0287** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · reduceMotion=on [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
288. **MODE-TUTORIAL-0288** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · узкое окно [tutorial]: счёт неотрицателен на всём протяжении
289. **MODE-TUTORIAL-0289** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · узкое окно [tutorial]: ни одного исключения/краша за партию
290. **MODE-TUTORIAL-0290** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · узкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
291. **MODE-TUTORIAL-0291** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · узкое окно [tutorial]: число ходов конечно (партия сходится)
292. **MODE-TUTORIAL-0292** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · узкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
293. **MODE-TUTORIAL-0293** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · узкое окно [tutorial]: ни одной невалидной постановки не принято
294. **MODE-TUTORIAL-0294** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · узкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
295. **MODE-TUTORIAL-0295** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · широкое окно [tutorial]: счёт неотрицателен на всём протяжении
296. **MODE-TUTORIAL-0296** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · широкое окно [tutorial]: ни одного исключения/краша за партию
297. **MODE-TUTORIAL-0297** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · широкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
298. **MODE-TUTORIAL-0298** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · широкое окно [tutorial]: число ходов конечно (партия сходится)
299. **MODE-TUTORIAL-0299** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · широкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
300. **MODE-TUTORIAL-0300** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · широкое окно [tutorial]: ни одной невалидной постановки не принято
301. **MODE-TUTORIAL-0301** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · широкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
302. **MODE-TUTORIAL-0302** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · тема=neutral [tutorial]: счёт неотрицателен на всём протяжении
303. **MODE-TUTORIAL-0303** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · тема=neutral [tutorial]: ни одного исключения/краша за партию
304. **MODE-TUTORIAL-0304** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · тема=neutral [tutorial]: resume в середине — бит-в-бит то же продолжение
305. **MODE-TUTORIAL-0305** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · тема=neutral [tutorial]: число ходов конечно (партия сходится)
306. **MODE-TUTORIAL-0306** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · тема=neutral [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
307. **MODE-TUTORIAL-0307** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · тема=neutral [tutorial]: ни одной невалидной постановки не принято
308. **MODE-TUTORIAL-0308** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · тема=neutral [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
309. **MODE-TUTORIAL-0309** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · тема=candy [tutorial]: счёт неотрицателен на всём протяжении
310. **MODE-TUTORIAL-0310** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · тема=candy [tutorial]: ни одного исключения/краша за партию
311. **MODE-TUTORIAL-0311** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · тема=candy [tutorial]: resume в середине — бит-в-бит то же продолжение
312. **MODE-TUTORIAL-0312** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · тема=candy [tutorial]: число ходов конечно (партия сходится)
313. **MODE-TUTORIAL-0313** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · тема=candy [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
314. **MODE-TUTORIAL-0314** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · тема=candy [tutorial]: ни одной невалидной постановки не принято
315. **MODE-TUTORIAL-0315** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · тема=candy [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
316. **MODE-TUTORIAL-0316** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · тема=night [tutorial]: счёт неотрицателен на всём протяжении
317. **MODE-TUTORIAL-0317** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · тема=night [tutorial]: ни одного исключения/краша за партию
318. **MODE-TUTORIAL-0318** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · тема=night [tutorial]: resume в середине — бит-в-бит то же продолжение
319. **MODE-TUTORIAL-0319** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · тема=night [tutorial]: число ходов конечно (партия сходится)
320. **MODE-TUTORIAL-0320** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · тема=night [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
321. **MODE-TUTORIAL-0321** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · тема=night [tutorial]: ни одной невалидной постановки не принято
322. **MODE-TUTORIAL-0322** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · тема=night [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
323. **MODE-TUTORIAL-0323** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · reduceMotion=on [tutorial]: счёт неотрицателен на всём протяжении
324. **MODE-TUTORIAL-0324** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · reduceMotion=on [tutorial]: ни одного исключения/краша за партию
325. **MODE-TUTORIAL-0325** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · reduceMotion=on [tutorial]: resume в середине — бит-в-бит то же продолжение
326. **MODE-TUTORIAL-0326** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · reduceMotion=on [tutorial]: число ходов конечно (партия сходится)
327. **MODE-TUTORIAL-0327** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · reduceMotion=on [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
328. **MODE-TUTORIAL-0328** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · reduceMotion=on [tutorial]: ни одной невалидной постановки не принято
329. **MODE-TUTORIAL-0329** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · reduceMotion=on [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
330. **MODE-TUTORIAL-0330** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · узкое окно [tutorial]: счёт неотрицателен на всём протяжении
331. **MODE-TUTORIAL-0331** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · узкое окно [tutorial]: ни одного исключения/краша за партию
332. **MODE-TUTORIAL-0332** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · узкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
333. **MODE-TUTORIAL-0333** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · узкое окно [tutorial]: число ходов конечно (партия сходится)
334. **MODE-TUTORIAL-0334** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · узкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
335. **MODE-TUTORIAL-0335** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · узкое окно [tutorial]: ни одной невалидной постановки не принято
336. **MODE-TUTORIAL-0336** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · узкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
337. **MODE-TUTORIAL-0337** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · широкое окно [tutorial]: счёт неотрицателен на всём протяжении
338. **MODE-TUTORIAL-0338** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · широкое окно [tutorial]: ни одного исключения/краша за партию
339. **MODE-TUTORIAL-0339** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · широкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
340. **MODE-TUTORIAL-0340** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · широкое окно [tutorial]: число ходов конечно (партия сходится)
341. **MODE-TUTORIAL-0341** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · широкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
342. **MODE-TUTORIAL-0342** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · широкое окно [tutorial]: ни одной невалидной постановки не принято
343. **MODE-TUTORIAL-0343** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · широкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
344. **MODE-TUTORIAL-0344** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · тема=neutral [tutorial]: счёт неотрицателен на всём протяжении
345. **MODE-TUTORIAL-0345** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · тема=neutral [tutorial]: ни одного исключения/краша за партию
346. **MODE-TUTORIAL-0346** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · тема=neutral [tutorial]: resume в середине — бит-в-бит то же продолжение
347. **MODE-TUTORIAL-0347** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · тема=neutral [tutorial]: число ходов конечно (партия сходится)
348. **MODE-TUTORIAL-0348** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · тема=neutral [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
349. **MODE-TUTORIAL-0349** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · тема=neutral [tutorial]: ни одной невалидной постановки не принято
350. **MODE-TUTORIAL-0350** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · тема=neutral [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
351. **MODE-TUTORIAL-0351** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · тема=candy [tutorial]: счёт неотрицателен на всём протяжении
352. **MODE-TUTORIAL-0352** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · тема=candy [tutorial]: ни одного исключения/краша за партию
353. **MODE-TUTORIAL-0353** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · тема=candy [tutorial]: resume в середине — бит-в-бит то же продолжение
354. **MODE-TUTORIAL-0354** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · тема=candy [tutorial]: число ходов конечно (партия сходится)
355. **MODE-TUTORIAL-0355** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · тема=candy [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
356. **MODE-TUTORIAL-0356** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · тема=candy [tutorial]: ни одной невалидной постановки не принято
357. **MODE-TUTORIAL-0357** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · тема=candy [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
358. **MODE-TUTORIAL-0358** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · тема=night [tutorial]: счёт неотрицателен на всём протяжении
359. **MODE-TUTORIAL-0359** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · тема=night [tutorial]: ни одного исключения/краша за партию
360. **MODE-TUTORIAL-0360** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · тема=night [tutorial]: resume в середине — бит-в-бит то же продолжение
361. **MODE-TUTORIAL-0361** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · тема=night [tutorial]: число ходов конечно (партия сходится)
362. **MODE-TUTORIAL-0362** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · тема=night [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
363. **MODE-TUTORIAL-0363** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · тема=night [tutorial]: ни одной невалидной постановки не принято
364. **MODE-TUTORIAL-0364** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · тема=night [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
365. **MODE-TUTORIAL-0365** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · reduceMotion=on [tutorial]: счёт неотрицателен на всём протяжении
366. **MODE-TUTORIAL-0366** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · reduceMotion=on [tutorial]: ни одного исключения/краша за партию
367. **MODE-TUTORIAL-0367** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · reduceMotion=on [tutorial]: resume в середине — бит-в-бит то же продолжение
368. **MODE-TUTORIAL-0368** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · reduceMotion=on [tutorial]: число ходов конечно (партия сходится)
369. **MODE-TUTORIAL-0369** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · reduceMotion=on [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
370. **MODE-TUTORIAL-0370** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · reduceMotion=on [tutorial]: ни одной невалидной постановки не принято
371. **MODE-TUTORIAL-0371** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · reduceMotion=on [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
372. **MODE-TUTORIAL-0372** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · узкое окно [tutorial]: счёт неотрицателен на всём протяжении
373. **MODE-TUTORIAL-0373** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · узкое окно [tutorial]: ни одного исключения/краша за партию
374. **MODE-TUTORIAL-0374** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · узкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
375. **MODE-TUTORIAL-0375** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · узкое окно [tutorial]: число ходов конечно (партия сходится)
376. **MODE-TUTORIAL-0376** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · узкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
377. **MODE-TUTORIAL-0377** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · узкое окно [tutorial]: ни одной невалидной постановки не принято
378. **MODE-TUTORIAL-0378** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · узкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
379. **MODE-TUTORIAL-0379** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · широкое окно [tutorial]: счёт неотрицателен на всём протяжении
380. **MODE-TUTORIAL-0380** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · широкое окно [tutorial]: ни одного исключения/краша за партию
381. **MODE-TUTORIAL-0381** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · широкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
382. **MODE-TUTORIAL-0382** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · широкое окно [tutorial]: число ходов конечно (партия сходится)
383. **MODE-TUTORIAL-0383** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · широкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
384. **MODE-TUTORIAL-0384** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · широкое окно [tutorial]: ни одной невалидной постановки не принято
385. **MODE-TUTORIAL-0385** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · широкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
386. **MODE-TUTORIAL-0386** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · тема=neutral [tutorial]: счёт неотрицателен на всём протяжении
387. **MODE-TUTORIAL-0387** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · тема=neutral [tutorial]: ни одного исключения/краша за партию
388. **MODE-TUTORIAL-0388** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · тема=neutral [tutorial]: resume в середине — бит-в-бит то же продолжение
389. **MODE-TUTORIAL-0389** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · тема=neutral [tutorial]: число ходов конечно (партия сходится)
390. **MODE-TUTORIAL-0390** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · тема=neutral [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
391. **MODE-TUTORIAL-0391** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · тема=neutral [tutorial]: ни одной невалидной постановки не принято
392. **MODE-TUTORIAL-0392** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · тема=neutral [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
393. **MODE-TUTORIAL-0393** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · тема=candy [tutorial]: счёт неотрицателен на всём протяжении
394. **MODE-TUTORIAL-0394** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · тема=candy [tutorial]: ни одного исключения/краша за партию
395. **MODE-TUTORIAL-0395** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · тема=candy [tutorial]: resume в середине — бит-в-бит то же продолжение
396. **MODE-TUTORIAL-0396** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · тема=candy [tutorial]: число ходов конечно (партия сходится)
397. **MODE-TUTORIAL-0397** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · тема=candy [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
398. **MODE-TUTORIAL-0398** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · тема=candy [tutorial]: ни одной невалидной постановки не принято
399. **MODE-TUTORIAL-0399** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · тема=candy [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
400. **MODE-TUTORIAL-0400** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · тема=night [tutorial]: счёт неотрицателен на всём протяжении
401. **MODE-TUTORIAL-0401** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · тема=night [tutorial]: ни одного исключения/краша за партию
402. **MODE-TUTORIAL-0402** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · тема=night [tutorial]: resume в середине — бит-в-бит то же продолжение
403. **MODE-TUTORIAL-0403** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · тема=night [tutorial]: число ходов конечно (партия сходится)
404. **MODE-TUTORIAL-0404** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · тема=night [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
405. **MODE-TUTORIAL-0405** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · тема=night [tutorial]: ни одной невалидной постановки не принято
406. **MODE-TUTORIAL-0406** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · тема=night [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
407. **MODE-TUTORIAL-0407** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · reduceMotion=on [tutorial]: счёт неотрицателен на всём протяжении
408. **MODE-TUTORIAL-0408** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · reduceMotion=on [tutorial]: ни одного исключения/краша за партию
409. **MODE-TUTORIAL-0409** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · reduceMotion=on [tutorial]: resume в середине — бит-в-бит то же продолжение
410. **MODE-TUTORIAL-0410** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · reduceMotion=on [tutorial]: число ходов конечно (партия сходится)
411. **MODE-TUTORIAL-0411** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · reduceMotion=on [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
412. **MODE-TUTORIAL-0412** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · reduceMotion=on [tutorial]: ни одной невалидной постановки не принято
413. **MODE-TUTORIAL-0413** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · reduceMotion=on [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
414. **MODE-TUTORIAL-0414** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · узкое окно [tutorial]: счёт неотрицателен на всём протяжении
415. **MODE-TUTORIAL-0415** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · узкое окно [tutorial]: ни одного исключения/краша за партию
416. **MODE-TUTORIAL-0416** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · узкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
417. **MODE-TUTORIAL-0417** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · узкое окно [tutorial]: число ходов конечно (партия сходится)
418. **MODE-TUTORIAL-0418** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · узкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
419. **MODE-TUTORIAL-0419** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · узкое окно [tutorial]: ни одной невалидной постановки не принято
420. **MODE-TUTORIAL-0420** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · узкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
421. **MODE-TUTORIAL-0421** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · широкое окно [tutorial]: счёт неотрицателен на всём протяжении
422. **MODE-TUTORIAL-0422** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · широкое окно [tutorial]: ни одного исключения/краша за партию
423. **MODE-TUTORIAL-0423** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · широкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
424. **MODE-TUTORIAL-0424** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · широкое окно [tutorial]: число ходов конечно (партия сходится)
425. **MODE-TUTORIAL-0425** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · широкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
426. **MODE-TUTORIAL-0426** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · широкое окно [tutorial]: ни одной невалидной постановки не принято
427. **MODE-TUTORIAL-0427** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · широкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
428. **MODE-TUTORIAL-0428** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · тема=neutral [tutorial]: счёт неотрицателен на всём протяжении
429. **MODE-TUTORIAL-0429** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · тема=neutral [tutorial]: ни одного исключения/краша за партию
430. **MODE-TUTORIAL-0430** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · тема=neutral [tutorial]: resume в середине — бит-в-бит то же продолжение
431. **MODE-TUTORIAL-0431** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · тема=neutral [tutorial]: число ходов конечно (партия сходится)
432. **MODE-TUTORIAL-0432** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · тема=neutral [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
433. **MODE-TUTORIAL-0433** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · тема=neutral [tutorial]: ни одной невалидной постановки не принято
434. **MODE-TUTORIAL-0434** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · тема=neutral [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
435. **MODE-TUTORIAL-0435** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · тема=candy [tutorial]: счёт неотрицателен на всём протяжении
436. **MODE-TUTORIAL-0436** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · тема=candy [tutorial]: ни одного исключения/краша за партию
437. **MODE-TUTORIAL-0437** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · тема=candy [tutorial]: resume в середине — бит-в-бит то же продолжение
438. **MODE-TUTORIAL-0438** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · тема=candy [tutorial]: число ходов конечно (партия сходится)
439. **MODE-TUTORIAL-0439** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · тема=candy [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
440. **MODE-TUTORIAL-0440** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · тема=candy [tutorial]: ни одной невалидной постановки не принято
441. **MODE-TUTORIAL-0441** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · тема=candy [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
442. **MODE-TUTORIAL-0442** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · тема=night [tutorial]: счёт неотрицателен на всём протяжении
443. **MODE-TUTORIAL-0443** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · тема=night [tutorial]: ни одного исключения/краша за партию
444. **MODE-TUTORIAL-0444** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · тема=night [tutorial]: resume в середине — бит-в-бит то же продолжение
445. **MODE-TUTORIAL-0445** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · тема=night [tutorial]: число ходов конечно (партия сходится)
446. **MODE-TUTORIAL-0446** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · тема=night [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
447. **MODE-TUTORIAL-0447** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · тема=night [tutorial]: ни одной невалидной постановки не принято
448. **MODE-TUTORIAL-0448** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · тема=night [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
449. **MODE-TUTORIAL-0449** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · reduceMotion=on [tutorial]: счёт неотрицателен на всём протяжении
450. **MODE-TUTORIAL-0450** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · reduceMotion=on [tutorial]: ни одного исключения/краша за партию
451. **MODE-TUTORIAL-0451** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · reduceMotion=on [tutorial]: resume в середине — бит-в-бит то же продолжение
452. **MODE-TUTORIAL-0452** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · reduceMotion=on [tutorial]: число ходов конечно (партия сходится)
453. **MODE-TUTORIAL-0453** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · reduceMotion=on [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
454. **MODE-TUTORIAL-0454** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · reduceMotion=on [tutorial]: ни одной невалидной постановки не принято
455. **MODE-TUTORIAL-0455** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · reduceMotion=on [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
456. **MODE-TUTORIAL-0456** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · узкое окно [tutorial]: счёт неотрицателен на всём протяжении
457. **MODE-TUTORIAL-0457** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · узкое окно [tutorial]: ни одного исключения/краша за партию
458. **MODE-TUTORIAL-0458** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · узкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
459. **MODE-TUTORIAL-0459** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · узкое окно [tutorial]: число ходов конечно (партия сходится)
460. **MODE-TUTORIAL-0460** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · узкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
461. **MODE-TUTORIAL-0461** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · узкое окно [tutorial]: ни одной невалидной постановки не принято
462. **MODE-TUTORIAL-0462** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · узкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
463. **MODE-TUTORIAL-0463** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · широкое окно [tutorial]: счёт неотрицателен на всём протяжении
464. **MODE-TUTORIAL-0464** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · широкое окно [tutorial]: ни одного исключения/краша за партию
465. **MODE-TUTORIAL-0465** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · широкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
466. **MODE-TUTORIAL-0466** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · широкое окно [tutorial]: число ходов конечно (партия сходится)
467. **MODE-TUTORIAL-0467** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · широкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
468. **MODE-TUTORIAL-0468** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · широкое окно [tutorial]: ни одной невалидной постановки не принято
469. **MODE-TUTORIAL-0469** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · широкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
470. **MODE-TUTORIAL-0470** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · тема=neutral [tutorial]: счёт неотрицателен на всём протяжении
471. **MODE-TUTORIAL-0471** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · тема=neutral [tutorial]: ни одного исключения/краша за партию
472. **MODE-TUTORIAL-0472** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · тема=neutral [tutorial]: resume в середине — бит-в-бит то же продолжение
473. **MODE-TUTORIAL-0473** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · тема=neutral [tutorial]: число ходов конечно (партия сходится)
474. **MODE-TUTORIAL-0474** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · тема=neutral [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
475. **MODE-TUTORIAL-0475** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · тема=neutral [tutorial]: ни одной невалидной постановки не принято
476. **MODE-TUTORIAL-0476** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · тема=neutral [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
477. **MODE-TUTORIAL-0477** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · тема=candy [tutorial]: счёт неотрицателен на всём протяжении
478. **MODE-TUTORIAL-0478** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · тема=candy [tutorial]: ни одного исключения/краша за партию
479. **MODE-TUTORIAL-0479** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · тема=candy [tutorial]: resume в середине — бит-в-бит то же продолжение
480. **MODE-TUTORIAL-0480** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · тема=candy [tutorial]: число ходов конечно (партия сходится)
481. **MODE-TUTORIAL-0481** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · тема=candy [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
482. **MODE-TUTORIAL-0482** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · тема=candy [tutorial]: ни одной невалидной постановки не принято
483. **MODE-TUTORIAL-0483** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · тема=candy [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
484. **MODE-TUTORIAL-0484** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · тема=night [tutorial]: счёт неотрицателен на всём протяжении
485. **MODE-TUTORIAL-0485** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · тема=night [tutorial]: ни одного исключения/краша за партию
486. **MODE-TUTORIAL-0486** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · тема=night [tutorial]: resume в середине — бит-в-бит то же продолжение
487. **MODE-TUTORIAL-0487** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · тема=night [tutorial]: число ходов конечно (партия сходится)
488. **MODE-TUTORIAL-0488** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · тема=night [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
489. **MODE-TUTORIAL-0489** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · тема=night [tutorial]: ни одной невалидной постановки не принято
490. **MODE-TUTORIAL-0490** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · тема=night [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
491. **MODE-TUTORIAL-0491** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · reduceMotion=on [tutorial]: счёт неотрицателен на всём протяжении
492. **MODE-TUTORIAL-0492** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · reduceMotion=on [tutorial]: ни одного исключения/краша за партию
493. **MODE-TUTORIAL-0493** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · reduceMotion=on [tutorial]: resume в середине — бит-в-бит то же продолжение
494. **MODE-TUTORIAL-0494** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · reduceMotion=on [tutorial]: число ходов конечно (партия сходится)
495. **MODE-TUTORIAL-0495** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · reduceMotion=on [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
496. **MODE-TUTORIAL-0496** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · reduceMotion=on [tutorial]: ни одной невалидной постановки не принято
497. **MODE-TUTORIAL-0497** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · reduceMotion=on [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
498. **MODE-TUTORIAL-0498** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · узкое окно [tutorial]: счёт неотрицателен на всём протяжении
499. **MODE-TUTORIAL-0499** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · узкое окно [tutorial]: ни одного исключения/краша за партию
500. **MODE-TUTORIAL-0500** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · узкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
501. **MODE-TUTORIAL-0501** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · узкое окно [tutorial]: число ходов конечно (партия сходится)
502. **MODE-TUTORIAL-0502** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · узкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
503. **MODE-TUTORIAL-0503** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · узкое окно [tutorial]: ни одной невалидной постановки не принято
504. **MODE-TUTORIAL-0504** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · узкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
505. **MODE-TUTORIAL-0505** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · широкое окно [tutorial]: счёт неотрицателен на всём протяжении
506. **MODE-TUTORIAL-0506** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · широкое окно [tutorial]: ни одного исключения/краша за партию
507. **MODE-TUTORIAL-0507** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · широкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
508. **MODE-TUTORIAL-0508** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · широкое окно [tutorial]: число ходов конечно (партия сходится)
509. **MODE-TUTORIAL-0509** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · широкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
510. **MODE-TUTORIAL-0510** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · широкое окно [tutorial]: ни одной невалидной постановки не принято
511. **MODE-TUTORIAL-0511** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · широкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
512. **MODE-TUTORIAL-0512** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · тема=neutral [tutorial]: счёт неотрицателен на всём протяжении
513. **MODE-TUTORIAL-0513** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · тема=neutral [tutorial]: ни одного исключения/краша за партию
514. **MODE-TUTORIAL-0514** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · тема=neutral [tutorial]: resume в середине — бит-в-бит то же продолжение
515. **MODE-TUTORIAL-0515** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · тема=neutral [tutorial]: число ходов конечно (партия сходится)
516. **MODE-TUTORIAL-0516** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · тема=neutral [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
517. **MODE-TUTORIAL-0517** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · тема=neutral [tutorial]: ни одной невалидной постановки не принято
518. **MODE-TUTORIAL-0518** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · тема=neutral [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
519. **MODE-TUTORIAL-0519** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · тема=candy [tutorial]: счёт неотрицателен на всём протяжении
520. **MODE-TUTORIAL-0520** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · тема=candy [tutorial]: ни одного исключения/краша за партию
521. **MODE-TUTORIAL-0521** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · тема=candy [tutorial]: resume в середине — бит-в-бит то же продолжение
522. **MODE-TUTORIAL-0522** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · тема=candy [tutorial]: число ходов конечно (партия сходится)
523. **MODE-TUTORIAL-0523** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · тема=candy [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
524. **MODE-TUTORIAL-0524** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · тема=candy [tutorial]: ни одной невалидной постановки не принято
525. **MODE-TUTORIAL-0525** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · тема=candy [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
526. **MODE-TUTORIAL-0526** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · тема=night [tutorial]: счёт неотрицателен на всём протяжении
527. **MODE-TUTORIAL-0527** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · тема=night [tutorial]: ни одного исключения/краша за партию
528. **MODE-TUTORIAL-0528** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · тема=night [tutorial]: resume в середине — бит-в-бит то же продолжение
529. **MODE-TUTORIAL-0529** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · тема=night [tutorial]: число ходов конечно (партия сходится)
530. **MODE-TUTORIAL-0530** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · тема=night [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
531. **MODE-TUTORIAL-0531** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · тема=night [tutorial]: ни одной невалидной постановки не принято
532. **MODE-TUTORIAL-0532** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · тема=night [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
533. **MODE-TUTORIAL-0533** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · reduceMotion=on [tutorial]: счёт неотрицателен на всём протяжении
534. **MODE-TUTORIAL-0534** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · reduceMotion=on [tutorial]: ни одного исключения/краша за партию
535. **MODE-TUTORIAL-0535** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · reduceMotion=on [tutorial]: resume в середине — бит-в-бит то же продолжение
536. **MODE-TUTORIAL-0536** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · reduceMotion=on [tutorial]: число ходов конечно (партия сходится)
537. **MODE-TUTORIAL-0537** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · reduceMotion=on [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
538. **MODE-TUTORIAL-0538** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · reduceMotion=on [tutorial]: ни одной невалидной постановки не принято
539. **MODE-TUTORIAL-0539** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · reduceMotion=on [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
540. **MODE-TUTORIAL-0540** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · узкое окно [tutorial]: счёт неотрицателен на всём протяжении
541. **MODE-TUTORIAL-0541** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · узкое окно [tutorial]: ни одного исключения/краша за партию
542. **MODE-TUTORIAL-0542** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · узкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
543. **MODE-TUTORIAL-0543** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · узкое окно [tutorial]: число ходов конечно (партия сходится)
544. **MODE-TUTORIAL-0544** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · узкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
545. **MODE-TUTORIAL-0545** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · узкое окно [tutorial]: ни одной невалидной постановки не принято
546. **MODE-TUTORIAL-0546** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · узкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
547. **MODE-TUTORIAL-0547** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · широкое окно [tutorial]: счёт неотрицателен на всём протяжении
548. **MODE-TUTORIAL-0548** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · широкое окно [tutorial]: ни одного исключения/краша за партию
549. **MODE-TUTORIAL-0549** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · широкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
550. **MODE-TUTORIAL-0550** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · широкое окно [tutorial]: число ходов конечно (партия сходится)
551. **MODE-TUTORIAL-0551** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · широкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
552. **MODE-TUTORIAL-0552** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · широкое окно [tutorial]: ни одной невалидной постановки не принято
553. **MODE-TUTORIAL-0553** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · широкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
554. **MODE-TUTORIAL-0554** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · тема=neutral [tutorial]: счёт неотрицателен на всём протяжении
555. **MODE-TUTORIAL-0555** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · тема=neutral [tutorial]: ни одного исключения/краша за партию
556. **MODE-TUTORIAL-0556** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · тема=neutral [tutorial]: resume в середине — бит-в-бит то же продолжение
557. **MODE-TUTORIAL-0557** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · тема=neutral [tutorial]: число ходов конечно (партия сходится)
558. **MODE-TUTORIAL-0558** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · тема=neutral [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
559. **MODE-TUTORIAL-0559** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · тема=neutral [tutorial]: ни одной невалидной постановки не принято
560. **MODE-TUTORIAL-0560** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · тема=neutral [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
561. **MODE-TUTORIAL-0561** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · тема=candy [tutorial]: счёт неотрицателен на всём протяжении
562. **MODE-TUTORIAL-0562** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · тема=candy [tutorial]: ни одного исключения/краша за партию
563. **MODE-TUTORIAL-0563** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · тема=candy [tutorial]: resume в середине — бит-в-бит то же продолжение
564. **MODE-TUTORIAL-0564** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · тема=candy [tutorial]: число ходов конечно (партия сходится)
565. **MODE-TUTORIAL-0565** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · тема=candy [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
566. **MODE-TUTORIAL-0566** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · тема=candy [tutorial]: ни одной невалидной постановки не принято
567. **MODE-TUTORIAL-0567** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · тема=candy [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
568. **MODE-TUTORIAL-0568** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · тема=night [tutorial]: счёт неотрицателен на всём протяжении
569. **MODE-TUTORIAL-0569** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · тема=night [tutorial]: ни одного исключения/краша за партию
570. **MODE-TUTORIAL-0570** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · тема=night [tutorial]: resume в середине — бит-в-бит то же продолжение
571. **MODE-TUTORIAL-0571** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · тема=night [tutorial]: число ходов конечно (партия сходится)
572. **MODE-TUTORIAL-0572** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · тема=night [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
573. **MODE-TUTORIAL-0573** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · тема=night [tutorial]: ни одной невалидной постановки не принято
574. **MODE-TUTORIAL-0574** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · тема=night [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
575. **MODE-TUTORIAL-0575** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · reduceMotion=on [tutorial]: счёт неотрицателен на всём протяжении
576. **MODE-TUTORIAL-0576** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · reduceMotion=on [tutorial]: ни одного исключения/краша за партию
577. **MODE-TUTORIAL-0577** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · reduceMotion=on [tutorial]: resume в середине — бит-в-бит то же продолжение
578. **MODE-TUTORIAL-0578** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · reduceMotion=on [tutorial]: число ходов конечно (партия сходится)
579. **MODE-TUTORIAL-0579** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · reduceMotion=on [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
580. **MODE-TUTORIAL-0580** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · reduceMotion=on [tutorial]: ни одной невалидной постановки не принято
581. **MODE-TUTORIAL-0581** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · reduceMotion=on [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
582. **MODE-TUTORIAL-0582** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · узкое окно [tutorial]: счёт неотрицателен на всём протяжении
583. **MODE-TUTORIAL-0583** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · узкое окно [tutorial]: ни одного исключения/краша за партию
584. **MODE-TUTORIAL-0584** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · узкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
585. **MODE-TUTORIAL-0585** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · узкое окно [tutorial]: число ходов конечно (партия сходится)
586. **MODE-TUTORIAL-0586** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · узкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
587. **MODE-TUTORIAL-0587** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · узкое окно [tutorial]: ни одной невалидной постановки не принято
588. **MODE-TUTORIAL-0588** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · узкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
589. **MODE-TUTORIAL-0589** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · широкое окно [tutorial]: счёт неотрицателен на всём протяжении
590. **MODE-TUTORIAL-0590** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · широкое окно [tutorial]: ни одного исключения/краша за партию
591. **MODE-TUTORIAL-0591** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · широкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
592. **MODE-TUTORIAL-0592** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · широкое окно [tutorial]: число ходов конечно (партия сходится)
593. **MODE-TUTORIAL-0593** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · широкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
594. **MODE-TUTORIAL-0594** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · широкое окно [tutorial]: ни одной невалидной постановки не принято
595. **MODE-TUTORIAL-0595** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · широкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
596. **MODE-TUTORIAL-0596** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · тема=neutral [tutorial]: счёт неотрицателен на всём протяжении
597. **MODE-TUTORIAL-0597** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · тема=neutral [tutorial]: ни одного исключения/краша за партию
598. **MODE-TUTORIAL-0598** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · тема=neutral [tutorial]: resume в середине — бит-в-бит то же продолжение
599. **MODE-TUTORIAL-0599** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · тема=neutral [tutorial]: число ходов конечно (партия сходится)
600. **MODE-TUTORIAL-0600** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · тема=neutral [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
601. **MODE-TUTORIAL-0601** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · тема=neutral [tutorial]: ни одной невалидной постановки не принято
602. **MODE-TUTORIAL-0602** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · тема=neutral [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
603. **MODE-TUTORIAL-0603** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · тема=candy [tutorial]: счёт неотрицателен на всём протяжении
604. **MODE-TUTORIAL-0604** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · тема=candy [tutorial]: ни одного исключения/краша за партию
605. **MODE-TUTORIAL-0605** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · тема=candy [tutorial]: resume в середине — бит-в-бит то же продолжение
606. **MODE-TUTORIAL-0606** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · тема=candy [tutorial]: число ходов конечно (партия сходится)
607. **MODE-TUTORIAL-0607** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · тема=candy [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
608. **MODE-TUTORIAL-0608** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · тема=candy [tutorial]: ни одной невалидной постановки не принято
609. **MODE-TUTORIAL-0609** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · тема=candy [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
610. **MODE-TUTORIAL-0610** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · тема=night [tutorial]: счёт неотрицателен на всём протяжении
611. **MODE-TUTORIAL-0611** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · тема=night [tutorial]: ни одного исключения/краша за партию
612. **MODE-TUTORIAL-0612** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · тема=night [tutorial]: resume в середине — бит-в-бит то же продолжение
613. **MODE-TUTORIAL-0613** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · тема=night [tutorial]: число ходов конечно (партия сходится)
614. **MODE-TUTORIAL-0614** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · тема=night [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
615. **MODE-TUTORIAL-0615** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · тема=night [tutorial]: ни одной невалидной постановки не принято
616. **MODE-TUTORIAL-0616** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · тема=night [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
617. **MODE-TUTORIAL-0617** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · reduceMotion=on [tutorial]: счёт неотрицателен на всём протяжении
618. **MODE-TUTORIAL-0618** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · reduceMotion=on [tutorial]: ни одного исключения/краша за партию
619. **MODE-TUTORIAL-0619** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · reduceMotion=on [tutorial]: resume в середине — бит-в-бит то же продолжение
620. **MODE-TUTORIAL-0620** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · reduceMotion=on [tutorial]: число ходов конечно (партия сходится)
621. **MODE-TUTORIAL-0621** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · reduceMotion=on [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
622. **MODE-TUTORIAL-0622** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · reduceMotion=on [tutorial]: ни одной невалидной постановки не принято
623. **MODE-TUTORIAL-0623** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · reduceMotion=on [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
624. **MODE-TUTORIAL-0624** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · узкое окно [tutorial]: счёт неотрицателен на всём протяжении
625. **MODE-TUTORIAL-0625** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · узкое окно [tutorial]: ни одного исключения/краша за партию
626. **MODE-TUTORIAL-0626** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · узкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
627. **MODE-TUTORIAL-0627** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · узкое окно [tutorial]: число ходов конечно (партия сходится)
628. **MODE-TUTORIAL-0628** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · узкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
629. **MODE-TUTORIAL-0629** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · узкое окно [tutorial]: ни одной невалидной постановки не принято
630. **MODE-TUTORIAL-0630** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · узкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
631. **MODE-TUTORIAL-0631** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · широкое окно [tutorial]: счёт неотрицателен на всём протяжении
632. **MODE-TUTORIAL-0632** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · широкое окно [tutorial]: ни одного исключения/краша за партию
633. **MODE-TUTORIAL-0633** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · широкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
634. **MODE-TUTORIAL-0634** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · широкое окно [tutorial]: число ходов конечно (партия сходится)
635. **MODE-TUTORIAL-0635** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · широкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
636. **MODE-TUTORIAL-0636** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · широкое окно [tutorial]: ни одной невалидной постановки не принято
637. **MODE-TUTORIAL-0637** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=12 · широкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
638. **MODE-TUTORIAL-0638** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · тема=neutral [tutorial]: счёт неотрицателен на всём протяжении
639. **MODE-TUTORIAL-0639** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · тема=neutral [tutorial]: ни одного исключения/краша за партию
640. **MODE-TUTORIAL-0640** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · тема=neutral [tutorial]: resume в середине — бит-в-бит то же продолжение
641. **MODE-TUTORIAL-0641** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · тема=neutral [tutorial]: число ходов конечно (партия сходится)
642. **MODE-TUTORIAL-0642** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · тема=neutral [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
643. **MODE-TUTORIAL-0643** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · тема=neutral [tutorial]: ни одной невалидной постановки не принято
644. **MODE-TUTORIAL-0644** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · тема=neutral [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
645. **MODE-TUTORIAL-0645** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · тема=candy [tutorial]: счёт неотрицателен на всём протяжении
646. **MODE-TUTORIAL-0646** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · тема=candy [tutorial]: ни одного исключения/краша за партию
647. **MODE-TUTORIAL-0647** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · тема=candy [tutorial]: resume в середине — бит-в-бит то же продолжение
648. **MODE-TUTORIAL-0648** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · тема=candy [tutorial]: число ходов конечно (партия сходится)
649. **MODE-TUTORIAL-0649** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · тема=candy [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
650. **MODE-TUTORIAL-0650** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · тема=candy [tutorial]: ни одной невалидной постановки не принято
651. **MODE-TUTORIAL-0651** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · тема=candy [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
652. **MODE-TUTORIAL-0652** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · тема=night [tutorial]: счёт неотрицателен на всём протяжении
653. **MODE-TUTORIAL-0653** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · тема=night [tutorial]: ни одного исключения/краша за партию
654. **MODE-TUTORIAL-0654** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · тема=night [tutorial]: resume в середине — бит-в-бит то же продолжение
655. **MODE-TUTORIAL-0655** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · тема=night [tutorial]: число ходов конечно (партия сходится)
656. **MODE-TUTORIAL-0656** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · тема=night [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
657. **MODE-TUTORIAL-0657** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · тема=night [tutorial]: ни одной невалидной постановки не принято
658. **MODE-TUTORIAL-0658** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · тема=night [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
659. **MODE-TUTORIAL-0659** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · reduceMotion=on [tutorial]: счёт неотрицателен на всём протяжении
660. **MODE-TUTORIAL-0660** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · reduceMotion=on [tutorial]: ни одного исключения/краша за партию
661. **MODE-TUTORIAL-0661** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · reduceMotion=on [tutorial]: resume в середине — бит-в-бит то же продолжение
662. **MODE-TUTORIAL-0662** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · reduceMotion=on [tutorial]: число ходов конечно (партия сходится)
663. **MODE-TUTORIAL-0663** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · reduceMotion=on [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
664. **MODE-TUTORIAL-0664** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · reduceMotion=on [tutorial]: ни одной невалидной постановки не принято
665. **MODE-TUTORIAL-0665** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · reduceMotion=on [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
666. **MODE-TUTORIAL-0666** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · узкое окно [tutorial]: счёт неотрицателен на всём протяжении
667. **MODE-TUTORIAL-0667** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · узкое окно [tutorial]: ни одного исключения/краша за партию
668. **MODE-TUTORIAL-0668** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · узкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
669. **MODE-TUTORIAL-0669** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · узкое окно [tutorial]: число ходов конечно (партия сходится)
670. **MODE-TUTORIAL-0670** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · узкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
671. **MODE-TUTORIAL-0671** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · узкое окно [tutorial]: ни одной невалидной постановки не принято
672. **MODE-TUTORIAL-0672** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · узкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
673. **MODE-TUTORIAL-0673** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · широкое окно [tutorial]: счёт неотрицателен на всём протяжении
674. **MODE-TUTORIAL-0674** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · широкое окно [tutorial]: ни одного исключения/краша за партию
675. **MODE-TUTORIAL-0675** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · широкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
676. **MODE-TUTORIAL-0676** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · широкое окно [tutorial]: число ходов конечно (партия сходится)
677. **MODE-TUTORIAL-0677** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · широкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
678. **MODE-TUTORIAL-0678** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · широкое окно [tutorial]: ни одной невалидной постановки не принято
679. **MODE-TUTORIAL-0679** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=13 · широкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
680. **MODE-TUTORIAL-0680** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · тема=neutral [tutorial]: счёт неотрицателен на всём протяжении
681. **MODE-TUTORIAL-0681** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · тема=neutral [tutorial]: ни одного исключения/краша за партию
682. **MODE-TUTORIAL-0682** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · тема=neutral [tutorial]: resume в середине — бит-в-бит то же продолжение
683. **MODE-TUTORIAL-0683** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · тема=neutral [tutorial]: число ходов конечно (партия сходится)
684. **MODE-TUTORIAL-0684** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · тема=neutral [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
685. **MODE-TUTORIAL-0685** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · тема=neutral [tutorial]: ни одной невалидной постановки не принято
686. **MODE-TUTORIAL-0686** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · тема=neutral [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
687. **MODE-TUTORIAL-0687** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · тема=candy [tutorial]: счёт неотрицателен на всём протяжении
688. **MODE-TUTORIAL-0688** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · тема=candy [tutorial]: ни одного исключения/краша за партию
689. **MODE-TUTORIAL-0689** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · тема=candy [tutorial]: resume в середине — бит-в-бит то же продолжение
690. **MODE-TUTORIAL-0690** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · тема=candy [tutorial]: число ходов конечно (партия сходится)
691. **MODE-TUTORIAL-0691** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · тема=candy [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
692. **MODE-TUTORIAL-0692** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · тема=candy [tutorial]: ни одной невалидной постановки не принято
693. **MODE-TUTORIAL-0693** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · тема=candy [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
694. **MODE-TUTORIAL-0694** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · тема=night [tutorial]: счёт неотрицателен на всём протяжении
695. **MODE-TUTORIAL-0695** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · тема=night [tutorial]: ни одного исключения/краша за партию
696. **MODE-TUTORIAL-0696** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · тема=night [tutorial]: resume в середине — бит-в-бит то же продолжение
697. **MODE-TUTORIAL-0697** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · тема=night [tutorial]: число ходов конечно (партия сходится)
698. **MODE-TUTORIAL-0698** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · тема=night [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
699. **MODE-TUTORIAL-0699** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · тема=night [tutorial]: ни одной невалидной постановки не принято
700. **MODE-TUTORIAL-0700** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · тема=night [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
701. **MODE-TUTORIAL-0701** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · reduceMotion=on [tutorial]: счёт неотрицателен на всём протяжении
702. **MODE-TUTORIAL-0702** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · reduceMotion=on [tutorial]: ни одного исключения/краша за партию
703. **MODE-TUTORIAL-0703** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · reduceMotion=on [tutorial]: resume в середине — бит-в-бит то же продолжение
704. **MODE-TUTORIAL-0704** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · reduceMotion=on [tutorial]: число ходов конечно (партия сходится)
705. **MODE-TUTORIAL-0705** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · reduceMotion=on [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
706. **MODE-TUTORIAL-0706** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · reduceMotion=on [tutorial]: ни одной невалидной постановки не принято
707. **MODE-TUTORIAL-0707** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · reduceMotion=on [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
708. **MODE-TUTORIAL-0708** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · узкое окно [tutorial]: счёт неотрицателен на всём протяжении
709. **MODE-TUTORIAL-0709** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · узкое окно [tutorial]: ни одного исключения/краша за партию
710. **MODE-TUTORIAL-0710** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · узкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
711. **MODE-TUTORIAL-0711** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · узкое окно [tutorial]: число ходов конечно (партия сходится)
712. **MODE-TUTORIAL-0712** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · узкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
713. **MODE-TUTORIAL-0713** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · узкое окно [tutorial]: ни одной невалидной постановки не принято
714. **MODE-TUTORIAL-0714** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · узкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
715. **MODE-TUTORIAL-0715** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · широкое окно [tutorial]: счёт неотрицателен на всём протяжении
716. **MODE-TUTORIAL-0716** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · широкое окно [tutorial]: ни одного исключения/краша за партию
717. **MODE-TUTORIAL-0717** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · широкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
718. **MODE-TUTORIAL-0718** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · широкое окно [tutorial]: число ходов конечно (партия сходится)
719. **MODE-TUTORIAL-0719** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · широкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
720. **MODE-TUTORIAL-0720** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · широкое окно [tutorial]: ни одной невалидной постановки не принято
721. **MODE-TUTORIAL-0721** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=14 · широкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
722. **MODE-TUTORIAL-0722** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · тема=neutral [tutorial]: счёт неотрицателен на всём протяжении
723. **MODE-TUTORIAL-0723** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · тема=neutral [tutorial]: ни одного исключения/краша за партию
724. **MODE-TUTORIAL-0724** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · тема=neutral [tutorial]: resume в середине — бит-в-бит то же продолжение
725. **MODE-TUTORIAL-0725** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · тема=neutral [tutorial]: число ходов конечно (партия сходится)
726. **MODE-TUTORIAL-0726** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · тема=neutral [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
727. **MODE-TUTORIAL-0727** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · тема=neutral [tutorial]: ни одной невалидной постановки не принято
728. **MODE-TUTORIAL-0728** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · тема=neutral [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
729. **MODE-TUTORIAL-0729** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · тема=candy [tutorial]: счёт неотрицателен на всём протяжении
730. **MODE-TUTORIAL-0730** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · тема=candy [tutorial]: ни одного исключения/краша за партию
731. **MODE-TUTORIAL-0731** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · тема=candy [tutorial]: resume в середине — бит-в-бит то же продолжение
732. **MODE-TUTORIAL-0732** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · тема=candy [tutorial]: число ходов конечно (партия сходится)
733. **MODE-TUTORIAL-0733** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · тема=candy [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
734. **MODE-TUTORIAL-0734** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · тема=candy [tutorial]: ни одной невалидной постановки не принято
735. **MODE-TUTORIAL-0735** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · тема=candy [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
736. **MODE-TUTORIAL-0736** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · тема=night [tutorial]: счёт неотрицателен на всём протяжении
737. **MODE-TUTORIAL-0737** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · тема=night [tutorial]: ни одного исключения/краша за партию
738. **MODE-TUTORIAL-0738** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · тема=night [tutorial]: resume в середине — бит-в-бит то же продолжение
739. **MODE-TUTORIAL-0739** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · тема=night [tutorial]: число ходов конечно (партия сходится)
740. **MODE-TUTORIAL-0740** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · тема=night [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
741. **MODE-TUTORIAL-0741** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · тема=night [tutorial]: ни одной невалидной постановки не принято
742. **MODE-TUTORIAL-0742** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · тема=night [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
743. **MODE-TUTORIAL-0743** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · reduceMotion=on [tutorial]: счёт неотрицателен на всём протяжении
744. **MODE-TUTORIAL-0744** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · reduceMotion=on [tutorial]: ни одного исключения/краша за партию
745. **MODE-TUTORIAL-0745** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · reduceMotion=on [tutorial]: resume в середине — бит-в-бит то же продолжение
746. **MODE-TUTORIAL-0746** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · reduceMotion=on [tutorial]: число ходов конечно (партия сходится)
747. **MODE-TUTORIAL-0747** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · reduceMotion=on [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
748. **MODE-TUTORIAL-0748** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · reduceMotion=on [tutorial]: ни одной невалидной постановки не принято
749. **MODE-TUTORIAL-0749** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · reduceMotion=on [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
750. **MODE-TUTORIAL-0750** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · узкое окно [tutorial]: счёт неотрицателен на всём протяжении
751. **MODE-TUTORIAL-0751** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · узкое окно [tutorial]: ни одного исключения/краша за партию
752. **MODE-TUTORIAL-0752** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · узкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
753. **MODE-TUTORIAL-0753** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · узкое окно [tutorial]: число ходов конечно (партия сходится)
754. **MODE-TUTORIAL-0754** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · узкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
755. **MODE-TUTORIAL-0755** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · узкое окно [tutorial]: ни одной невалидной постановки не принято
756. **MODE-TUTORIAL-0756** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · узкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
757. **MODE-TUTORIAL-0757** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · широкое окно [tutorial]: счёт неотрицателен на всём протяжении
758. **MODE-TUTORIAL-0758** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · широкое окно [tutorial]: ни одного исключения/краша за партию
759. **MODE-TUTORIAL-0759** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · широкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
760. **MODE-TUTORIAL-0760** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · широкое окно [tutorial]: число ходов конечно (партия сходится)
761. **MODE-TUTORIAL-0761** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · широкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
762. **MODE-TUTORIAL-0762** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · широкое окно [tutorial]: ни одной невалидной постановки не принято
763. **MODE-TUTORIAL-0763** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=15 · широкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
764. **MODE-TUTORIAL-0764** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · тема=neutral [tutorial]: счёт неотрицателен на всём протяжении
765. **MODE-TUTORIAL-0765** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · тема=neutral [tutorial]: ни одного исключения/краша за партию
766. **MODE-TUTORIAL-0766** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · тема=neutral [tutorial]: resume в середине — бит-в-бит то же продолжение
767. **MODE-TUTORIAL-0767** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · тема=neutral [tutorial]: число ходов конечно (партия сходится)
768. **MODE-TUTORIAL-0768** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · тема=neutral [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
769. **MODE-TUTORIAL-0769** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · тема=neutral [tutorial]: ни одной невалидной постановки не принято
770. **MODE-TUTORIAL-0770** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · тема=neutral [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
771. **MODE-TUTORIAL-0771** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · тема=candy [tutorial]: счёт неотрицателен на всём протяжении
772. **MODE-TUTORIAL-0772** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · тема=candy [tutorial]: ни одного исключения/краша за партию
773. **MODE-TUTORIAL-0773** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · тема=candy [tutorial]: resume в середине — бит-в-бит то же продолжение
774. **MODE-TUTORIAL-0774** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · тема=candy [tutorial]: число ходов конечно (партия сходится)
775. **MODE-TUTORIAL-0775** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · тема=candy [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
776. **MODE-TUTORIAL-0776** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · тема=candy [tutorial]: ни одной невалидной постановки не принято
777. **MODE-TUTORIAL-0777** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · тема=candy [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
778. **MODE-TUTORIAL-0778** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · тема=night [tutorial]: счёт неотрицателен на всём протяжении
779. **MODE-TUTORIAL-0779** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · тема=night [tutorial]: ни одного исключения/краша за партию
780. **MODE-TUTORIAL-0780** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · тема=night [tutorial]: resume в середине — бит-в-бит то же продолжение
781. **MODE-TUTORIAL-0781** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · тема=night [tutorial]: число ходов конечно (партия сходится)
782. **MODE-TUTORIAL-0782** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · тема=night [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
783. **MODE-TUTORIAL-0783** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · тема=night [tutorial]: ни одной невалидной постановки не принято
784. **MODE-TUTORIAL-0784** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · тема=night [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
785. **MODE-TUTORIAL-0785** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · reduceMotion=on [tutorial]: счёт неотрицателен на всём протяжении
786. **MODE-TUTORIAL-0786** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · reduceMotion=on [tutorial]: ни одного исключения/краша за партию
787. **MODE-TUTORIAL-0787** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · reduceMotion=on [tutorial]: resume в середине — бит-в-бит то же продолжение
788. **MODE-TUTORIAL-0788** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · reduceMotion=on [tutorial]: число ходов конечно (партия сходится)
789. **MODE-TUTORIAL-0789** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · reduceMotion=on [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
790. **MODE-TUTORIAL-0790** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · reduceMotion=on [tutorial]: ни одной невалидной постановки не принято
791. **MODE-TUTORIAL-0791** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · reduceMotion=on [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
792. **MODE-TUTORIAL-0792** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · узкое окно [tutorial]: счёт неотрицателен на всём протяжении
793. **MODE-TUTORIAL-0793** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · узкое окно [tutorial]: ни одного исключения/краша за партию
794. **MODE-TUTORIAL-0794** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · узкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
795. **MODE-TUTORIAL-0795** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · узкое окно [tutorial]: число ходов конечно (партия сходится)
796. **MODE-TUTORIAL-0796** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · узкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
797. **MODE-TUTORIAL-0797** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · узкое окно [tutorial]: ни одной невалидной постановки не принято
798. **MODE-TUTORIAL-0798** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · узкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
799. **MODE-TUTORIAL-0799** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · широкое окно [tutorial]: счёт неотрицателен на всём протяжении
800. **MODE-TUTORIAL-0800** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · широкое окно [tutorial]: ни одного исключения/краша за партию
801. **MODE-TUTORIAL-0801** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · широкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
802. **MODE-TUTORIAL-0802** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · широкое окно [tutorial]: число ходов конечно (партия сходится)
803. **MODE-TUTORIAL-0803** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · широкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
804. **MODE-TUTORIAL-0804** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · широкое окно [tutorial]: ни одной невалидной постановки не принято
805. **MODE-TUTORIAL-0805** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=16 · широкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
806. **MODE-TUTORIAL-0806** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · тема=neutral [tutorial]: счёт неотрицателен на всём протяжении
807. **MODE-TUTORIAL-0807** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · тема=neutral [tutorial]: ни одного исключения/краша за партию
808. **MODE-TUTORIAL-0808** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · тема=neutral [tutorial]: resume в середине — бит-в-бит то же продолжение
809. **MODE-TUTORIAL-0809** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · тема=neutral [tutorial]: число ходов конечно (партия сходится)
810. **MODE-TUTORIAL-0810** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · тема=neutral [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
811. **MODE-TUTORIAL-0811** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · тема=neutral [tutorial]: ни одной невалидной постановки не принято
812. **MODE-TUTORIAL-0812** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · тема=neutral [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
813. **MODE-TUTORIAL-0813** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · тема=candy [tutorial]: счёт неотрицателен на всём протяжении
814. **MODE-TUTORIAL-0814** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · тема=candy [tutorial]: ни одного исключения/краша за партию
815. **MODE-TUTORIAL-0815** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · тема=candy [tutorial]: resume в середине — бит-в-бит то же продолжение
816. **MODE-TUTORIAL-0816** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · тема=candy [tutorial]: число ходов конечно (партия сходится)
817. **MODE-TUTORIAL-0817** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · тема=candy [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
818. **MODE-TUTORIAL-0818** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · тема=candy [tutorial]: ни одной невалидной постановки не принято
819. **MODE-TUTORIAL-0819** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · тема=candy [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
820. **MODE-TUTORIAL-0820** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · тема=night [tutorial]: счёт неотрицателен на всём протяжении
821. **MODE-TUTORIAL-0821** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · тема=night [tutorial]: ни одного исключения/краша за партию
822. **MODE-TUTORIAL-0822** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · тема=night [tutorial]: resume в середине — бит-в-бит то же продолжение
823. **MODE-TUTORIAL-0823** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · тема=night [tutorial]: число ходов конечно (партия сходится)
824. **MODE-TUTORIAL-0824** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · тема=night [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
825. **MODE-TUTORIAL-0825** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · тема=night [tutorial]: ни одной невалидной постановки не принято
826. **MODE-TUTORIAL-0826** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · тема=night [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
827. **MODE-TUTORIAL-0827** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · reduceMotion=on [tutorial]: счёт неотрицателен на всём протяжении
828. **MODE-TUTORIAL-0828** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · reduceMotion=on [tutorial]: ни одного исключения/краша за партию
829. **MODE-TUTORIAL-0829** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · reduceMotion=on [tutorial]: resume в середине — бит-в-бит то же продолжение
830. **MODE-TUTORIAL-0830** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · reduceMotion=on [tutorial]: число ходов конечно (партия сходится)
831. **MODE-TUTORIAL-0831** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · reduceMotion=on [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
832. **MODE-TUTORIAL-0832** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · reduceMotion=on [tutorial]: ни одной невалидной постановки не принято
833. **MODE-TUTORIAL-0833** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · reduceMotion=on [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
834. **MODE-TUTORIAL-0834** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · узкое окно [tutorial]: счёт неотрицателен на всём протяжении
835. **MODE-TUTORIAL-0835** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · узкое окно [tutorial]: ни одного исключения/краша за партию
836. **MODE-TUTORIAL-0836** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · узкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
837. **MODE-TUTORIAL-0837** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · узкое окно [tutorial]: число ходов конечно (партия сходится)
838. **MODE-TUTORIAL-0838** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · узкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
839. **MODE-TUTORIAL-0839** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · узкое окно [tutorial]: ни одной невалидной постановки не принято
840. **MODE-TUTORIAL-0840** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · узкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
841. **MODE-TUTORIAL-0841** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · широкое окно [tutorial]: счёт неотрицателен на всём протяжении
842. **MODE-TUTORIAL-0842** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · широкое окно [tutorial]: ни одного исключения/краша за партию
843. **MODE-TUTORIAL-0843** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · широкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
844. **MODE-TUTORIAL-0844** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · широкое окно [tutorial]: число ходов конечно (партия сходится)
845. **MODE-TUTORIAL-0845** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · широкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
846. **MODE-TUTORIAL-0846** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · широкое окно [tutorial]: ни одной невалидной постановки не принято
847. **MODE-TUTORIAL-0847** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=17 · широкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
848. **MODE-TUTORIAL-0848** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · тема=neutral [tutorial]: счёт неотрицателен на всём протяжении
849. **MODE-TUTORIAL-0849** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · тема=neutral [tutorial]: ни одного исключения/краша за партию
850. **MODE-TUTORIAL-0850** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · тема=neutral [tutorial]: resume в середине — бит-в-бит то же продолжение
851. **MODE-TUTORIAL-0851** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · тема=neutral [tutorial]: число ходов конечно (партия сходится)
852. **MODE-TUTORIAL-0852** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · тема=neutral [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
853. **MODE-TUTORIAL-0853** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · тема=neutral [tutorial]: ни одной невалидной постановки не принято
854. **MODE-TUTORIAL-0854** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · тема=neutral [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
855. **MODE-TUTORIAL-0855** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · тема=candy [tutorial]: счёт неотрицателен на всём протяжении
856. **MODE-TUTORIAL-0856** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · тема=candy [tutorial]: ни одного исключения/краша за партию
857. **MODE-TUTORIAL-0857** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · тема=candy [tutorial]: resume в середине — бит-в-бит то же продолжение
858. **MODE-TUTORIAL-0858** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · тема=candy [tutorial]: число ходов конечно (партия сходится)
859. **MODE-TUTORIAL-0859** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · тема=candy [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
860. **MODE-TUTORIAL-0860** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · тема=candy [tutorial]: ни одной невалидной постановки не принято
861. **MODE-TUTORIAL-0861** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · тема=candy [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
862. **MODE-TUTORIAL-0862** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · тема=night [tutorial]: счёт неотрицателен на всём протяжении
863. **MODE-TUTORIAL-0863** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · тема=night [tutorial]: ни одного исключения/краша за партию
864. **MODE-TUTORIAL-0864** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · тема=night [tutorial]: resume в середине — бит-в-бит то же продолжение
865. **MODE-TUTORIAL-0865** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · тема=night [tutorial]: число ходов конечно (партия сходится)
866. **MODE-TUTORIAL-0866** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · тема=night [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
867. **MODE-TUTORIAL-0867** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · тема=night [tutorial]: ни одной невалидной постановки не принято
868. **MODE-TUTORIAL-0868** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · тема=night [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
869. **MODE-TUTORIAL-0869** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · reduceMotion=on [tutorial]: счёт неотрицателен на всём протяжении
870. **MODE-TUTORIAL-0870** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · reduceMotion=on [tutorial]: ни одного исключения/краша за партию
871. **MODE-TUTORIAL-0871** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · reduceMotion=on [tutorial]: resume в середине — бит-в-бит то же продолжение
872. **MODE-TUTORIAL-0872** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · reduceMotion=on [tutorial]: число ходов конечно (партия сходится)
873. **MODE-TUTORIAL-0873** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · reduceMotion=on [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
874. **MODE-TUTORIAL-0874** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · reduceMotion=on [tutorial]: ни одной невалидной постановки не принято
875. **MODE-TUTORIAL-0875** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · reduceMotion=on [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
876. **MODE-TUTORIAL-0876** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · узкое окно [tutorial]: счёт неотрицателен на всём протяжении
877. **MODE-TUTORIAL-0877** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · узкое окно [tutorial]: ни одного исключения/краша за партию
878. **MODE-TUTORIAL-0878** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · узкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
879. **MODE-TUTORIAL-0879** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · узкое окно [tutorial]: число ходов конечно (партия сходится)
880. **MODE-TUTORIAL-0880** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · узкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
881. **MODE-TUTORIAL-0881** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · узкое окно [tutorial]: ни одной невалидной постановки не принято
882. **MODE-TUTORIAL-0882** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · узкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
883. **MODE-TUTORIAL-0883** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · широкое окно [tutorial]: счёт неотрицателен на всём протяжении
884. **MODE-TUTORIAL-0884** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · широкое окно [tutorial]: ни одного исключения/краша за партию
885. **MODE-TUTORIAL-0885** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · широкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
886. **MODE-TUTORIAL-0886** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · широкое окно [tutorial]: число ходов конечно (партия сходится)
887. **MODE-TUTORIAL-0887** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · широкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
888. **MODE-TUTORIAL-0888** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · широкое окно [tutorial]: ни одной невалидной постановки не принято
889. **MODE-TUTORIAL-0889** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=18 · широкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
890. **MODE-TUTORIAL-0890** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · тема=neutral [tutorial]: счёт неотрицателен на всём протяжении
891. **MODE-TUTORIAL-0891** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · тема=neutral [tutorial]: ни одного исключения/краша за партию
892. **MODE-TUTORIAL-0892** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · тема=neutral [tutorial]: resume в середине — бит-в-бит то же продолжение
893. **MODE-TUTORIAL-0893** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · тема=neutral [tutorial]: число ходов конечно (партия сходится)
894. **MODE-TUTORIAL-0894** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · тема=neutral [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
895. **MODE-TUTORIAL-0895** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · тема=neutral [tutorial]: ни одной невалидной постановки не принято
896. **MODE-TUTORIAL-0896** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · тема=neutral [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
897. **MODE-TUTORIAL-0897** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · тема=candy [tutorial]: счёт неотрицателен на всём протяжении
898. **MODE-TUTORIAL-0898** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · тема=candy [tutorial]: ни одного исключения/краша за партию
899. **MODE-TUTORIAL-0899** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · тема=candy [tutorial]: resume в середине — бит-в-бит то же продолжение
900. **MODE-TUTORIAL-0900** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · тема=candy [tutorial]: число ходов конечно (партия сходится)
901. **MODE-TUTORIAL-0901** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · тема=candy [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
902. **MODE-TUTORIAL-0902** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · тема=candy [tutorial]: ни одной невалидной постановки не принято
903. **MODE-TUTORIAL-0903** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · тема=candy [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
904. **MODE-TUTORIAL-0904** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · тема=night [tutorial]: счёт неотрицателен на всём протяжении
905. **MODE-TUTORIAL-0905** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · тема=night [tutorial]: ни одного исключения/краша за партию
906. **MODE-TUTORIAL-0906** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · тема=night [tutorial]: resume в середине — бит-в-бит то же продолжение
907. **MODE-TUTORIAL-0907** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · тема=night [tutorial]: число ходов конечно (партия сходится)
908. **MODE-TUTORIAL-0908** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · тема=night [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
909. **MODE-TUTORIAL-0909** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · тема=night [tutorial]: ни одной невалидной постановки не принято
910. **MODE-TUTORIAL-0910** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · тема=night [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
911. **MODE-TUTORIAL-0911** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · reduceMotion=on [tutorial]: счёт неотрицателен на всём протяжении
912. **MODE-TUTORIAL-0912** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · reduceMotion=on [tutorial]: ни одного исключения/краша за партию
913. **MODE-TUTORIAL-0913** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · reduceMotion=on [tutorial]: resume в середине — бит-в-бит то же продолжение
914. **MODE-TUTORIAL-0914** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · reduceMotion=on [tutorial]: число ходов конечно (партия сходится)
915. **MODE-TUTORIAL-0915** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · reduceMotion=on [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
916. **MODE-TUTORIAL-0916** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · reduceMotion=on [tutorial]: ни одной невалидной постановки не принято
917. **MODE-TUTORIAL-0917** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · reduceMotion=on [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
918. **MODE-TUTORIAL-0918** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · узкое окно [tutorial]: счёт неотрицателен на всём протяжении
919. **MODE-TUTORIAL-0919** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · узкое окно [tutorial]: ни одного исключения/краша за партию
920. **MODE-TUTORIAL-0920** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · узкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
921. **MODE-TUTORIAL-0921** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · узкое окно [tutorial]: число ходов конечно (партия сходится)
922. **MODE-TUTORIAL-0922** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · узкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
923. **MODE-TUTORIAL-0923** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · узкое окно [tutorial]: ни одной невалидной постановки не принято
924. **MODE-TUTORIAL-0924** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · узкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
925. **MODE-TUTORIAL-0925** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · широкое окно [tutorial]: счёт неотрицателен на всём протяжении
926. **MODE-TUTORIAL-0926** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · широкое окно [tutorial]: ни одного исключения/краша за партию
927. **MODE-TUTORIAL-0927** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · широкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
928. **MODE-TUTORIAL-0928** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · широкое окно [tutorial]: число ходов конечно (партия сходится)
929. **MODE-TUTORIAL-0929** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · широкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
930. **MODE-TUTORIAL-0930** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · широкое окно [tutorial]: ни одной невалидной постановки не принято
931. **MODE-TUTORIAL-0931** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=19 · широкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
932. **MODE-TUTORIAL-0932** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · тема=neutral [tutorial]: счёт неотрицателен на всём протяжении
933. **MODE-TUTORIAL-0933** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · тема=neutral [tutorial]: ни одного исключения/краша за партию
934. **MODE-TUTORIAL-0934** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · тема=neutral [tutorial]: resume в середине — бит-в-бит то же продолжение
935. **MODE-TUTORIAL-0935** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · тема=neutral [tutorial]: число ходов конечно (партия сходится)
936. **MODE-TUTORIAL-0936** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · тема=neutral [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
937. **MODE-TUTORIAL-0937** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · тема=neutral [tutorial]: ни одной невалидной постановки не принято
938. **MODE-TUTORIAL-0938** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · тема=neutral [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
939. **MODE-TUTORIAL-0939** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · тема=candy [tutorial]: счёт неотрицателен на всём протяжении
940. **MODE-TUTORIAL-0940** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · тема=candy [tutorial]: ни одного исключения/краша за партию
941. **MODE-TUTORIAL-0941** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · тема=candy [tutorial]: resume в середине — бит-в-бит то же продолжение
942. **MODE-TUTORIAL-0942** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · тема=candy [tutorial]: число ходов конечно (партия сходится)
943. **MODE-TUTORIAL-0943** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · тема=candy [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
944. **MODE-TUTORIAL-0944** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · тема=candy [tutorial]: ни одной невалидной постановки не принято
945. **MODE-TUTORIAL-0945** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · тема=candy [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
946. **MODE-TUTORIAL-0946** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · тема=night [tutorial]: счёт неотрицателен на всём протяжении
947. **MODE-TUTORIAL-0947** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · тема=night [tutorial]: ни одного исключения/краша за партию
948. **MODE-TUTORIAL-0948** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · тема=night [tutorial]: resume в середине — бит-в-бит то же продолжение
949. **MODE-TUTORIAL-0949** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · тема=night [tutorial]: число ходов конечно (партия сходится)
950. **MODE-TUTORIAL-0950** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · тема=night [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
951. **MODE-TUTORIAL-0951** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · тема=night [tutorial]: ни одной невалидной постановки не принято
952. **MODE-TUTORIAL-0952** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · тема=night [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
953. **MODE-TUTORIAL-0953** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · reduceMotion=on [tutorial]: счёт неотрицателен на всём протяжении
954. **MODE-TUTORIAL-0954** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · reduceMotion=on [tutorial]: ни одного исключения/краша за партию
955. **MODE-TUTORIAL-0955** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · reduceMotion=on [tutorial]: resume в середине — бит-в-бит то же продолжение
956. **MODE-TUTORIAL-0956** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · reduceMotion=on [tutorial]: число ходов конечно (партия сходится)
957. **MODE-TUTORIAL-0957** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · reduceMotion=on [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
958. **MODE-TUTORIAL-0958** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · reduceMotion=on [tutorial]: ни одной невалидной постановки не принято
959. **MODE-TUTORIAL-0959** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · reduceMotion=on [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
960. **MODE-TUTORIAL-0960** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · узкое окно [tutorial]: счёт неотрицателен на всём протяжении
961. **MODE-TUTORIAL-0961** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · узкое окно [tutorial]: ни одного исключения/краша за партию
962. **MODE-TUTORIAL-0962** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · узкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
963. **MODE-TUTORIAL-0963** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · узкое окно [tutorial]: число ходов конечно (партия сходится)
964. **MODE-TUTORIAL-0964** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · узкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
965. **MODE-TUTORIAL-0965** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · узкое окно [tutorial]: ни одной невалидной постановки не принято
966. **MODE-TUTORIAL-0966** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · узкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
967. **MODE-TUTORIAL-0967** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · широкое окно [tutorial]: счёт неотрицателен на всём протяжении
968. **MODE-TUTORIAL-0968** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · широкое окно [tutorial]: ни одного исключения/краша за партию
969. **MODE-TUTORIAL-0969** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · широкое окно [tutorial]: resume в середине — бит-в-бит то же продолжение
970. **MODE-TUTORIAL-0970** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · широкое окно [tutorial]: число ходов конечно (партия сходится)
971. **MODE-TUTORIAL-0971** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · широкое окно [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
972. **MODE-TUTORIAL-0972** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · широкое окно [tutorial]: ни одной невалидной постановки не принято
973. **MODE-TUTORIAL-0973** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=20 · широкое окно [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
974. **MODE-TUTORIAL-0974** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=21 · тема=neutral [tutorial]: счёт неотрицателен на всём протяжении
975. **MODE-TUTORIAL-0975** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=21 · тема=neutral [tutorial]: ни одного исключения/краша за партию
976. **MODE-TUTORIAL-0976** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=21 · тема=neutral [tutorial]: resume в середине — бит-в-бит то же продолжение
977. **MODE-TUTORIAL-0977** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=21 · тема=neutral [tutorial]: число ходов конечно (партия сходится)
978. **MODE-TUTORIAL-0978** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=21 · тема=neutral [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
979. **MODE-TUTORIAL-0979** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=21 · тема=neutral [tutorial]: ни одной невалидной постановки не принято
980. **MODE-TUTORIAL-0980** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=21 · тема=neutral [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
981. **MODE-TUTORIAL-0981** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=21 · тема=candy [tutorial]: счёт неотрицателен на всём протяжении
982. **MODE-TUTORIAL-0982** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=21 · тема=candy [tutorial]: ни одного исключения/краша за партию
983. **MODE-TUTORIAL-0983** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=21 · тема=candy [tutorial]: resume в середине — бит-в-бит то же продолжение
984. **MODE-TUTORIAL-0984** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=21 · тема=candy [tutorial]: число ходов конечно (партия сходится)
985. **MODE-TUTORIAL-0985** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=21 · тема=candy [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
986. **MODE-TUTORIAL-0986** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=21 · тема=candy [tutorial]: ни одной невалидной постановки не принято
987. **MODE-TUTORIAL-0987** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=21 · тема=candy [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
988. **MODE-TUTORIAL-0988** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=21 · тема=night [tutorial]: счёт неотрицателен на всём протяжении
989. **MODE-TUTORIAL-0989** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=21 · тема=night [tutorial]: ни одного исключения/краша за партию
990. **MODE-TUTORIAL-0990** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=21 · тема=night [tutorial]: resume в середине — бит-в-бит то же продолжение
991. **MODE-TUTORIAL-0991** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=21 · тема=night [tutorial]: число ходов конечно (партия сходится)
992. **MODE-TUTORIAL-0992** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=21 · тема=night [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
993. **MODE-TUTORIAL-0993** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=21 · тема=night [tutorial]: ни одной невалидной постановки не принято
994. **MODE-TUTORIAL-0994** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=21 · тема=night [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
995. **MODE-TUTORIAL-0995** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=21 · reduceMotion=on [tutorial]: счёт неотрицателен на всём протяжении
996. **MODE-TUTORIAL-0996** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=21 · reduceMotion=on [tutorial]: ни одного исключения/краша за партию
997. **MODE-TUTORIAL-0997** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=21 · reduceMotion=on [tutorial]: resume в середине — бит-в-бит то же продолжение
998. **MODE-TUTORIAL-0998** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=21 · reduceMotion=on [tutorial]: число ходов конечно (партия сходится)
999. **MODE-TUTORIAL-0999** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=21 · reduceMotion=on [tutorial]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
1000. **MODE-TUTORIAL-1000** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=21 · reduceMotion=on [tutorial]: ни одной невалидной постановки не принято
1001. **MODE-TUTORIAL-1001** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=21 · reduceMotion=on [tutorial]: инвариант доски: заполненные клетки не «протекают» за границы
