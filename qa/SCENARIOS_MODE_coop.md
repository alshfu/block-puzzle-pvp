# BlockDuel — Каталог сценариев режима «Co-op Tetris (10×20)»

**Ровно 1001 сценариев** режима «Co-op Tetris (10×20)» («1000 и 1»).
Доска: 10×20. Маршрут: `/coop`. Сгенерировано детерминированно
из `tools/gen_scenarios.py` (ось «режим») — НЕ редактировать вручную.

- **ID** `MODE-COOP-0001…MODE-COOP-1001` стабильны между прогонами.
- **[C]** — критический smoke-срез (каждый ~7-й).
- «Механика режима» — уникальная логика; хвост «Property-фаззинг» —
  осмысленная матрица (seed × конфиг × инвариант) для добора до 1001 в
  режимах малой игровой поверхности (это valid property-тесты, не филлер).

## Покрытие по областям

- Запуск и жизненный цикл: 9
- Рендер и темы: 35
- Ввод: 25
- Механика режима: 408
- Прогрессия и рекорды: 4
- Настройки в режиме: 15
- Ошибки и устойчивость: 4
- Property-фаззинг (seed×конфиг): 501

## Сценарии

1. **MODE-COOP-0001** — [C] [Запуск и жизненный цикл] Запуск «Co-op Tetris (10×20)» из меню по маршруту /coop — экран рисуется без ошибок
2. **MODE-COOP-0002** — [Рендер и темы] «Co-op Tetris (10×20)»: поверхность «доска» в теме «neutral» — цвета/контраст по токенам
3. **MODE-COOP-0003** — [Ввод] «Co-op Tetris (10×20)»: выбор и постановка фигуры I через тап — без двойных срабатываний
4. **MODE-COOP-0004** — [Механика режима] Фигура I (0°): постановка в столбец 0 поля 10×20 — валидация
5. **MODE-COOP-0005** — [Прогрессия и рекорды] «Co-op Tetris (10×20)»: завершение партии пишет результат в статистику режима
6. **MODE-COOP-0006** — [Настройки в режиме] «Co-op Tetris (10×20)»: настройка «Звук (soundOn)» применяется в режиме и переживает перезапуск
7. **MODE-COOP-0007** — [Ошибки и устойчивость] «Co-op Tetris (10×20)»: повреждённая сохранёнка → graceful fallback, без краша
8. **MODE-COOP-0008** — [C] [Запуск и жизненный цикл] Возврат из «Co-op Tetris (10×20)» в меню — стек навигации корректен, без утечек
9. **MODE-COOP-0009** — [Рендер и темы] «Co-op Tetris (10×20)»: поверхность «доска» в теме «candy» — цвета/контраст по токенам
10. **MODE-COOP-0010** — [Ввод] «Co-op Tetris (10×20)»: выбор и постановка фигуры O через тап — без двойных срабатываний
11. **MODE-COOP-0011** — [Механика режима] Фигура I (0°): постановка в столбец 1 поля 10×20 — валидация
12. **MODE-COOP-0012** — [Прогрессия и рекорды] «Co-op Tetris (10×20)»: вклад в composite-score (сводный рейтинг) учитывается
13. **MODE-COOP-0013** — [Настройки в режиме] «Co-op Tetris (10×20)»: настройка «Музыка (musicOn)» применяется в режиме и переживает перезапуск
14. **MODE-COOP-0014** — [Ошибки и устойчивость] «Co-op Tetris (10×20)»: сворачивание в момент хода → без рассинхрона состояния
15. **MODE-COOP-0015** — [C] [Запуск и жизненный цикл] Холодный старт «Co-op Tetris (10×20)» в теме «neutral» — без белой вспышки
16. **MODE-COOP-0016** — [Рендер и темы] «Co-op Tetris (10×20)»: поверхность «доска» в теме «night» — цвета/контраст по токенам
17. **MODE-COOP-0017** — [Ввод] «Co-op Tetris (10×20)»: выбор и постановка фигуры T через тап — без двойных срабатываний
18. **MODE-COOP-0018** — [Механика режима] Фигура I (0°): постановка в столбец 2 поля 10×20 — валидация
19. **MODE-COOP-0019** — [Прогрессия и рекорды] «Co-op Tetris (10×20)»: XP по исходу начисляется один раз (ничья ×0.5, поражение 0)
20. **MODE-COOP-0020** — [Настройки в режиме] «Co-op Tetris (10×20)»: настройка «Громкость SFX» применяется в режиме и переживает перезапуск
21. **MODE-COOP-0021** — [Ошибки и устойчивость] «Co-op Tetris (10×20)»: быстрый повторный ввод не даёт двойного хода
22. **MODE-COOP-0022** — [C] [Запуск и жизненный цикл] Холодный старт «Co-op Tetris (10×20)» в теме «candy» — без белой вспышки
23. **MODE-COOP-0023** — [Рендер и темы] «Co-op Tetris (10×20)»: «доска» при reduceMotion=on — статичный декор
24. **MODE-COOP-0024** — [Ввод] «Co-op Tetris (10×20)»: выбор и постановка фигуры S через тап — без двойных срабатываний
25. **MODE-COOP-0025** — [Механика режима] Фигура I (0°): постановка в столбец 3 поля 10×20 — валидация
26. **MODE-COOP-0026** — [Прогрессия и рекорды] «Co-op Tetris (10×20)»: личный рекорд обновляется только при улучшении
27. **MODE-COOP-0027** — [Настройки в режиме] «Co-op Tetris (10×20)»: настройка «Громкость музыки» применяется в режиме и переживает перезапуск
28. **MODE-COOP-0028** — [Ошибки и устойчивость] «Co-op Tetris (10×20)»: возврат/пауза в критический момент не ломает партию
29. **MODE-COOP-0029** — [C] [Запуск и жизненный цикл] Холодный старт «Co-op Tetris (10×20)» в теме «night» — без белой вспышки
30. **MODE-COOP-0030** — [Рендер и темы] «Co-op Tetris (10×20)»: «доска» на узком окне — нет overflow
31. **MODE-COOP-0031** — [Ввод] «Co-op Tetris (10×20)»: выбор и постановка фигуры Z через тап — без двойных срабатываний
32. **MODE-COOP-0032** — [Механика режима] Фигура I (0°): постановка в столбец 4 поля 10×20 — валидация
33. **MODE-COOP-0033** — [Настройки в режиме] «Co-op Tetris (10×20)»: настройка «Вибрация» применяется в режиме и переживает перезапуск
34. **MODE-COOP-0034** — [Запуск и жизненный цикл] Сворачивание во время «Co-op Tetris (10×20)» → resume восстанавливает состояние точно
35. **MODE-COOP-0035** — [Рендер и темы] «Co-op Tetris (10×20)»: «доска» на широком окне — адаптивная боковая раскладка
36. **MODE-COOP-0036** — [C] [Ввод] «Co-op Tetris (10×20)»: выбор и постановка фигуры J через тап — без двойных срабатываний
37. **MODE-COOP-0037** — [Механика режима] Фигура I (0°): постановка в столбец 5 поля 10×20 — валидация
38. **MODE-COOP-0038** — [Настройки в режиме] «Co-op Tetris (10×20)»: настройка «Уменьшить движение (reduceMotion)» применяется в режиме и переживает перезапуск
39. **MODE-COOP-0039** — [Запуск и жизненный цикл] Перезапуск во время «Co-op Tetris (10×20)» → карточка «Продолжить» предлагает resume
40. **MODE-COOP-0040** — [Рендер и темы] «Co-op Tetris (10×20)»: «доска» с нейтральными клетками (a11y) — не зависит от цвета владельца
41. **MODE-COOP-0041** — [Ввод] «Co-op Tetris (10×20)»: выбор и постановка фигуры L через тап — без двойных срабатываний
42. **MODE-COOP-0042** — [Механика режима] Фигура I (0°): постановка в столбец 6 поля 10×20 — валидация
43. **MODE-COOP-0043** — [C] [Настройки в режиме] «Co-op Tetris (10×20)»: настройка «Конфетти» применяется в режиме и переживает перезапуск
44. **MODE-COOP-0044** — [Запуск и жизненный цикл] resume «Co-op Tetris (10×20)» детерминистичен (та же раздача/seed)
45. **MODE-COOP-0045** — [Рендер и темы] «Co-op Tetris (10×20)»: поверхность «HUD (счёт/таймер)» в теме «neutral» — цвета/контраст по токенам
46. **MODE-COOP-0046** — [Ввод] «Co-op Tetris (10×20)»: выбор и постановка фигуры I через drag-and-drop — без двойных срабатываний
47. **MODE-COOP-0047** — [Механика режима] Фигура I (0°): постановка в столбец 7 поля 10×20 — валидация
48. **MODE-COOP-0048** — [Настройки в режиме] «Co-op Tetris (10×20)»: настройка «Маскоты» применяется в режиме и переживает перезапуск
49. **MODE-COOP-0049** — [Запуск и жизненный цикл] Двойной быстрый тап по старту «Co-op Tetris (10×20)» не создаёт две партии
50. **MODE-COOP-0050** — [C] [Рендер и темы] «Co-op Tetris (10×20)»: поверхность «HUD (счёт/таймер)» в теме «candy» — цвета/контраст по токенам
51. **MODE-COOP-0051** — [Ввод] «Co-op Tetris (10×20)»: выбор и постановка фигуры O через drag-and-drop — без двойных срабатываний
52. **MODE-COOP-0052** — [Механика режима] Фигура I (0°): постановка в столбец 8 поля 10×20 — валидация
53. **MODE-COOP-0053** — [Настройки в режиме] «Co-op Tetris (10×20)»: настройка «Призрак фигуры (ghost)» применяется в режиме и переживает перезапуск
54. **MODE-COOP-0054** — [Рендер и темы] «Co-op Tetris (10×20)»: поверхность «HUD (счёт/таймер)» в теме «night» — цвета/контраст по токенам
55. **MODE-COOP-0055** — [Ввод] «Co-op Tetris (10×20)»: выбор и постановка фигуры T через drag-and-drop — без двойных срабатываний
56. **MODE-COOP-0056** — [Механика режима] Фигура I (0°): постановка в столбец 9 поля 10×20 — валидация
57. **MODE-COOP-0057** — [C] [Настройки в режиме] «Co-op Tetris (10×20)»: настройка «Задержка бота» применяется в режиме и переживает перезапуск
58. **MODE-COOP-0058** — [Рендер и темы] «Co-op Tetris (10×20)»: «HUD (счёт/таймер)» при reduceMotion=on — статичный декор
59. **MODE-COOP-0059** — [Ввод] «Co-op Tetris (10×20)»: выбор и постановка фигуры S через drag-and-drop — без двойных срабатываний
60. **MODE-COOP-0060** — [Механика режима] Фигура I (90°): постановка в столбец 0 поля 10×20 — валидация
61. **MODE-COOP-0061** — [Настройки в режиме] «Co-op Tetris (10×20)»: настройка «Тема по умолчанию» применяется в режиме и переживает перезапуск
62. **MODE-COOP-0062** — [Рендер и темы] «Co-op Tetris (10×20)»: «HUD (счёт/таймер)» на узком окне — нет overflow
63. **MODE-COOP-0063** — [Ввод] «Co-op Tetris (10×20)»: выбор и постановка фигуры Z через drag-and-drop — без двойных срабатываний
64. **MODE-COOP-0064** — [C] [Механика режима] Фигура I (90°): постановка в столбец 1 поля 10×20 — валидация
65. **MODE-COOP-0065** — [Настройки в режиме] «Co-op Tetris (10×20)»: настройка «Правила по умолчанию (handSize/rotation/flip)» применяется в режиме и переживает перезапуск
66. **MODE-COOP-0066** — [Рендер и темы] «Co-op Tetris (10×20)»: «HUD (счёт/таймер)» на широком окне — адаптивная боковая раскладка
67. **MODE-COOP-0067** — [Ввод] «Co-op Tetris (10×20)»: выбор и постановка фигуры J через drag-and-drop — без двойных срабатываний
68. **MODE-COOP-0068** — [Механика режима] Фигура I (90°): постановка в столбец 2 поля 10×20 — валидация
69. **MODE-COOP-0069** — [Настройки в режиме] «Co-op Tetris (10×20)»: настройка «Пресет блица» применяется в режиме и переживает перезапуск
70. **MODE-COOP-0070** — [Рендер и темы] «Co-op Tetris (10×20)»: «HUD (счёт/таймер)» с нейтральными клетками (a11y) — не зависит от цвета владельца
71. **MODE-COOP-0071** — [C] [Ввод] «Co-op Tetris (10×20)»: выбор и постановка фигуры L через drag-and-drop — без двойных срабатываний
72. **MODE-COOP-0072** — [Механика режима] Фигура I (90°): постановка в столбец 3 поля 10×20 — валидация
73. **MODE-COOP-0073** — [Настройки в режиме] «Co-op Tetris (10×20)»: настройка «Нейтральные клетки (accessibility)» применяется в режиме и переживает перезапуск
74. **MODE-COOP-0074** — [Рендер и темы] «Co-op Tetris (10×20)»: поверхность «панель руки» в теме «neutral» — цвета/контраст по токенам
75. **MODE-COOP-0075** — [Ввод] «Co-op Tetris (10×20)»: выбор и постановка фигуры I через клавиатура — без двойных срабатываний
76. **MODE-COOP-0076** — [Механика режима] Фигура I (90°): постановка в столбец 4 поля 10×20 — валидация
77. **MODE-COOP-0077** — [Настройки в режиме] «Co-op Tetris (10×20)»: настройка «Язык интерфейса» применяется в режиме и переживает перезапуск
78. **MODE-COOP-0078** — [C] [Рендер и темы] «Co-op Tetris (10×20)»: поверхность «панель руки» в теме «candy» — цвета/контраст по токенам
79. **MODE-COOP-0079** — [Ввод] «Co-op Tetris (10×20)»: выбор и постановка фигуры O через клавиатура — без двойных срабатываний
80. **MODE-COOP-0080** — [Механика режима] Фигура I (90°): постановка в столбец 5 поля 10×20 — валидация
81. **MODE-COOP-0081** — [Рендер и темы] «Co-op Tetris (10×20)»: поверхность «панель руки» в теме «night» — цвета/контраст по токенам
82. **MODE-COOP-0082** — [Ввод] «Co-op Tetris (10×20)»: выбор и постановка фигуры T через клавиатура — без двойных срабатываний
83. **MODE-COOP-0083** — [Механика режима] Фигура I (90°): постановка в столбец 6 поля 10×20 — валидация
84. **MODE-COOP-0084** — [Рендер и темы] «Co-op Tetris (10×20)»: «панель руки» при reduceMotion=on — статичный декор
85. **MODE-COOP-0085** — [C] [Ввод] «Co-op Tetris (10×20)»: выбор и постановка фигуры S через клавиатура — без двойных срабатываний
86. **MODE-COOP-0086** — [Механика режима] Фигура I (90°): постановка в столбец 7 поля 10×20 — валидация
87. **MODE-COOP-0087** — [Рендер и темы] «Co-op Tetris (10×20)»: «панель руки» на узком окне — нет overflow
88. **MODE-COOP-0088** — [Ввод] «Co-op Tetris (10×20)»: выбор и постановка фигуры Z через клавиатура — без двойных срабатываний
89. **MODE-COOP-0089** — [Механика режима] Фигура I (90°): постановка в столбец 8 поля 10×20 — валидация
90. **MODE-COOP-0090** — [Рендер и темы] «Co-op Tetris (10×20)»: «панель руки» на широком окне — адаптивная боковая раскладка
91. **MODE-COOP-0091** — [Ввод] «Co-op Tetris (10×20)»: выбор и постановка фигуры J через клавиатура — без двойных срабатываний
92. **MODE-COOP-0092** — [C] [Механика режима] Фигура I (90°): постановка в столбец 9 поля 10×20 — валидация
93. **MODE-COOP-0093** — [Рендер и темы] «Co-op Tetris (10×20)»: «панель руки» с нейтральными клетками (a11y) — не зависит от цвета владельца
94. **MODE-COOP-0094** — [Ввод] «Co-op Tetris (10×20)»: выбор и постановка фигуры L через клавиатура — без двойных срабатываний
95. **MODE-COOP-0095** — [Механика режима] Фигура I (180°): постановка в столбец 0 поля 10×20 — валидация
96. **MODE-COOP-0096** — [Рендер и темы] «Co-op Tetris (10×20)»: поверхность «экран результата» в теме «neutral» — цвета/контраст по токенам
97. **MODE-COOP-0097** — [Ввод] «Co-op Tetris (10×20)»: поворот выбранной фигуры (если rotation вкл.) меняет ориентацию
98. **MODE-COOP-0098** — [Механика режима] Фигура I (180°): постановка в столбец 1 поля 10×20 — валидация
99. **MODE-COOP-0099** — [C] [Рендер и темы] «Co-op Tetris (10×20)»: поверхность «экран результата» в теме «candy» — цвета/контраст по токенам
100. **MODE-COOP-0100** — [Ввод] «Co-op Tetris (10×20)»: отражение (если flip вкл.) даёт зеркальную фигуру
101. **MODE-COOP-0101** — [Механика режима] Фигура I (180°): постановка в столбец 2 поля 10×20 — валидация
102. **MODE-COOP-0102** — [Рендер и темы] «Co-op Tetris (10×20)»: поверхность «экран результата» в теме «night» — цвета/контраст по токенам
103. **MODE-COOP-0103** — [Ввод] «Co-op Tetris (10×20)»: призрак показывает валидность до отпускания
104. **MODE-COOP-0104** — [Механика режима] Фигура I (180°): постановка в столбец 3 поля 10×20 — валидация
105. **MODE-COOP-0105** — [Рендер и темы] «Co-op Tetris (10×20)»: «экран результата» при reduceMotion=on — статичный декор
106. **MODE-COOP-0106** — [C] [Ввод] «Co-op Tetris (10×20)»: постановка на занятые/за край — отклонена без изменения доски
107. **MODE-COOP-0107** — [Механика режима] Фигура I (180°): постановка в столбец 4 поля 10×20 — валидация
108. **MODE-COOP-0108** — [Рендер и темы] «Co-op Tetris (10×20)»: «экран результата» на узком окне — нет overflow
109. **MODE-COOP-0109** — [Механика режима] Фигура I (180°): постановка в столбец 5 поля 10×20 — валидация
110. **MODE-COOP-0110** — [Рендер и темы] «Co-op Tetris (10×20)»: «экран результата» на широком окне — адаптивная боковая раскладка
111. **MODE-COOP-0111** — [Механика режима] Фигура I (180°): постановка в столбец 6 поля 10×20 — валидация
112. **MODE-COOP-0112** — [Рендер и темы] «Co-op Tetris (10×20)»: «экран результата» с нейтральными клетками (a11y) — не зависит от цвета владельца
113. **MODE-COOP-0113** — [C] [Механика режима] Фигура I (180°): постановка в столбец 7 поля 10×20 — валидация
114. **MODE-COOP-0114** — [Рендер и темы] «Co-op Tetris (10×20)»: поверхность «пауза/меню выхода» в теме «neutral» — цвета/контраст по токенам
115. **MODE-COOP-0115** — [Механика режима] Фигура I (180°): постановка в столбец 8 поля 10×20 — валидация
116. **MODE-COOP-0116** — [Рендер и темы] «Co-op Tetris (10×20)»: поверхность «пауза/меню выхода» в теме «candy» — цвета/контраст по токенам
117. **MODE-COOP-0117** — [Механика режима] Фигура I (180°): постановка в столбец 9 поля 10×20 — валидация
118. **MODE-COOP-0118** — [Рендер и темы] «Co-op Tetris (10×20)»: поверхность «пауза/меню выхода» в теме «night» — цвета/контраст по токенам
119. **MODE-COOP-0119** — [Механика режима] Фигура I (270°): постановка в столбец 0 поля 10×20 — валидация
120. **MODE-COOP-0120** — [C] [Рендер и темы] «Co-op Tetris (10×20)»: «пауза/меню выхода» при reduceMotion=on — статичный декор
121. **MODE-COOP-0121** — [Механика режима] Фигура I (270°): постановка в столбец 1 поля 10×20 — валидация
122. **MODE-COOP-0122** — [Рендер и темы] «Co-op Tetris (10×20)»: «пауза/меню выхода» на узком окне — нет overflow
123. **MODE-COOP-0123** — [Механика режима] Фигура I (270°): постановка в столбец 2 поля 10×20 — валидация
124. **MODE-COOP-0124** — [Рендер и темы] «Co-op Tetris (10×20)»: «пауза/меню выхода» на широком окне — адаптивная боковая раскладка
125. **MODE-COOP-0125** — [Механика режима] Фигура I (270°): постановка в столбец 3 поля 10×20 — валидация
126. **MODE-COOP-0126** — [Рендер и темы] «Co-op Tetris (10×20)»: «пауза/меню выхода» с нейтральными клетками (a11y) — не зависит от цвета владельца
127. **MODE-COOP-0127** — [C] [Механика режима] Фигура I (270°): постановка в столбец 4 поля 10×20 — валидация
128. **MODE-COOP-0128** — [Механика режима] Фигура I (270°): постановка в столбец 5 поля 10×20 — валидация
129. **MODE-COOP-0129** — [Механика режима] Фигура I (270°): постановка в столбец 6 поля 10×20 — валидация
130. **MODE-COOP-0130** — [Механика режима] Фигура I (270°): постановка в столбец 7 поля 10×20 — валидация
131. **MODE-COOP-0131** — [Механика режима] Фигура I (270°): постановка в столбец 8 поля 10×20 — валидация
132. **MODE-COOP-0132** — [Механика режима] Фигура I (270°): постановка в столбец 9 поля 10×20 — валидация
133. **MODE-COOP-0133** — [Механика режима] Фигура O (0°): постановка в столбец 0 поля 10×20 — валидация
134. **MODE-COOP-0134** — [C] [Механика режима] Фигура O (0°): постановка в столбец 1 поля 10×20 — валидация
135. **MODE-COOP-0135** — [Механика режима] Фигура O (0°): постановка в столбец 2 поля 10×20 — валидация
136. **MODE-COOP-0136** — [Механика режима] Фигура O (0°): постановка в столбец 3 поля 10×20 — валидация
137. **MODE-COOP-0137** — [Механика режима] Фигура O (0°): постановка в столбец 4 поля 10×20 — валидация
138. **MODE-COOP-0138** — [Механика режима] Фигура O (0°): постановка в столбец 5 поля 10×20 — валидация
139. **MODE-COOP-0139** — [Механика режима] Фигура O (0°): постановка в столбец 6 поля 10×20 — валидация
140. **MODE-COOP-0140** — [Механика режима] Фигура O (0°): постановка в столбец 7 поля 10×20 — валидация
141. **MODE-COOP-0141** — [C] [Механика режима] Фигура O (0°): постановка в столбец 8 поля 10×20 — валидация
142. **MODE-COOP-0142** — [Механика режима] Фигура O (0°): постановка в столбец 9 поля 10×20 — валидация
143. **MODE-COOP-0143** — [Механика режима] Фигура O (90°): постановка в столбец 0 поля 10×20 — валидация
144. **MODE-COOP-0144** — [Механика режима] Фигура O (90°): постановка в столбец 1 поля 10×20 — валидация
145. **MODE-COOP-0145** — [Механика режима] Фигура O (90°): постановка в столбец 2 поля 10×20 — валидация
146. **MODE-COOP-0146** — [Механика режима] Фигура O (90°): постановка в столбец 3 поля 10×20 — валидация
147. **MODE-COOP-0147** — [Механика режима] Фигура O (90°): постановка в столбец 4 поля 10×20 — валидация
148. **MODE-COOP-0148** — [C] [Механика режима] Фигура O (90°): постановка в столбец 5 поля 10×20 — валидация
149. **MODE-COOP-0149** — [Механика режима] Фигура O (90°): постановка в столбец 6 поля 10×20 — валидация
150. **MODE-COOP-0150** — [Механика режима] Фигура O (90°): постановка в столбец 7 поля 10×20 — валидация
151. **MODE-COOP-0151** — [Механика режима] Фигура O (90°): постановка в столбец 8 поля 10×20 — валидация
152. **MODE-COOP-0152** — [Механика режима] Фигура O (90°): постановка в столбец 9 поля 10×20 — валидация
153. **MODE-COOP-0153** — [Механика режима] Фигура O (180°): постановка в столбец 0 поля 10×20 — валидация
154. **MODE-COOP-0154** — [Механика режима] Фигура O (180°): постановка в столбец 1 поля 10×20 — валидация
155. **MODE-COOP-0155** — [C] [Механика режима] Фигура O (180°): постановка в столбец 2 поля 10×20 — валидация
156. **MODE-COOP-0156** — [Механика режима] Фигура O (180°): постановка в столбец 3 поля 10×20 — валидация
157. **MODE-COOP-0157** — [Механика режима] Фигура O (180°): постановка в столбец 4 поля 10×20 — валидация
158. **MODE-COOP-0158** — [Механика режима] Фигура O (180°): постановка в столбец 5 поля 10×20 — валидация
159. **MODE-COOP-0159** — [Механика режима] Фигура O (180°): постановка в столбец 6 поля 10×20 — валидация
160. **MODE-COOP-0160** — [Механика режима] Фигура O (180°): постановка в столбец 7 поля 10×20 — валидация
161. **MODE-COOP-0161** — [Механика режима] Фигура O (180°): постановка в столбец 8 поля 10×20 — валидация
162. **MODE-COOP-0162** — [C] [Механика режима] Фигура O (180°): постановка в столбец 9 поля 10×20 — валидация
163. **MODE-COOP-0163** — [Механика режима] Фигура O (270°): постановка в столбец 0 поля 10×20 — валидация
164. **MODE-COOP-0164** — [Механика режима] Фигура O (270°): постановка в столбец 1 поля 10×20 — валидация
165. **MODE-COOP-0165** — [Механика режима] Фигура O (270°): постановка в столбец 2 поля 10×20 — валидация
166. **MODE-COOP-0166** — [Механика режима] Фигура O (270°): постановка в столбец 3 поля 10×20 — валидация
167. **MODE-COOP-0167** — [Механика режима] Фигура O (270°): постановка в столбец 4 поля 10×20 — валидация
168. **MODE-COOP-0168** — [Механика режима] Фигура O (270°): постановка в столбец 5 поля 10×20 — валидация
169. **MODE-COOP-0169** — [C] [Механика режима] Фигура O (270°): постановка в столбец 6 поля 10×20 — валидация
170. **MODE-COOP-0170** — [Механика режима] Фигура O (270°): постановка в столбец 7 поля 10×20 — валидация
171. **MODE-COOP-0171** — [Механика режима] Фигура O (270°): постановка в столбец 8 поля 10×20 — валидация
172. **MODE-COOP-0172** — [Механика режима] Фигура O (270°): постановка в столбец 9 поля 10×20 — валидация
173. **MODE-COOP-0173** — [Механика режима] Фигура T (0°): постановка в столбец 0 поля 10×20 — валидация
174. **MODE-COOP-0174** — [Механика режима] Фигура T (0°): постановка в столбец 1 поля 10×20 — валидация
175. **MODE-COOP-0175** — [Механика режима] Фигура T (0°): постановка в столбец 2 поля 10×20 — валидация
176. **MODE-COOP-0176** — [C] [Механика режима] Фигура T (0°): постановка в столбец 3 поля 10×20 — валидация
177. **MODE-COOP-0177** — [Механика режима] Фигура T (0°): постановка в столбец 4 поля 10×20 — валидация
178. **MODE-COOP-0178** — [Механика режима] Фигура T (0°): постановка в столбец 5 поля 10×20 — валидация
179. **MODE-COOP-0179** — [Механика режима] Фигура T (0°): постановка в столбец 6 поля 10×20 — валидация
180. **MODE-COOP-0180** — [Механика режима] Фигура T (0°): постановка в столбец 7 поля 10×20 — валидация
181. **MODE-COOP-0181** — [Механика режима] Фигура T (0°): постановка в столбец 8 поля 10×20 — валидация
182. **MODE-COOP-0182** — [Механика режима] Фигура T (0°): постановка в столбец 9 поля 10×20 — валидация
183. **MODE-COOP-0183** — [C] [Механика режима] Фигура T (90°): постановка в столбец 0 поля 10×20 — валидация
184. **MODE-COOP-0184** — [Механика режима] Фигура T (90°): постановка в столбец 1 поля 10×20 — валидация
185. **MODE-COOP-0185** — [Механика режима] Фигура T (90°): постановка в столбец 2 поля 10×20 — валидация
186. **MODE-COOP-0186** — [Механика режима] Фигура T (90°): постановка в столбец 3 поля 10×20 — валидация
187. **MODE-COOP-0187** — [Механика режима] Фигура T (90°): постановка в столбец 4 поля 10×20 — валидация
188. **MODE-COOP-0188** — [Механика режима] Фигура T (90°): постановка в столбец 5 поля 10×20 — валидация
189. **MODE-COOP-0189** — [Механика режима] Фигура T (90°): постановка в столбец 6 поля 10×20 — валидация
190. **MODE-COOP-0190** — [C] [Механика режима] Фигура T (90°): постановка в столбец 7 поля 10×20 — валидация
191. **MODE-COOP-0191** — [Механика режима] Фигура T (90°): постановка в столбец 8 поля 10×20 — валидация
192. **MODE-COOP-0192** — [Механика режима] Фигура T (90°): постановка в столбец 9 поля 10×20 — валидация
193. **MODE-COOP-0193** — [Механика режима] Фигура T (180°): постановка в столбец 0 поля 10×20 — валидация
194. **MODE-COOP-0194** — [Механика режима] Фигура T (180°): постановка в столбец 1 поля 10×20 — валидация
195. **MODE-COOP-0195** — [Механика режима] Фигура T (180°): постановка в столбец 2 поля 10×20 — валидация
196. **MODE-COOP-0196** — [Механика режима] Фигура T (180°): постановка в столбец 3 поля 10×20 — валидация
197. **MODE-COOP-0197** — [C] [Механика режима] Фигура T (180°): постановка в столбец 4 поля 10×20 — валидация
198. **MODE-COOP-0198** — [Механика режима] Фигура T (180°): постановка в столбец 5 поля 10×20 — валидация
199. **MODE-COOP-0199** — [Механика режима] Фигура T (180°): постановка в столбец 6 поля 10×20 — валидация
200. **MODE-COOP-0200** — [Механика режима] Фигура T (180°): постановка в столбец 7 поля 10×20 — валидация
201. **MODE-COOP-0201** — [Механика режима] Фигура T (180°): постановка в столбец 8 поля 10×20 — валидация
202. **MODE-COOP-0202** — [Механика режима] Фигура T (180°): постановка в столбец 9 поля 10×20 — валидация
203. **MODE-COOP-0203** — [Механика режима] Фигура T (270°): постановка в столбец 0 поля 10×20 — валидация
204. **MODE-COOP-0204** — [C] [Механика режима] Фигура T (270°): постановка в столбец 1 поля 10×20 — валидация
205. **MODE-COOP-0205** — [Механика режима] Фигура T (270°): постановка в столбец 2 поля 10×20 — валидация
206. **MODE-COOP-0206** — [Механика режима] Фигура T (270°): постановка в столбец 3 поля 10×20 — валидация
207. **MODE-COOP-0207** — [Механика режима] Фигура T (270°): постановка в столбец 4 поля 10×20 — валидация
208. **MODE-COOP-0208** — [Механика режима] Фигура T (270°): постановка в столбец 5 поля 10×20 — валидация
209. **MODE-COOP-0209** — [Механика режима] Фигура T (270°): постановка в столбец 6 поля 10×20 — валидация
210. **MODE-COOP-0210** — [Механика режима] Фигура T (270°): постановка в столбец 7 поля 10×20 — валидация
211. **MODE-COOP-0211** — [C] [Механика режима] Фигура T (270°): постановка в столбец 8 поля 10×20 — валидация
212. **MODE-COOP-0212** — [Механика режима] Фигура T (270°): постановка в столбец 9 поля 10×20 — валидация
213. **MODE-COOP-0213** — [Механика режима] Фигура S (0°): постановка в столбец 0 поля 10×20 — валидация
214. **MODE-COOP-0214** — [Механика режима] Фигура S (0°): постановка в столбец 1 поля 10×20 — валидация
215. **MODE-COOP-0215** — [Механика режима] Фигура S (0°): постановка в столбец 2 поля 10×20 — валидация
216. **MODE-COOP-0216** — [Механика режима] Фигура S (0°): постановка в столбец 3 поля 10×20 — валидация
217. **MODE-COOP-0217** — [Механика режима] Фигура S (0°): постановка в столбец 4 поля 10×20 — валидация
218. **MODE-COOP-0218** — [C] [Механика режима] Фигура S (0°): постановка в столбец 5 поля 10×20 — валидация
219. **MODE-COOP-0219** — [Механика режима] Фигура S (0°): постановка в столбец 6 поля 10×20 — валидация
220. **MODE-COOP-0220** — [Механика режима] Фигура S (0°): постановка в столбец 7 поля 10×20 — валидация
221. **MODE-COOP-0221** — [Механика режима] Фигура S (0°): постановка в столбец 8 поля 10×20 — валидация
222. **MODE-COOP-0222** — [Механика режима] Фигура S (0°): постановка в столбец 9 поля 10×20 — валидация
223. **MODE-COOP-0223** — [Механика режима] Фигура S (90°): постановка в столбец 0 поля 10×20 — валидация
224. **MODE-COOP-0224** — [Механика режима] Фигура S (90°): постановка в столбец 1 поля 10×20 — валидация
225. **MODE-COOP-0225** — [C] [Механика режима] Фигура S (90°): постановка в столбец 2 поля 10×20 — валидация
226. **MODE-COOP-0226** — [Механика режима] Фигура S (90°): постановка в столбец 3 поля 10×20 — валидация
227. **MODE-COOP-0227** — [Механика режима] Фигура S (90°): постановка в столбец 4 поля 10×20 — валидация
228. **MODE-COOP-0228** — [Механика режима] Фигура S (90°): постановка в столбец 5 поля 10×20 — валидация
229. **MODE-COOP-0229** — [Механика режима] Фигура S (90°): постановка в столбец 6 поля 10×20 — валидация
230. **MODE-COOP-0230** — [Механика режима] Фигура S (90°): постановка в столбец 7 поля 10×20 — валидация
231. **MODE-COOP-0231** — [Механика режима] Фигура S (90°): постановка в столбец 8 поля 10×20 — валидация
232. **MODE-COOP-0232** — [C] [Механика режима] Фигура S (90°): постановка в столбец 9 поля 10×20 — валидация
233. **MODE-COOP-0233** — [Механика режима] Фигура S (180°): постановка в столбец 0 поля 10×20 — валидация
234. **MODE-COOP-0234** — [Механика режима] Фигура S (180°): постановка в столбец 1 поля 10×20 — валидация
235. **MODE-COOP-0235** — [Механика режима] Фигура S (180°): постановка в столбец 2 поля 10×20 — валидация
236. **MODE-COOP-0236** — [Механика режима] Фигура S (180°): постановка в столбец 3 поля 10×20 — валидация
237. **MODE-COOP-0237** — [Механика режима] Фигура S (180°): постановка в столбец 4 поля 10×20 — валидация
238. **MODE-COOP-0238** — [Механика режима] Фигура S (180°): постановка в столбец 5 поля 10×20 — валидация
239. **MODE-COOP-0239** — [C] [Механика режима] Фигура S (180°): постановка в столбец 6 поля 10×20 — валидация
240. **MODE-COOP-0240** — [Механика режима] Фигура S (180°): постановка в столбец 7 поля 10×20 — валидация
241. **MODE-COOP-0241** — [Механика режима] Фигура S (180°): постановка в столбец 8 поля 10×20 — валидация
242. **MODE-COOP-0242** — [Механика режима] Фигура S (180°): постановка в столбец 9 поля 10×20 — валидация
243. **MODE-COOP-0243** — [Механика режима] Фигура S (270°): постановка в столбец 0 поля 10×20 — валидация
244. **MODE-COOP-0244** — [Механика режима] Фигура S (270°): постановка в столбец 1 поля 10×20 — валидация
245. **MODE-COOP-0245** — [Механика режима] Фигура S (270°): постановка в столбец 2 поля 10×20 — валидация
246. **MODE-COOP-0246** — [C] [Механика режима] Фигура S (270°): постановка в столбец 3 поля 10×20 — валидация
247. **MODE-COOP-0247** — [Механика режима] Фигура S (270°): постановка в столбец 4 поля 10×20 — валидация
248. **MODE-COOP-0248** — [Механика режима] Фигура S (270°): постановка в столбец 5 поля 10×20 — валидация
249. **MODE-COOP-0249** — [Механика режима] Фигура S (270°): постановка в столбец 6 поля 10×20 — валидация
250. **MODE-COOP-0250** — [Механика режима] Фигура S (270°): постановка в столбец 7 поля 10×20 — валидация
251. **MODE-COOP-0251** — [Механика режима] Фигура S (270°): постановка в столбец 8 поля 10×20 — валидация
252. **MODE-COOP-0252** — [Механика режима] Фигура S (270°): постановка в столбец 9 поля 10×20 — валидация
253. **MODE-COOP-0253** — [C] [Механика режима] Фигура Z (0°): постановка в столбец 0 поля 10×20 — валидация
254. **MODE-COOP-0254** — [Механика режима] Фигура Z (0°): постановка в столбец 1 поля 10×20 — валидация
255. **MODE-COOP-0255** — [Механика режима] Фигура Z (0°): постановка в столбец 2 поля 10×20 — валидация
256. **MODE-COOP-0256** — [Механика режима] Фигура Z (0°): постановка в столбец 3 поля 10×20 — валидация
257. **MODE-COOP-0257** — [Механика режима] Фигура Z (0°): постановка в столбец 4 поля 10×20 — валидация
258. **MODE-COOP-0258** — [Механика режима] Фигура Z (0°): постановка в столбец 5 поля 10×20 — валидация
259. **MODE-COOP-0259** — [Механика режима] Фигура Z (0°): постановка в столбец 6 поля 10×20 — валидация
260. **MODE-COOP-0260** — [C] [Механика режима] Фигура Z (0°): постановка в столбец 7 поля 10×20 — валидация
261. **MODE-COOP-0261** — [Механика режима] Фигура Z (0°): постановка в столбец 8 поля 10×20 — валидация
262. **MODE-COOP-0262** — [Механика режима] Фигура Z (0°): постановка в столбец 9 поля 10×20 — валидация
263. **MODE-COOP-0263** — [Механика режима] Фигура Z (90°): постановка в столбец 0 поля 10×20 — валидация
264. **MODE-COOP-0264** — [Механика режима] Фигура Z (90°): постановка в столбец 1 поля 10×20 — валидация
265. **MODE-COOP-0265** — [Механика режима] Фигура Z (90°): постановка в столбец 2 поля 10×20 — валидация
266. **MODE-COOP-0266** — [Механика режима] Фигура Z (90°): постановка в столбец 3 поля 10×20 — валидация
267. **MODE-COOP-0267** — [C] [Механика режима] Фигура Z (90°): постановка в столбец 4 поля 10×20 — валидация
268. **MODE-COOP-0268** — [Механика режима] Фигура Z (90°): постановка в столбец 5 поля 10×20 — валидация
269. **MODE-COOP-0269** — [Механика режима] Фигура Z (90°): постановка в столбец 6 поля 10×20 — валидация
270. **MODE-COOP-0270** — [Механика режима] Фигура Z (90°): постановка в столбец 7 поля 10×20 — валидация
271. **MODE-COOP-0271** — [Механика режима] Фигура Z (90°): постановка в столбец 8 поля 10×20 — валидация
272. **MODE-COOP-0272** — [Механика режима] Фигура Z (90°): постановка в столбец 9 поля 10×20 — валидация
273. **MODE-COOP-0273** — [Механика режима] Фигура Z (180°): постановка в столбец 0 поля 10×20 — валидация
274. **MODE-COOP-0274** — [C] [Механика режима] Фигура Z (180°): постановка в столбец 1 поля 10×20 — валидация
275. **MODE-COOP-0275** — [Механика режима] Фигура Z (180°): постановка в столбец 2 поля 10×20 — валидация
276. **MODE-COOP-0276** — [Механика режима] Фигура Z (180°): постановка в столбец 3 поля 10×20 — валидация
277. **MODE-COOP-0277** — [Механика режима] Фигура Z (180°): постановка в столбец 4 поля 10×20 — валидация
278. **MODE-COOP-0278** — [Механика режима] Фигура Z (180°): постановка в столбец 5 поля 10×20 — валидация
279. **MODE-COOP-0279** — [Механика режима] Фигура Z (180°): постановка в столбец 6 поля 10×20 — валидация
280. **MODE-COOP-0280** — [Механика режима] Фигура Z (180°): постановка в столбец 7 поля 10×20 — валидация
281. **MODE-COOP-0281** — [C] [Механика режима] Фигура Z (180°): постановка в столбец 8 поля 10×20 — валидация
282. **MODE-COOP-0282** — [Механика режима] Фигура Z (180°): постановка в столбец 9 поля 10×20 — валидация
283. **MODE-COOP-0283** — [Механика режима] Фигура Z (270°): постановка в столбец 0 поля 10×20 — валидация
284. **MODE-COOP-0284** — [Механика режима] Фигура Z (270°): постановка в столбец 1 поля 10×20 — валидация
285. **MODE-COOP-0285** — [Механика режима] Фигура Z (270°): постановка в столбец 2 поля 10×20 — валидация
286. **MODE-COOP-0286** — [Механика режима] Фигура Z (270°): постановка в столбец 3 поля 10×20 — валидация
287. **MODE-COOP-0287** — [Механика режима] Фигура Z (270°): постановка в столбец 4 поля 10×20 — валидация
288. **MODE-COOP-0288** — [C] [Механика режима] Фигура Z (270°): постановка в столбец 5 поля 10×20 — валидация
289. **MODE-COOP-0289** — [Механика режима] Фигура Z (270°): постановка в столбец 6 поля 10×20 — валидация
290. **MODE-COOP-0290** — [Механика режима] Фигура Z (270°): постановка в столбец 7 поля 10×20 — валидация
291. **MODE-COOP-0291** — [Механика режима] Фигура Z (270°): постановка в столбец 8 поля 10×20 — валидация
292. **MODE-COOP-0292** — [Механика режима] Фигура Z (270°): постановка в столбец 9 поля 10×20 — валидация
293. **MODE-COOP-0293** — [Механика режима] Фигура J (0°): постановка в столбец 0 поля 10×20 — валидация
294. **MODE-COOP-0294** — [Механика режима] Фигура J (0°): постановка в столбец 1 поля 10×20 — валидация
295. **MODE-COOP-0295** — [C] [Механика режима] Фигура J (0°): постановка в столбец 2 поля 10×20 — валидация
296. **MODE-COOP-0296** — [Механика режима] Фигура J (0°): постановка в столбец 3 поля 10×20 — валидация
297. **MODE-COOP-0297** — [Механика режима] Фигура J (0°): постановка в столбец 4 поля 10×20 — валидация
298. **MODE-COOP-0298** — [Механика режима] Фигура J (0°): постановка в столбец 5 поля 10×20 — валидация
299. **MODE-COOP-0299** — [Механика режима] Фигура J (0°): постановка в столбец 6 поля 10×20 — валидация
300. **MODE-COOP-0300** — [Механика режима] Фигура J (0°): постановка в столбец 7 поля 10×20 — валидация
301. **MODE-COOP-0301** — [Механика режима] Фигура J (0°): постановка в столбец 8 поля 10×20 — валидация
302. **MODE-COOP-0302** — [C] [Механика режима] Фигура J (0°): постановка в столбец 9 поля 10×20 — валидация
303. **MODE-COOP-0303** — [Механика режима] Фигура J (90°): постановка в столбец 0 поля 10×20 — валидация
304. **MODE-COOP-0304** — [Механика режима] Фигура J (90°): постановка в столбец 1 поля 10×20 — валидация
305. **MODE-COOP-0305** — [Механика режима] Фигура J (90°): постановка в столбец 2 поля 10×20 — валидация
306. **MODE-COOP-0306** — [Механика режима] Фигура J (90°): постановка в столбец 3 поля 10×20 — валидация
307. **MODE-COOP-0307** — [Механика режима] Фигура J (90°): постановка в столбец 4 поля 10×20 — валидация
308. **MODE-COOP-0308** — [Механика режима] Фигура J (90°): постановка в столбец 5 поля 10×20 — валидация
309. **MODE-COOP-0309** — [C] [Механика режима] Фигура J (90°): постановка в столбец 6 поля 10×20 — валидация
310. **MODE-COOP-0310** — [Механика режима] Фигура J (90°): постановка в столбец 7 поля 10×20 — валидация
311. **MODE-COOP-0311** — [Механика режима] Фигура J (90°): постановка в столбец 8 поля 10×20 — валидация
312. **MODE-COOP-0312** — [Механика режима] Фигура J (90°): постановка в столбец 9 поля 10×20 — валидация
313. **MODE-COOP-0313** — [Механика режима] Фигура J (180°): постановка в столбец 0 поля 10×20 — валидация
314. **MODE-COOP-0314** — [Механика режима] Фигура J (180°): постановка в столбец 1 поля 10×20 — валидация
315. **MODE-COOP-0315** — [Механика режима] Фигура J (180°): постановка в столбец 2 поля 10×20 — валидация
316. **MODE-COOP-0316** — [C] [Механика режима] Фигура J (180°): постановка в столбец 3 поля 10×20 — валидация
317. **MODE-COOP-0317** — [Механика режима] Фигура J (180°): постановка в столбец 4 поля 10×20 — валидация
318. **MODE-COOP-0318** — [Механика режима] Фигура J (180°): постановка в столбец 5 поля 10×20 — валидация
319. **MODE-COOP-0319** — [Механика режима] Фигура J (180°): постановка в столбец 6 поля 10×20 — валидация
320. **MODE-COOP-0320** — [Механика режима] Фигура J (180°): постановка в столбец 7 поля 10×20 — валидация
321. **MODE-COOP-0321** — [Механика режима] Фигура J (180°): постановка в столбец 8 поля 10×20 — валидация
322. **MODE-COOP-0322** — [Механика режима] Фигура J (180°): постановка в столбец 9 поля 10×20 — валидация
323. **MODE-COOP-0323** — [C] [Механика режима] Фигура J (270°): постановка в столбец 0 поля 10×20 — валидация
324. **MODE-COOP-0324** — [Механика режима] Фигура J (270°): постановка в столбец 1 поля 10×20 — валидация
325. **MODE-COOP-0325** — [Механика режима] Фигура J (270°): постановка в столбец 2 поля 10×20 — валидация
326. **MODE-COOP-0326** — [Механика режима] Фигура J (270°): постановка в столбец 3 поля 10×20 — валидация
327. **MODE-COOP-0327** — [Механика режима] Фигура J (270°): постановка в столбец 4 поля 10×20 — валидация
328. **MODE-COOP-0328** — [Механика режима] Фигура J (270°): постановка в столбец 5 поля 10×20 — валидация
329. **MODE-COOP-0329** — [Механика режима] Фигура J (270°): постановка в столбец 6 поля 10×20 — валидация
330. **MODE-COOP-0330** — [C] [Механика режима] Фигура J (270°): постановка в столбец 7 поля 10×20 — валидация
331. **MODE-COOP-0331** — [Механика режима] Фигура J (270°): постановка в столбец 8 поля 10×20 — валидация
332. **MODE-COOP-0332** — [Механика режима] Фигура J (270°): постановка в столбец 9 поля 10×20 — валидация
333. **MODE-COOP-0333** — [Механика режима] Фигура L (0°): постановка в столбец 0 поля 10×20 — валидация
334. **MODE-COOP-0334** — [Механика режима] Фигура L (0°): постановка в столбец 1 поля 10×20 — валидация
335. **MODE-COOP-0335** — [Механика режима] Фигура L (0°): постановка в столбец 2 поля 10×20 — валидация
336. **MODE-COOP-0336** — [Механика режима] Фигура L (0°): постановка в столбец 3 поля 10×20 — валидация
337. **MODE-COOP-0337** — [C] [Механика режима] Фигура L (0°): постановка в столбец 4 поля 10×20 — валидация
338. **MODE-COOP-0338** — [Механика режима] Фигура L (0°): постановка в столбец 5 поля 10×20 — валидация
339. **MODE-COOP-0339** — [Механика режима] Фигура L (0°): постановка в столбец 6 поля 10×20 — валидация
340. **MODE-COOP-0340** — [Механика режима] Фигура L (0°): постановка в столбец 7 поля 10×20 — валидация
341. **MODE-COOP-0341** — [Механика режима] Фигура L (0°): постановка в столбец 8 поля 10×20 — валидация
342. **MODE-COOP-0342** — [Механика режима] Фигура L (0°): постановка в столбец 9 поля 10×20 — валидация
343. **MODE-COOP-0343** — [Механика режима] Фигура L (90°): постановка в столбец 0 поля 10×20 — валидация
344. **MODE-COOP-0344** — [C] [Механика режима] Фигура L (90°): постановка в столбец 1 поля 10×20 — валидация
345. **MODE-COOP-0345** — [Механика режима] Фигура L (90°): постановка в столбец 2 поля 10×20 — валидация
346. **MODE-COOP-0346** — [Механика режима] Фигура L (90°): постановка в столбец 3 поля 10×20 — валидация
347. **MODE-COOP-0347** — [Механика режима] Фигура L (90°): постановка в столбец 4 поля 10×20 — валидация
348. **MODE-COOP-0348** — [Механика режима] Фигура L (90°): постановка в столбец 5 поля 10×20 — валидация
349. **MODE-COOP-0349** — [Механика режима] Фигура L (90°): постановка в столбец 6 поля 10×20 — валидация
350. **MODE-COOP-0350** — [Механика режима] Фигура L (90°): постановка в столбец 7 поля 10×20 — валидация
351. **MODE-COOP-0351** — [C] [Механика режима] Фигура L (90°): постановка в столбец 8 поля 10×20 — валидация
352. **MODE-COOP-0352** — [Механика режима] Фигура L (90°): постановка в столбец 9 поля 10×20 — валидация
353. **MODE-COOP-0353** — [Механика режима] Фигура L (180°): постановка в столбец 0 поля 10×20 — валидация
354. **MODE-COOP-0354** — [Механика режима] Фигура L (180°): постановка в столбец 1 поля 10×20 — валидация
355. **MODE-COOP-0355** — [Механика режима] Фигура L (180°): постановка в столбец 2 поля 10×20 — валидация
356. **MODE-COOP-0356** — [Механика режима] Фигура L (180°): постановка в столбец 3 поля 10×20 — валидация
357. **MODE-COOP-0357** — [Механика режима] Фигура L (180°): постановка в столбец 4 поля 10×20 — валидация
358. **MODE-COOP-0358** — [C] [Механика режима] Фигура L (180°): постановка в столбец 5 поля 10×20 — валидация
359. **MODE-COOP-0359** — [Механика режима] Фигура L (180°): постановка в столбец 6 поля 10×20 — валидация
360. **MODE-COOP-0360** — [Механика режима] Фигура L (180°): постановка в столбец 7 поля 10×20 — валидация
361. **MODE-COOP-0361** — [Механика режима] Фигура L (180°): постановка в столбец 8 поля 10×20 — валидация
362. **MODE-COOP-0362** — [Механика режима] Фигура L (180°): постановка в столбец 9 поля 10×20 — валидация
363. **MODE-COOP-0363** — [Механика режима] Фигура L (270°): постановка в столбец 0 поля 10×20 — валидация
364. **MODE-COOP-0364** — [Механика режима] Фигура L (270°): постановка в столбец 1 поля 10×20 — валидация
365. **MODE-COOP-0365** — [C] [Механика режима] Фигура L (270°): постановка в столбец 2 поля 10×20 — валидация
366. **MODE-COOP-0366** — [Механика режима] Фигура L (270°): постановка в столбец 3 поля 10×20 — валидация
367. **MODE-COOP-0367** — [Механика режима] Фигура L (270°): постановка в столбец 4 поля 10×20 — валидация
368. **MODE-COOP-0368** — [Механика режима] Фигура L (270°): постановка в столбец 5 поля 10×20 — валидация
369. **MODE-COOP-0369** — [Механика режима] Фигура L (270°): постановка в столбец 6 поля 10×20 — валидация
370. **MODE-COOP-0370** — [Механика режима] Фигура L (270°): постановка в столбец 7 поля 10×20 — валидация
371. **MODE-COOP-0371** — [Механика режима] Фигура L (270°): постановка в столбец 8 поля 10×20 — валидация
372. **MODE-COOP-0372** — [C] [Механика режима] Фигура L (270°): постановка в столбец 9 поля 10×20 — валидация
373. **MODE-COOP-0373** — [Механика режима] Столбец 0: фигура ложится на высоту ~0 без наложения
374. **MODE-COOP-0374** — [Механика режима] Столбец 0: фигура ложится на высоту ~2 без наложения
375. **MODE-COOP-0375** — [Механика режима] Столбец 0: фигура ложится на высоту ~4 без наложения
376. **MODE-COOP-0376** — [Механика режима] Столбец 0: фигура ложится на высоту ~6 без наложения
377. **MODE-COOP-0377** — [Механика режима] Столбец 0: фигура ложится на высоту ~8 без наложения
378. **MODE-COOP-0378** — [Механика режима] Столбец 0: фигура ложится на высоту ~10 без наложения
379. **MODE-COOP-0379** — [C] [Механика режима] Столбец 0: фигура ложится на высоту ~12 без наложения
380. **MODE-COOP-0380** — [Механика режима] Столбец 0: фигура ложится на высоту ~14 без наложения
381. **MODE-COOP-0381** — [Механика режима] Столбец 0: фигура ложится на высоту ~16 без наложения
382. **MODE-COOP-0382** — [Механика режима] Столбец 0: фигура ложится на высоту ~18 без наложения
383. **MODE-COOP-0383** — [Механика режима] Столбец 1: фигура ложится на высоту ~0 без наложения
384. **MODE-COOP-0384** — [Механика режима] Столбец 1: фигура ложится на высоту ~2 без наложения
385. **MODE-COOP-0385** — [Механика режима] Столбец 1: фигура ложится на высоту ~4 без наложения
386. **MODE-COOP-0386** — [C] [Механика режима] Столбец 1: фигура ложится на высоту ~6 без наложения
387. **MODE-COOP-0387** — [Механика режима] Столбец 1: фигура ложится на высоту ~8 без наложения
388. **MODE-COOP-0388** — [Механика режима] Столбец 1: фигура ложится на высоту ~10 без наложения
389. **MODE-COOP-0389** — [Механика режима] Столбец 1: фигура ложится на высоту ~12 без наложения
390. **MODE-COOP-0390** — [Механика режима] Столбец 1: фигура ложится на высоту ~14 без наложения
391. **MODE-COOP-0391** — [Механика режима] Столбец 1: фигура ложится на высоту ~16 без наложения
392. **MODE-COOP-0392** — [Механика режима] Столбец 1: фигура ложится на высоту ~18 без наложения
393. **MODE-COOP-0393** — [C] [Механика режима] Столбец 2: фигура ложится на высоту ~0 без наложения
394. **MODE-COOP-0394** — [Механика режима] Столбец 2: фигура ложится на высоту ~2 без наложения
395. **MODE-COOP-0395** — [Механика режима] Столбец 2: фигура ложится на высоту ~4 без наложения
396. **MODE-COOP-0396** — [Механика режима] Столбец 2: фигура ложится на высоту ~6 без наложения
397. **MODE-COOP-0397** — [Механика режима] Столбец 2: фигура ложится на высоту ~8 без наложения
398. **MODE-COOP-0398** — [Механика режима] Столбец 2: фигура ложится на высоту ~10 без наложения
399. **MODE-COOP-0399** — [Механика режима] Столбец 2: фигура ложится на высоту ~12 без наложения
400. **MODE-COOP-0400** — [C] [Механика режима] Столбец 2: фигура ложится на высоту ~14 без наложения
401. **MODE-COOP-0401** — [Механика режима] Столбец 2: фигура ложится на высоту ~16 без наложения
402. **MODE-COOP-0402** — [Механика режима] Столбец 2: фигура ложится на высоту ~18 без наложения
403. **MODE-COOP-0403** — [Механика режима] Столбец 3: фигура ложится на высоту ~0 без наложения
404. **MODE-COOP-0404** — [Механика режима] Столбец 3: фигура ложится на высоту ~2 без наложения
405. **MODE-COOP-0405** — [Механика режима] Столбец 3: фигура ложится на высоту ~4 без наложения
406. **MODE-COOP-0406** — [Механика режима] Столбец 3: фигура ложится на высоту ~6 без наложения
407. **MODE-COOP-0407** — [C] [Механика режима] Столбец 3: фигура ложится на высоту ~8 без наложения
408. **MODE-COOP-0408** — [Механика режима] Столбец 3: фигура ложится на высоту ~10 без наложения
409. **MODE-COOP-0409** — [Механика режима] Столбец 3: фигура ложится на высоту ~12 без наложения
410. **MODE-COOP-0410** — [Механика режима] Столбец 3: фигура ложится на высоту ~14 без наложения
411. **MODE-COOP-0411** — [Механика режима] Столбец 3: фигура ложится на высоту ~16 без наложения
412. **MODE-COOP-0412** — [Механика режима] Столбец 3: фигура ложится на высоту ~18 без наложения
413. **MODE-COOP-0413** — [Механика режима] Столбец 4: фигура ложится на высоту ~0 без наложения
414. **MODE-COOP-0414** — [C] [Механика режима] Столбец 4: фигура ложится на высоту ~2 без наложения
415. **MODE-COOP-0415** — [Механика режима] Столбец 4: фигура ложится на высоту ~4 без наложения
416. **MODE-COOP-0416** — [Механика режима] Столбец 4: фигура ложится на высоту ~6 без наложения
417. **MODE-COOP-0417** — [Механика режима] Столбец 4: фигура ложится на высоту ~8 без наложения
418. **MODE-COOP-0418** — [Механика режима] Столбец 4: фигура ложится на высоту ~10 без наложения
419. **MODE-COOP-0419** — [Механика режима] Столбец 4: фигура ложится на высоту ~12 без наложения
420. **MODE-COOP-0420** — [Механика режима] Столбец 4: фигура ложится на высоту ~14 без наложения
421. **MODE-COOP-0421** — [C] [Механика режима] Столбец 4: фигура ложится на высоту ~16 без наложения
422. **MODE-COOP-0422** — [Механика режима] Столбец 4: фигура ложится на высоту ~18 без наложения
423. **MODE-COOP-0423** — [Механика режима] Столбец 5: фигура ложится на высоту ~0 без наложения
424. **MODE-COOP-0424** — [Механика режима] Столбец 5: фигура ложится на высоту ~2 без наложения
425. **MODE-COOP-0425** — [Механика режима] Столбец 5: фигура ложится на высоту ~4 без наложения
426. **MODE-COOP-0426** — [Механика режима] Столбец 5: фигура ложится на высоту ~6 без наложения
427. **MODE-COOP-0427** — [Механика режима] Столбец 5: фигура ложится на высоту ~8 без наложения
428. **MODE-COOP-0428** — [C] [Механика режима] Столбец 5: фигура ложится на высоту ~10 без наложения
429. **MODE-COOP-0429** — [Механика режима] Столбец 5: фигура ложится на высоту ~12 без наложения
430. **MODE-COOP-0430** — [Механика режима] Столбец 5: фигура ложится на высоту ~14 без наложения
431. **MODE-COOP-0431** — [Механика режима] Столбец 5: фигура ложится на высоту ~16 без наложения
432. **MODE-COOP-0432** — [Механика режима] Столбец 5: фигура ложится на высоту ~18 без наложения
433. **MODE-COOP-0433** — [Механика режима] Столбец 6: фигура ложится на высоту ~0 без наложения
434. **MODE-COOP-0434** — [Механика режима] Столбец 6: фигура ложится на высоту ~2 без наложения
435. **MODE-COOP-0435** — [C] [Механика режима] Столбец 6: фигура ложится на высоту ~4 без наложения
436. **MODE-COOP-0436** — [Механика режима] Столбец 6: фигура ложится на высоту ~6 без наложения
437. **MODE-COOP-0437** — [Механика режима] Столбец 6: фигура ложится на высоту ~8 без наложения
438. **MODE-COOP-0438** — [Механика режима] Столбец 6: фигура ложится на высоту ~10 без наложения
439. **MODE-COOP-0439** — [Механика режима] Столбец 6: фигура ложится на высоту ~12 без наложения
440. **MODE-COOP-0440** — [Механика режима] Столбец 6: фигура ложится на высоту ~14 без наложения
441. **MODE-COOP-0441** — [Механика режима] Столбец 6: фигура ложится на высоту ~16 без наложения
442. **MODE-COOP-0442** — [C] [Механика режима] Столбец 6: фигура ложится на высоту ~18 без наложения
443. **MODE-COOP-0443** — [Механика режима] Столбец 7: фигура ложится на высоту ~0 без наложения
444. **MODE-COOP-0444** — [Механика режима] Столбец 7: фигура ложится на высоту ~2 без наложения
445. **MODE-COOP-0445** — [Механика режима] Столбец 7: фигура ложится на высоту ~4 без наложения
446. **MODE-COOP-0446** — [Механика режима] Столбец 7: фигура ложится на высоту ~6 без наложения
447. **MODE-COOP-0447** — [Механика режима] Столбец 7: фигура ложится на высоту ~8 без наложения
448. **MODE-COOP-0448** — [Механика режима] Столбец 7: фигура ложится на высоту ~10 без наложения
449. **MODE-COOP-0449** — [C] [Механика режима] Столбец 7: фигура ложится на высоту ~12 без наложения
450. **MODE-COOP-0450** — [Механика режима] Столбец 7: фигура ложится на высоту ~14 без наложения
451. **MODE-COOP-0451** — [Механика режима] Столбец 7: фигура ложится на высоту ~16 без наложения
452. **MODE-COOP-0452** — [Механика режима] Столбец 7: фигура ложится на высоту ~18 без наложения
453. **MODE-COOP-0453** — [Механика режима] Столбец 8: фигура ложится на высоту ~0 без наложения
454. **MODE-COOP-0454** — [Механика режима] Столбец 8: фигура ложится на высоту ~2 без наложения
455. **MODE-COOP-0455** — [Механика режима] Столбец 8: фигура ложится на высоту ~4 без наложения
456. **MODE-COOP-0456** — [C] [Механика режима] Столбец 8: фигура ложится на высоту ~6 без наложения
457. **MODE-COOP-0457** — [Механика режима] Столбец 8: фигура ложится на высоту ~8 без наложения
458. **MODE-COOP-0458** — [Механика режима] Столбец 8: фигура ложится на высоту ~10 без наложения
459. **MODE-COOP-0459** — [Механика режима] Столбец 8: фигура ложится на высоту ~12 без наложения
460. **MODE-COOP-0460** — [Механика режима] Столбец 8: фигура ложится на высоту ~14 без наложения
461. **MODE-COOP-0461** — [Механика режима] Столбец 8: фигура ложится на высоту ~16 без наложения
462. **MODE-COOP-0462** — [Механика режима] Столбец 8: фигура ложится на высоту ~18 без наложения
463. **MODE-COOP-0463** — [C] [Механика режима] Столбец 9: фигура ложится на высоту ~0 без наложения
464. **MODE-COOP-0464** — [Механика режима] Столбец 9: фигура ложится на высоту ~2 без наложения
465. **MODE-COOP-0465** — [Механика режима] Столбец 9: фигура ложится на высоту ~4 без наложения
466. **MODE-COOP-0466** — [Механика режима] Столбец 9: фигура ложится на высоту ~6 без наложения
467. **MODE-COOP-0467** — [Механика режима] Столбец 9: фигура ложится на высоту ~8 без наложения
468. **MODE-COOP-0468** — [Механика режима] Столбец 9: фигура ложится на высоту ~10 без наложения
469. **MODE-COOP-0469** — [Механика режима] Столбец 9: фигура ложится на высоту ~12 без наложения
470. **MODE-COOP-0470** — [C] [Механика режима] Столбец 9: фигура ложится на высоту ~14 без наложения
471. **MODE-COOP-0471** — [Механика режима] Столбец 9: фигура ложится на высоту ~16 без наложения
472. **MODE-COOP-0472** — [Механика режима] Столбец 9: фигура ложится на высоту ~18 без наложения
473. **MODE-COOP-0473** — [Механика режима] Полная строка 0 (10×20): очистка, общий счёт растёт
474. **MODE-COOP-0474** — [Механика режима] Полная строка 1 (10×20): очистка, общий счёт растёт
475. **MODE-COOP-0475** — [Механика режима] Полная строка 2 (10×20): очистка, общий счёт растёт
476. **MODE-COOP-0476** — [Механика режима] Полная строка 3 (10×20): очистка, общий счёт растёт
477. **MODE-COOP-0477** — [C] [Механика режима] Полная строка 4 (10×20): очистка, общий счёт растёт
478. **MODE-COOP-0478** — [Механика режима] Полная строка 5 (10×20): очистка, общий счёт растёт
479. **MODE-COOP-0479** — [Механика режима] Полная строка 6 (10×20): очистка, общий счёт растёт
480. **MODE-COOP-0480** — [Механика режима] Полная строка 7 (10×20): очистка, общий счёт растёт
481. **MODE-COOP-0481** — [Механика режима] Полная строка 8 (10×20): очистка, общий счёт растёт
482. **MODE-COOP-0482** — [Механика режима] Полная строка 9 (10×20): очистка, общий счёт растёт
483. **MODE-COOP-0483** — [Механика режима] Полная строка 10 (10×20): очистка, общий счёт растёт
484. **MODE-COOP-0484** — [C] [Механика режима] Полная строка 11 (10×20): очистка, общий счёт растёт
485. **MODE-COOP-0485** — [Механика режима] Полная строка 12 (10×20): очистка, общий счёт растёт
486. **MODE-COOP-0486** — [Механика режима] Полная строка 13 (10×20): очистка, общий счёт растёт
487. **MODE-COOP-0487** — [Механика режима] Полная строка 14 (10×20): очистка, общий счёт растёт
488. **MODE-COOP-0488** — [Механика режима] Полная строка 15 (10×20): очистка, общий счёт растёт
489. **MODE-COOP-0489** — [Механика режима] Полная строка 16 (10×20): очистка, общий счёт растёт
490. **MODE-COOP-0490** — [Механика режима] Полная строка 17 (10×20): очистка, общий счёт растёт
491. **MODE-COOP-0491** — [C] [Механика режима] Полная строка 18 (10×20): очистка, общий счёт растёт
492. **MODE-COOP-0492** — [Механика режима] Полная строка 19 (10×20): очистка, общий счёт растёт
493. **MODE-COOP-0493** — [Механика режима] Очистка 1 строк за ход: линейная база × бонус +25%/доп.строка
494. **MODE-COOP-0494** — [Механика режима] Очистка 2 строк за ход: линейная база × бонус +25%/доп.строка
495. **MODE-COOP-0495** — [Механика режима] Очистка 3 строк за ход: линейная база × бонус +25%/доп.строка
496. **MODE-COOP-0496** — [Механика режима] Очистка 4 строк за ход: линейная база × бонус +25%/доп.строка
497. **MODE-COOP-0497** — [Механика режима] Ходы по очереди, счёт общий (кооператив)
498. **MODE-COOP-0498** — [C] [Механика режима] Тупик по высоте: партия завершается
499. **MODE-COOP-0499** — [Механика режима] Очистка без «падения» верхних строк (по правилам coop)
500. **MODE-COOP-0500** — [Механика режима] resume 10×20 восстанавливает доску и общий счёт
501. **MODE-COOP-0501** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · скорость=низкая [coop]: счёт неотрицателен на всём протяжении
502. **MODE-COOP-0502** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · скорость=низкая [coop]: ни одного исключения/краша за партию
503. **MODE-COOP-0503** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · скорость=низкая [coop]: resume в середине — бит-в-бит то же продолжение
504. **MODE-COOP-0504** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · скорость=низкая [coop]: число ходов конечно (партия сходится)
505. **MODE-COOP-0505** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · скорость=низкая [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
506. **MODE-COOP-0506** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · скорость=низкая [coop]: ни одной невалидной постановки не принято
507. **MODE-COOP-0507** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · скорость=низкая [coop]: инвариант доски: заполненные клетки не «протекают» за границы
508. **MODE-COOP-0508** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · скорость=низкая [coop]: очистка строки сдвигает/освобождает корректно
509. **MODE-COOP-0509** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · скорость=высокая [coop]: счёт неотрицателен на всём протяжении
510. **MODE-COOP-0510** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · скорость=высокая [coop]: ни одного исключения/краша за партию
511. **MODE-COOP-0511** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · скорость=высокая [coop]: resume в середине — бит-в-бит то же продолжение
512. **MODE-COOP-0512** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · скорость=высокая [coop]: число ходов конечно (партия сходится)
513. **MODE-COOP-0513** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · скорость=высокая [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
514. **MODE-COOP-0514** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · скорость=высокая [coop]: ни одной невалидной постановки не принято
515. **MODE-COOP-0515** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · скорость=высокая [coop]: инвариант доски: заполненные клетки не «протекают» за границы
516. **MODE-COOP-0516** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · скорость=высокая [coop]: очистка строки сдвигает/освобождает корректно
517. **MODE-COOP-0517** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · поле почти полное [coop]: счёт неотрицателен на всём протяжении
518. **MODE-COOP-0518** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · поле почти полное [coop]: ни одного исключения/краша за партию
519. **MODE-COOP-0519** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · поле почти полное [coop]: resume в середине — бит-в-бит то же продолжение
520. **MODE-COOP-0520** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · поле почти полное [coop]: число ходов конечно (партия сходится)
521. **MODE-COOP-0521** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · поле почти полное [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
522. **MODE-COOP-0522** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · поле почти полное [coop]: ни одной невалидной постановки не принято
523. **MODE-COOP-0523** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · поле почти полное [coop]: инвариант доски: заполненные клетки не «протекают» за границы
524. **MODE-COOP-0524** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · поле почти полное [coop]: очистка строки сдвигает/освобождает корректно
525. **MODE-COOP-0525** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · поле пустое [coop]: счёт неотрицателен на всём протяжении
526. **MODE-COOP-0526** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · поле пустое [coop]: ни одного исключения/краша за партию
527. **MODE-COOP-0527** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · поле пустое [coop]: resume в середине — бит-в-бит то же продолжение
528. **MODE-COOP-0528** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · поле пустое [coop]: число ходов конечно (партия сходится)
529. **MODE-COOP-0529** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · поле пустое [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
530. **MODE-COOP-0530** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · поле пустое [coop]: ни одной невалидной постановки не принято
531. **MODE-COOP-0531** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · поле пустое [coop]: инвариант доски: заполненные клетки не «протекают» за границы
532. **MODE-COOP-0532** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · поле пустое [coop]: очистка строки сдвигает/освобождает корректно
533. **MODE-COOP-0533** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · серия очисток [coop]: счёт неотрицателен на всём протяжении
534. **MODE-COOP-0534** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · серия очисток [coop]: ни одного исключения/краша за партию
535. **MODE-COOP-0535** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · серия очисток [coop]: resume в середине — бит-в-бит то же продолжение
536. **MODE-COOP-0536** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · серия очисток [coop]: число ходов конечно (партия сходится)
537. **MODE-COOP-0537** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · серия очисток [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
538. **MODE-COOP-0538** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · серия очисток [coop]: ни одной невалидной постановки не принято
539. **MODE-COOP-0539** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · серия очисток [coop]: инвариант доски: заполненные клетки не «протекают» за границы
540. **MODE-COOP-0540** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · серия очисток [coop]: очистка строки сдвигает/освобождает корректно
541. **MODE-COOP-0541** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · одиночная очистка [coop]: счёт неотрицателен на всём протяжении
542. **MODE-COOP-0542** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · одиночная очистка [coop]: ни одного исключения/краша за партию
543. **MODE-COOP-0543** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · одиночная очистка [coop]: resume в середине — бит-в-бит то же продолжение
544. **MODE-COOP-0544** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · одиночная очистка [coop]: число ходов конечно (партия сходится)
545. **MODE-COOP-0545** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · одиночная очистка [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
546. **MODE-COOP-0546** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · одиночная очистка [coop]: ни одной невалидной постановки не принято
547. **MODE-COOP-0547** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · одиночная очистка [coop]: инвариант доски: заполненные клетки не «протекают» за границы
548. **MODE-COOP-0548** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · одиночная очистка [coop]: очистка строки сдвигает/освобождает корректно
549. **MODE-COOP-0549** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · скорость=низкая [coop]: счёт неотрицателен на всём протяжении
550. **MODE-COOP-0550** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · скорость=низкая [coop]: ни одного исключения/краша за партию
551. **MODE-COOP-0551** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · скорость=низкая [coop]: resume в середине — бит-в-бит то же продолжение
552. **MODE-COOP-0552** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · скорость=низкая [coop]: число ходов конечно (партия сходится)
553. **MODE-COOP-0553** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · скорость=низкая [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
554. **MODE-COOP-0554** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · скорость=низкая [coop]: ни одной невалидной постановки не принято
555. **MODE-COOP-0555** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · скорость=низкая [coop]: инвариант доски: заполненные клетки не «протекают» за границы
556. **MODE-COOP-0556** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · скорость=низкая [coop]: очистка строки сдвигает/освобождает корректно
557. **MODE-COOP-0557** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · скорость=высокая [coop]: счёт неотрицателен на всём протяжении
558. **MODE-COOP-0558** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · скорость=высокая [coop]: ни одного исключения/краша за партию
559. **MODE-COOP-0559** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · скорость=высокая [coop]: resume в середине — бит-в-бит то же продолжение
560. **MODE-COOP-0560** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · скорость=высокая [coop]: число ходов конечно (партия сходится)
561. **MODE-COOP-0561** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · скорость=высокая [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
562. **MODE-COOP-0562** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · скорость=высокая [coop]: ни одной невалидной постановки не принято
563. **MODE-COOP-0563** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · скорость=высокая [coop]: инвариант доски: заполненные клетки не «протекают» за границы
564. **MODE-COOP-0564** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · скорость=высокая [coop]: очистка строки сдвигает/освобождает корректно
565. **MODE-COOP-0565** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · поле почти полное [coop]: счёт неотрицателен на всём протяжении
566. **MODE-COOP-0566** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · поле почти полное [coop]: ни одного исключения/краша за партию
567. **MODE-COOP-0567** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · поле почти полное [coop]: resume в середине — бит-в-бит то же продолжение
568. **MODE-COOP-0568** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · поле почти полное [coop]: число ходов конечно (партия сходится)
569. **MODE-COOP-0569** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · поле почти полное [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
570. **MODE-COOP-0570** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · поле почти полное [coop]: ни одной невалидной постановки не принято
571. **MODE-COOP-0571** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · поле почти полное [coop]: инвариант доски: заполненные клетки не «протекают» за границы
572. **MODE-COOP-0572** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · поле почти полное [coop]: очистка строки сдвигает/освобождает корректно
573. **MODE-COOP-0573** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · поле пустое [coop]: счёт неотрицателен на всём протяжении
574. **MODE-COOP-0574** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · поле пустое [coop]: ни одного исключения/краша за партию
575. **MODE-COOP-0575** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · поле пустое [coop]: resume в середине — бит-в-бит то же продолжение
576. **MODE-COOP-0576** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · поле пустое [coop]: число ходов конечно (партия сходится)
577. **MODE-COOP-0577** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · поле пустое [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
578. **MODE-COOP-0578** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · поле пустое [coop]: ни одной невалидной постановки не принято
579. **MODE-COOP-0579** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · поле пустое [coop]: инвариант доски: заполненные клетки не «протекают» за границы
580. **MODE-COOP-0580** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · поле пустое [coop]: очистка строки сдвигает/освобождает корректно
581. **MODE-COOP-0581** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · серия очисток [coop]: счёт неотрицателен на всём протяжении
582. **MODE-COOP-0582** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · серия очисток [coop]: ни одного исключения/краша за партию
583. **MODE-COOP-0583** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · серия очисток [coop]: resume в середине — бит-в-бит то же продолжение
584. **MODE-COOP-0584** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · серия очисток [coop]: число ходов конечно (партия сходится)
585. **MODE-COOP-0585** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · серия очисток [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
586. **MODE-COOP-0586** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · серия очисток [coop]: ни одной невалидной постановки не принято
587. **MODE-COOP-0587** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · серия очисток [coop]: инвариант доски: заполненные клетки не «протекают» за границы
588. **MODE-COOP-0588** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · серия очисток [coop]: очистка строки сдвигает/освобождает корректно
589. **MODE-COOP-0589** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · одиночная очистка [coop]: счёт неотрицателен на всём протяжении
590. **MODE-COOP-0590** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · одиночная очистка [coop]: ни одного исключения/краша за партию
591. **MODE-COOP-0591** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · одиночная очистка [coop]: resume в середине — бит-в-бит то же продолжение
592. **MODE-COOP-0592** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · одиночная очистка [coop]: число ходов конечно (партия сходится)
593. **MODE-COOP-0593** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · одиночная очистка [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
594. **MODE-COOP-0594** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · одиночная очистка [coop]: ни одной невалидной постановки не принято
595. **MODE-COOP-0595** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · одиночная очистка [coop]: инвариант доски: заполненные клетки не «протекают» за границы
596. **MODE-COOP-0596** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · одиночная очистка [coop]: очистка строки сдвигает/освобождает корректно
597. **MODE-COOP-0597** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · скорость=низкая [coop]: счёт неотрицателен на всём протяжении
598. **MODE-COOP-0598** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · скорость=низкая [coop]: ни одного исключения/краша за партию
599. **MODE-COOP-0599** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · скорость=низкая [coop]: resume в середине — бит-в-бит то же продолжение
600. **MODE-COOP-0600** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · скорость=низкая [coop]: число ходов конечно (партия сходится)
601. **MODE-COOP-0601** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · скорость=низкая [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
602. **MODE-COOP-0602** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · скорость=низкая [coop]: ни одной невалидной постановки не принято
603. **MODE-COOP-0603** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · скорость=низкая [coop]: инвариант доски: заполненные клетки не «протекают» за границы
604. **MODE-COOP-0604** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · скорость=низкая [coop]: очистка строки сдвигает/освобождает корректно
605. **MODE-COOP-0605** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · скорость=высокая [coop]: счёт неотрицателен на всём протяжении
606. **MODE-COOP-0606** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · скорость=высокая [coop]: ни одного исключения/краша за партию
607. **MODE-COOP-0607** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · скорость=высокая [coop]: resume в середине — бит-в-бит то же продолжение
608. **MODE-COOP-0608** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · скорость=высокая [coop]: число ходов конечно (партия сходится)
609. **MODE-COOP-0609** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · скорость=высокая [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
610. **MODE-COOP-0610** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · скорость=высокая [coop]: ни одной невалидной постановки не принято
611. **MODE-COOP-0611** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · скорость=высокая [coop]: инвариант доски: заполненные клетки не «протекают» за границы
612. **MODE-COOP-0612** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · скорость=высокая [coop]: очистка строки сдвигает/освобождает корректно
613. **MODE-COOP-0613** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · поле почти полное [coop]: счёт неотрицателен на всём протяжении
614. **MODE-COOP-0614** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · поле почти полное [coop]: ни одного исключения/краша за партию
615. **MODE-COOP-0615** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · поле почти полное [coop]: resume в середине — бит-в-бит то же продолжение
616. **MODE-COOP-0616** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · поле почти полное [coop]: число ходов конечно (партия сходится)
617. **MODE-COOP-0617** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · поле почти полное [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
618. **MODE-COOP-0618** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · поле почти полное [coop]: ни одной невалидной постановки не принято
619. **MODE-COOP-0619** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · поле почти полное [coop]: инвариант доски: заполненные клетки не «протекают» за границы
620. **MODE-COOP-0620** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · поле почти полное [coop]: очистка строки сдвигает/освобождает корректно
621. **MODE-COOP-0621** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · поле пустое [coop]: счёт неотрицателен на всём протяжении
622. **MODE-COOP-0622** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · поле пустое [coop]: ни одного исключения/краша за партию
623. **MODE-COOP-0623** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · поле пустое [coop]: resume в середине — бит-в-бит то же продолжение
624. **MODE-COOP-0624** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · поле пустое [coop]: число ходов конечно (партия сходится)
625. **MODE-COOP-0625** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · поле пустое [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
626. **MODE-COOP-0626** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · поле пустое [coop]: ни одной невалидной постановки не принято
627. **MODE-COOP-0627** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · поле пустое [coop]: инвариант доски: заполненные клетки не «протекают» за границы
628. **MODE-COOP-0628** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · поле пустое [coop]: очистка строки сдвигает/освобождает корректно
629. **MODE-COOP-0629** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · серия очисток [coop]: счёт неотрицателен на всём протяжении
630. **MODE-COOP-0630** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · серия очисток [coop]: ни одного исключения/краша за партию
631. **MODE-COOP-0631** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · серия очисток [coop]: resume в середине — бит-в-бит то же продолжение
632. **MODE-COOP-0632** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · серия очисток [coop]: число ходов конечно (партия сходится)
633. **MODE-COOP-0633** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · серия очисток [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
634. **MODE-COOP-0634** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · серия очисток [coop]: ни одной невалидной постановки не принято
635. **MODE-COOP-0635** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · серия очисток [coop]: инвариант доски: заполненные клетки не «протекают» за границы
636. **MODE-COOP-0636** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · серия очисток [coop]: очистка строки сдвигает/освобождает корректно
637. **MODE-COOP-0637** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · одиночная очистка [coop]: счёт неотрицателен на всём протяжении
638. **MODE-COOP-0638** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · одиночная очистка [coop]: ни одного исключения/краша за партию
639. **MODE-COOP-0639** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · одиночная очистка [coop]: resume в середине — бит-в-бит то же продолжение
640. **MODE-COOP-0640** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · одиночная очистка [coop]: число ходов конечно (партия сходится)
641. **MODE-COOP-0641** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · одиночная очистка [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
642. **MODE-COOP-0642** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · одиночная очистка [coop]: ни одной невалидной постановки не принято
643. **MODE-COOP-0643** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · одиночная очистка [coop]: инвариант доски: заполненные клетки не «протекают» за границы
644. **MODE-COOP-0644** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · одиночная очистка [coop]: очистка строки сдвигает/освобождает корректно
645. **MODE-COOP-0645** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · скорость=низкая [coop]: счёт неотрицателен на всём протяжении
646. **MODE-COOP-0646** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · скорость=низкая [coop]: ни одного исключения/краша за партию
647. **MODE-COOP-0647** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · скорость=низкая [coop]: resume в середине — бит-в-бит то же продолжение
648. **MODE-COOP-0648** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · скорость=низкая [coop]: число ходов конечно (партия сходится)
649. **MODE-COOP-0649** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · скорость=низкая [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
650. **MODE-COOP-0650** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · скорость=низкая [coop]: ни одной невалидной постановки не принято
651. **MODE-COOP-0651** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · скорость=низкая [coop]: инвариант доски: заполненные клетки не «протекают» за границы
652. **MODE-COOP-0652** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · скорость=низкая [coop]: очистка строки сдвигает/освобождает корректно
653. **MODE-COOP-0653** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · скорость=высокая [coop]: счёт неотрицателен на всём протяжении
654. **MODE-COOP-0654** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · скорость=высокая [coop]: ни одного исключения/краша за партию
655. **MODE-COOP-0655** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · скорость=высокая [coop]: resume в середине — бит-в-бит то же продолжение
656. **MODE-COOP-0656** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · скорость=высокая [coop]: число ходов конечно (партия сходится)
657. **MODE-COOP-0657** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · скорость=высокая [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
658. **MODE-COOP-0658** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · скорость=высокая [coop]: ни одной невалидной постановки не принято
659. **MODE-COOP-0659** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · скорость=высокая [coop]: инвариант доски: заполненные клетки не «протекают» за границы
660. **MODE-COOP-0660** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · скорость=высокая [coop]: очистка строки сдвигает/освобождает корректно
661. **MODE-COOP-0661** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · поле почти полное [coop]: счёт неотрицателен на всём протяжении
662. **MODE-COOP-0662** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · поле почти полное [coop]: ни одного исключения/краша за партию
663. **MODE-COOP-0663** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · поле почти полное [coop]: resume в середине — бит-в-бит то же продолжение
664. **MODE-COOP-0664** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · поле почти полное [coop]: число ходов конечно (партия сходится)
665. **MODE-COOP-0665** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · поле почти полное [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
666. **MODE-COOP-0666** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · поле почти полное [coop]: ни одной невалидной постановки не принято
667. **MODE-COOP-0667** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · поле почти полное [coop]: инвариант доски: заполненные клетки не «протекают» за границы
668. **MODE-COOP-0668** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · поле почти полное [coop]: очистка строки сдвигает/освобождает корректно
669. **MODE-COOP-0669** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · поле пустое [coop]: счёт неотрицателен на всём протяжении
670. **MODE-COOP-0670** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · поле пустое [coop]: ни одного исключения/краша за партию
671. **MODE-COOP-0671** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · поле пустое [coop]: resume в середине — бит-в-бит то же продолжение
672. **MODE-COOP-0672** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · поле пустое [coop]: число ходов конечно (партия сходится)
673. **MODE-COOP-0673** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · поле пустое [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
674. **MODE-COOP-0674** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · поле пустое [coop]: ни одной невалидной постановки не принято
675. **MODE-COOP-0675** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · поле пустое [coop]: инвариант доски: заполненные клетки не «протекают» за границы
676. **MODE-COOP-0676** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · поле пустое [coop]: очистка строки сдвигает/освобождает корректно
677. **MODE-COOP-0677** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · серия очисток [coop]: счёт неотрицателен на всём протяжении
678. **MODE-COOP-0678** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · серия очисток [coop]: ни одного исключения/краша за партию
679. **MODE-COOP-0679** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · серия очисток [coop]: resume в середине — бит-в-бит то же продолжение
680. **MODE-COOP-0680** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · серия очисток [coop]: число ходов конечно (партия сходится)
681. **MODE-COOP-0681** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · серия очисток [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
682. **MODE-COOP-0682** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · серия очисток [coop]: ни одной невалидной постановки не принято
683. **MODE-COOP-0683** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · серия очисток [coop]: инвариант доски: заполненные клетки не «протекают» за границы
684. **MODE-COOP-0684** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · серия очисток [coop]: очистка строки сдвигает/освобождает корректно
685. **MODE-COOP-0685** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · одиночная очистка [coop]: счёт неотрицателен на всём протяжении
686. **MODE-COOP-0686** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · одиночная очистка [coop]: ни одного исключения/краша за партию
687. **MODE-COOP-0687** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · одиночная очистка [coop]: resume в середине — бит-в-бит то же продолжение
688. **MODE-COOP-0688** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · одиночная очистка [coop]: число ходов конечно (партия сходится)
689. **MODE-COOP-0689** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · одиночная очистка [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
690. **MODE-COOP-0690** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · одиночная очистка [coop]: ни одной невалидной постановки не принято
691. **MODE-COOP-0691** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · одиночная очистка [coop]: инвариант доски: заполненные клетки не «протекают» за границы
692. **MODE-COOP-0692** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · одиночная очистка [coop]: очистка строки сдвигает/освобождает корректно
693. **MODE-COOP-0693** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · скорость=низкая [coop]: счёт неотрицателен на всём протяжении
694. **MODE-COOP-0694** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · скорость=низкая [coop]: ни одного исключения/краша за партию
695. **MODE-COOP-0695** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · скорость=низкая [coop]: resume в середине — бит-в-бит то же продолжение
696. **MODE-COOP-0696** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · скорость=низкая [coop]: число ходов конечно (партия сходится)
697. **MODE-COOP-0697** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · скорость=низкая [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
698. **MODE-COOP-0698** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · скорость=низкая [coop]: ни одной невалидной постановки не принято
699. **MODE-COOP-0699** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · скорость=низкая [coop]: инвариант доски: заполненные клетки не «протекают» за границы
700. **MODE-COOP-0700** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · скорость=низкая [coop]: очистка строки сдвигает/освобождает корректно
701. **MODE-COOP-0701** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · скорость=высокая [coop]: счёт неотрицателен на всём протяжении
702. **MODE-COOP-0702** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · скорость=высокая [coop]: ни одного исключения/краша за партию
703. **MODE-COOP-0703** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · скорость=высокая [coop]: resume в середине — бит-в-бит то же продолжение
704. **MODE-COOP-0704** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · скорость=высокая [coop]: число ходов конечно (партия сходится)
705. **MODE-COOP-0705** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · скорость=высокая [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
706. **MODE-COOP-0706** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · скорость=высокая [coop]: ни одной невалидной постановки не принято
707. **MODE-COOP-0707** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · скорость=высокая [coop]: инвариант доски: заполненные клетки не «протекают» за границы
708. **MODE-COOP-0708** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · скорость=высокая [coop]: очистка строки сдвигает/освобождает корректно
709. **MODE-COOP-0709** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · поле почти полное [coop]: счёт неотрицателен на всём протяжении
710. **MODE-COOP-0710** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · поле почти полное [coop]: ни одного исключения/краша за партию
711. **MODE-COOP-0711** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · поле почти полное [coop]: resume в середине — бит-в-бит то же продолжение
712. **MODE-COOP-0712** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · поле почти полное [coop]: число ходов конечно (партия сходится)
713. **MODE-COOP-0713** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · поле почти полное [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
714. **MODE-COOP-0714** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · поле почти полное [coop]: ни одной невалидной постановки не принято
715. **MODE-COOP-0715** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · поле почти полное [coop]: инвариант доски: заполненные клетки не «протекают» за границы
716. **MODE-COOP-0716** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · поле почти полное [coop]: очистка строки сдвигает/освобождает корректно
717. **MODE-COOP-0717** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · поле пустое [coop]: счёт неотрицателен на всём протяжении
718. **MODE-COOP-0718** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · поле пустое [coop]: ни одного исключения/краша за партию
719. **MODE-COOP-0719** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · поле пустое [coop]: resume в середине — бит-в-бит то же продолжение
720. **MODE-COOP-0720** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · поле пустое [coop]: число ходов конечно (партия сходится)
721. **MODE-COOP-0721** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · поле пустое [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
722. **MODE-COOP-0722** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · поле пустое [coop]: ни одной невалидной постановки не принято
723. **MODE-COOP-0723** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · поле пустое [coop]: инвариант доски: заполненные клетки не «протекают» за границы
724. **MODE-COOP-0724** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · поле пустое [coop]: очистка строки сдвигает/освобождает корректно
725. **MODE-COOP-0725** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · серия очисток [coop]: счёт неотрицателен на всём протяжении
726. **MODE-COOP-0726** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · серия очисток [coop]: ни одного исключения/краша за партию
727. **MODE-COOP-0727** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · серия очисток [coop]: resume в середине — бит-в-бит то же продолжение
728. **MODE-COOP-0728** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · серия очисток [coop]: число ходов конечно (партия сходится)
729. **MODE-COOP-0729** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · серия очисток [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
730. **MODE-COOP-0730** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · серия очисток [coop]: ни одной невалидной постановки не принято
731. **MODE-COOP-0731** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · серия очисток [coop]: инвариант доски: заполненные клетки не «протекают» за границы
732. **MODE-COOP-0732** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · серия очисток [coop]: очистка строки сдвигает/освобождает корректно
733. **MODE-COOP-0733** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · одиночная очистка [coop]: счёт неотрицателен на всём протяжении
734. **MODE-COOP-0734** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · одиночная очистка [coop]: ни одного исключения/краша за партию
735. **MODE-COOP-0735** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · одиночная очистка [coop]: resume в середине — бит-в-бит то же продолжение
736. **MODE-COOP-0736** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · одиночная очистка [coop]: число ходов конечно (партия сходится)
737. **MODE-COOP-0737** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · одиночная очистка [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
738. **MODE-COOP-0738** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · одиночная очистка [coop]: ни одной невалидной постановки не принято
739. **MODE-COOP-0739** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · одиночная очистка [coop]: инвариант доски: заполненные клетки не «протекают» за границы
740. **MODE-COOP-0740** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · одиночная очистка [coop]: очистка строки сдвигает/освобождает корректно
741. **MODE-COOP-0741** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · скорость=низкая [coop]: счёт неотрицателен на всём протяжении
742. **MODE-COOP-0742** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · скорость=низкая [coop]: ни одного исключения/краша за партию
743. **MODE-COOP-0743** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · скорость=низкая [coop]: resume в середине — бит-в-бит то же продолжение
744. **MODE-COOP-0744** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · скорость=низкая [coop]: число ходов конечно (партия сходится)
745. **MODE-COOP-0745** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · скорость=низкая [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
746. **MODE-COOP-0746** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · скорость=низкая [coop]: ни одной невалидной постановки не принято
747. **MODE-COOP-0747** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · скорость=низкая [coop]: инвариант доски: заполненные клетки не «протекают» за границы
748. **MODE-COOP-0748** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · скорость=низкая [coop]: очистка строки сдвигает/освобождает корректно
749. **MODE-COOP-0749** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · скорость=высокая [coop]: счёт неотрицателен на всём протяжении
750. **MODE-COOP-0750** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · скорость=высокая [coop]: ни одного исключения/краша за партию
751. **MODE-COOP-0751** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · скорость=высокая [coop]: resume в середине — бит-в-бит то же продолжение
752. **MODE-COOP-0752** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · скорость=высокая [coop]: число ходов конечно (партия сходится)
753. **MODE-COOP-0753** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · скорость=высокая [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
754. **MODE-COOP-0754** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · скорость=высокая [coop]: ни одной невалидной постановки не принято
755. **MODE-COOP-0755** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · скорость=высокая [coop]: инвариант доски: заполненные клетки не «протекают» за границы
756. **MODE-COOP-0756** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · скорость=высокая [coop]: очистка строки сдвигает/освобождает корректно
757. **MODE-COOP-0757** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · поле почти полное [coop]: счёт неотрицателен на всём протяжении
758. **MODE-COOP-0758** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · поле почти полное [coop]: ни одного исключения/краша за партию
759. **MODE-COOP-0759** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · поле почти полное [coop]: resume в середине — бит-в-бит то же продолжение
760. **MODE-COOP-0760** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · поле почти полное [coop]: число ходов конечно (партия сходится)
761. **MODE-COOP-0761** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · поле почти полное [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
762. **MODE-COOP-0762** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · поле почти полное [coop]: ни одной невалидной постановки не принято
763. **MODE-COOP-0763** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · поле почти полное [coop]: инвариант доски: заполненные клетки не «протекают» за границы
764. **MODE-COOP-0764** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · поле почти полное [coop]: очистка строки сдвигает/освобождает корректно
765. **MODE-COOP-0765** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · поле пустое [coop]: счёт неотрицателен на всём протяжении
766. **MODE-COOP-0766** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · поле пустое [coop]: ни одного исключения/краша за партию
767. **MODE-COOP-0767** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · поле пустое [coop]: resume в середине — бит-в-бит то же продолжение
768. **MODE-COOP-0768** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · поле пустое [coop]: число ходов конечно (партия сходится)
769. **MODE-COOP-0769** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · поле пустое [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
770. **MODE-COOP-0770** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · поле пустое [coop]: ни одной невалидной постановки не принято
771. **MODE-COOP-0771** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · поле пустое [coop]: инвариант доски: заполненные клетки не «протекают» за границы
772. **MODE-COOP-0772** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · поле пустое [coop]: очистка строки сдвигает/освобождает корректно
773. **MODE-COOP-0773** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · серия очисток [coop]: счёт неотрицателен на всём протяжении
774. **MODE-COOP-0774** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · серия очисток [coop]: ни одного исключения/краша за партию
775. **MODE-COOP-0775** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · серия очисток [coop]: resume в середине — бит-в-бит то же продолжение
776. **MODE-COOP-0776** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · серия очисток [coop]: число ходов конечно (партия сходится)
777. **MODE-COOP-0777** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · серия очисток [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
778. **MODE-COOP-0778** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · серия очисток [coop]: ни одной невалидной постановки не принято
779. **MODE-COOP-0779** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · серия очисток [coop]: инвариант доски: заполненные клетки не «протекают» за границы
780. **MODE-COOP-0780** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · серия очисток [coop]: очистка строки сдвигает/освобождает корректно
781. **MODE-COOP-0781** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · одиночная очистка [coop]: счёт неотрицателен на всём протяжении
782. **MODE-COOP-0782** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · одиночная очистка [coop]: ни одного исключения/краша за партию
783. **MODE-COOP-0783** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · одиночная очистка [coop]: resume в середине — бит-в-бит то же продолжение
784. **MODE-COOP-0784** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · одиночная очистка [coop]: число ходов конечно (партия сходится)
785. **MODE-COOP-0785** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · одиночная очистка [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
786. **MODE-COOP-0786** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · одиночная очистка [coop]: ни одной невалидной постановки не принято
787. **MODE-COOP-0787** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · одиночная очистка [coop]: инвариант доски: заполненные клетки не «протекают» за границы
788. **MODE-COOP-0788** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · одиночная очистка [coop]: очистка строки сдвигает/освобождает корректно
789. **MODE-COOP-0789** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · скорость=низкая [coop]: счёт неотрицателен на всём протяжении
790. **MODE-COOP-0790** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · скорость=низкая [coop]: ни одного исключения/краша за партию
791. **MODE-COOP-0791** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · скорость=низкая [coop]: resume в середине — бит-в-бит то же продолжение
792. **MODE-COOP-0792** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · скорость=низкая [coop]: число ходов конечно (партия сходится)
793. **MODE-COOP-0793** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · скорость=низкая [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
794. **MODE-COOP-0794** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · скорость=низкая [coop]: ни одной невалидной постановки не принято
795. **MODE-COOP-0795** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · скорость=низкая [coop]: инвариант доски: заполненные клетки не «протекают» за границы
796. **MODE-COOP-0796** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · скорость=низкая [coop]: очистка строки сдвигает/освобождает корректно
797. **MODE-COOP-0797** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · скорость=высокая [coop]: счёт неотрицателен на всём протяжении
798. **MODE-COOP-0798** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · скорость=высокая [coop]: ни одного исключения/краша за партию
799. **MODE-COOP-0799** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · скорость=высокая [coop]: resume в середине — бит-в-бит то же продолжение
800. **MODE-COOP-0800** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · скорость=высокая [coop]: число ходов конечно (партия сходится)
801. **MODE-COOP-0801** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · скорость=высокая [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
802. **MODE-COOP-0802** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · скорость=высокая [coop]: ни одной невалидной постановки не принято
803. **MODE-COOP-0803** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · скорость=высокая [coop]: инвариант доски: заполненные клетки не «протекают» за границы
804. **MODE-COOP-0804** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · скорость=высокая [coop]: очистка строки сдвигает/освобождает корректно
805. **MODE-COOP-0805** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · поле почти полное [coop]: счёт неотрицателен на всём протяжении
806. **MODE-COOP-0806** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · поле почти полное [coop]: ни одного исключения/краша за партию
807. **MODE-COOP-0807** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · поле почти полное [coop]: resume в середине — бит-в-бит то же продолжение
808. **MODE-COOP-0808** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · поле почти полное [coop]: число ходов конечно (партия сходится)
809. **MODE-COOP-0809** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · поле почти полное [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
810. **MODE-COOP-0810** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · поле почти полное [coop]: ни одной невалидной постановки не принято
811. **MODE-COOP-0811** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · поле почти полное [coop]: инвариант доски: заполненные клетки не «протекают» за границы
812. **MODE-COOP-0812** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · поле почти полное [coop]: очистка строки сдвигает/освобождает корректно
813. **MODE-COOP-0813** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · поле пустое [coop]: счёт неотрицателен на всём протяжении
814. **MODE-COOP-0814** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · поле пустое [coop]: ни одного исключения/краша за партию
815. **MODE-COOP-0815** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · поле пустое [coop]: resume в середине — бит-в-бит то же продолжение
816. **MODE-COOP-0816** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · поле пустое [coop]: число ходов конечно (партия сходится)
817. **MODE-COOP-0817** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · поле пустое [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
818. **MODE-COOP-0818** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · поле пустое [coop]: ни одной невалидной постановки не принято
819. **MODE-COOP-0819** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · поле пустое [coop]: инвариант доски: заполненные клетки не «протекают» за границы
820. **MODE-COOP-0820** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · поле пустое [coop]: очистка строки сдвигает/освобождает корректно
821. **MODE-COOP-0821** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · серия очисток [coop]: счёт неотрицателен на всём протяжении
822. **MODE-COOP-0822** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · серия очисток [coop]: ни одного исключения/краша за партию
823. **MODE-COOP-0823** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · серия очисток [coop]: resume в середине — бит-в-бит то же продолжение
824. **MODE-COOP-0824** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · серия очисток [coop]: число ходов конечно (партия сходится)
825. **MODE-COOP-0825** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · серия очисток [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
826. **MODE-COOP-0826** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · серия очисток [coop]: ни одной невалидной постановки не принято
827. **MODE-COOP-0827** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · серия очисток [coop]: инвариант доски: заполненные клетки не «протекают» за границы
828. **MODE-COOP-0828** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · серия очисток [coop]: очистка строки сдвигает/освобождает корректно
829. **MODE-COOP-0829** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · одиночная очистка [coop]: счёт неотрицателен на всём протяжении
830. **MODE-COOP-0830** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · одиночная очистка [coop]: ни одного исключения/краша за партию
831. **MODE-COOP-0831** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · одиночная очистка [coop]: resume в середине — бит-в-бит то же продолжение
832. **MODE-COOP-0832** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · одиночная очистка [coop]: число ходов конечно (партия сходится)
833. **MODE-COOP-0833** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · одиночная очистка [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
834. **MODE-COOP-0834** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · одиночная очистка [coop]: ни одной невалидной постановки не принято
835. **MODE-COOP-0835** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · одиночная очистка [coop]: инвариант доски: заполненные клетки не «протекают» за границы
836. **MODE-COOP-0836** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · одиночная очистка [coop]: очистка строки сдвигает/освобождает корректно
837. **MODE-COOP-0837** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · скорость=низкая [coop]: счёт неотрицателен на всём протяжении
838. **MODE-COOP-0838** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · скорость=низкая [coop]: ни одного исключения/краша за партию
839. **MODE-COOP-0839** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · скорость=низкая [coop]: resume в середине — бит-в-бит то же продолжение
840. **MODE-COOP-0840** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · скорость=низкая [coop]: число ходов конечно (партия сходится)
841. **MODE-COOP-0841** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · скорость=низкая [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
842. **MODE-COOP-0842** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · скорость=низкая [coop]: ни одной невалидной постановки не принято
843. **MODE-COOP-0843** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · скорость=низкая [coop]: инвариант доски: заполненные клетки не «протекают» за границы
844. **MODE-COOP-0844** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · скорость=низкая [coop]: очистка строки сдвигает/освобождает корректно
845. **MODE-COOP-0845** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · скорость=высокая [coop]: счёт неотрицателен на всём протяжении
846. **MODE-COOP-0846** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · скорость=высокая [coop]: ни одного исключения/краша за партию
847. **MODE-COOP-0847** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · скорость=высокая [coop]: resume в середине — бит-в-бит то же продолжение
848. **MODE-COOP-0848** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · скорость=высокая [coop]: число ходов конечно (партия сходится)
849. **MODE-COOP-0849** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · скорость=высокая [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
850. **MODE-COOP-0850** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · скорость=высокая [coop]: ни одной невалидной постановки не принято
851. **MODE-COOP-0851** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · скорость=высокая [coop]: инвариант доски: заполненные клетки не «протекают» за границы
852. **MODE-COOP-0852** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · скорость=высокая [coop]: очистка строки сдвигает/освобождает корректно
853. **MODE-COOP-0853** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · поле почти полное [coop]: счёт неотрицателен на всём протяжении
854. **MODE-COOP-0854** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · поле почти полное [coop]: ни одного исключения/краша за партию
855. **MODE-COOP-0855** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · поле почти полное [coop]: resume в середине — бит-в-бит то же продолжение
856. **MODE-COOP-0856** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · поле почти полное [coop]: число ходов конечно (партия сходится)
857. **MODE-COOP-0857** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · поле почти полное [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
858. **MODE-COOP-0858** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · поле почти полное [coop]: ни одной невалидной постановки не принято
859. **MODE-COOP-0859** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · поле почти полное [coop]: инвариант доски: заполненные клетки не «протекают» за границы
860. **MODE-COOP-0860** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · поле почти полное [coop]: очистка строки сдвигает/освобождает корректно
861. **MODE-COOP-0861** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · поле пустое [coop]: счёт неотрицателен на всём протяжении
862. **MODE-COOP-0862** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · поле пустое [coop]: ни одного исключения/краша за партию
863. **MODE-COOP-0863** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · поле пустое [coop]: resume в середине — бит-в-бит то же продолжение
864. **MODE-COOP-0864** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · поле пустое [coop]: число ходов конечно (партия сходится)
865. **MODE-COOP-0865** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · поле пустое [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
866. **MODE-COOP-0866** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · поле пустое [coop]: ни одной невалидной постановки не принято
867. **MODE-COOP-0867** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · поле пустое [coop]: инвариант доски: заполненные клетки не «протекают» за границы
868. **MODE-COOP-0868** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · поле пустое [coop]: очистка строки сдвигает/освобождает корректно
869. **MODE-COOP-0869** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · серия очисток [coop]: счёт неотрицателен на всём протяжении
870. **MODE-COOP-0870** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · серия очисток [coop]: ни одного исключения/краша за партию
871. **MODE-COOP-0871** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · серия очисток [coop]: resume в середине — бит-в-бит то же продолжение
872. **MODE-COOP-0872** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · серия очисток [coop]: число ходов конечно (партия сходится)
873. **MODE-COOP-0873** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · серия очисток [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
874. **MODE-COOP-0874** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · серия очисток [coop]: ни одной невалидной постановки не принято
875. **MODE-COOP-0875** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · серия очисток [coop]: инвариант доски: заполненные клетки не «протекают» за границы
876. **MODE-COOP-0876** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · серия очисток [coop]: очистка строки сдвигает/освобождает корректно
877. **MODE-COOP-0877** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · одиночная очистка [coop]: счёт неотрицателен на всём протяжении
878. **MODE-COOP-0878** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · одиночная очистка [coop]: ни одного исключения/краша за партию
879. **MODE-COOP-0879** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · одиночная очистка [coop]: resume в середине — бит-в-бит то же продолжение
880. **MODE-COOP-0880** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · одиночная очистка [coop]: число ходов конечно (партия сходится)
881. **MODE-COOP-0881** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · одиночная очистка [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
882. **MODE-COOP-0882** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · одиночная очистка [coop]: ни одной невалидной постановки не принято
883. **MODE-COOP-0883** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · одиночная очистка [coop]: инвариант доски: заполненные клетки не «протекают» за границы
884. **MODE-COOP-0884** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · одиночная очистка [coop]: очистка строки сдвигает/освобождает корректно
885. **MODE-COOP-0885** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · скорость=низкая [coop]: счёт неотрицателен на всём протяжении
886. **MODE-COOP-0886** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · скорость=низкая [coop]: ни одного исключения/краша за партию
887. **MODE-COOP-0887** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · скорость=низкая [coop]: resume в середине — бит-в-бит то же продолжение
888. **MODE-COOP-0888** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · скорость=низкая [coop]: число ходов конечно (партия сходится)
889. **MODE-COOP-0889** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · скорость=низкая [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
890. **MODE-COOP-0890** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · скорость=низкая [coop]: ни одной невалидной постановки не принято
891. **MODE-COOP-0891** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · скорость=низкая [coop]: инвариант доски: заполненные клетки не «протекают» за границы
892. **MODE-COOP-0892** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · скорость=низкая [coop]: очистка строки сдвигает/освобождает корректно
893. **MODE-COOP-0893** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · скорость=высокая [coop]: счёт неотрицателен на всём протяжении
894. **MODE-COOP-0894** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · скорость=высокая [coop]: ни одного исключения/краша за партию
895. **MODE-COOP-0895** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · скорость=высокая [coop]: resume в середине — бит-в-бит то же продолжение
896. **MODE-COOP-0896** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · скорость=высокая [coop]: число ходов конечно (партия сходится)
897. **MODE-COOP-0897** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · скорость=высокая [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
898. **MODE-COOP-0898** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · скорость=высокая [coop]: ни одной невалидной постановки не принято
899. **MODE-COOP-0899** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · скорость=высокая [coop]: инвариант доски: заполненные клетки не «протекают» за границы
900. **MODE-COOP-0900** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · скорость=высокая [coop]: очистка строки сдвигает/освобождает корректно
901. **MODE-COOP-0901** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · поле почти полное [coop]: счёт неотрицателен на всём протяжении
902. **MODE-COOP-0902** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · поле почти полное [coop]: ни одного исключения/краша за партию
903. **MODE-COOP-0903** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · поле почти полное [coop]: resume в середине — бит-в-бит то же продолжение
904. **MODE-COOP-0904** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · поле почти полное [coop]: число ходов конечно (партия сходится)
905. **MODE-COOP-0905** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · поле почти полное [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
906. **MODE-COOP-0906** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · поле почти полное [coop]: ни одной невалидной постановки не принято
907. **MODE-COOP-0907** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · поле почти полное [coop]: инвариант доски: заполненные клетки не «протекают» за границы
908. **MODE-COOP-0908** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · поле почти полное [coop]: очистка строки сдвигает/освобождает корректно
909. **MODE-COOP-0909** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · поле пустое [coop]: счёт неотрицателен на всём протяжении
910. **MODE-COOP-0910** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · поле пустое [coop]: ни одного исключения/краша за партию
911. **MODE-COOP-0911** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · поле пустое [coop]: resume в середине — бит-в-бит то же продолжение
912. **MODE-COOP-0912** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · поле пустое [coop]: число ходов конечно (партия сходится)
913. **MODE-COOP-0913** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · поле пустое [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
914. **MODE-COOP-0914** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · поле пустое [coop]: ни одной невалидной постановки не принято
915. **MODE-COOP-0915** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · поле пустое [coop]: инвариант доски: заполненные клетки не «протекают» за границы
916. **MODE-COOP-0916** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · поле пустое [coop]: очистка строки сдвигает/освобождает корректно
917. **MODE-COOP-0917** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · серия очисток [coop]: счёт неотрицателен на всём протяжении
918. **MODE-COOP-0918** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · серия очисток [coop]: ни одного исключения/краша за партию
919. **MODE-COOP-0919** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · серия очисток [coop]: resume в середине — бит-в-бит то же продолжение
920. **MODE-COOP-0920** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · серия очисток [coop]: число ходов конечно (партия сходится)
921. **MODE-COOP-0921** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · серия очисток [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
922. **MODE-COOP-0922** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · серия очисток [coop]: ни одной невалидной постановки не принято
923. **MODE-COOP-0923** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · серия очисток [coop]: инвариант доски: заполненные клетки не «протекают» за границы
924. **MODE-COOP-0924** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · серия очисток [coop]: очистка строки сдвигает/освобождает корректно
925. **MODE-COOP-0925** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · одиночная очистка [coop]: счёт неотрицателен на всём протяжении
926. **MODE-COOP-0926** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · одиночная очистка [coop]: ни одного исключения/краша за партию
927. **MODE-COOP-0927** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · одиночная очистка [coop]: resume в середине — бит-в-бит то же продолжение
928. **MODE-COOP-0928** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · одиночная очистка [coop]: число ходов конечно (партия сходится)
929. **MODE-COOP-0929** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · одиночная очистка [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
930. **MODE-COOP-0930** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · одиночная очистка [coop]: ни одной невалидной постановки не принято
931. **MODE-COOP-0931** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · одиночная очистка [coop]: инвариант доски: заполненные клетки не «протекают» за границы
932. **MODE-COOP-0932** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · одиночная очистка [coop]: очистка строки сдвигает/освобождает корректно
933. **MODE-COOP-0933** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · скорость=низкая [coop]: счёт неотрицателен на всём протяжении
934. **MODE-COOP-0934** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · скорость=низкая [coop]: ни одного исключения/краша за партию
935. **MODE-COOP-0935** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · скорость=низкая [coop]: resume в середине — бит-в-бит то же продолжение
936. **MODE-COOP-0936** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · скорость=низкая [coop]: число ходов конечно (партия сходится)
937. **MODE-COOP-0937** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · скорость=низкая [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
938. **MODE-COOP-0938** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · скорость=низкая [coop]: ни одной невалидной постановки не принято
939. **MODE-COOP-0939** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · скорость=низкая [coop]: инвариант доски: заполненные клетки не «протекают» за границы
940. **MODE-COOP-0940** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · скорость=низкая [coop]: очистка строки сдвигает/освобождает корректно
941. **MODE-COOP-0941** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · скорость=высокая [coop]: счёт неотрицателен на всём протяжении
942. **MODE-COOP-0942** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · скорость=высокая [coop]: ни одного исключения/краша за партию
943. **MODE-COOP-0943** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · скорость=высокая [coop]: resume в середине — бит-в-бит то же продолжение
944. **MODE-COOP-0944** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · скорость=высокая [coop]: число ходов конечно (партия сходится)
945. **MODE-COOP-0945** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · скорость=высокая [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
946. **MODE-COOP-0946** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · скорость=высокая [coop]: ни одной невалидной постановки не принято
947. **MODE-COOP-0947** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · скорость=высокая [coop]: инвариант доски: заполненные клетки не «протекают» за границы
948. **MODE-COOP-0948** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · скорость=высокая [coop]: очистка строки сдвигает/освобождает корректно
949. **MODE-COOP-0949** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · поле почти полное [coop]: счёт неотрицателен на всём протяжении
950. **MODE-COOP-0950** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · поле почти полное [coop]: ни одного исключения/краша за партию
951. **MODE-COOP-0951** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · поле почти полное [coop]: resume в середине — бит-в-бит то же продолжение
952. **MODE-COOP-0952** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · поле почти полное [coop]: число ходов конечно (партия сходится)
953. **MODE-COOP-0953** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · поле почти полное [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
954. **MODE-COOP-0954** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · поле почти полное [coop]: ни одной невалидной постановки не принято
955. **MODE-COOP-0955** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · поле почти полное [coop]: инвариант доски: заполненные клетки не «протекают» за границы
956. **MODE-COOP-0956** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · поле почти полное [coop]: очистка строки сдвигает/освобождает корректно
957. **MODE-COOP-0957** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · поле пустое [coop]: счёт неотрицателен на всём протяжении
958. **MODE-COOP-0958** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · поле пустое [coop]: ни одного исключения/краша за партию
959. **MODE-COOP-0959** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · поле пустое [coop]: resume в середине — бит-в-бит то же продолжение
960. **MODE-COOP-0960** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · поле пустое [coop]: число ходов конечно (партия сходится)
961. **MODE-COOP-0961** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · поле пустое [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
962. **MODE-COOP-0962** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · поле пустое [coop]: ни одной невалидной постановки не принято
963. **MODE-COOP-0963** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · поле пустое [coop]: инвариант доски: заполненные клетки не «протекают» за границы
964. **MODE-COOP-0964** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · поле пустое [coop]: очистка строки сдвигает/освобождает корректно
965. **MODE-COOP-0965** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · серия очисток [coop]: счёт неотрицателен на всём протяжении
966. **MODE-COOP-0966** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · серия очисток [coop]: ни одного исключения/краша за партию
967. **MODE-COOP-0967** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · серия очисток [coop]: resume в середине — бит-в-бит то же продолжение
968. **MODE-COOP-0968** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · серия очисток [coop]: число ходов конечно (партия сходится)
969. **MODE-COOP-0969** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · серия очисток [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
970. **MODE-COOP-0970** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · серия очисток [coop]: ни одной невалидной постановки не принято
971. **MODE-COOP-0971** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · серия очисток [coop]: инвариант доски: заполненные клетки не «протекают» за границы
972. **MODE-COOP-0972** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · серия очисток [coop]: очистка строки сдвигает/освобождает корректно
973. **MODE-COOP-0973** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · одиночная очистка [coop]: счёт неотрицателен на всём протяжении
974. **MODE-COOP-0974** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · одиночная очистка [coop]: ни одного исключения/краша за партию
975. **MODE-COOP-0975** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · одиночная очистка [coop]: resume в середине — бит-в-бит то же продолжение
976. **MODE-COOP-0976** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · одиночная очистка [coop]: число ходов конечно (партия сходится)
977. **MODE-COOP-0977** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · одиночная очистка [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
978. **MODE-COOP-0978** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · одиночная очистка [coop]: ни одной невалидной постановки не принято
979. **MODE-COOP-0979** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · одиночная очистка [coop]: инвариант доски: заполненные клетки не «протекают» за границы
980. **MODE-COOP-0980** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · одиночная очистка [coop]: очистка строки сдвигает/освобождает корректно
981. **MODE-COOP-0981** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · скорость=низкая [coop]: счёт неотрицателен на всём протяжении
982. **MODE-COOP-0982** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · скорость=низкая [coop]: ни одного исключения/краша за партию
983. **MODE-COOP-0983** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · скорость=низкая [coop]: resume в середине — бит-в-бит то же продолжение
984. **MODE-COOP-0984** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · скорость=низкая [coop]: число ходов конечно (партия сходится)
985. **MODE-COOP-0985** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · скорость=низкая [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
986. **MODE-COOP-0986** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · скорость=низкая [coop]: ни одной невалидной постановки не принято
987. **MODE-COOP-0987** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · скорость=низкая [coop]: инвариант доски: заполненные клетки не «протекают» за границы
988. **MODE-COOP-0988** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · скорость=низкая [coop]: очистка строки сдвигает/освобождает корректно
989. **MODE-COOP-0989** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · скорость=высокая [coop]: счёт неотрицателен на всём протяжении
990. **MODE-COOP-0990** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · скорость=высокая [coop]: ни одного исключения/краша за партию
991. **MODE-COOP-0991** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · скорость=высокая [coop]: resume в середине — бит-в-бит то же продолжение
992. **MODE-COOP-0992** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · скорость=высокая [coop]: число ходов конечно (партия сходится)
993. **MODE-COOP-0993** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · скорость=высокая [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
994. **MODE-COOP-0994** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · скорость=высокая [coop]: ни одной невалидной постановки не принято
995. **MODE-COOP-0995** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · скорость=высокая [coop]: инвариант доски: заполненные клетки не «протекают» за границы
996. **MODE-COOP-0996** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · скорость=высокая [coop]: очистка строки сдвигает/освобождает корректно
997. **MODE-COOP-0997** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · поле почти полное [coop]: счёт неотрицателен на всём протяжении
998. **MODE-COOP-0998** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · поле почти полное [coop]: ни одного исключения/краша за партию
999. **MODE-COOP-0999** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · поле почти полное [coop]: resume в середине — бит-в-бит то же продолжение
1000. **MODE-COOP-1000** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · поле почти полное [coop]: число ходов конечно (партия сходится)
1001. **MODE-COOP-1001** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · поле почти полное [coop]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
