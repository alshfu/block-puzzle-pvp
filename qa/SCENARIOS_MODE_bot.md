# BlockDuel — Каталог сценариев режима «С ботом (9×9)»

**Ровно 1001 сценариев** режима «С ботом (9×9)» («1000 и 1»).
Доска: 9×9. Маршрут: `/setup/bot`. Сгенерировано детерминированно
из `tools/gen_scenarios.py` (ось «режим») — НЕ редактировать вручную.

- **ID** `MODE-BOT-0001…MODE-BOT-1001` стабильны между прогонами.
- **[C]** — критический smoke-срез (каждый ~7-й).
- «Механика режима» — уникальная логика; хвост «Property-фаззинг» —
  осмысленная матрица (seed × конфиг × инвариант) для добора до 1001 в
  режимах малой игровой поверхности (это valid property-тесты, не филлер).

## Покрытие по областям

- Запуск и жизненный цикл: 12
- Рендер и темы: 35
- Ввод: 25
- Механика режима: 897
- Прогрессия и рекорды: 4
- Настройки в режиме: 15
- Ошибки и устойчивость: 4
- Property-фаззинг (seed×конфиг): 9

## Сценарии

1. **MODE-BOT-0001** — [C] [Запуск и жизненный цикл] Запуск «С ботом (9×9)» из меню по маршруту /setup/bot — экран рисуется без ошибок
2. **MODE-BOT-0002** — [Рендер и темы] «С ботом (9×9)»: поверхность «доска» в теме «neutral» — цвета/контраст по токенам
3. **MODE-BOT-0003** — [Ввод] «С ботом (9×9)»: выбор и постановка фигуры I через тап — без двойных срабатываний
4. **MODE-BOT-0004** — [Механика режима] Фигура I (0°): постановка в центр — призрак и валидация корректны
5. **MODE-BOT-0005** — [Прогрессия и рекорды] «С ботом (9×9)»: завершение партии пишет результат в статистику режима
6. **MODE-BOT-0006** — [Настройки в режиме] «С ботом (9×9)»: настройка «Звук (soundOn)» применяется в режиме и переживает перезапуск
7. **MODE-BOT-0007** — [Ошибки и устойчивость] «С ботом (9×9)»: повреждённая сохранёнка → graceful fallback, без краша
8. **MODE-BOT-0008** — [C] [Запуск и жизненный цикл] Возврат из «С ботом (9×9)» в меню — стек навигации корректен, без утечек
9. **MODE-BOT-0009** — [Рендер и темы] «С ботом (9×9)»: поверхность «доска» в теме «candy» — цвета/контраст по токенам
10. **MODE-BOT-0010** — [Ввод] «С ботом (9×9)»: выбор и постановка фигуры O через тап — без двойных срабатываний
11. **MODE-BOT-0011** — [Механика режима] Фигура I (0°): постановка к верхней границе — призрак и валидация корректны
12. **MODE-BOT-0012** — [Прогрессия и рекорды] «С ботом (9×9)»: вклад в composite-score (сводный рейтинг) учитывается
13. **MODE-BOT-0013** — [Настройки в режиме] «С ботом (9×9)»: настройка «Музыка (musicOn)» применяется в режиме и переживает перезапуск
14. **MODE-BOT-0014** — [Ошибки и устойчивость] «С ботом (9×9)»: сворачивание в момент хода → без рассинхрона состояния
15. **MODE-BOT-0015** — [C] [Запуск и жизненный цикл] Setup режима: выбор правил (handSize/rotation/flip) и старт партии
16. **MODE-BOT-0016** — [Рендер и темы] «С ботом (9×9)»: поверхность «доска» в теме «night» — цвета/контраст по токенам
17. **MODE-BOT-0017** — [Ввод] «С ботом (9×9)»: выбор и постановка фигуры T через тап — без двойных срабатываний
18. **MODE-BOT-0018** — [Механика режима] Фигура I (0°): постановка к нижней границе — призрак и валидация корректны
19. **MODE-BOT-0019** — [Прогрессия и рекорды] «С ботом (9×9)»: XP по исходу начисляется один раз (ничья ×0.5, поражение 0)
20. **MODE-BOT-0020** — [Настройки в режиме] «С ботом (9×9)»: настройка «Громкость SFX» применяется в режиме и переживает перезапуск
21. **MODE-BOT-0021** — [Ошибки и устойчивость] «С ботом (9×9)»: быстрый повторный ввод не даёт двойного хода
22. **MODE-BOT-0022** — [C] [Запуск и жизненный цикл] Setup режима: пресет блица применяется к первой партии
23. **MODE-BOT-0023** — [Рендер и темы] «С ботом (9×9)»: «доска» при reduceMotion=on — статичный декор
24. **MODE-BOT-0024** — [Ввод] «С ботом (9×9)»: выбор и постановка фигуры S через тап — без двойных срабатываний
25. **MODE-BOT-0025** — [Механика режима] Фигура I (0°): постановка к левому краю — призрак и валидация корректны
26. **MODE-BOT-0026** — [Прогрессия и рекорды] «С ботом (9×9)»: личный рекорд обновляется только при улучшении
27. **MODE-BOT-0027** — [Настройки в режиме] «С ботом (9×9)»: настройка «Громкость музыки» применяется в режиме и переживает перезапуск
28. **MODE-BOT-0028** — [Ошибки и устойчивость] «С ботом (9×9)»: возврат/пауза в критический момент не ломает партию
29. **MODE-BOT-0029** — [C] [Запуск и жизненный цикл] Setup режима: отмена возвращает в меню без создания партии
30. **MODE-BOT-0030** — [Рендер и темы] «С ботом (9×9)»: «доска» на узком окне — нет overflow
31. **MODE-BOT-0031** — [Ввод] «С ботом (9×9)»: выбор и постановка фигуры Z через тап — без двойных срабатываний
32. **MODE-BOT-0032** — [Механика режима] Фигура I (0°): постановка к правому краю — призрак и валидация корректны
33. **MODE-BOT-0033** — [Настройки в режиме] «С ботом (9×9)»: настройка «Вибрация» применяется в режиме и переживает перезапуск
34. **MODE-BOT-0034** — [Запуск и жизненный цикл] Холодный старт «С ботом (9×9)» в теме «neutral» — без белой вспышки
35. **MODE-BOT-0035** — [Рендер и темы] «С ботом (9×9)»: «доска» на широком окне — адаптивная боковая раскладка
36. **MODE-BOT-0036** — [C] [Ввод] «С ботом (9×9)»: выбор и постановка фигуры J через тап — без двойных срабатываний
37. **MODE-BOT-0037** — [Механика режима] Фигура I (0°): постановка в угол — призрак и валидация корректны
38. **MODE-BOT-0038** — [Настройки в режиме] «С ботом (9×9)»: настройка «Уменьшить движение (reduceMotion)» применяется в режиме и переживает перезапуск
39. **MODE-BOT-0039** — [Запуск и жизненный цикл] Холодный старт «С ботом (9×9)» в теме «candy» — без белой вспышки
40. **MODE-BOT-0040** — [Рендер и темы] «С ботом (9×9)»: «доска» с нейтральными клетками (a11y) — не зависит от цвета владельца
41. **MODE-BOT-0041** — [Ввод] «С ботом (9×9)»: выбор и постановка фигуры L через тап — без двойных срабатываний
42. **MODE-BOT-0042** — [Механика режима] Фигура I (0°): постановка на занятые клетки (отказ) — призрак и валидация корректны
43. **MODE-BOT-0043** — [C] [Настройки в режиме] «С ботом (9×9)»: настройка «Конфетти» применяется в режиме и переживает перезапуск
44. **MODE-BOT-0044** — [Запуск и жизненный цикл] Холодный старт «С ботом (9×9)» в теме «night» — без белой вспышки
45. **MODE-BOT-0045** — [Рендер и темы] «С ботом (9×9)»: поверхность «HUD (счёт/таймер)» в теме «neutral» — цвета/контраст по токенам
46. **MODE-BOT-0046** — [Ввод] «С ботом (9×9)»: выбор и постановка фигуры I через drag-and-drop — без двойных срабатываний
47. **MODE-BOT-0047** — [Механика режима] Фигура I (0°): постановка частично за доску (отказ) — призрак и валидация корректны
48. **MODE-BOT-0048** — [Настройки в режиме] «С ботом (9×9)»: настройка «Маскоты» применяется в режиме и переживает перезапуск
49. **MODE-BOT-0049** — [Запуск и жизненный цикл] Сворачивание во время «С ботом (9×9)» → resume восстанавливает состояние точно
50. **MODE-BOT-0050** — [C] [Рендер и темы] «С ботом (9×9)»: поверхность «HUD (счёт/таймер)» в теме «candy» — цвета/контраст по токенам
51. **MODE-BOT-0051** — [Ввод] «С ботом (9×9)»: выбор и постановка фигуры O через drag-and-drop — без двойных срабатываний
52. **MODE-BOT-0052** — [Механика режима] Фигура I (90°): постановка в центр — призрак и валидация корректны
53. **MODE-BOT-0053** — [Настройки в режиме] «С ботом (9×9)»: настройка «Призрак фигуры (ghost)» применяется в режиме и переживает перезапуск
54. **MODE-BOT-0054** — [Запуск и жизненный цикл] Перезапуск во время «С ботом (9×9)» → карточка «Продолжить» предлагает resume
55. **MODE-BOT-0055** — [Рендер и темы] «С ботом (9×9)»: поверхность «HUD (счёт/таймер)» в теме «night» — цвета/контраст по токенам
56. **MODE-BOT-0056** — [Ввод] «С ботом (9×9)»: выбор и постановка фигуры T через drag-and-drop — без двойных срабатываний
57. **MODE-BOT-0057** — [C] [Механика режима] Фигура I (90°): постановка к верхней границе — призрак и валидация корректны
58. **MODE-BOT-0058** — [Настройки в режиме] «С ботом (9×9)»: настройка «Задержка бота» применяется в режиме и переживает перезапуск
59. **MODE-BOT-0059** — [Запуск и жизненный цикл] resume «С ботом (9×9)» детерминистичен (та же раздача/seed)
60. **MODE-BOT-0060** — [Рендер и темы] «С ботом (9×9)»: «HUD (счёт/таймер)» при reduceMotion=on — статичный декор
61. **MODE-BOT-0061** — [Ввод] «С ботом (9×9)»: выбор и постановка фигуры S через drag-and-drop — без двойных срабатываний
62. **MODE-BOT-0062** — [Механика режима] Фигура I (90°): постановка к нижней границе — призрак и валидация корректны
63. **MODE-BOT-0063** — [Настройки в режиме] «С ботом (9×9)»: настройка «Тема по умолчанию» применяется в режиме и переживает перезапуск
64. **MODE-BOT-0064** — [C] [Запуск и жизненный цикл] Двойной быстрый тап по старту «С ботом (9×9)» не создаёт две партии
65. **MODE-BOT-0065** — [Рендер и темы] «С ботом (9×9)»: «HUD (счёт/таймер)» на узком окне — нет overflow
66. **MODE-BOT-0066** — [Ввод] «С ботом (9×9)»: выбор и постановка фигуры Z через drag-and-drop — без двойных срабатываний
67. **MODE-BOT-0067** — [Механика режима] Фигура I (90°): постановка к левому краю — призрак и валидация корректны
68. **MODE-BOT-0068** — [Настройки в режиме] «С ботом (9×9)»: настройка «Правила по умолчанию (handSize/rotation/flip)» применяется в режиме и переживает перезапуск
69. **MODE-BOT-0069** — [Рендер и темы] «С ботом (9×9)»: «HUD (счёт/таймер)» на широком окне — адаптивная боковая раскладка
70. **MODE-BOT-0070** — [Ввод] «С ботом (9×9)»: выбор и постановка фигуры J через drag-and-drop — без двойных срабатываний
71. **MODE-BOT-0071** — [C] [Механика режима] Фигура I (90°): постановка к правому краю — призрак и валидация корректны
72. **MODE-BOT-0072** — [Настройки в режиме] «С ботом (9×9)»: настройка «Пресет блица» применяется в режиме и переживает перезапуск
73. **MODE-BOT-0073** — [Рендер и темы] «С ботом (9×9)»: «HUD (счёт/таймер)» с нейтральными клетками (a11y) — не зависит от цвета владельца
74. **MODE-BOT-0074** — [Ввод] «С ботом (9×9)»: выбор и постановка фигуры L через drag-and-drop — без двойных срабатываний
75. **MODE-BOT-0075** — [Механика режима] Фигура I (90°): постановка в угол — призрак и валидация корректны
76. **MODE-BOT-0076** — [Настройки в режиме] «С ботом (9×9)»: настройка «Нейтральные клетки (accessibility)» применяется в режиме и переживает перезапуск
77. **MODE-BOT-0077** — [Рендер и темы] «С ботом (9×9)»: поверхность «панель руки» в теме «neutral» — цвета/контраст по токенам
78. **MODE-BOT-0078** — [C] [Ввод] «С ботом (9×9)»: выбор и постановка фигуры I через клавиатура — без двойных срабатываний
79. **MODE-BOT-0079** — [Механика режима] Фигура I (90°): постановка на занятые клетки (отказ) — призрак и валидация корректны
80. **MODE-BOT-0080** — [Настройки в режиме] «С ботом (9×9)»: настройка «Язык интерфейса» применяется в режиме и переживает перезапуск
81. **MODE-BOT-0081** — [Рендер и темы] «С ботом (9×9)»: поверхность «панель руки» в теме «candy» — цвета/контраст по токенам
82. **MODE-BOT-0082** — [Ввод] «С ботом (9×9)»: выбор и постановка фигуры O через клавиатура — без двойных срабатываний
83. **MODE-BOT-0083** — [Механика режима] Фигура I (90°): постановка частично за доску (отказ) — призрак и валидация корректны
84. **MODE-BOT-0084** — [Рендер и темы] «С ботом (9×9)»: поверхность «панель руки» в теме «night» — цвета/контраст по токенам
85. **MODE-BOT-0085** — [C] [Ввод] «С ботом (9×9)»: выбор и постановка фигуры T через клавиатура — без двойных срабатываний
86. **MODE-BOT-0086** — [Механика режима] Фигура I (180°): постановка в центр — призрак и валидация корректны
87. **MODE-BOT-0087** — [Рендер и темы] «С ботом (9×9)»: «панель руки» при reduceMotion=on — статичный декор
88. **MODE-BOT-0088** — [Ввод] «С ботом (9×9)»: выбор и постановка фигуры S через клавиатура — без двойных срабатываний
89. **MODE-BOT-0089** — [Механика режима] Фигура I (180°): постановка к верхней границе — призрак и валидация корректны
90. **MODE-BOT-0090** — [Рендер и темы] «С ботом (9×9)»: «панель руки» на узком окне — нет overflow
91. **MODE-BOT-0091** — [Ввод] «С ботом (9×9)»: выбор и постановка фигуры Z через клавиатура — без двойных срабатываний
92. **MODE-BOT-0092** — [C] [Механика режима] Фигура I (180°): постановка к нижней границе — призрак и валидация корректны
93. **MODE-BOT-0093** — [Рендер и темы] «С ботом (9×9)»: «панель руки» на широком окне — адаптивная боковая раскладка
94. **MODE-BOT-0094** — [Ввод] «С ботом (9×9)»: выбор и постановка фигуры J через клавиатура — без двойных срабатываний
95. **MODE-BOT-0095** — [Механика режима] Фигура I (180°): постановка к левому краю — призрак и валидация корректны
96. **MODE-BOT-0096** — [Рендер и темы] «С ботом (9×9)»: «панель руки» с нейтральными клетками (a11y) — не зависит от цвета владельца
97. **MODE-BOT-0097** — [Ввод] «С ботом (9×9)»: выбор и постановка фигуры L через клавиатура — без двойных срабатываний
98. **MODE-BOT-0098** — [Механика режима] Фигура I (180°): постановка к правому краю — призрак и валидация корректны
99. **MODE-BOT-0099** — [C] [Рендер и темы] «С ботом (9×9)»: поверхность «экран результата» в теме «neutral» — цвета/контраст по токенам
100. **MODE-BOT-0100** — [Ввод] «С ботом (9×9)»: поворот выбранной фигуры (если rotation вкл.) меняет ориентацию
101. **MODE-BOT-0101** — [Механика режима] Фигура I (180°): постановка в угол — призрак и валидация корректны
102. **MODE-BOT-0102** — [Рендер и темы] «С ботом (9×9)»: поверхность «экран результата» в теме «candy» — цвета/контраст по токенам
103. **MODE-BOT-0103** — [Ввод] «С ботом (9×9)»: отражение (если flip вкл.) даёт зеркальную фигуру
104. **MODE-BOT-0104** — [Механика режима] Фигура I (180°): постановка на занятые клетки (отказ) — призрак и валидация корректны
105. **MODE-BOT-0105** — [Рендер и темы] «С ботом (9×9)»: поверхность «экран результата» в теме «night» — цвета/контраст по токенам
106. **MODE-BOT-0106** — [C] [Ввод] «С ботом (9×9)»: призрак показывает валидность до отпускания
107. **MODE-BOT-0107** — [Механика режима] Фигура I (180°): постановка частично за доску (отказ) — призрак и валидация корректны
108. **MODE-BOT-0108** — [Рендер и темы] «С ботом (9×9)»: «экран результата» при reduceMotion=on — статичный декор
109. **MODE-BOT-0109** — [Ввод] «С ботом (9×9)»: постановка на занятые/за край — отклонена без изменения доски
110. **MODE-BOT-0110** — [Механика режима] Фигура I (270°): постановка в центр — призрак и валидация корректны
111. **MODE-BOT-0111** — [Рендер и темы] «С ботом (9×9)»: «экран результата» на узком окне — нет overflow
112. **MODE-BOT-0112** — [Механика режима] Фигура I (270°): постановка к верхней границе — призрак и валидация корректны
113. **MODE-BOT-0113** — [C] [Рендер и темы] «С ботом (9×9)»: «экран результата» на широком окне — адаптивная боковая раскладка
114. **MODE-BOT-0114** — [Механика режима] Фигура I (270°): постановка к нижней границе — призрак и валидация корректны
115. **MODE-BOT-0115** — [Рендер и темы] «С ботом (9×9)»: «экран результата» с нейтральными клетками (a11y) — не зависит от цвета владельца
116. **MODE-BOT-0116** — [Механика режима] Фигура I (270°): постановка к левому краю — призрак и валидация корректны
117. **MODE-BOT-0117** — [Рендер и темы] «С ботом (9×9)»: поверхность «пауза/меню выхода» в теме «neutral» — цвета/контраст по токенам
118. **MODE-BOT-0118** — [Механика режима] Фигура I (270°): постановка к правому краю — призрак и валидация корректны
119. **MODE-BOT-0119** — [Рендер и темы] «С ботом (9×9)»: поверхность «пауза/меню выхода» в теме «candy» — цвета/контраст по токенам
120. **MODE-BOT-0120** — [C] [Механика режима] Фигура I (270°): постановка в угол — призрак и валидация корректны
121. **MODE-BOT-0121** — [Рендер и темы] «С ботом (9×9)»: поверхность «пауза/меню выхода» в теме «night» — цвета/контраст по токенам
122. **MODE-BOT-0122** — [Механика режима] Фигура I (270°): постановка на занятые клетки (отказ) — призрак и валидация корректны
123. **MODE-BOT-0123** — [Рендер и темы] «С ботом (9×9)»: «пауза/меню выхода» при reduceMotion=on — статичный декор
124. **MODE-BOT-0124** — [Механика режима] Фигура I (270°): постановка частично за доску (отказ) — призрак и валидация корректны
125. **MODE-BOT-0125** — [Рендер и темы] «С ботом (9×9)»: «пауза/меню выхода» на узком окне — нет overflow
126. **MODE-BOT-0126** — [Механика режима] Фигура O (0°): постановка в центр — призрак и валидация корректны
127. **MODE-BOT-0127** — [C] [Рендер и темы] «С ботом (9×9)»: «пауза/меню выхода» на широком окне — адаптивная боковая раскладка
128. **MODE-BOT-0128** — [Механика режима] Фигура O (0°): постановка к верхней границе — призрак и валидация корректны
129. **MODE-BOT-0129** — [Рендер и темы] «С ботом (9×9)»: «пауза/меню выхода» с нейтральными клетками (a11y) — не зависит от цвета владельца
130. **MODE-BOT-0130** — [Механика режима] Фигура O (0°): постановка к нижней границе — призрак и валидация корректны
131. **MODE-BOT-0131** — [Механика режима] Фигура O (0°): постановка к левому краю — призрак и валидация корректны
132. **MODE-BOT-0132** — [Механика режима] Фигура O (0°): постановка к правому краю — призрак и валидация корректны
133. **MODE-BOT-0133** — [Механика режима] Фигура O (0°): постановка в угол — призрак и валидация корректны
134. **MODE-BOT-0134** — [C] [Механика режима] Фигура O (0°): постановка на занятые клетки (отказ) — призрак и валидация корректны
135. **MODE-BOT-0135** — [Механика режима] Фигура O (0°): постановка частично за доску (отказ) — призрак и валидация корректны
136. **MODE-BOT-0136** — [Механика режима] Фигура O (90°): постановка в центр — призрак и валидация корректны
137. **MODE-BOT-0137** — [Механика режима] Фигура O (90°): постановка к верхней границе — призрак и валидация корректны
138. **MODE-BOT-0138** — [Механика режима] Фигура O (90°): постановка к нижней границе — призрак и валидация корректны
139. **MODE-BOT-0139** — [Механика режима] Фигура O (90°): постановка к левому краю — призрак и валидация корректны
140. **MODE-BOT-0140** — [Механика режима] Фигура O (90°): постановка к правому краю — призрак и валидация корректны
141. **MODE-BOT-0141** — [C] [Механика режима] Фигура O (90°): постановка в угол — призрак и валидация корректны
142. **MODE-BOT-0142** — [Механика режима] Фигура O (90°): постановка на занятые клетки (отказ) — призрак и валидация корректны
143. **MODE-BOT-0143** — [Механика режима] Фигура O (90°): постановка частично за доску (отказ) — призрак и валидация корректны
144. **MODE-BOT-0144** — [Механика режима] Фигура O (180°): постановка в центр — призрак и валидация корректны
145. **MODE-BOT-0145** — [Механика режима] Фигура O (180°): постановка к верхней границе — призрак и валидация корректны
146. **MODE-BOT-0146** — [Механика режима] Фигура O (180°): постановка к нижней границе — призрак и валидация корректны
147. **MODE-BOT-0147** — [Механика режима] Фигура O (180°): постановка к левому краю — призрак и валидация корректны
148. **MODE-BOT-0148** — [C] [Механика режима] Фигура O (180°): постановка к правому краю — призрак и валидация корректны
149. **MODE-BOT-0149** — [Механика режима] Фигура O (180°): постановка в угол — призрак и валидация корректны
150. **MODE-BOT-0150** — [Механика режима] Фигура O (180°): постановка на занятые клетки (отказ) — призрак и валидация корректны
151. **MODE-BOT-0151** — [Механика режима] Фигура O (180°): постановка частично за доску (отказ) — призрак и валидация корректны
152. **MODE-BOT-0152** — [Механика режима] Фигура O (270°): постановка в центр — призрак и валидация корректны
153. **MODE-BOT-0153** — [Механика режима] Фигура O (270°): постановка к верхней границе — призрак и валидация корректны
154. **MODE-BOT-0154** — [Механика режима] Фигура O (270°): постановка к нижней границе — призрак и валидация корректны
155. **MODE-BOT-0155** — [C] [Механика режима] Фигура O (270°): постановка к левому краю — призрак и валидация корректны
156. **MODE-BOT-0156** — [Механика режима] Фигура O (270°): постановка к правому краю — призрак и валидация корректны
157. **MODE-BOT-0157** — [Механика режима] Фигура O (270°): постановка в угол — призрак и валидация корректны
158. **MODE-BOT-0158** — [Механика режима] Фигура O (270°): постановка на занятые клетки (отказ) — призрак и валидация корректны
159. **MODE-BOT-0159** — [Механика режима] Фигура O (270°): постановка частично за доску (отказ) — призрак и валидация корректны
160. **MODE-BOT-0160** — [Механика режима] Фигура T (0°): постановка в центр — призрак и валидация корректны
161. **MODE-BOT-0161** — [Механика режима] Фигура T (0°): постановка к верхней границе — призрак и валидация корректны
162. **MODE-BOT-0162** — [C] [Механика режима] Фигура T (0°): постановка к нижней границе — призрак и валидация корректны
163. **MODE-BOT-0163** — [Механика режима] Фигура T (0°): постановка к левому краю — призрак и валидация корректны
164. **MODE-BOT-0164** — [Механика режима] Фигура T (0°): постановка к правому краю — призрак и валидация корректны
165. **MODE-BOT-0165** — [Механика режима] Фигура T (0°): постановка в угол — призрак и валидация корректны
166. **MODE-BOT-0166** — [Механика режима] Фигура T (0°): постановка на занятые клетки (отказ) — призрак и валидация корректны
167. **MODE-BOT-0167** — [Механика режима] Фигура T (0°): постановка частично за доску (отказ) — призрак и валидация корректны
168. **MODE-BOT-0168** — [Механика режима] Фигура T (90°): постановка в центр — призрак и валидация корректны
169. **MODE-BOT-0169** — [C] [Механика режима] Фигура T (90°): постановка к верхней границе — призрак и валидация корректны
170. **MODE-BOT-0170** — [Механика режима] Фигура T (90°): постановка к нижней границе — призрак и валидация корректны
171. **MODE-BOT-0171** — [Механика режима] Фигура T (90°): постановка к левому краю — призрак и валидация корректны
172. **MODE-BOT-0172** — [Механика режима] Фигура T (90°): постановка к правому краю — призрак и валидация корректны
173. **MODE-BOT-0173** — [Механика режима] Фигура T (90°): постановка в угол — призрак и валидация корректны
174. **MODE-BOT-0174** — [Механика режима] Фигура T (90°): постановка на занятые клетки (отказ) — призрак и валидация корректны
175. **MODE-BOT-0175** — [Механика режима] Фигура T (90°): постановка частично за доску (отказ) — призрак и валидация корректны
176. **MODE-BOT-0176** — [C] [Механика режима] Фигура T (180°): постановка в центр — призрак и валидация корректны
177. **MODE-BOT-0177** — [Механика режима] Фигура T (180°): постановка к верхней границе — призрак и валидация корректны
178. **MODE-BOT-0178** — [Механика режима] Фигура T (180°): постановка к нижней границе — призрак и валидация корректны
179. **MODE-BOT-0179** — [Механика режима] Фигура T (180°): постановка к левому краю — призрак и валидация корректны
180. **MODE-BOT-0180** — [Механика режима] Фигура T (180°): постановка к правому краю — призрак и валидация корректны
181. **MODE-BOT-0181** — [Механика режима] Фигура T (180°): постановка в угол — призрак и валидация корректны
182. **MODE-BOT-0182** — [Механика режима] Фигура T (180°): постановка на занятые клетки (отказ) — призрак и валидация корректны
183. **MODE-BOT-0183** — [C] [Механика режима] Фигура T (180°): постановка частично за доску (отказ) — призрак и валидация корректны
184. **MODE-BOT-0184** — [Механика режима] Фигура T (270°): постановка в центр — призрак и валидация корректны
185. **MODE-BOT-0185** — [Механика режима] Фигура T (270°): постановка к верхней границе — призрак и валидация корректны
186. **MODE-BOT-0186** — [Механика режима] Фигура T (270°): постановка к нижней границе — призрак и валидация корректны
187. **MODE-BOT-0187** — [Механика режима] Фигура T (270°): постановка к левому краю — призрак и валидация корректны
188. **MODE-BOT-0188** — [Механика режима] Фигура T (270°): постановка к правому краю — призрак и валидация корректны
189. **MODE-BOT-0189** — [Механика режима] Фигура T (270°): постановка в угол — призрак и валидация корректны
190. **MODE-BOT-0190** — [C] [Механика режима] Фигура T (270°): постановка на занятые клетки (отказ) — призрак и валидация корректны
191. **MODE-BOT-0191** — [Механика режима] Фигура T (270°): постановка частично за доску (отказ) — призрак и валидация корректны
192. **MODE-BOT-0192** — [Механика режима] Фигура S (0°): постановка в центр — призрак и валидация корректны
193. **MODE-BOT-0193** — [Механика режима] Фигура S (0°): постановка к верхней границе — призрак и валидация корректны
194. **MODE-BOT-0194** — [Механика режима] Фигура S (0°): постановка к нижней границе — призрак и валидация корректны
195. **MODE-BOT-0195** — [Механика режима] Фигура S (0°): постановка к левому краю — призрак и валидация корректны
196. **MODE-BOT-0196** — [Механика режима] Фигура S (0°): постановка к правому краю — призрак и валидация корректны
197. **MODE-BOT-0197** — [C] [Механика режима] Фигура S (0°): постановка в угол — призрак и валидация корректны
198. **MODE-BOT-0198** — [Механика режима] Фигура S (0°): постановка на занятые клетки (отказ) — призрак и валидация корректны
199. **MODE-BOT-0199** — [Механика режима] Фигура S (0°): постановка частично за доску (отказ) — призрак и валидация корректны
200. **MODE-BOT-0200** — [Механика режима] Фигура S (90°): постановка в центр — призрак и валидация корректны
201. **MODE-BOT-0201** — [Механика режима] Фигура S (90°): постановка к верхней границе — призрак и валидация корректны
202. **MODE-BOT-0202** — [Механика режима] Фигура S (90°): постановка к нижней границе — призрак и валидация корректны
203. **MODE-BOT-0203** — [Механика режима] Фигура S (90°): постановка к левому краю — призрак и валидация корректны
204. **MODE-BOT-0204** — [C] [Механика режима] Фигура S (90°): постановка к правому краю — призрак и валидация корректны
205. **MODE-BOT-0205** — [Механика режима] Фигура S (90°): постановка в угол — призрак и валидация корректны
206. **MODE-BOT-0206** — [Механика режима] Фигура S (90°): постановка на занятые клетки (отказ) — призрак и валидация корректны
207. **MODE-BOT-0207** — [Механика режима] Фигура S (90°): постановка частично за доску (отказ) — призрак и валидация корректны
208. **MODE-BOT-0208** — [Механика режима] Фигура S (180°): постановка в центр — призрак и валидация корректны
209. **MODE-BOT-0209** — [Механика режима] Фигура S (180°): постановка к верхней границе — призрак и валидация корректны
210. **MODE-BOT-0210** — [Механика режима] Фигура S (180°): постановка к нижней границе — призрак и валидация корректны
211. **MODE-BOT-0211** — [C] [Механика режима] Фигура S (180°): постановка к левому краю — призрак и валидация корректны
212. **MODE-BOT-0212** — [Механика режима] Фигура S (180°): постановка к правому краю — призрак и валидация корректны
213. **MODE-BOT-0213** — [Механика режима] Фигура S (180°): постановка в угол — призрак и валидация корректны
214. **MODE-BOT-0214** — [Механика режима] Фигура S (180°): постановка на занятые клетки (отказ) — призрак и валидация корректны
215. **MODE-BOT-0215** — [Механика режима] Фигура S (180°): постановка частично за доску (отказ) — призрак и валидация корректны
216. **MODE-BOT-0216** — [Механика режима] Фигура S (270°): постановка в центр — призрак и валидация корректны
217. **MODE-BOT-0217** — [Механика режима] Фигура S (270°): постановка к верхней границе — призрак и валидация корректны
218. **MODE-BOT-0218** — [C] [Механика режима] Фигура S (270°): постановка к нижней границе — призрак и валидация корректны
219. **MODE-BOT-0219** — [Механика режима] Фигура S (270°): постановка к левому краю — призрак и валидация корректны
220. **MODE-BOT-0220** — [Механика режима] Фигура S (270°): постановка к правому краю — призрак и валидация корректны
221. **MODE-BOT-0221** — [Механика режима] Фигура S (270°): постановка в угол — призрак и валидация корректны
222. **MODE-BOT-0222** — [Механика режима] Фигура S (270°): постановка на занятые клетки (отказ) — призрак и валидация корректны
223. **MODE-BOT-0223** — [Механика режима] Фигура S (270°): постановка частично за доску (отказ) — призрак и валидация корректны
224. **MODE-BOT-0224** — [Механика режима] Фигура Z (0°): постановка в центр — призрак и валидация корректны
225. **MODE-BOT-0225** — [C] [Механика режима] Фигура Z (0°): постановка к верхней границе — призрак и валидация корректны
226. **MODE-BOT-0226** — [Механика режима] Фигура Z (0°): постановка к нижней границе — призрак и валидация корректны
227. **MODE-BOT-0227** — [Механика режима] Фигура Z (0°): постановка к левому краю — призрак и валидация корректны
228. **MODE-BOT-0228** — [Механика режима] Фигура Z (0°): постановка к правому краю — призрак и валидация корректны
229. **MODE-BOT-0229** — [Механика режима] Фигура Z (0°): постановка в угол — призрак и валидация корректны
230. **MODE-BOT-0230** — [Механика режима] Фигура Z (0°): постановка на занятые клетки (отказ) — призрак и валидация корректны
231. **MODE-BOT-0231** — [Механика режима] Фигура Z (0°): постановка частично за доску (отказ) — призрак и валидация корректны
232. **MODE-BOT-0232** — [C] [Механика режима] Фигура Z (90°): постановка в центр — призрак и валидация корректны
233. **MODE-BOT-0233** — [Механика режима] Фигура Z (90°): постановка к верхней границе — призрак и валидация корректны
234. **MODE-BOT-0234** — [Механика режима] Фигура Z (90°): постановка к нижней границе — призрак и валидация корректны
235. **MODE-BOT-0235** — [Механика режима] Фигура Z (90°): постановка к левому краю — призрак и валидация корректны
236. **MODE-BOT-0236** — [Механика режима] Фигура Z (90°): постановка к правому краю — призрак и валидация корректны
237. **MODE-BOT-0237** — [Механика режима] Фигура Z (90°): постановка в угол — призрак и валидация корректны
238. **MODE-BOT-0238** — [Механика режима] Фигура Z (90°): постановка на занятые клетки (отказ) — призрак и валидация корректны
239. **MODE-BOT-0239** — [C] [Механика режима] Фигура Z (90°): постановка частично за доску (отказ) — призрак и валидация корректны
240. **MODE-BOT-0240** — [Механика режима] Фигура Z (180°): постановка в центр — призрак и валидация корректны
241. **MODE-BOT-0241** — [Механика режима] Фигура Z (180°): постановка к верхней границе — призрак и валидация корректны
242. **MODE-BOT-0242** — [Механика режима] Фигура Z (180°): постановка к нижней границе — призрак и валидация корректны
243. **MODE-BOT-0243** — [Механика режима] Фигура Z (180°): постановка к левому краю — призрак и валидация корректны
244. **MODE-BOT-0244** — [Механика режима] Фигура Z (180°): постановка к правому краю — призрак и валидация корректны
245. **MODE-BOT-0245** — [Механика режима] Фигура Z (180°): постановка в угол — призрак и валидация корректны
246. **MODE-BOT-0246** — [C] [Механика режима] Фигура Z (180°): постановка на занятые клетки (отказ) — призрак и валидация корректны
247. **MODE-BOT-0247** — [Механика режима] Фигура Z (180°): постановка частично за доску (отказ) — призрак и валидация корректны
248. **MODE-BOT-0248** — [Механика режима] Фигура Z (270°): постановка в центр — призрак и валидация корректны
249. **MODE-BOT-0249** — [Механика режима] Фигура Z (270°): постановка к верхней границе — призрак и валидация корректны
250. **MODE-BOT-0250** — [Механика режима] Фигура Z (270°): постановка к нижней границе — призрак и валидация корректны
251. **MODE-BOT-0251** — [Механика режима] Фигура Z (270°): постановка к левому краю — призрак и валидация корректны
252. **MODE-BOT-0252** — [Механика режима] Фигура Z (270°): постановка к правому краю — призрак и валидация корректны
253. **MODE-BOT-0253** — [C] [Механика режима] Фигура Z (270°): постановка в угол — призрак и валидация корректны
254. **MODE-BOT-0254** — [Механика режима] Фигура Z (270°): постановка на занятые клетки (отказ) — призрак и валидация корректны
255. **MODE-BOT-0255** — [Механика режима] Фигура Z (270°): постановка частично за доску (отказ) — призрак и валидация корректны
256. **MODE-BOT-0256** — [Механика режима] Фигура J (0°): постановка в центр — призрак и валидация корректны
257. **MODE-BOT-0257** — [Механика режима] Фигура J (0°): постановка к верхней границе — призрак и валидация корректны
258. **MODE-BOT-0258** — [Механика режима] Фигура J (0°): постановка к нижней границе — призрак и валидация корректны
259. **MODE-BOT-0259** — [Механика режима] Фигура J (0°): постановка к левому краю — призрак и валидация корректны
260. **MODE-BOT-0260** — [C] [Механика режима] Фигура J (0°): постановка к правому краю — призрак и валидация корректны
261. **MODE-BOT-0261** — [Механика режима] Фигура J (0°): постановка в угол — призрак и валидация корректны
262. **MODE-BOT-0262** — [Механика режима] Фигура J (0°): постановка на занятые клетки (отказ) — призрак и валидация корректны
263. **MODE-BOT-0263** — [Механика режима] Фигура J (0°): постановка частично за доску (отказ) — призрак и валидация корректны
264. **MODE-BOT-0264** — [Механика режима] Фигура J (90°): постановка в центр — призрак и валидация корректны
265. **MODE-BOT-0265** — [Механика режима] Фигура J (90°): постановка к верхней границе — призрак и валидация корректны
266. **MODE-BOT-0266** — [Механика режима] Фигура J (90°): постановка к нижней границе — призрак и валидация корректны
267. **MODE-BOT-0267** — [C] [Механика режима] Фигура J (90°): постановка к левому краю — призрак и валидация корректны
268. **MODE-BOT-0268** — [Механика режима] Фигура J (90°): постановка к правому краю — призрак и валидация корректны
269. **MODE-BOT-0269** — [Механика режима] Фигура J (90°): постановка в угол — призрак и валидация корректны
270. **MODE-BOT-0270** — [Механика режима] Фигура J (90°): постановка на занятые клетки (отказ) — призрак и валидация корректны
271. **MODE-BOT-0271** — [Механика режима] Фигура J (90°): постановка частично за доску (отказ) — призрак и валидация корректны
272. **MODE-BOT-0272** — [Механика режима] Фигура J (180°): постановка в центр — призрак и валидация корректны
273. **MODE-BOT-0273** — [Механика режима] Фигура J (180°): постановка к верхней границе — призрак и валидация корректны
274. **MODE-BOT-0274** — [C] [Механика режима] Фигура J (180°): постановка к нижней границе — призрак и валидация корректны
275. **MODE-BOT-0275** — [Механика режима] Фигура J (180°): постановка к левому краю — призрак и валидация корректны
276. **MODE-BOT-0276** — [Механика режима] Фигура J (180°): постановка к правому краю — призрак и валидация корректны
277. **MODE-BOT-0277** — [Механика режима] Фигура J (180°): постановка в угол — призрак и валидация корректны
278. **MODE-BOT-0278** — [Механика режима] Фигура J (180°): постановка на занятые клетки (отказ) — призрак и валидация корректны
279. **MODE-BOT-0279** — [Механика режима] Фигура J (180°): постановка частично за доску (отказ) — призрак и валидация корректны
280. **MODE-BOT-0280** — [Механика режима] Фигура J (270°): постановка в центр — призрак и валидация корректны
281. **MODE-BOT-0281** — [C] [Механика режима] Фигура J (270°): постановка к верхней границе — призрак и валидация корректны
282. **MODE-BOT-0282** — [Механика режима] Фигура J (270°): постановка к нижней границе — призрак и валидация корректны
283. **MODE-BOT-0283** — [Механика режима] Фигура J (270°): постановка к левому краю — призрак и валидация корректны
284. **MODE-BOT-0284** — [Механика режима] Фигура J (270°): постановка к правому краю — призрак и валидация корректны
285. **MODE-BOT-0285** — [Механика режима] Фигура J (270°): постановка в угол — призрак и валидация корректны
286. **MODE-BOT-0286** — [Механика режима] Фигура J (270°): постановка на занятые клетки (отказ) — призрак и валидация корректны
287. **MODE-BOT-0287** — [Механика режима] Фигура J (270°): постановка частично за доску (отказ) — призрак и валидация корректны
288. **MODE-BOT-0288** — [C] [Механика режима] Фигура L (0°): постановка в центр — призрак и валидация корректны
289. **MODE-BOT-0289** — [Механика режима] Фигура L (0°): постановка к верхней границе — призрак и валидация корректны
290. **MODE-BOT-0290** — [Механика режима] Фигура L (0°): постановка к нижней границе — призрак и валидация корректны
291. **MODE-BOT-0291** — [Механика режима] Фигура L (0°): постановка к левому краю — призрак и валидация корректны
292. **MODE-BOT-0292** — [Механика режима] Фигура L (0°): постановка к правому краю — призрак и валидация корректны
293. **MODE-BOT-0293** — [Механика режима] Фигура L (0°): постановка в угол — призрак и валидация корректны
294. **MODE-BOT-0294** — [Механика режима] Фигура L (0°): постановка на занятые клетки (отказ) — призрак и валидация корректны
295. **MODE-BOT-0295** — [C] [Механика режима] Фигура L (0°): постановка частично за доску (отказ) — призрак и валидация корректны
296. **MODE-BOT-0296** — [Механика режима] Фигура L (90°): постановка в центр — призрак и валидация корректны
297. **MODE-BOT-0297** — [Механика режима] Фигура L (90°): постановка к верхней границе — призрак и валидация корректны
298. **MODE-BOT-0298** — [Механика режима] Фигура L (90°): постановка к нижней границе — призрак и валидация корректны
299. **MODE-BOT-0299** — [Механика режима] Фигура L (90°): постановка к левому краю — призрак и валидация корректны
300. **MODE-BOT-0300** — [Механика режима] Фигура L (90°): постановка к правому краю — призрак и валидация корректны
301. **MODE-BOT-0301** — [Механика режима] Фигура L (90°): постановка в угол — призрак и валидация корректны
302. **MODE-BOT-0302** — [C] [Механика режима] Фигура L (90°): постановка на занятые клетки (отказ) — призрак и валидация корректны
303. **MODE-BOT-0303** — [Механика режима] Фигура L (90°): постановка частично за доску (отказ) — призрак и валидация корректны
304. **MODE-BOT-0304** — [Механика режима] Фигура L (180°): постановка в центр — призрак и валидация корректны
305. **MODE-BOT-0305** — [Механика режима] Фигура L (180°): постановка к верхней границе — призрак и валидация корректны
306. **MODE-BOT-0306** — [Механика режима] Фигура L (180°): постановка к нижней границе — призрак и валидация корректны
307. **MODE-BOT-0307** — [Механика режима] Фигура L (180°): постановка к левому краю — призрак и валидация корректны
308. **MODE-BOT-0308** — [Механика режима] Фигура L (180°): постановка к правому краю — призрак и валидация корректны
309. **MODE-BOT-0309** — [C] [Механика режима] Фигура L (180°): постановка в угол — призрак и валидация корректны
310. **MODE-BOT-0310** — [Механика режима] Фигура L (180°): постановка на занятые клетки (отказ) — призрак и валидация корректны
311. **MODE-BOT-0311** — [Механика режима] Фигура L (180°): постановка частично за доску (отказ) — призрак и валидация корректны
312. **MODE-BOT-0312** — [Механика режима] Фигура L (270°): постановка в центр — призрак и валидация корректны
313. **MODE-BOT-0313** — [Механика режима] Фигура L (270°): постановка к верхней границе — призрак и валидация корректны
314. **MODE-BOT-0314** — [Механика режима] Фигура L (270°): постановка к нижней границе — призрак и валидация корректны
315. **MODE-BOT-0315** — [Механика режима] Фигура L (270°): постановка к левому краю — призрак и валидация корректны
316. **MODE-BOT-0316** — [C] [Механика режима] Фигура L (270°): постановка к правому краю — призрак и валидация корректны
317. **MODE-BOT-0317** — [Механика режима] Фигура L (270°): постановка в угол — призрак и валидация корректны
318. **MODE-BOT-0318** — [Механика режима] Фигура L (270°): постановка на занятые клетки (отказ) — призрак и валидация корректны
319. **MODE-BOT-0319** — [Механика режима] Фигура L (270°): постановка частично за доску (отказ) — призрак и валидация корректны
320. **MODE-BOT-0320** — [Механика режима] Фигура I: якорь (0,0) — валидна если помещается, иначе отклонена
321. **MODE-BOT-0321** — [Механика режима] Фигура I: якорь (0,1) — валидна если помещается, иначе отклонена
322. **MODE-BOT-0322** — [Механика режима] Фигура I: якорь (0,2) — валидна если помещается, иначе отклонена
323. **MODE-BOT-0323** — [C] [Механика режима] Фигура I: якорь (0,3) — валидна если помещается, иначе отклонена
324. **MODE-BOT-0324** — [Механика режима] Фигура I: якорь (0,4) — валидна если помещается, иначе отклонена
325. **MODE-BOT-0325** — [Механика режима] Фигура I: якорь (0,5) — валидна если помещается, иначе отклонена
326. **MODE-BOT-0326** — [Механика режима] Фигура I: якорь (0,6) — валидна если помещается, иначе отклонена
327. **MODE-BOT-0327** — [Механика режима] Фигура I: якорь (0,7) — валидна если помещается, иначе отклонена
328. **MODE-BOT-0328** — [Механика режима] Фигура I: якорь (0,8) — валидна если помещается, иначе отклонена
329. **MODE-BOT-0329** — [Механика режима] Фигура I: якорь (1,0) — валидна если помещается, иначе отклонена
330. **MODE-BOT-0330** — [C] [Механика режима] Фигура I: якорь (1,1) — валидна если помещается, иначе отклонена
331. **MODE-BOT-0331** — [Механика режима] Фигура I: якорь (1,2) — валидна если помещается, иначе отклонена
332. **MODE-BOT-0332** — [Механика режима] Фигура I: якорь (1,3) — валидна если помещается, иначе отклонена
333. **MODE-BOT-0333** — [Механика режима] Фигура I: якорь (1,4) — валидна если помещается, иначе отклонена
334. **MODE-BOT-0334** — [Механика режима] Фигура I: якорь (1,5) — валидна если помещается, иначе отклонена
335. **MODE-BOT-0335** — [Механика режима] Фигура I: якорь (1,6) — валидна если помещается, иначе отклонена
336. **MODE-BOT-0336** — [Механика режима] Фигура I: якорь (1,7) — валидна если помещается, иначе отклонена
337. **MODE-BOT-0337** — [C] [Механика режима] Фигура I: якорь (1,8) — валидна если помещается, иначе отклонена
338. **MODE-BOT-0338** — [Механика режима] Фигура I: якорь (2,0) — валидна если помещается, иначе отклонена
339. **MODE-BOT-0339** — [Механика режима] Фигура I: якорь (2,1) — валидна если помещается, иначе отклонена
340. **MODE-BOT-0340** — [Механика режима] Фигура I: якорь (2,2) — валидна если помещается, иначе отклонена
341. **MODE-BOT-0341** — [Механика режима] Фигура I: якорь (2,3) — валидна если помещается, иначе отклонена
342. **MODE-BOT-0342** — [Механика режима] Фигура I: якорь (2,4) — валидна если помещается, иначе отклонена
343. **MODE-BOT-0343** — [Механика режима] Фигура I: якорь (2,5) — валидна если помещается, иначе отклонена
344. **MODE-BOT-0344** — [C] [Механика режима] Фигура I: якорь (2,6) — валидна если помещается, иначе отклонена
345. **MODE-BOT-0345** — [Механика режима] Фигура I: якорь (2,7) — валидна если помещается, иначе отклонена
346. **MODE-BOT-0346** — [Механика режима] Фигура I: якорь (2,8) — валидна если помещается, иначе отклонена
347. **MODE-BOT-0347** — [Механика режима] Фигура I: якорь (3,0) — валидна если помещается, иначе отклонена
348. **MODE-BOT-0348** — [Механика режима] Фигура I: якорь (3,1) — валидна если помещается, иначе отклонена
349. **MODE-BOT-0349** — [Механика режима] Фигура I: якорь (3,2) — валидна если помещается, иначе отклонена
350. **MODE-BOT-0350** — [Механика режима] Фигура I: якорь (3,3) — валидна если помещается, иначе отклонена
351. **MODE-BOT-0351** — [C] [Механика режима] Фигура I: якорь (3,4) — валидна если помещается, иначе отклонена
352. **MODE-BOT-0352** — [Механика режима] Фигура I: якорь (3,5) — валидна если помещается, иначе отклонена
353. **MODE-BOT-0353** — [Механика режима] Фигура I: якорь (3,6) — валидна если помещается, иначе отклонена
354. **MODE-BOT-0354** — [Механика режима] Фигура I: якорь (3,7) — валидна если помещается, иначе отклонена
355. **MODE-BOT-0355** — [Механика режима] Фигура I: якорь (3,8) — валидна если помещается, иначе отклонена
356. **MODE-BOT-0356** — [Механика режима] Фигура I: якорь (4,0) — валидна если помещается, иначе отклонена
357. **MODE-BOT-0357** — [Механика режима] Фигура I: якорь (4,1) — валидна если помещается, иначе отклонена
358. **MODE-BOT-0358** — [C] [Механика режима] Фигура I: якорь (4,2) — валидна если помещается, иначе отклонена
359. **MODE-BOT-0359** — [Механика режима] Фигура I: якорь (4,3) — валидна если помещается, иначе отклонена
360. **MODE-BOT-0360** — [Механика режима] Фигура I: якорь (4,4) — валидна если помещается, иначе отклонена
361. **MODE-BOT-0361** — [Механика режима] Фигура I: якорь (4,5) — валидна если помещается, иначе отклонена
362. **MODE-BOT-0362** — [Механика режима] Фигура I: якорь (4,6) — валидна если помещается, иначе отклонена
363. **MODE-BOT-0363** — [Механика режима] Фигура I: якорь (4,7) — валидна если помещается, иначе отклонена
364. **MODE-BOT-0364** — [Механика режима] Фигура I: якорь (4,8) — валидна если помещается, иначе отклонена
365. **MODE-BOT-0365** — [C] [Механика режима] Фигура I: якорь (5,0) — валидна если помещается, иначе отклонена
366. **MODE-BOT-0366** — [Механика режима] Фигура I: якорь (5,1) — валидна если помещается, иначе отклонена
367. **MODE-BOT-0367** — [Механика режима] Фигура I: якорь (5,2) — валидна если помещается, иначе отклонена
368. **MODE-BOT-0368** — [Механика режима] Фигура I: якорь (5,3) — валидна если помещается, иначе отклонена
369. **MODE-BOT-0369** — [Механика режима] Фигура I: якорь (5,4) — валидна если помещается, иначе отклонена
370. **MODE-BOT-0370** — [Механика режима] Фигура I: якорь (5,5) — валидна если помещается, иначе отклонена
371. **MODE-BOT-0371** — [Механика режима] Фигура I: якорь (5,6) — валидна если помещается, иначе отклонена
372. **MODE-BOT-0372** — [C] [Механика режима] Фигура I: якорь (5,7) — валидна если помещается, иначе отклонена
373. **MODE-BOT-0373** — [Механика режима] Фигура I: якорь (5,8) — валидна если помещается, иначе отклонена
374. **MODE-BOT-0374** — [Механика режима] Фигура I: якорь (6,0) — валидна если помещается, иначе отклонена
375. **MODE-BOT-0375** — [Механика режима] Фигура I: якорь (6,1) — валидна если помещается, иначе отклонена
376. **MODE-BOT-0376** — [Механика режима] Фигура I: якорь (6,2) — валидна если помещается, иначе отклонена
377. **MODE-BOT-0377** — [Механика режима] Фигура I: якорь (6,3) — валидна если помещается, иначе отклонена
378. **MODE-BOT-0378** — [Механика режима] Фигура I: якорь (6,4) — валидна если помещается, иначе отклонена
379. **MODE-BOT-0379** — [C] [Механика режима] Фигура I: якорь (6,5) — валидна если помещается, иначе отклонена
380. **MODE-BOT-0380** — [Механика режима] Фигура I: якорь (6,6) — валидна если помещается, иначе отклонена
381. **MODE-BOT-0381** — [Механика режима] Фигура I: якорь (6,7) — валидна если помещается, иначе отклонена
382. **MODE-BOT-0382** — [Механика режима] Фигура I: якорь (6,8) — валидна если помещается, иначе отклонена
383. **MODE-BOT-0383** — [Механика режима] Фигура I: якорь (7,0) — валидна если помещается, иначе отклонена
384. **MODE-BOT-0384** — [Механика режима] Фигура I: якорь (7,1) — валидна если помещается, иначе отклонена
385. **MODE-BOT-0385** — [Механика режима] Фигура I: якорь (7,2) — валидна если помещается, иначе отклонена
386. **MODE-BOT-0386** — [C] [Механика режима] Фигура I: якорь (7,3) — валидна если помещается, иначе отклонена
387. **MODE-BOT-0387** — [Механика режима] Фигура I: якорь (7,4) — валидна если помещается, иначе отклонена
388. **MODE-BOT-0388** — [Механика режима] Фигура I: якорь (7,5) — валидна если помещается, иначе отклонена
389. **MODE-BOT-0389** — [Механика режима] Фигура I: якорь (7,6) — валидна если помещается, иначе отклонена
390. **MODE-BOT-0390** — [Механика режима] Фигура I: якорь (7,7) — валидна если помещается, иначе отклонена
391. **MODE-BOT-0391** — [Механика режима] Фигура I: якорь (7,8) — валидна если помещается, иначе отклонена
392. **MODE-BOT-0392** — [Механика режима] Фигура I: якорь (8,0) — валидна если помещается, иначе отклонена
393. **MODE-BOT-0393** — [C] [Механика режима] Фигура I: якорь (8,1) — валидна если помещается, иначе отклонена
394. **MODE-BOT-0394** — [Механика режима] Фигура I: якорь (8,2) — валидна если помещается, иначе отклонена
395. **MODE-BOT-0395** — [Механика режима] Фигура I: якорь (8,3) — валидна если помещается, иначе отклонена
396. **MODE-BOT-0396** — [Механика режима] Фигура I: якорь (8,4) — валидна если помещается, иначе отклонена
397. **MODE-BOT-0397** — [Механика режима] Фигура I: якорь (8,5) — валидна если помещается, иначе отклонена
398. **MODE-BOT-0398** — [Механика режима] Фигура I: якорь (8,6) — валидна если помещается, иначе отклонена
399. **MODE-BOT-0399** — [Механика режима] Фигура I: якорь (8,7) — валидна если помещается, иначе отклонена
400. **MODE-BOT-0400** — [C] [Механика режима] Фигура I: якорь (8,8) — валидна если помещается, иначе отклонена
401. **MODE-BOT-0401** — [Механика режима] Фигура O: якорь (0,0) — валидна если помещается, иначе отклонена
402. **MODE-BOT-0402** — [Механика режима] Фигура O: якорь (0,1) — валидна если помещается, иначе отклонена
403. **MODE-BOT-0403** — [Механика режима] Фигура O: якорь (0,2) — валидна если помещается, иначе отклонена
404. **MODE-BOT-0404** — [Механика режима] Фигура O: якорь (0,3) — валидна если помещается, иначе отклонена
405. **MODE-BOT-0405** — [Механика режима] Фигура O: якорь (0,4) — валидна если помещается, иначе отклонена
406. **MODE-BOT-0406** — [Механика режима] Фигура O: якорь (0,5) — валидна если помещается, иначе отклонена
407. **MODE-BOT-0407** — [C] [Механика режима] Фигура O: якорь (0,6) — валидна если помещается, иначе отклонена
408. **MODE-BOT-0408** — [Механика режима] Фигура O: якорь (0,7) — валидна если помещается, иначе отклонена
409. **MODE-BOT-0409** — [Механика режима] Фигура O: якорь (0,8) — валидна если помещается, иначе отклонена
410. **MODE-BOT-0410** — [Механика режима] Фигура O: якорь (1,0) — валидна если помещается, иначе отклонена
411. **MODE-BOT-0411** — [Механика режима] Фигура O: якорь (1,1) — валидна если помещается, иначе отклонена
412. **MODE-BOT-0412** — [Механика режима] Фигура O: якорь (1,2) — валидна если помещается, иначе отклонена
413. **MODE-BOT-0413** — [Механика режима] Фигура O: якорь (1,3) — валидна если помещается, иначе отклонена
414. **MODE-BOT-0414** — [C] [Механика режима] Фигура O: якорь (1,4) — валидна если помещается, иначе отклонена
415. **MODE-BOT-0415** — [Механика режима] Фигура O: якорь (1,5) — валидна если помещается, иначе отклонена
416. **MODE-BOT-0416** — [Механика режима] Фигура O: якорь (1,6) — валидна если помещается, иначе отклонена
417. **MODE-BOT-0417** — [Механика режима] Фигура O: якорь (1,7) — валидна если помещается, иначе отклонена
418. **MODE-BOT-0418** — [Механика режима] Фигура O: якорь (1,8) — валидна если помещается, иначе отклонена
419. **MODE-BOT-0419** — [Механика режима] Фигура O: якорь (2,0) — валидна если помещается, иначе отклонена
420. **MODE-BOT-0420** — [Механика режима] Фигура O: якорь (2,1) — валидна если помещается, иначе отклонена
421. **MODE-BOT-0421** — [C] [Механика режима] Фигура O: якорь (2,2) — валидна если помещается, иначе отклонена
422. **MODE-BOT-0422** — [Механика режима] Фигура O: якорь (2,3) — валидна если помещается, иначе отклонена
423. **MODE-BOT-0423** — [Механика режима] Фигура O: якорь (2,4) — валидна если помещается, иначе отклонена
424. **MODE-BOT-0424** — [Механика режима] Фигура O: якорь (2,5) — валидна если помещается, иначе отклонена
425. **MODE-BOT-0425** — [Механика режима] Фигура O: якорь (2,6) — валидна если помещается, иначе отклонена
426. **MODE-BOT-0426** — [Механика режима] Фигура O: якорь (2,7) — валидна если помещается, иначе отклонена
427. **MODE-BOT-0427** — [Механика режима] Фигура O: якорь (2,8) — валидна если помещается, иначе отклонена
428. **MODE-BOT-0428** — [C] [Механика режима] Фигура O: якорь (3,0) — валидна если помещается, иначе отклонена
429. **MODE-BOT-0429** — [Механика режима] Фигура O: якорь (3,1) — валидна если помещается, иначе отклонена
430. **MODE-BOT-0430** — [Механика режима] Фигура O: якорь (3,2) — валидна если помещается, иначе отклонена
431. **MODE-BOT-0431** — [Механика режима] Фигура O: якорь (3,3) — валидна если помещается, иначе отклонена
432. **MODE-BOT-0432** — [Механика режима] Фигура O: якорь (3,4) — валидна если помещается, иначе отклонена
433. **MODE-BOT-0433** — [Механика режима] Фигура O: якорь (3,5) — валидна если помещается, иначе отклонена
434. **MODE-BOT-0434** — [Механика режима] Фигура O: якорь (3,6) — валидна если помещается, иначе отклонена
435. **MODE-BOT-0435** — [C] [Механика режима] Фигура O: якорь (3,7) — валидна если помещается, иначе отклонена
436. **MODE-BOT-0436** — [Механика режима] Фигура O: якорь (3,8) — валидна если помещается, иначе отклонена
437. **MODE-BOT-0437** — [Механика режима] Фигура O: якорь (4,0) — валидна если помещается, иначе отклонена
438. **MODE-BOT-0438** — [Механика режима] Фигура O: якорь (4,1) — валидна если помещается, иначе отклонена
439. **MODE-BOT-0439** — [Механика режима] Фигура O: якорь (4,2) — валидна если помещается, иначе отклонена
440. **MODE-BOT-0440** — [Механика режима] Фигура O: якорь (4,3) — валидна если помещается, иначе отклонена
441. **MODE-BOT-0441** — [Механика режима] Фигура O: якорь (4,4) — валидна если помещается, иначе отклонена
442. **MODE-BOT-0442** — [C] [Механика режима] Фигура O: якорь (4,5) — валидна если помещается, иначе отклонена
443. **MODE-BOT-0443** — [Механика режима] Фигура O: якорь (4,6) — валидна если помещается, иначе отклонена
444. **MODE-BOT-0444** — [Механика режима] Фигура O: якорь (4,7) — валидна если помещается, иначе отклонена
445. **MODE-BOT-0445** — [Механика режима] Фигура O: якорь (4,8) — валидна если помещается, иначе отклонена
446. **MODE-BOT-0446** — [Механика режима] Фигура O: якорь (5,0) — валидна если помещается, иначе отклонена
447. **MODE-BOT-0447** — [Механика режима] Фигура O: якорь (5,1) — валидна если помещается, иначе отклонена
448. **MODE-BOT-0448** — [Механика режима] Фигура O: якорь (5,2) — валидна если помещается, иначе отклонена
449. **MODE-BOT-0449** — [C] [Механика режима] Фигура O: якорь (5,3) — валидна если помещается, иначе отклонена
450. **MODE-BOT-0450** — [Механика режима] Фигура O: якорь (5,4) — валидна если помещается, иначе отклонена
451. **MODE-BOT-0451** — [Механика режима] Фигура O: якорь (5,5) — валидна если помещается, иначе отклонена
452. **MODE-BOT-0452** — [Механика режима] Фигура O: якорь (5,6) — валидна если помещается, иначе отклонена
453. **MODE-BOT-0453** — [Механика режима] Фигура O: якорь (5,7) — валидна если помещается, иначе отклонена
454. **MODE-BOT-0454** — [Механика режима] Фигура O: якорь (5,8) — валидна если помещается, иначе отклонена
455. **MODE-BOT-0455** — [Механика режима] Фигура O: якорь (6,0) — валидна если помещается, иначе отклонена
456. **MODE-BOT-0456** — [C] [Механика режима] Фигура O: якорь (6,1) — валидна если помещается, иначе отклонена
457. **MODE-BOT-0457** — [Механика режима] Фигура O: якорь (6,2) — валидна если помещается, иначе отклонена
458. **MODE-BOT-0458** — [Механика режима] Фигура O: якорь (6,3) — валидна если помещается, иначе отклонена
459. **MODE-BOT-0459** — [Механика режима] Фигура O: якорь (6,4) — валидна если помещается, иначе отклонена
460. **MODE-BOT-0460** — [Механика режима] Фигура O: якорь (6,5) — валидна если помещается, иначе отклонена
461. **MODE-BOT-0461** — [Механика режима] Фигура O: якорь (6,6) — валидна если помещается, иначе отклонена
462. **MODE-BOT-0462** — [Механика режима] Фигура O: якорь (6,7) — валидна если помещается, иначе отклонена
463. **MODE-BOT-0463** — [C] [Механика режима] Фигура O: якорь (6,8) — валидна если помещается, иначе отклонена
464. **MODE-BOT-0464** — [Механика режима] Фигура O: якорь (7,0) — валидна если помещается, иначе отклонена
465. **MODE-BOT-0465** — [Механика режима] Фигура O: якорь (7,1) — валидна если помещается, иначе отклонена
466. **MODE-BOT-0466** — [Механика режима] Фигура O: якорь (7,2) — валидна если помещается, иначе отклонена
467. **MODE-BOT-0467** — [Механика режима] Фигура O: якорь (7,3) — валидна если помещается, иначе отклонена
468. **MODE-BOT-0468** — [Механика режима] Фигура O: якорь (7,4) — валидна если помещается, иначе отклонена
469. **MODE-BOT-0469** — [Механика режима] Фигура O: якорь (7,5) — валидна если помещается, иначе отклонена
470. **MODE-BOT-0470** — [C] [Механика режима] Фигура O: якорь (7,6) — валидна если помещается, иначе отклонена
471. **MODE-BOT-0471** — [Механика режима] Фигура O: якорь (7,7) — валидна если помещается, иначе отклонена
472. **MODE-BOT-0472** — [Механика режима] Фигура O: якорь (7,8) — валидна если помещается, иначе отклонена
473. **MODE-BOT-0473** — [Механика режима] Фигура O: якорь (8,0) — валидна если помещается, иначе отклонена
474. **MODE-BOT-0474** — [Механика режима] Фигура O: якорь (8,1) — валидна если помещается, иначе отклонена
475. **MODE-BOT-0475** — [Механика режима] Фигура O: якорь (8,2) — валидна если помещается, иначе отклонена
476. **MODE-BOT-0476** — [Механика режима] Фигура O: якорь (8,3) — валидна если помещается, иначе отклонена
477. **MODE-BOT-0477** — [C] [Механика режима] Фигура O: якорь (8,4) — валидна если помещается, иначе отклонена
478. **MODE-BOT-0478** — [Механика режима] Фигура O: якорь (8,5) — валидна если помещается, иначе отклонена
479. **MODE-BOT-0479** — [Механика режима] Фигура O: якорь (8,6) — валидна если помещается, иначе отклонена
480. **MODE-BOT-0480** — [Механика режима] Фигура O: якорь (8,7) — валидна если помещается, иначе отклонена
481. **MODE-BOT-0481** — [Механика режима] Фигура O: якорь (8,8) — валидна если помещается, иначе отклонена
482. **MODE-BOT-0482** — [Механика режима] Фигура T: якорь (0,0) — валидна если помещается, иначе отклонена
483. **MODE-BOT-0483** — [Механика режима] Фигура T: якорь (0,1) — валидна если помещается, иначе отклонена
484. **MODE-BOT-0484** — [C] [Механика режима] Фигура T: якорь (0,2) — валидна если помещается, иначе отклонена
485. **MODE-BOT-0485** — [Механика режима] Фигура T: якорь (0,3) — валидна если помещается, иначе отклонена
486. **MODE-BOT-0486** — [Механика режима] Фигура T: якорь (0,4) — валидна если помещается, иначе отклонена
487. **MODE-BOT-0487** — [Механика режима] Фигура T: якорь (0,5) — валидна если помещается, иначе отклонена
488. **MODE-BOT-0488** — [Механика режима] Фигура T: якорь (0,6) — валидна если помещается, иначе отклонена
489. **MODE-BOT-0489** — [Механика режима] Фигура T: якорь (0,7) — валидна если помещается, иначе отклонена
490. **MODE-BOT-0490** — [Механика режима] Фигура T: якорь (0,8) — валидна если помещается, иначе отклонена
491. **MODE-BOT-0491** — [C] [Механика режима] Фигура T: якорь (1,0) — валидна если помещается, иначе отклонена
492. **MODE-BOT-0492** — [Механика режима] Фигура T: якорь (1,1) — валидна если помещается, иначе отклонена
493. **MODE-BOT-0493** — [Механика режима] Фигура T: якорь (1,2) — валидна если помещается, иначе отклонена
494. **MODE-BOT-0494** — [Механика режима] Фигура T: якорь (1,3) — валидна если помещается, иначе отклонена
495. **MODE-BOT-0495** — [Механика режима] Фигура T: якорь (1,4) — валидна если помещается, иначе отклонена
496. **MODE-BOT-0496** — [Механика режима] Фигура T: якорь (1,5) — валидна если помещается, иначе отклонена
497. **MODE-BOT-0497** — [Механика режима] Фигура T: якорь (1,6) — валидна если помещается, иначе отклонена
498. **MODE-BOT-0498** — [C] [Механика режима] Фигура T: якорь (1,7) — валидна если помещается, иначе отклонена
499. **MODE-BOT-0499** — [Механика режима] Фигура T: якорь (1,8) — валидна если помещается, иначе отклонена
500. **MODE-BOT-0500** — [Механика режима] Фигура T: якорь (2,0) — валидна если помещается, иначе отклонена
501. **MODE-BOT-0501** — [Механика режима] Фигура T: якорь (2,1) — валидна если помещается, иначе отклонена
502. **MODE-BOT-0502** — [Механика режима] Фигура T: якорь (2,2) — валидна если помещается, иначе отклонена
503. **MODE-BOT-0503** — [Механика режима] Фигура T: якорь (2,3) — валидна если помещается, иначе отклонена
504. **MODE-BOT-0504** — [Механика режима] Фигура T: якорь (2,4) — валидна если помещается, иначе отклонена
505. **MODE-BOT-0505** — [C] [Механика режима] Фигура T: якорь (2,5) — валидна если помещается, иначе отклонена
506. **MODE-BOT-0506** — [Механика режима] Фигура T: якорь (2,6) — валидна если помещается, иначе отклонена
507. **MODE-BOT-0507** — [Механика режима] Фигура T: якорь (2,7) — валидна если помещается, иначе отклонена
508. **MODE-BOT-0508** — [Механика режима] Фигура T: якорь (2,8) — валидна если помещается, иначе отклонена
509. **MODE-BOT-0509** — [Механика режима] Фигура T: якорь (3,0) — валидна если помещается, иначе отклонена
510. **MODE-BOT-0510** — [Механика режима] Фигура T: якорь (3,1) — валидна если помещается, иначе отклонена
511. **MODE-BOT-0511** — [Механика режима] Фигура T: якорь (3,2) — валидна если помещается, иначе отклонена
512. **MODE-BOT-0512** — [C] [Механика режима] Фигура T: якорь (3,3) — валидна если помещается, иначе отклонена
513. **MODE-BOT-0513** — [Механика режима] Фигура T: якорь (3,4) — валидна если помещается, иначе отклонена
514. **MODE-BOT-0514** — [Механика режима] Фигура T: якорь (3,5) — валидна если помещается, иначе отклонена
515. **MODE-BOT-0515** — [Механика режима] Фигура T: якорь (3,6) — валидна если помещается, иначе отклонена
516. **MODE-BOT-0516** — [Механика режима] Фигура T: якорь (3,7) — валидна если помещается, иначе отклонена
517. **MODE-BOT-0517** — [Механика режима] Фигура T: якорь (3,8) — валидна если помещается, иначе отклонена
518. **MODE-BOT-0518** — [Механика режима] Фигура T: якорь (4,0) — валидна если помещается, иначе отклонена
519. **MODE-BOT-0519** — [C] [Механика режима] Фигура T: якорь (4,1) — валидна если помещается, иначе отклонена
520. **MODE-BOT-0520** — [Механика режима] Фигура T: якорь (4,2) — валидна если помещается, иначе отклонена
521. **MODE-BOT-0521** — [Механика режима] Фигура T: якорь (4,3) — валидна если помещается, иначе отклонена
522. **MODE-BOT-0522** — [Механика режима] Фигура T: якорь (4,4) — валидна если помещается, иначе отклонена
523. **MODE-BOT-0523** — [Механика режима] Фигура T: якорь (4,5) — валидна если помещается, иначе отклонена
524. **MODE-BOT-0524** — [Механика режима] Фигура T: якорь (4,6) — валидна если помещается, иначе отклонена
525. **MODE-BOT-0525** — [Механика режима] Фигура T: якорь (4,7) — валидна если помещается, иначе отклонена
526. **MODE-BOT-0526** — [C] [Механика режима] Фигура T: якорь (4,8) — валидна если помещается, иначе отклонена
527. **MODE-BOT-0527** — [Механика режима] Фигура T: якорь (5,0) — валидна если помещается, иначе отклонена
528. **MODE-BOT-0528** — [Механика режима] Фигура T: якорь (5,1) — валидна если помещается, иначе отклонена
529. **MODE-BOT-0529** — [Механика режима] Фигура T: якорь (5,2) — валидна если помещается, иначе отклонена
530. **MODE-BOT-0530** — [Механика режима] Фигура T: якорь (5,3) — валидна если помещается, иначе отклонена
531. **MODE-BOT-0531** — [Механика режима] Фигура T: якорь (5,4) — валидна если помещается, иначе отклонена
532. **MODE-BOT-0532** — [Механика режима] Фигура T: якорь (5,5) — валидна если помещается, иначе отклонена
533. **MODE-BOT-0533** — [C] [Механика режима] Фигура T: якорь (5,6) — валидна если помещается, иначе отклонена
534. **MODE-BOT-0534** — [Механика режима] Фигура T: якорь (5,7) — валидна если помещается, иначе отклонена
535. **MODE-BOT-0535** — [Механика режима] Фигура T: якорь (5,8) — валидна если помещается, иначе отклонена
536. **MODE-BOT-0536** — [Механика режима] Фигура T: якорь (6,0) — валидна если помещается, иначе отклонена
537. **MODE-BOT-0537** — [Механика режима] Фигура T: якорь (6,1) — валидна если помещается, иначе отклонена
538. **MODE-BOT-0538** — [Механика режима] Фигура T: якорь (6,2) — валидна если помещается, иначе отклонена
539. **MODE-BOT-0539** — [Механика режима] Фигура T: якорь (6,3) — валидна если помещается, иначе отклонена
540. **MODE-BOT-0540** — [C] [Механика режима] Фигура T: якорь (6,4) — валидна если помещается, иначе отклонена
541. **MODE-BOT-0541** — [Механика режима] Фигура T: якорь (6,5) — валидна если помещается, иначе отклонена
542. **MODE-BOT-0542** — [Механика режима] Фигура T: якорь (6,6) — валидна если помещается, иначе отклонена
543. **MODE-BOT-0543** — [Механика режима] Фигура T: якорь (6,7) — валидна если помещается, иначе отклонена
544. **MODE-BOT-0544** — [Механика режима] Фигура T: якорь (6,8) — валидна если помещается, иначе отклонена
545. **MODE-BOT-0545** — [Механика режима] Фигура T: якорь (7,0) — валидна если помещается, иначе отклонена
546. **MODE-BOT-0546** — [Механика режима] Фигура T: якорь (7,1) — валидна если помещается, иначе отклонена
547. **MODE-BOT-0547** — [C] [Механика режима] Фигура T: якорь (7,2) — валидна если помещается, иначе отклонена
548. **MODE-BOT-0548** — [Механика режима] Фигура T: якорь (7,3) — валидна если помещается, иначе отклонена
549. **MODE-BOT-0549** — [Механика режима] Фигура T: якорь (7,4) — валидна если помещается, иначе отклонена
550. **MODE-BOT-0550** — [Механика режима] Фигура T: якорь (7,5) — валидна если помещается, иначе отклонена
551. **MODE-BOT-0551** — [Механика режима] Фигура T: якорь (7,6) — валидна если помещается, иначе отклонена
552. **MODE-BOT-0552** — [Механика режима] Фигура T: якорь (7,7) — валидна если помещается, иначе отклонена
553. **MODE-BOT-0553** — [Механика режима] Фигура T: якорь (7,8) — валидна если помещается, иначе отклонена
554. **MODE-BOT-0554** — [C] [Механика режима] Фигура T: якорь (8,0) — валидна если помещается, иначе отклонена
555. **MODE-BOT-0555** — [Механика режима] Фигура T: якорь (8,1) — валидна если помещается, иначе отклонена
556. **MODE-BOT-0556** — [Механика режима] Фигура T: якорь (8,2) — валидна если помещается, иначе отклонена
557. **MODE-BOT-0557** — [Механика режима] Фигура T: якорь (8,3) — валидна если помещается, иначе отклонена
558. **MODE-BOT-0558** — [Механика режима] Фигура T: якорь (8,4) — валидна если помещается, иначе отклонена
559. **MODE-BOT-0559** — [Механика режима] Фигура T: якорь (8,5) — валидна если помещается, иначе отклонена
560. **MODE-BOT-0560** — [Механика режима] Фигура T: якорь (8,6) — валидна если помещается, иначе отклонена
561. **MODE-BOT-0561** — [C] [Механика режима] Фигура T: якорь (8,7) — валидна если помещается, иначе отклонена
562. **MODE-BOT-0562** — [Механика режима] Фигура T: якорь (8,8) — валидна если помещается, иначе отклонена
563. **MODE-BOT-0563** — [Механика режима] Фигура S: якорь (0,0) — валидна если помещается, иначе отклонена
564. **MODE-BOT-0564** — [Механика режима] Фигура S: якорь (0,1) — валидна если помещается, иначе отклонена
565. **MODE-BOT-0565** — [Механика режима] Фигура S: якорь (0,2) — валидна если помещается, иначе отклонена
566. **MODE-BOT-0566** — [Механика режима] Фигура S: якорь (0,3) — валидна если помещается, иначе отклонена
567. **MODE-BOT-0567** — [Механика режима] Фигура S: якорь (0,4) — валидна если помещается, иначе отклонена
568. **MODE-BOT-0568** — [C] [Механика режима] Фигура S: якорь (0,5) — валидна если помещается, иначе отклонена
569. **MODE-BOT-0569** — [Механика режима] Фигура S: якорь (0,6) — валидна если помещается, иначе отклонена
570. **MODE-BOT-0570** — [Механика режима] Фигура S: якорь (0,7) — валидна если помещается, иначе отклонена
571. **MODE-BOT-0571** — [Механика режима] Фигура S: якорь (0,8) — валидна если помещается, иначе отклонена
572. **MODE-BOT-0572** — [Механика режима] Фигура S: якорь (1,0) — валидна если помещается, иначе отклонена
573. **MODE-BOT-0573** — [Механика режима] Фигура S: якорь (1,1) — валидна если помещается, иначе отклонена
574. **MODE-BOT-0574** — [Механика режима] Фигура S: якорь (1,2) — валидна если помещается, иначе отклонена
575. **MODE-BOT-0575** — [C] [Механика режима] Фигура S: якорь (1,3) — валидна если помещается, иначе отклонена
576. **MODE-BOT-0576** — [Механика режима] Фигура S: якорь (1,4) — валидна если помещается, иначе отклонена
577. **MODE-BOT-0577** — [Механика режима] Фигура S: якорь (1,5) — валидна если помещается, иначе отклонена
578. **MODE-BOT-0578** — [Механика режима] Фигура S: якорь (1,6) — валидна если помещается, иначе отклонена
579. **MODE-BOT-0579** — [Механика режима] Фигура S: якорь (1,7) — валидна если помещается, иначе отклонена
580. **MODE-BOT-0580** — [Механика режима] Фигура S: якорь (1,8) — валидна если помещается, иначе отклонена
581. **MODE-BOT-0581** — [Механика режима] Фигура S: якорь (2,0) — валидна если помещается, иначе отклонена
582. **MODE-BOT-0582** — [C] [Механика режима] Фигура S: якорь (2,1) — валидна если помещается, иначе отклонена
583. **MODE-BOT-0583** — [Механика режима] Фигура S: якорь (2,2) — валидна если помещается, иначе отклонена
584. **MODE-BOT-0584** — [Механика режима] Фигура S: якорь (2,3) — валидна если помещается, иначе отклонена
585. **MODE-BOT-0585** — [Механика режима] Фигура S: якорь (2,4) — валидна если помещается, иначе отклонена
586. **MODE-BOT-0586** — [Механика режима] Фигура S: якорь (2,5) — валидна если помещается, иначе отклонена
587. **MODE-BOT-0587** — [Механика режима] Фигура S: якорь (2,6) — валидна если помещается, иначе отклонена
588. **MODE-BOT-0588** — [Механика режима] Фигура S: якорь (2,7) — валидна если помещается, иначе отклонена
589. **MODE-BOT-0589** — [C] [Механика режима] Фигура S: якорь (2,8) — валидна если помещается, иначе отклонена
590. **MODE-BOT-0590** — [Механика режима] Фигура S: якорь (3,0) — валидна если помещается, иначе отклонена
591. **MODE-BOT-0591** — [Механика режима] Фигура S: якорь (3,1) — валидна если помещается, иначе отклонена
592. **MODE-BOT-0592** — [Механика режима] Фигура S: якорь (3,2) — валидна если помещается, иначе отклонена
593. **MODE-BOT-0593** — [Механика режима] Фигура S: якорь (3,3) — валидна если помещается, иначе отклонена
594. **MODE-BOT-0594** — [Механика режима] Фигура S: якорь (3,4) — валидна если помещается, иначе отклонена
595. **MODE-BOT-0595** — [Механика режима] Фигура S: якорь (3,5) — валидна если помещается, иначе отклонена
596. **MODE-BOT-0596** — [C] [Механика режима] Фигура S: якорь (3,6) — валидна если помещается, иначе отклонена
597. **MODE-BOT-0597** — [Механика режима] Фигура S: якорь (3,7) — валидна если помещается, иначе отклонена
598. **MODE-BOT-0598** — [Механика режима] Фигура S: якорь (3,8) — валидна если помещается, иначе отклонена
599. **MODE-BOT-0599** — [Механика режима] Фигура S: якорь (4,0) — валидна если помещается, иначе отклонена
600. **MODE-BOT-0600** — [Механика режима] Фигура S: якорь (4,1) — валидна если помещается, иначе отклонена
601. **MODE-BOT-0601** — [Механика режима] Фигура S: якорь (4,2) — валидна если помещается, иначе отклонена
602. **MODE-BOT-0602** — [Механика режима] Фигура S: якорь (4,3) — валидна если помещается, иначе отклонена
603. **MODE-BOT-0603** — [C] [Механика режима] Фигура S: якорь (4,4) — валидна если помещается, иначе отклонена
604. **MODE-BOT-0604** — [Механика режима] Фигура S: якорь (4,5) — валидна если помещается, иначе отклонена
605. **MODE-BOT-0605** — [Механика режима] Фигура S: якорь (4,6) — валидна если помещается, иначе отклонена
606. **MODE-BOT-0606** — [Механика режима] Фигура S: якорь (4,7) — валидна если помещается, иначе отклонена
607. **MODE-BOT-0607** — [Механика режима] Фигура S: якорь (4,8) — валидна если помещается, иначе отклонена
608. **MODE-BOT-0608** — [Механика режима] Фигура S: якорь (5,0) — валидна если помещается, иначе отклонена
609. **MODE-BOT-0609** — [Механика режима] Фигура S: якорь (5,1) — валидна если помещается, иначе отклонена
610. **MODE-BOT-0610** — [C] [Механика режима] Фигура S: якорь (5,2) — валидна если помещается, иначе отклонена
611. **MODE-BOT-0611** — [Механика режима] Фигура S: якорь (5,3) — валидна если помещается, иначе отклонена
612. **MODE-BOT-0612** — [Механика режима] Фигура S: якорь (5,4) — валидна если помещается, иначе отклонена
613. **MODE-BOT-0613** — [Механика режима] Фигура S: якорь (5,5) — валидна если помещается, иначе отклонена
614. **MODE-BOT-0614** — [Механика режима] Фигура S: якорь (5,6) — валидна если помещается, иначе отклонена
615. **MODE-BOT-0615** — [Механика режима] Фигура S: якорь (5,7) — валидна если помещается, иначе отклонена
616. **MODE-BOT-0616** — [Механика режима] Фигура S: якорь (5,8) — валидна если помещается, иначе отклонена
617. **MODE-BOT-0617** — [C] [Механика режима] Фигура S: якорь (6,0) — валидна если помещается, иначе отклонена
618. **MODE-BOT-0618** — [Механика режима] Фигура S: якорь (6,1) — валидна если помещается, иначе отклонена
619. **MODE-BOT-0619** — [Механика режима] Фигура S: якорь (6,2) — валидна если помещается, иначе отклонена
620. **MODE-BOT-0620** — [Механика режима] Фигура S: якорь (6,3) — валидна если помещается, иначе отклонена
621. **MODE-BOT-0621** — [Механика режима] Фигура S: якорь (6,4) — валидна если помещается, иначе отклонена
622. **MODE-BOT-0622** — [Механика режима] Фигура S: якорь (6,5) — валидна если помещается, иначе отклонена
623. **MODE-BOT-0623** — [Механика режима] Фигура S: якорь (6,6) — валидна если помещается, иначе отклонена
624. **MODE-BOT-0624** — [C] [Механика режима] Фигура S: якорь (6,7) — валидна если помещается, иначе отклонена
625. **MODE-BOT-0625** — [Механика режима] Фигура S: якорь (6,8) — валидна если помещается, иначе отклонена
626. **MODE-BOT-0626** — [Механика режима] Фигура S: якорь (7,0) — валидна если помещается, иначе отклонена
627. **MODE-BOT-0627** — [Механика режима] Фигура S: якорь (7,1) — валидна если помещается, иначе отклонена
628. **MODE-BOT-0628** — [Механика режима] Фигура S: якорь (7,2) — валидна если помещается, иначе отклонена
629. **MODE-BOT-0629** — [Механика режима] Фигура S: якорь (7,3) — валидна если помещается, иначе отклонена
630. **MODE-BOT-0630** — [Механика режима] Фигура S: якорь (7,4) — валидна если помещается, иначе отклонена
631. **MODE-BOT-0631** — [C] [Механика режима] Фигура S: якорь (7,5) — валидна если помещается, иначе отклонена
632. **MODE-BOT-0632** — [Механика режима] Фигура S: якорь (7,6) — валидна если помещается, иначе отклонена
633. **MODE-BOT-0633** — [Механика режима] Фигура S: якорь (7,7) — валидна если помещается, иначе отклонена
634. **MODE-BOT-0634** — [Механика режима] Фигура S: якорь (7,8) — валидна если помещается, иначе отклонена
635. **MODE-BOT-0635** — [Механика режима] Фигура S: якорь (8,0) — валидна если помещается, иначе отклонена
636. **MODE-BOT-0636** — [Механика режима] Фигура S: якорь (8,1) — валидна если помещается, иначе отклонена
637. **MODE-BOT-0637** — [Механика режима] Фигура S: якорь (8,2) — валидна если помещается, иначе отклонена
638. **MODE-BOT-0638** — [C] [Механика режима] Фигура S: якорь (8,3) — валидна если помещается, иначе отклонена
639. **MODE-BOT-0639** — [Механика режима] Фигура S: якорь (8,4) — валидна если помещается, иначе отклонена
640. **MODE-BOT-0640** — [Механика режима] Фигура S: якорь (8,5) — валидна если помещается, иначе отклонена
641. **MODE-BOT-0641** — [Механика режима] Фигура S: якорь (8,6) — валидна если помещается, иначе отклонена
642. **MODE-BOT-0642** — [Механика режима] Фигура S: якорь (8,7) — валидна если помещается, иначе отклонена
643. **MODE-BOT-0643** — [Механика режима] Фигура S: якорь (8,8) — валидна если помещается, иначе отклонена
644. **MODE-BOT-0644** — [Механика режима] Фигура Z: якорь (0,0) — валидна если помещается, иначе отклонена
645. **MODE-BOT-0645** — [C] [Механика режима] Фигура Z: якорь (0,1) — валидна если помещается, иначе отклонена
646. **MODE-BOT-0646** — [Механика режима] Фигура Z: якорь (0,2) — валидна если помещается, иначе отклонена
647. **MODE-BOT-0647** — [Механика режима] Фигура Z: якорь (0,3) — валидна если помещается, иначе отклонена
648. **MODE-BOT-0648** — [Механика режима] Фигура Z: якорь (0,4) — валидна если помещается, иначе отклонена
649. **MODE-BOT-0649** — [Механика режима] Фигура Z: якорь (0,5) — валидна если помещается, иначе отклонена
650. **MODE-BOT-0650** — [Механика режима] Фигура Z: якорь (0,6) — валидна если помещается, иначе отклонена
651. **MODE-BOT-0651** — [Механика режима] Фигура Z: якорь (0,7) — валидна если помещается, иначе отклонена
652. **MODE-BOT-0652** — [C] [Механика режима] Фигура Z: якорь (0,8) — валидна если помещается, иначе отклонена
653. **MODE-BOT-0653** — [Механика режима] Фигура Z: якорь (1,0) — валидна если помещается, иначе отклонена
654. **MODE-BOT-0654** — [Механика режима] Фигура Z: якорь (1,1) — валидна если помещается, иначе отклонена
655. **MODE-BOT-0655** — [Механика режима] Фигура Z: якорь (1,2) — валидна если помещается, иначе отклонена
656. **MODE-BOT-0656** — [Механика режима] Фигура Z: якорь (1,3) — валидна если помещается, иначе отклонена
657. **MODE-BOT-0657** — [Механика режима] Фигура Z: якорь (1,4) — валидна если помещается, иначе отклонена
658. **MODE-BOT-0658** — [Механика режима] Фигура Z: якорь (1,5) — валидна если помещается, иначе отклонена
659. **MODE-BOT-0659** — [C] [Механика режима] Фигура Z: якорь (1,6) — валидна если помещается, иначе отклонена
660. **MODE-BOT-0660** — [Механика режима] Фигура Z: якорь (1,7) — валидна если помещается, иначе отклонена
661. **MODE-BOT-0661** — [Механика режима] Фигура Z: якорь (1,8) — валидна если помещается, иначе отклонена
662. **MODE-BOT-0662** — [Механика режима] Фигура Z: якорь (2,0) — валидна если помещается, иначе отклонена
663. **MODE-BOT-0663** — [Механика режима] Фигура Z: якорь (2,1) — валидна если помещается, иначе отклонена
664. **MODE-BOT-0664** — [Механика режима] Фигура Z: якорь (2,2) — валидна если помещается, иначе отклонена
665. **MODE-BOT-0665** — [Механика режима] Фигура Z: якорь (2,3) — валидна если помещается, иначе отклонена
666. **MODE-BOT-0666** — [C] [Механика режима] Фигура Z: якорь (2,4) — валидна если помещается, иначе отклонена
667. **MODE-BOT-0667** — [Механика режима] Фигура Z: якорь (2,5) — валидна если помещается, иначе отклонена
668. **MODE-BOT-0668** — [Механика режима] Фигура Z: якорь (2,6) — валидна если помещается, иначе отклонена
669. **MODE-BOT-0669** — [Механика режима] Фигура Z: якорь (2,7) — валидна если помещается, иначе отклонена
670. **MODE-BOT-0670** — [Механика режима] Фигура Z: якорь (2,8) — валидна если помещается, иначе отклонена
671. **MODE-BOT-0671** — [Механика режима] Фигура Z: якорь (3,0) — валидна если помещается, иначе отклонена
672. **MODE-BOT-0672** — [Механика режима] Фигура Z: якорь (3,1) — валидна если помещается, иначе отклонена
673. **MODE-BOT-0673** — [C] [Механика режима] Фигура Z: якорь (3,2) — валидна если помещается, иначе отклонена
674. **MODE-BOT-0674** — [Механика режима] Фигура Z: якорь (3,3) — валидна если помещается, иначе отклонена
675. **MODE-BOT-0675** — [Механика режима] Фигура Z: якорь (3,4) — валидна если помещается, иначе отклонена
676. **MODE-BOT-0676** — [Механика режима] Фигура Z: якорь (3,5) — валидна если помещается, иначе отклонена
677. **MODE-BOT-0677** — [Механика режима] Фигура Z: якорь (3,6) — валидна если помещается, иначе отклонена
678. **MODE-BOT-0678** — [Механика режима] Фигура Z: якорь (3,7) — валидна если помещается, иначе отклонена
679. **MODE-BOT-0679** — [Механика режима] Фигура Z: якорь (3,8) — валидна если помещается, иначе отклонена
680. **MODE-BOT-0680** — [C] [Механика режима] Фигура Z: якорь (4,0) — валидна если помещается, иначе отклонена
681. **MODE-BOT-0681** — [Механика режима] Фигура Z: якорь (4,1) — валидна если помещается, иначе отклонена
682. **MODE-BOT-0682** — [Механика режима] Фигура Z: якорь (4,2) — валидна если помещается, иначе отклонена
683. **MODE-BOT-0683** — [Механика режима] Фигура Z: якорь (4,3) — валидна если помещается, иначе отклонена
684. **MODE-BOT-0684** — [Механика режима] Фигура Z: якорь (4,4) — валидна если помещается, иначе отклонена
685. **MODE-BOT-0685** — [Механика режима] Фигура Z: якорь (4,5) — валидна если помещается, иначе отклонена
686. **MODE-BOT-0686** — [Механика режима] Фигура Z: якорь (4,6) — валидна если помещается, иначе отклонена
687. **MODE-BOT-0687** — [C] [Механика режима] Фигура Z: якорь (4,7) — валидна если помещается, иначе отклонена
688. **MODE-BOT-0688** — [Механика режима] Фигура Z: якорь (4,8) — валидна если помещается, иначе отклонена
689. **MODE-BOT-0689** — [Механика режима] Фигура Z: якорь (5,0) — валидна если помещается, иначе отклонена
690. **MODE-BOT-0690** — [Механика режима] Фигура Z: якорь (5,1) — валидна если помещается, иначе отклонена
691. **MODE-BOT-0691** — [Механика режима] Фигура Z: якорь (5,2) — валидна если помещается, иначе отклонена
692. **MODE-BOT-0692** — [Механика режима] Фигура Z: якорь (5,3) — валидна если помещается, иначе отклонена
693. **MODE-BOT-0693** — [Механика режима] Фигура Z: якорь (5,4) — валидна если помещается, иначе отклонена
694. **MODE-BOT-0694** — [C] [Механика режима] Фигура Z: якорь (5,5) — валидна если помещается, иначе отклонена
695. **MODE-BOT-0695** — [Механика режима] Фигура Z: якорь (5,6) — валидна если помещается, иначе отклонена
696. **MODE-BOT-0696** — [Механика режима] Фигура Z: якорь (5,7) — валидна если помещается, иначе отклонена
697. **MODE-BOT-0697** — [Механика режима] Фигура Z: якорь (5,8) — валидна если помещается, иначе отклонена
698. **MODE-BOT-0698** — [Механика режима] Фигура Z: якорь (6,0) — валидна если помещается, иначе отклонена
699. **MODE-BOT-0699** — [Механика режима] Фигура Z: якорь (6,1) — валидна если помещается, иначе отклонена
700. **MODE-BOT-0700** — [Механика режима] Фигура Z: якорь (6,2) — валидна если помещается, иначе отклонена
701. **MODE-BOT-0701** — [C] [Механика режима] Фигура Z: якорь (6,3) — валидна если помещается, иначе отклонена
702. **MODE-BOT-0702** — [Механика режима] Фигура Z: якорь (6,4) — валидна если помещается, иначе отклонена
703. **MODE-BOT-0703** — [Механика режима] Фигура Z: якорь (6,5) — валидна если помещается, иначе отклонена
704. **MODE-BOT-0704** — [Механика режима] Фигура Z: якорь (6,6) — валидна если помещается, иначе отклонена
705. **MODE-BOT-0705** — [Механика режима] Фигура Z: якорь (6,7) — валидна если помещается, иначе отклонена
706. **MODE-BOT-0706** — [Механика режима] Фигура Z: якорь (6,8) — валидна если помещается, иначе отклонена
707. **MODE-BOT-0707** — [Механика режима] Фигура Z: якорь (7,0) — валидна если помещается, иначе отклонена
708. **MODE-BOT-0708** — [C] [Механика режима] Фигура Z: якорь (7,1) — валидна если помещается, иначе отклонена
709. **MODE-BOT-0709** — [Механика режима] Фигура Z: якорь (7,2) — валидна если помещается, иначе отклонена
710. **MODE-BOT-0710** — [Механика режима] Фигура Z: якорь (7,3) — валидна если помещается, иначе отклонена
711. **MODE-BOT-0711** — [Механика режима] Фигура Z: якорь (7,4) — валидна если помещается, иначе отклонена
712. **MODE-BOT-0712** — [Механика режима] Фигура Z: якорь (7,5) — валидна если помещается, иначе отклонена
713. **MODE-BOT-0713** — [Механика режима] Фигура Z: якорь (7,6) — валидна если помещается, иначе отклонена
714. **MODE-BOT-0714** — [Механика режима] Фигура Z: якорь (7,7) — валидна если помещается, иначе отклонена
715. **MODE-BOT-0715** — [C] [Механика режима] Фигура Z: якорь (7,8) — валидна если помещается, иначе отклонена
716. **MODE-BOT-0716** — [Механика режима] Фигура Z: якорь (8,0) — валидна если помещается, иначе отклонена
717. **MODE-BOT-0717** — [Механика режима] Фигура Z: якорь (8,1) — валидна если помещается, иначе отклонена
718. **MODE-BOT-0718** — [Механика режима] Фигура Z: якорь (8,2) — валидна если помещается, иначе отклонена
719. **MODE-BOT-0719** — [Механика режима] Фигура Z: якорь (8,3) — валидна если помещается, иначе отклонена
720. **MODE-BOT-0720** — [Механика режима] Фигура Z: якорь (8,4) — валидна если помещается, иначе отклонена
721. **MODE-BOT-0721** — [Механика режима] Фигура Z: якорь (8,5) — валидна если помещается, иначе отклонена
722. **MODE-BOT-0722** — [C] [Механика режима] Фигура Z: якорь (8,6) — валидна если помещается, иначе отклонена
723. **MODE-BOT-0723** — [Механика режима] Фигура Z: якорь (8,7) — валидна если помещается, иначе отклонена
724. **MODE-BOT-0724** — [Механика режима] Фигура Z: якорь (8,8) — валидна если помещается, иначе отклонена
725. **MODE-BOT-0725** — [Механика режима] Фигура J: якорь (0,0) — валидна если помещается, иначе отклонена
726. **MODE-BOT-0726** — [Механика режима] Фигура J: якорь (0,1) — валидна если помещается, иначе отклонена
727. **MODE-BOT-0727** — [Механика режима] Фигура J: якорь (0,2) — валидна если помещается, иначе отклонена
728. **MODE-BOT-0728** — [Механика режима] Фигура J: якорь (0,3) — валидна если помещается, иначе отклонена
729. **MODE-BOT-0729** — [C] [Механика режима] Фигура J: якорь (0,4) — валидна если помещается, иначе отклонена
730. **MODE-BOT-0730** — [Механика режима] Фигура J: якорь (0,5) — валидна если помещается, иначе отклонена
731. **MODE-BOT-0731** — [Механика режима] Фигура J: якорь (0,6) — валидна если помещается, иначе отклонена
732. **MODE-BOT-0732** — [Механика режима] Фигура J: якорь (0,7) — валидна если помещается, иначе отклонена
733. **MODE-BOT-0733** — [Механика режима] Фигура J: якорь (0,8) — валидна если помещается, иначе отклонена
734. **MODE-BOT-0734** — [Механика режима] Фигура J: якорь (1,0) — валидна если помещается, иначе отклонена
735. **MODE-BOT-0735** — [Механика режима] Фигура J: якорь (1,1) — валидна если помещается, иначе отклонена
736. **MODE-BOT-0736** — [C] [Механика режима] Фигура J: якорь (1,2) — валидна если помещается, иначе отклонена
737. **MODE-BOT-0737** — [Механика режима] Фигура J: якорь (1,3) — валидна если помещается, иначе отклонена
738. **MODE-BOT-0738** — [Механика режима] Фигура J: якорь (1,4) — валидна если помещается, иначе отклонена
739. **MODE-BOT-0739** — [Механика режима] Фигура J: якорь (1,5) — валидна если помещается, иначе отклонена
740. **MODE-BOT-0740** — [Механика режима] Фигура J: якорь (1,6) — валидна если помещается, иначе отклонена
741. **MODE-BOT-0741** — [Механика режима] Фигура J: якорь (1,7) — валидна если помещается, иначе отклонена
742. **MODE-BOT-0742** — [Механика режима] Фигура J: якорь (1,8) — валидна если помещается, иначе отклонена
743. **MODE-BOT-0743** — [C] [Механика режима] Фигура J: якорь (2,0) — валидна если помещается, иначе отклонена
744. **MODE-BOT-0744** — [Механика режима] Фигура J: якорь (2,1) — валидна если помещается, иначе отклонена
745. **MODE-BOT-0745** — [Механика режима] Фигура J: якорь (2,2) — валидна если помещается, иначе отклонена
746. **MODE-BOT-0746** — [Механика режима] Фигура J: якорь (2,3) — валидна если помещается, иначе отклонена
747. **MODE-BOT-0747** — [Механика режима] Фигура J: якорь (2,4) — валидна если помещается, иначе отклонена
748. **MODE-BOT-0748** — [Механика режима] Фигура J: якорь (2,5) — валидна если помещается, иначе отклонена
749. **MODE-BOT-0749** — [Механика режима] Фигура J: якорь (2,6) — валидна если помещается, иначе отклонена
750. **MODE-BOT-0750** — [C] [Механика режима] Фигура J: якорь (2,7) — валидна если помещается, иначе отклонена
751. **MODE-BOT-0751** — [Механика режима] Фигура J: якорь (2,8) — валидна если помещается, иначе отклонена
752. **MODE-BOT-0752** — [Механика режима] Фигура J: якорь (3,0) — валидна если помещается, иначе отклонена
753. **MODE-BOT-0753** — [Механика режима] Фигура J: якорь (3,1) — валидна если помещается, иначе отклонена
754. **MODE-BOT-0754** — [Механика режима] Фигура J: якорь (3,2) — валидна если помещается, иначе отклонена
755. **MODE-BOT-0755** — [Механика режима] Фигура J: якорь (3,3) — валидна если помещается, иначе отклонена
756. **MODE-BOT-0756** — [Механика режима] Фигура J: якорь (3,4) — валидна если помещается, иначе отклонена
757. **MODE-BOT-0757** — [C] [Механика режима] Фигура J: якорь (3,5) — валидна если помещается, иначе отклонена
758. **MODE-BOT-0758** — [Механика режима] Фигура J: якорь (3,6) — валидна если помещается, иначе отклонена
759. **MODE-BOT-0759** — [Механика режима] Фигура J: якорь (3,7) — валидна если помещается, иначе отклонена
760. **MODE-BOT-0760** — [Механика режима] Фигура J: якорь (3,8) — валидна если помещается, иначе отклонена
761. **MODE-BOT-0761** — [Механика режима] Фигура J: якорь (4,0) — валидна если помещается, иначе отклонена
762. **MODE-BOT-0762** — [Механика режима] Фигура J: якорь (4,1) — валидна если помещается, иначе отклонена
763. **MODE-BOT-0763** — [Механика режима] Фигура J: якорь (4,2) — валидна если помещается, иначе отклонена
764. **MODE-BOT-0764** — [C] [Механика режима] Фигура J: якорь (4,3) — валидна если помещается, иначе отклонена
765. **MODE-BOT-0765** — [Механика режима] Фигура J: якорь (4,4) — валидна если помещается, иначе отклонена
766. **MODE-BOT-0766** — [Механика режима] Фигура J: якорь (4,5) — валидна если помещается, иначе отклонена
767. **MODE-BOT-0767** — [Механика режима] Фигура J: якорь (4,6) — валидна если помещается, иначе отклонена
768. **MODE-BOT-0768** — [Механика режима] Фигура J: якорь (4,7) — валидна если помещается, иначе отклонена
769. **MODE-BOT-0769** — [Механика режима] Фигура J: якорь (4,8) — валидна если помещается, иначе отклонена
770. **MODE-BOT-0770** — [Механика режима] Фигура J: якорь (5,0) — валидна если помещается, иначе отклонена
771. **MODE-BOT-0771** — [C] [Механика режима] Фигура J: якорь (5,1) — валидна если помещается, иначе отклонена
772. **MODE-BOT-0772** — [Механика режима] Фигура J: якорь (5,2) — валидна если помещается, иначе отклонена
773. **MODE-BOT-0773** — [Механика режима] Фигура J: якорь (5,3) — валидна если помещается, иначе отклонена
774. **MODE-BOT-0774** — [Механика режима] Фигура J: якорь (5,4) — валидна если помещается, иначе отклонена
775. **MODE-BOT-0775** — [Механика режима] Фигура J: якорь (5,5) — валидна если помещается, иначе отклонена
776. **MODE-BOT-0776** — [Механика режима] Фигура J: якорь (5,6) — валидна если помещается, иначе отклонена
777. **MODE-BOT-0777** — [Механика режима] Фигура J: якорь (5,7) — валидна если помещается, иначе отклонена
778. **MODE-BOT-0778** — [C] [Механика режима] Фигура J: якорь (5,8) — валидна если помещается, иначе отклонена
779. **MODE-BOT-0779** — [Механика режима] Фигура J: якорь (6,0) — валидна если помещается, иначе отклонена
780. **MODE-BOT-0780** — [Механика режима] Фигура J: якорь (6,1) — валидна если помещается, иначе отклонена
781. **MODE-BOT-0781** — [Механика режима] Фигура J: якорь (6,2) — валидна если помещается, иначе отклонена
782. **MODE-BOT-0782** — [Механика режима] Фигура J: якорь (6,3) — валидна если помещается, иначе отклонена
783. **MODE-BOT-0783** — [Механика режима] Фигура J: якорь (6,4) — валидна если помещается, иначе отклонена
784. **MODE-BOT-0784** — [Механика режима] Фигура J: якорь (6,5) — валидна если помещается, иначе отклонена
785. **MODE-BOT-0785** — [C] [Механика режима] Фигура J: якорь (6,6) — валидна если помещается, иначе отклонена
786. **MODE-BOT-0786** — [Механика режима] Фигура J: якорь (6,7) — валидна если помещается, иначе отклонена
787. **MODE-BOT-0787** — [Механика режима] Фигура J: якорь (6,8) — валидна если помещается, иначе отклонена
788. **MODE-BOT-0788** — [Механика режима] Фигура J: якорь (7,0) — валидна если помещается, иначе отклонена
789. **MODE-BOT-0789** — [Механика режима] Фигура J: якорь (7,1) — валидна если помещается, иначе отклонена
790. **MODE-BOT-0790** — [Механика режима] Фигура J: якорь (7,2) — валидна если помещается, иначе отклонена
791. **MODE-BOT-0791** — [Механика режима] Фигура J: якорь (7,3) — валидна если помещается, иначе отклонена
792. **MODE-BOT-0792** — [C] [Механика режима] Фигура J: якорь (7,4) — валидна если помещается, иначе отклонена
793. **MODE-BOT-0793** — [Механика режима] Фигура J: якорь (7,5) — валидна если помещается, иначе отклонена
794. **MODE-BOT-0794** — [Механика режима] Фигура J: якорь (7,6) — валидна если помещается, иначе отклонена
795. **MODE-BOT-0795** — [Механика режима] Фигура J: якорь (7,7) — валидна если помещается, иначе отклонена
796. **MODE-BOT-0796** — [Механика режима] Фигура J: якорь (7,8) — валидна если помещается, иначе отклонена
797. **MODE-BOT-0797** — [Механика режима] Фигура J: якорь (8,0) — валидна если помещается, иначе отклонена
798. **MODE-BOT-0798** — [Механика режима] Фигура J: якорь (8,1) — валидна если помещается, иначе отклонена
799. **MODE-BOT-0799** — [C] [Механика режима] Фигура J: якорь (8,2) — валидна если помещается, иначе отклонена
800. **MODE-BOT-0800** — [Механика режима] Фигура J: якорь (8,3) — валидна если помещается, иначе отклонена
801. **MODE-BOT-0801** — [Механика режима] Фигура J: якорь (8,4) — валидна если помещается, иначе отклонена
802. **MODE-BOT-0802** — [Механика режима] Фигура J: якорь (8,5) — валидна если помещается, иначе отклонена
803. **MODE-BOT-0803** — [Механика режима] Фигура J: якорь (8,6) — валидна если помещается, иначе отклонена
804. **MODE-BOT-0804** — [Механика режима] Фигура J: якорь (8,7) — валидна если помещается, иначе отклонена
805. **MODE-BOT-0805** — [Механика режима] Фигура J: якорь (8,8) — валидна если помещается, иначе отклонена
806. **MODE-BOT-0806** — [C] [Механика режима] Фигура L: якорь (0,0) — валидна если помещается, иначе отклонена
807. **MODE-BOT-0807** — [Механика режима] Фигура L: якорь (0,1) — валидна если помещается, иначе отклонена
808. **MODE-BOT-0808** — [Механика режима] Фигура L: якорь (0,2) — валидна если помещается, иначе отклонена
809. **MODE-BOT-0809** — [Механика режима] Фигура L: якорь (0,3) — валидна если помещается, иначе отклонена
810. **MODE-BOT-0810** — [Механика режима] Фигура L: якорь (0,4) — валидна если помещается, иначе отклонена
811. **MODE-BOT-0811** — [Механика режима] Фигура L: якорь (0,5) — валидна если помещается, иначе отклонена
812. **MODE-BOT-0812** — [Механика режима] Фигура L: якорь (0,6) — валидна если помещается, иначе отклонена
813. **MODE-BOT-0813** — [C] [Механика режима] Фигура L: якорь (0,7) — валидна если помещается, иначе отклонена
814. **MODE-BOT-0814** — [Механика режима] Фигура L: якорь (0,8) — валидна если помещается, иначе отклонена
815. **MODE-BOT-0815** — [Механика режима] Фигура L: якорь (1,0) — валидна если помещается, иначе отклонена
816. **MODE-BOT-0816** — [Механика режима] Фигура L: якорь (1,1) — валидна если помещается, иначе отклонена
817. **MODE-BOT-0817** — [Механика режима] Фигура L: якорь (1,2) — валидна если помещается, иначе отклонена
818. **MODE-BOT-0818** — [Механика режима] Фигура L: якорь (1,3) — валидна если помещается, иначе отклонена
819. **MODE-BOT-0819** — [Механика режима] Фигура L: якорь (1,4) — валидна если помещается, иначе отклонена
820. **MODE-BOT-0820** — [C] [Механика режима] Фигура L: якорь (1,5) — валидна если помещается, иначе отклонена
821. **MODE-BOT-0821** — [Механика режима] Фигура L: якорь (1,6) — валидна если помещается, иначе отклонена
822. **MODE-BOT-0822** — [Механика режима] Фигура L: якорь (1,7) — валидна если помещается, иначе отклонена
823. **MODE-BOT-0823** — [Механика режима] Фигура L: якорь (1,8) — валидна если помещается, иначе отклонена
824. **MODE-BOT-0824** — [Механика режима] Фигура L: якорь (2,0) — валидна если помещается, иначе отклонена
825. **MODE-BOT-0825** — [Механика режима] Фигура L: якорь (2,1) — валидна если помещается, иначе отклонена
826. **MODE-BOT-0826** — [Механика режима] Фигура L: якорь (2,2) — валидна если помещается, иначе отклонена
827. **MODE-BOT-0827** — [C] [Механика режима] Фигура L: якорь (2,3) — валидна если помещается, иначе отклонена
828. **MODE-BOT-0828** — [Механика режима] Фигура L: якорь (2,4) — валидна если помещается, иначе отклонена
829. **MODE-BOT-0829** — [Механика режима] Фигура L: якорь (2,5) — валидна если помещается, иначе отклонена
830. **MODE-BOT-0830** — [Механика режима] Фигура L: якорь (2,6) — валидна если помещается, иначе отклонена
831. **MODE-BOT-0831** — [Механика режима] Фигура L: якорь (2,7) — валидна если помещается, иначе отклонена
832. **MODE-BOT-0832** — [Механика режима] Фигура L: якорь (2,8) — валидна если помещается, иначе отклонена
833. **MODE-BOT-0833** — [Механика режима] Фигура L: якорь (3,0) — валидна если помещается, иначе отклонена
834. **MODE-BOT-0834** — [C] [Механика режима] Фигура L: якорь (3,1) — валидна если помещается, иначе отклонена
835. **MODE-BOT-0835** — [Механика режима] Фигура L: якорь (3,2) — валидна если помещается, иначе отклонена
836. **MODE-BOT-0836** — [Механика режима] Фигура L: якорь (3,3) — валидна если помещается, иначе отклонена
837. **MODE-BOT-0837** — [Механика режима] Фигура L: якорь (3,4) — валидна если помещается, иначе отклонена
838. **MODE-BOT-0838** — [Механика режима] Фигура L: якорь (3,5) — валидна если помещается, иначе отклонена
839. **MODE-BOT-0839** — [Механика режима] Фигура L: якорь (3,6) — валидна если помещается, иначе отклонена
840. **MODE-BOT-0840** — [Механика режима] Фигура L: якорь (3,7) — валидна если помещается, иначе отклонена
841. **MODE-BOT-0841** — [C] [Механика режима] Фигура L: якорь (3,8) — валидна если помещается, иначе отклонена
842. **MODE-BOT-0842** — [Механика режима] Фигура L: якорь (4,0) — валидна если помещается, иначе отклонена
843. **MODE-BOT-0843** — [Механика режима] Фигура L: якорь (4,1) — валидна если помещается, иначе отклонена
844. **MODE-BOT-0844** — [Механика режима] Фигура L: якорь (4,2) — валидна если помещается, иначе отклонена
845. **MODE-BOT-0845** — [Механика режима] Фигура L: якорь (4,3) — валидна если помещается, иначе отклонена
846. **MODE-BOT-0846** — [Механика режима] Фигура L: якорь (4,4) — валидна если помещается, иначе отклонена
847. **MODE-BOT-0847** — [Механика режима] Фигура L: якорь (4,5) — валидна если помещается, иначе отклонена
848. **MODE-BOT-0848** — [C] [Механика режима] Фигура L: якорь (4,6) — валидна если помещается, иначе отклонена
849. **MODE-BOT-0849** — [Механика режима] Фигура L: якорь (4,7) — валидна если помещается, иначе отклонена
850. **MODE-BOT-0850** — [Механика режима] Фигура L: якорь (4,8) — валидна если помещается, иначе отклонена
851. **MODE-BOT-0851** — [Механика режима] Фигура L: якорь (5,0) — валидна если помещается, иначе отклонена
852. **MODE-BOT-0852** — [Механика режима] Фигура L: якорь (5,1) — валидна если помещается, иначе отклонена
853. **MODE-BOT-0853** — [Механика режима] Фигура L: якорь (5,2) — валидна если помещается, иначе отклонена
854. **MODE-BOT-0854** — [Механика режима] Фигура L: якорь (5,3) — валидна если помещается, иначе отклонена
855. **MODE-BOT-0855** — [C] [Механика режима] Фигура L: якорь (5,4) — валидна если помещается, иначе отклонена
856. **MODE-BOT-0856** — [Механика режима] Фигура L: якорь (5,5) — валидна если помещается, иначе отклонена
857. **MODE-BOT-0857** — [Механика режима] Фигура L: якорь (5,6) — валидна если помещается, иначе отклонена
858. **MODE-BOT-0858** — [Механика режима] Фигура L: якорь (5,7) — валидна если помещается, иначе отклонена
859. **MODE-BOT-0859** — [Механика режима] Фигура L: якорь (5,8) — валидна если помещается, иначе отклонена
860. **MODE-BOT-0860** — [Механика режима] Фигура L: якорь (6,0) — валидна если помещается, иначе отклонена
861. **MODE-BOT-0861** — [Механика режима] Фигура L: якорь (6,1) — валидна если помещается, иначе отклонена
862. **MODE-BOT-0862** — [C] [Механика режима] Фигура L: якорь (6,2) — валидна если помещается, иначе отклонена
863. **MODE-BOT-0863** — [Механика режима] Фигура L: якорь (6,3) — валидна если помещается, иначе отклонена
864. **MODE-BOT-0864** — [Механика режима] Фигура L: якорь (6,4) — валидна если помещается, иначе отклонена
865. **MODE-BOT-0865** — [Механика режима] Фигура L: якорь (6,5) — валидна если помещается, иначе отклонена
866. **MODE-BOT-0866** — [Механика режима] Фигура L: якорь (6,6) — валидна если помещается, иначе отклонена
867. **MODE-BOT-0867** — [Механика режима] Фигура L: якорь (6,7) — валидна если помещается, иначе отклонена
868. **MODE-BOT-0868** — [Механика режима] Фигура L: якорь (6,8) — валидна если помещается, иначе отклонена
869. **MODE-BOT-0869** — [C] [Механика режима] Фигура L: якорь (7,0) — валидна если помещается, иначе отклонена
870. **MODE-BOT-0870** — [Механика режима] Фигура L: якорь (7,1) — валидна если помещается, иначе отклонена
871. **MODE-BOT-0871** — [Механика режима] Фигура L: якорь (7,2) — валидна если помещается, иначе отклонена
872. **MODE-BOT-0872** — [Механика режима] Фигура L: якорь (7,3) — валидна если помещается, иначе отклонена
873. **MODE-BOT-0873** — [Механика режима] Фигура L: якорь (7,4) — валидна если помещается, иначе отклонена
874. **MODE-BOT-0874** — [Механика режима] Фигура L: якорь (7,5) — валидна если помещается, иначе отклонена
875. **MODE-BOT-0875** — [Механика режима] Фигура L: якорь (7,6) — валидна если помещается, иначе отклонена
876. **MODE-BOT-0876** — [C] [Механика режима] Фигура L: якорь (7,7) — валидна если помещается, иначе отклонена
877. **MODE-BOT-0877** — [Механика режима] Фигура L: якорь (7,8) — валидна если помещается, иначе отклонена
878. **MODE-BOT-0878** — [Механика режима] Фигура L: якорь (8,0) — валидна если помещается, иначе отклонена
879. **MODE-BOT-0879** — [Механика режима] Фигура L: якорь (8,1) — валидна если помещается, иначе отклонена
880. **MODE-BOT-0880** — [Механика режима] Фигура L: якорь (8,2) — валидна если помещается, иначе отклонена
881. **MODE-BOT-0881** — [Механика режима] Фигура L: якорь (8,3) — валидна если помещается, иначе отклонена
882. **MODE-BOT-0882** — [Механика режима] Фигура L: якорь (8,4) — валидна если помещается, иначе отклонена
883. **MODE-BOT-0883** — [C] [Механика режима] Фигура L: якорь (8,5) — валидна если помещается, иначе отклонена
884. **MODE-BOT-0884** — [Механика режима] Фигура L: якорь (8,6) — валидна если помещается, иначе отклонена
885. **MODE-BOT-0885** — [Механика режима] Фигура L: якорь (8,7) — валидна если помещается, иначе отклонена
886. **MODE-BOT-0886** — [Механика режима] Фигура L: якорь (8,8) — валидна если помещается, иначе отклонена
887. **MODE-BOT-0887** — [Механика режима] Полная строка 0: очистка и начисление очков ходившему
888. **MODE-BOT-0888** — [Механика режима] Полная строка 1: очистка и начисление очков ходившему
889. **MODE-BOT-0889** — [Механика режима] Полная строка 2: очистка и начисление очков ходившему
890. **MODE-BOT-0890** — [C] [Механика режима] Полная строка 3: очистка и начисление очков ходившему
891. **MODE-BOT-0891** — [Механика режима] Полная строка 4: очистка и начисление очков ходившему
892. **MODE-BOT-0892** — [Механика режима] Полная строка 5: очистка и начисление очков ходившему
893. **MODE-BOT-0893** — [Механика режима] Полная строка 6: очистка и начисление очков ходившему
894. **MODE-BOT-0894** — [Механика режима] Полная строка 7: очистка и начисление очков ходившему
895. **MODE-BOT-0895** — [Механика режима] Полная строка 8: очистка и начисление очков ходившему
896. **MODE-BOT-0896** — [Механика режима] Полный столбец 0: очистка и начисление очков ходившему
897. **MODE-BOT-0897** — [C] [Механика режима] Полный столбец 1: очистка и начисление очков ходившему
898. **MODE-BOT-0898** — [Механика режима] Полный столбец 2: очистка и начисление очков ходившему
899. **MODE-BOT-0899** — [Механика режима] Полный столбец 3: очистка и начисление очков ходившему
900. **MODE-BOT-0900** — [Механика режима] Полный столбец 4: очистка и начисление очков ходившему
901. **MODE-BOT-0901** — [Механика режима] Полный столбец 5: очистка и начисление очков ходившему
902. **MODE-BOT-0902** — [Механика режима] Полный столбец 6: очистка и начисление очков ходившему
903. **MODE-BOT-0903** — [Механика режима] Полный столбец 7: очистка и начисление очков ходившему
904. **MODE-BOT-0904** — [C] [Механика режима] Полный столбец 8: очистка и начисление очков ходившему
905. **MODE-BOT-0905** — [Механика режима] Бокс 3×3 [0,0]: очистка и начисление
906. **MODE-BOT-0906** — [Механика режима] Бокс 3×3 [0,1]: очистка и начисление
907. **MODE-BOT-0907** — [Механика режима] Бокс 3×3 [0,2]: очистка и начисление
908. **MODE-BOT-0908** — [Механика режима] Бокс 3×3 [1,0]: очистка и начисление
909. **MODE-BOT-0909** — [Механика режима] Бокс 3×3 [1,1]: очистка и начисление
910. **MODE-BOT-0910** — [Механика режима] Бокс 3×3 [1,2]: очистка и начисление
911. **MODE-BOT-0911** — [C] [Механика режима] Бокс 3×3 [2,0]: очистка и начисление
912. **MODE-BOT-0912** — [Механика режима] Бокс 3×3 [2,1]: очистка и начисление
913. **MODE-BOT-0913** — [Механика режима] Бокс 3×3 [2,2]: очистка и начисление
914. **MODE-BOT-0914** — [Механика режима] Очистка «строка» при комбо=1: множитель и очки по формуле
915. **MODE-BOT-0915** — [Механика режима] Очистка «строка» при комбо=2: множитель и очки по формуле
916. **MODE-BOT-0916** — [Механика режима] Очистка «строка» при комбо=3: множитель и очки по формуле
917. **MODE-BOT-0917** — [Механика режима] Очистка «строка» при комбо=4: множитель и очки по формуле
918. **MODE-BOT-0918** — [C] [Механика режима] Очистка «строка» при комбо=5: множитель и очки по формуле
919. **MODE-BOT-0919** — [Механика режима] Очистка «строка» при комбо=6: множитель и очки по формуле
920. **MODE-BOT-0920** — [Механика режима] Очистка «строка» при комбо=7: множитель и очки по формуле
921. **MODE-BOT-0921** — [Механика режима] Очистка «строка» при комбо=8: множитель и очки по формуле
922. **MODE-BOT-0922** — [Механика режима] Очистка «строка» при комбо=9: множитель и очки по формуле
923. **MODE-BOT-0923** — [Механика режима] Очистка «строка» при комбо=10: множитель и очки по формуле
924. **MODE-BOT-0924** — [Механика режима] Очистка «столбец» при комбо=1: множитель и очки по формуле
925. **MODE-BOT-0925** — [C] [Механика режима] Очистка «столбец» при комбо=2: множитель и очки по формуле
926. **MODE-BOT-0926** — [Механика режима] Очистка «столбец» при комбо=3: множитель и очки по формуле
927. **MODE-BOT-0927** — [Механика режима] Очистка «столбец» при комбо=4: множитель и очки по формуле
928. **MODE-BOT-0928** — [Механика режима] Очистка «столбец» при комбо=5: множитель и очки по формуле
929. **MODE-BOT-0929** — [Механика режима] Очистка «столбец» при комбо=6: множитель и очки по формуле
930. **MODE-BOT-0930** — [Механика режима] Очистка «столбец» при комбо=7: множитель и очки по формуле
931. **MODE-BOT-0931** — [Механика режима] Очистка «столбец» при комбо=8: множитель и очки по формуле
932. **MODE-BOT-0932** — [C] [Механика режима] Очистка «столбец» при комбо=9: множитель и очки по формуле
933. **MODE-BOT-0933** — [Механика режима] Очистка «столбец» при комбо=10: множитель и очки по формуле
934. **MODE-BOT-0934** — [Механика режима] Очистка «бокс 3×3» при комбо=1: множитель и очки по формуле
935. **MODE-BOT-0935** — [Механика режима] Очистка «бокс 3×3» при комбо=2: множитель и очки по формуле
936. **MODE-BOT-0936** — [Механика режима] Очистка «бокс 3×3» при комбо=3: множитель и очки по формуле
937. **MODE-BOT-0937** — [Механика режима] Очистка «бокс 3×3» при комбо=4: множитель и очки по формуле
938. **MODE-BOT-0938** — [Механика режима] Очистка «бокс 3×3» при комбо=5: множитель и очки по формуле
939. **MODE-BOT-0939** — [C] [Механика режима] Очистка «бокс 3×3» при комбо=6: множитель и очки по формуле
940. **MODE-BOT-0940** — [Механика режима] Очистка «бокс 3×3» при комбо=7: множитель и очки по формуле
941. **MODE-BOT-0941** — [Механика режима] Очистка «бокс 3×3» при комбо=8: множитель и очки по формуле
942. **MODE-BOT-0942** — [Механика режима] Очистка «бокс 3×3» при комбо=9: множитель и очки по формуле
943. **MODE-BOT-0943** — [Механика режима] Очистка «бокс 3×3» при комбо=10: множитель и очки по формуле
944. **MODE-BOT-0944** — [Механика режима] Очистка «мульти-2» при комбо=1: множитель и очки по формуле
945. **MODE-BOT-0945** — [Механика режима] Очистка «мульти-2» при комбо=2: множитель и очки по формуле
946. **MODE-BOT-0946** — [C] [Механика режима] Очистка «мульти-2» при комбо=3: множитель и очки по формуле
947. **MODE-BOT-0947** — [Механика режима] Очистка «мульти-2» при комбо=4: множитель и очки по формуле
948. **MODE-BOT-0948** — [Механика режима] Очистка «мульти-2» при комбо=5: множитель и очки по формуле
949. **MODE-BOT-0949** — [Механика режима] Очистка «мульти-2» при комбо=6: множитель и очки по формуле
950. **MODE-BOT-0950** — [Механика режима] Очистка «мульти-2» при комбо=7: множитель и очки по формуле
951. **MODE-BOT-0951** — [Механика режима] Очистка «мульти-2» при комбо=8: множитель и очки по формуле
952. **MODE-BOT-0952** — [Механика режима] Очистка «мульти-2» при комбо=9: множитель и очки по формуле
953. **MODE-BOT-0953** — [C] [Механика режима] Очистка «мульти-2» при комбо=10: множитель и очки по формуле
954. **MODE-BOT-0954** — [Механика режима] Очистка «мульти-3» при комбо=1: множитель и очки по формуле
955. **MODE-BOT-0955** — [Механика режима] Очистка «мульти-3» при комбо=2: множитель и очки по формуле
956. **MODE-BOT-0956** — [Механика режима] Очистка «мульти-3» при комбо=3: множитель и очки по формуле
957. **MODE-BOT-0957** — [Механика режима] Очистка «мульти-3» при комбо=4: множитель и очки по формуле
958. **MODE-BOT-0958** — [Механика режима] Очистка «мульти-3» при комбо=5: множитель и очки по формуле
959. **MODE-BOT-0959** — [Механика режима] Очистка «мульти-3» при комбо=6: множитель и очки по формуле
960. **MODE-BOT-0960** — [C] [Механика режима] Очистка «мульти-3» при комбо=7: множитель и очки по формуле
961. **MODE-BOT-0961** — [Механика режима] Очистка «мульти-3» при комбо=8: множитель и очки по формуле
962. **MODE-BOT-0962** — [Механика режима] Очистка «мульти-3» при комбо=9: множитель и очки по формуле
963. **MODE-BOT-0963** — [Механика режима] Очистка «мульти-3» при комбо=10: множитель и очки по формуле
964. **MODE-BOT-0964** — [Механика режима] Очистка «мульти-4» при комбо=1: множитель и очки по формуле
965. **MODE-BOT-0965** — [Механика режима] Очистка «мульти-4» при комбо=2: множитель и очки по формуле
966. **MODE-BOT-0966** — [Механика режима] Очистка «мульти-4» при комбо=3: множитель и очки по формуле
967. **MODE-BOT-0967** — [C] [Механика режима] Очистка «мульти-4» при комбо=4: множитель и очки по формуле
968. **MODE-BOT-0968** — [Механика режима] Очистка «мульти-4» при комбо=5: множитель и очки по формуле
969. **MODE-BOT-0969** — [Механика режима] Очистка «мульти-4» при комбо=6: множитель и очки по формуле
970. **MODE-BOT-0970** — [Механика режима] Очистка «мульти-4» при комбо=7: множитель и очки по формуле
971. **MODE-BOT-0971** — [Механика режима] Очистка «мульти-4» при комбо=8: множитель и очки по формуле
972. **MODE-BOT-0972** — [Механика режима] Очистка «мульти-4» при комбо=9: множитель и очки по формуле
973. **MODE-BOT-0973** — [Механика режима] Очистка «мульти-4» при комбо=10: множитель и очки по формуле
974. **MODE-BOT-0974** — [C] [Механика режима] Perfect clear: +15 и конфетти/звук
975. **MODE-BOT-0975** — [Механика режима] base=N·(N+1)/2; placement-бонус I=+5/L,J=+3/T,S,Z=+1/O=0
976. **MODE-BOT-0976** — [Механика режима] Тупик: партия завершается, победитель по очкам, ничья при равенстве
977. **MODE-BOT-0977** — [Механика режима] 7-bag: свой мешок у игрока, каждая фигура по разу за цикл
978. **MODE-BOT-0978** — [Механика режима] Блиц «normal»: старт-время/decay применяются
979. **MODE-BOT-0979** — [Механика режима] Блиц «normal»: таймаут → force-place; при отсутствии ходов — конец партии
980. **MODE-BOT-0980** — [Механика режима] Блиц «fast»: старт-время/decay применяются
981. **MODE-BOT-0981** — [C] [Механика режима] Блиц «fast»: таймаут → force-place; при отсутствии ходов — конец партии
982. **MODE-BOT-0982** — [Механика режима] Блиц «hardcore»: старт-время/decay применяются
983. **MODE-BOT-0983** — [Механика режима] Блиц «hardcore»: таймаут → force-place; при отсутствии ходов — конец партии
984. **MODE-BOT-0984** — [Механика режима] Блиц «выключен»: старт-время/decay применяются
985. **MODE-BOT-0985** — [Механика режима] Блиц «выключен»: таймаут → force-place; при отсутствии ходов — конец партии
986. **MODE-BOT-0986** — [Механика режима] Бот «easy»: делает валидный ход в разумное время
987. **MODE-BOT-0987** — [Механика режима] Бот «easy»: не ставит фигуру за край/на занятое
988. **MODE-BOT-0988** — [C] [Механика режима] Бот «medium»: делает валидный ход в разумное время
989. **MODE-BOT-0989** — [Механика режима] Бот «medium»: не ставит фигуру за край/на занятое
990. **MODE-BOT-0990** — [Механика режима] Бот «hard»: делает валидный ход в разумное время
991. **MODE-BOT-0991** — [Механика режима] Бот «hard»: не ставит фигуру за край/на занятое
992. **MODE-BOT-0992** — [Механика режима] Задержка бота уважает настройку скорости
993. **MODE-BOT-0993** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · blitz=normal [bot]: счёт неотрицателен на всём протяжении
994. **MODE-BOT-0994** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · blitz=normal [bot]: ни одного исключения/краша за партию
995. **MODE-BOT-0995** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · blitz=normal [bot]: resume в середине — бит-в-бит то же продолжение
996. **MODE-BOT-0996** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · blitz=normal [bot]: число ходов конечно (партия сходится)
997. **MODE-BOT-0997** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · blitz=normal [bot]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
998. **MODE-BOT-0998** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · blitz=normal [bot]: ни одной невалидной постановки не принято
999. **MODE-BOT-0999** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · blitz=normal [bot]: инвариант доски: заполненные клетки не «протекают» за границы
1000. **MODE-BOT-1000** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · blitz=normal [bot]: очистка начисляется ходившему игроку
1001. **MODE-BOT-1001** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · blitz=normal [bot]: тупик корректно завершает партию
