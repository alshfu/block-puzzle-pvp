# BlockDuel — Каталог сценариев режима «Match-3 PvP (8×8)»

**Ровно 1001 сценариев** режима «Match-3 PvP (8×8)» («1000 и 1»).
Доска: 8×8. Маршрут: `/match3`. Сгенерировано детерминированно
из `tools/gen_scenarios.py` (ось «режим») — НЕ редактировать вручную.

- **ID** `MODE-MATCH3-0001…MODE-MATCH3-1001` стабильны между прогонами.
- **[C]** — критический smoke-срез (каждый ~7-й).
- «Механика режима» — уникальная логика; хвост «Property-фаззинг» —
  осмысленная матрица (seed × конфиг × инвариант) для добора до 1001 в
  режимах малой игровой поверхности (это valid property-тесты, не филлер).

## Покрытие по областям

- Запуск и жизненный цикл: 6
- Рендер и темы: 35
- Ввод: 4
- Механика режима: 375
- Прогрессия и рекорды: 4
- Настройки в режиме: 15
- Ошибки и устойчивость: 4
- Property-фаззинг (seed×конфиг): 558

## Сценарии

1. **MODE-MATCH3-0001** — [C] [Запуск и жизненный цикл] Запуск «Match-3 PvP (8×8)» из меню по маршруту /match3 — экран рисуется без ошибок
2. **MODE-MATCH3-0002** — [Рендер и темы] «Match-3 PvP (8×8)»: поверхность «доска» в теме «neutral» — цвета/контраст по токенам
3. **MODE-MATCH3-0003** — [Ввод] «Match-3 PvP (8×8)»: своп соседей через тап — анимация и разрешение
4. **MODE-MATCH3-0004** — [Механика режима] Своп (0,0)↔(0,1) [вправо]: разрешён только если даёт серию, иначе откат
5. **MODE-MATCH3-0005** — [Прогрессия и рекорды] «Match-3 PvP (8×8)»: завершение партии пишет результат в статистику режима
6. **MODE-MATCH3-0006** — [Настройки в режиме] «Match-3 PvP (8×8)»: настройка «Звук (soundOn)» применяется в режиме и переживает перезапуск
7. **MODE-MATCH3-0007** — [Ошибки и устойчивость] «Match-3 PvP (8×8)»: повреждённая сохранёнка → graceful fallback, без краша
8. **MODE-MATCH3-0008** — [C] [Запуск и жизненный цикл] Возврат из «Match-3 PvP (8×8)» в меню — стек навигации корректен, без утечек
9. **MODE-MATCH3-0009** — [Рендер и темы] «Match-3 PvP (8×8)»: поверхность «доска» в теме «candy» — цвета/контраст по токенам
10. **MODE-MATCH3-0010** — [Ввод] «Match-3 PvP (8×8)»: своп соседей через drag-and-drop — анимация и разрешение
11. **MODE-MATCH3-0011** — [Механика режима] Своп (0,0)↔(1,0) [вниз]: разрешён только если даёт серию, иначе откат
12. **MODE-MATCH3-0012** — [Прогрессия и рекорды] «Match-3 PvP (8×8)»: вклад в composite-score (сводный рейтинг) учитывается
13. **MODE-MATCH3-0013** — [Настройки в режиме] «Match-3 PvP (8×8)»: настройка «Музыка (musicOn)» применяется в режиме и переживает перезапуск
14. **MODE-MATCH3-0014** — [Ошибки и устойчивость] «Match-3 PvP (8×8)»: сворачивание в момент хода → без рассинхрона состояния
15. **MODE-MATCH3-0015** — [C] [Запуск и жизненный цикл] Холодный старт «Match-3 PvP (8×8)» в теме «neutral» — без белой вспышки
16. **MODE-MATCH3-0016** — [Рендер и темы] «Match-3 PvP (8×8)»: поверхность «доска» в теме «night» — цвета/контраст по токенам
17. **MODE-MATCH3-0017** — [Ввод] «Match-3 PvP (8×8)»: своп несоседних клеток — запрещён
18. **MODE-MATCH3-0018** — [Механика режима] Своп (0,1)↔(0,2) [вправо]: разрешён только если даёт серию, иначе откат
19. **MODE-MATCH3-0019** — [Прогрессия и рекорды] «Match-3 PvP (8×8)»: XP по исходу начисляется один раз (ничья ×0.5, поражение 0)
20. **MODE-MATCH3-0020** — [Настройки в режиме] «Match-3 PvP (8×8)»: настройка «Громкость SFX» применяется в режиме и переживает перезапуск
21. **MODE-MATCH3-0021** — [Ошибки и устойчивость] «Match-3 PvP (8×8)»: быстрый повторный ввод не даёт двойного хода
22. **MODE-MATCH3-0022** — [C] [Запуск и жизненный цикл] Холодный старт «Match-3 PvP (8×8)» в теме «candy» — без белой вспышки
23. **MODE-MATCH3-0023** — [Рендер и темы] «Match-3 PvP (8×8)»: «доска» при reduceMotion=on — статичный декор
24. **MODE-MATCH3-0024** — [Ввод] «Match-3 PvP (8×8)»: своп без серии — откат к исходному
25. **MODE-MATCH3-0025** — [Механика режима] Своп (0,1)↔(1,1) [вниз]: разрешён только если даёт серию, иначе откат
26. **MODE-MATCH3-0026** — [Прогрессия и рекорды] «Match-3 PvP (8×8)»: личный рекорд обновляется только при улучшении
27. **MODE-MATCH3-0027** — [Настройки в режиме] «Match-3 PvP (8×8)»: настройка «Громкость музыки» применяется в режиме и переживает перезапуск
28. **MODE-MATCH3-0028** — [Ошибки и устойчивость] «Match-3 PvP (8×8)»: возврат/пауза в критический момент не ломает партию
29. **MODE-MATCH3-0029** — [C] [Запуск и жизненный цикл] Холодный старт «Match-3 PvP (8×8)» в теме «night» — без белой вспышки
30. **MODE-MATCH3-0030** — [Рендер и темы] «Match-3 PvP (8×8)»: «доска» на узком окне — нет overflow
31. **MODE-MATCH3-0031** — [Механика режима] Своп (0,1)↔(0,0) [влево]: разрешён только если даёт серию, иначе откат
32. **MODE-MATCH3-0032** — [Настройки в режиме] «Match-3 PvP (8×8)»: настройка «Вибрация» применяется в режиме и переживает перезапуск
33. **MODE-MATCH3-0033** — [Запуск и жизненный цикл] Двойной быстрый тап по старту «Match-3 PvP (8×8)» не создаёт две партии
34. **MODE-MATCH3-0034** — [Рендер и темы] «Match-3 PvP (8×8)»: «доска» на широком окне — адаптивная боковая раскладка
35. **MODE-MATCH3-0035** — [Механика режима] Своп (0,2)↔(0,3) [вправо]: разрешён только если даёт серию, иначе откат
36. **MODE-MATCH3-0036** — [C] [Настройки в режиме] «Match-3 PvP (8×8)»: настройка «Уменьшить движение (reduceMotion)» применяется в режиме и переживает перезапуск
37. **MODE-MATCH3-0037** — [Рендер и темы] «Match-3 PvP (8×8)»: «доска» с нейтральными клетками (a11y) — не зависит от цвета владельца
38. **MODE-MATCH3-0038** — [Механика режима] Своп (0,2)↔(1,2) [вниз]: разрешён только если даёт серию, иначе откат
39. **MODE-MATCH3-0039** — [Настройки в режиме] «Match-3 PvP (8×8)»: настройка «Конфетти» применяется в режиме и переживает перезапуск
40. **MODE-MATCH3-0040** — [Рендер и темы] «Match-3 PvP (8×8)»: поверхность «HUD (счёт/таймер)» в теме «neutral» — цвета/контраст по токенам
41. **MODE-MATCH3-0041** — [Механика режима] Своп (0,2)↔(0,1) [влево]: разрешён только если даёт серию, иначе откат
42. **MODE-MATCH3-0042** — [Настройки в режиме] «Match-3 PvP (8×8)»: настройка «Маскоты» применяется в режиме и переживает перезапуск
43. **MODE-MATCH3-0043** — [C] [Рендер и темы] «Match-3 PvP (8×8)»: поверхность «HUD (счёт/таймер)» в теме «candy» — цвета/контраст по токенам
44. **MODE-MATCH3-0044** — [Механика режима] Своп (0,3)↔(0,4) [вправо]: разрешён только если даёт серию, иначе откат
45. **MODE-MATCH3-0045** — [Настройки в режиме] «Match-3 PvP (8×8)»: настройка «Призрак фигуры (ghost)» применяется в режиме и переживает перезапуск
46. **MODE-MATCH3-0046** — [Рендер и темы] «Match-3 PvP (8×8)»: поверхность «HUD (счёт/таймер)» в теме «night» — цвета/контраст по токенам
47. **MODE-MATCH3-0047** — [Механика режима] Своп (0,3)↔(1,3) [вниз]: разрешён только если даёт серию, иначе откат
48. **MODE-MATCH3-0048** — [Настройки в режиме] «Match-3 PvP (8×8)»: настройка «Задержка бота» применяется в режиме и переживает перезапуск
49. **MODE-MATCH3-0049** — [Рендер и темы] «Match-3 PvP (8×8)»: «HUD (счёт/таймер)» при reduceMotion=on — статичный декор
50. **MODE-MATCH3-0050** — [C] [Механика режима] Своп (0,3)↔(0,2) [влево]: разрешён только если даёт серию, иначе откат
51. **MODE-MATCH3-0051** — [Настройки в режиме] «Match-3 PvP (8×8)»: настройка «Тема по умолчанию» применяется в режиме и переживает перезапуск
52. **MODE-MATCH3-0052** — [Рендер и темы] «Match-3 PvP (8×8)»: «HUD (счёт/таймер)» на узком окне — нет overflow
53. **MODE-MATCH3-0053** — [Механика режима] Своп (0,4)↔(0,5) [вправо]: разрешён только если даёт серию, иначе откат
54. **MODE-MATCH3-0054** — [Настройки в режиме] «Match-3 PvP (8×8)»: настройка «Правила по умолчанию (handSize/rotation/flip)» применяется в режиме и переживает перезапуск
55. **MODE-MATCH3-0055** — [Рендер и темы] «Match-3 PvP (8×8)»: «HUD (счёт/таймер)» на широком окне — адаптивная боковая раскладка
56. **MODE-MATCH3-0056** — [Механика режима] Своп (0,4)↔(1,4) [вниз]: разрешён только если даёт серию, иначе откат
57. **MODE-MATCH3-0057** — [C] [Настройки в режиме] «Match-3 PvP (8×8)»: настройка «Пресет блица» применяется в режиме и переживает перезапуск
58. **MODE-MATCH3-0058** — [Рендер и темы] «Match-3 PvP (8×8)»: «HUD (счёт/таймер)» с нейтральными клетками (a11y) — не зависит от цвета владельца
59. **MODE-MATCH3-0059** — [Механика режима] Своп (0,4)↔(0,3) [влево]: разрешён только если даёт серию, иначе откат
60. **MODE-MATCH3-0060** — [Настройки в режиме] «Match-3 PvP (8×8)»: настройка «Нейтральные клетки (accessibility)» применяется в режиме и переживает перезапуск
61. **MODE-MATCH3-0061** — [Рендер и темы] «Match-3 PvP (8×8)»: поверхность «панель руки» в теме «neutral» — цвета/контраст по токенам
62. **MODE-MATCH3-0062** — [Механика режима] Своп (0,5)↔(0,6) [вправо]: разрешён только если даёт серию, иначе откат
63. **MODE-MATCH3-0063** — [Настройки в режиме] «Match-3 PvP (8×8)»: настройка «Язык интерфейса» применяется в режиме и переживает перезапуск
64. **MODE-MATCH3-0064** — [C] [Рендер и темы] «Match-3 PvP (8×8)»: поверхность «панель руки» в теме «candy» — цвета/контраст по токенам
65. **MODE-MATCH3-0065** — [Механика режима] Своп (0,5)↔(1,5) [вниз]: разрешён только если даёт серию, иначе откат
66. **MODE-MATCH3-0066** — [Рендер и темы] «Match-3 PvP (8×8)»: поверхность «панель руки» в теме «night» — цвета/контраст по токенам
67. **MODE-MATCH3-0067** — [Механика режима] Своп (0,5)↔(0,4) [влево]: разрешён только если даёт серию, иначе откат
68. **MODE-MATCH3-0068** — [Рендер и темы] «Match-3 PvP (8×8)»: «панель руки» при reduceMotion=on — статичный декор
69. **MODE-MATCH3-0069** — [Механика режима] Своп (0,6)↔(0,7) [вправо]: разрешён только если даёт серию, иначе откат
70. **MODE-MATCH3-0070** — [Рендер и темы] «Match-3 PvP (8×8)»: «панель руки» на узком окне — нет overflow
71. **MODE-MATCH3-0071** — [C] [Механика режима] Своп (0,6)↔(1,6) [вниз]: разрешён только если даёт серию, иначе откат
72. **MODE-MATCH3-0072** — [Рендер и темы] «Match-3 PvP (8×8)»: «панель руки» на широком окне — адаптивная боковая раскладка
73. **MODE-MATCH3-0073** — [Механика режима] Своп (0,6)↔(0,5) [влево]: разрешён только если даёт серию, иначе откат
74. **MODE-MATCH3-0074** — [Рендер и темы] «Match-3 PvP (8×8)»: «панель руки» с нейтральными клетками (a11y) — не зависит от цвета владельца
75. **MODE-MATCH3-0075** — [Механика режима] Своп (0,7)↔(1,7) [вниз]: разрешён только если даёт серию, иначе откат
76. **MODE-MATCH3-0076** — [Рендер и темы] «Match-3 PvP (8×8)»: поверхность «экран результата» в теме «neutral» — цвета/контраст по токенам
77. **MODE-MATCH3-0077** — [Механика режима] Своп (0,7)↔(0,6) [влево]: разрешён только если даёт серию, иначе откат
78. **MODE-MATCH3-0078** — [C] [Рендер и темы] «Match-3 PvP (8×8)»: поверхность «экран результата» в теме «candy» — цвета/контраст по токенам
79. **MODE-MATCH3-0079** — [Механика режима] Своп (1,0)↔(1,1) [вправо]: разрешён только если даёт серию, иначе откат
80. **MODE-MATCH3-0080** — [Рендер и темы] «Match-3 PvP (8×8)»: поверхность «экран результата» в теме «night» — цвета/контраст по токенам
81. **MODE-MATCH3-0081** — [Механика режима] Своп (1,0)↔(2,0) [вниз]: разрешён только если даёт серию, иначе откат
82. **MODE-MATCH3-0082** — [Рендер и темы] «Match-3 PvP (8×8)»: «экран результата» при reduceMotion=on — статичный декор
83. **MODE-MATCH3-0083** — [Механика режима] Своп (1,0)↔(0,0) [вверх]: разрешён только если даёт серию, иначе откат
84. **MODE-MATCH3-0084** — [Рендер и темы] «Match-3 PvP (8×8)»: «экран результата» на узком окне — нет overflow
85. **MODE-MATCH3-0085** — [C] [Механика режима] Своп (1,1)↔(1,2) [вправо]: разрешён только если даёт серию, иначе откат
86. **MODE-MATCH3-0086** — [Рендер и темы] «Match-3 PvP (8×8)»: «экран результата» на широком окне — адаптивная боковая раскладка
87. **MODE-MATCH3-0087** — [Механика режима] Своп (1,1)↔(2,1) [вниз]: разрешён только если даёт серию, иначе откат
88. **MODE-MATCH3-0088** — [Рендер и темы] «Match-3 PvP (8×8)»: «экран результата» с нейтральными клетками (a11y) — не зависит от цвета владельца
89. **MODE-MATCH3-0089** — [Механика режима] Своп (1,1)↔(1,0) [влево]: разрешён только если даёт серию, иначе откат
90. **MODE-MATCH3-0090** — [Рендер и темы] «Match-3 PvP (8×8)»: поверхность «пауза/меню выхода» в теме «neutral» — цвета/контраст по токенам
91. **MODE-MATCH3-0091** — [Механика режима] Своп (1,1)↔(0,1) [вверх]: разрешён только если даёт серию, иначе откат
92. **MODE-MATCH3-0092** — [C] [Рендер и темы] «Match-3 PvP (8×8)»: поверхность «пауза/меню выхода» в теме «candy» — цвета/контраст по токенам
93. **MODE-MATCH3-0093** — [Механика режима] Своп (1,2)↔(1,3) [вправо]: разрешён только если даёт серию, иначе откат
94. **MODE-MATCH3-0094** — [Рендер и темы] «Match-3 PvP (8×8)»: поверхность «пауза/меню выхода» в теме «night» — цвета/контраст по токенам
95. **MODE-MATCH3-0095** — [Механика режима] Своп (1,2)↔(2,2) [вниз]: разрешён только если даёт серию, иначе откат
96. **MODE-MATCH3-0096** — [Рендер и темы] «Match-3 PvP (8×8)»: «пауза/меню выхода» при reduceMotion=on — статичный декор
97. **MODE-MATCH3-0097** — [Механика режима] Своп (1,2)↔(1,1) [влево]: разрешён только если даёт серию, иначе откат
98. **MODE-MATCH3-0098** — [Рендер и темы] «Match-3 PvP (8×8)»: «пауза/меню выхода» на узком окне — нет overflow
99. **MODE-MATCH3-0099** — [C] [Механика режима] Своп (1,2)↔(0,2) [вверх]: разрешён только если даёт серию, иначе откат
100. **MODE-MATCH3-0100** — [Рендер и темы] «Match-3 PvP (8×8)»: «пауза/меню выхода» на широком окне — адаптивная боковая раскладка
101. **MODE-MATCH3-0101** — [Механика режима] Своп (1,3)↔(1,4) [вправо]: разрешён только если даёт серию, иначе откат
102. **MODE-MATCH3-0102** — [Рендер и темы] «Match-3 PvP (8×8)»: «пауза/меню выхода» с нейтральными клетками (a11y) — не зависит от цвета владельца
103. **MODE-MATCH3-0103** — [Механика режима] Своп (1,3)↔(2,3) [вниз]: разрешён только если даёт серию, иначе откат
104. **MODE-MATCH3-0104** — [Механика режима] Своп (1,3)↔(1,2) [влево]: разрешён только если даёт серию, иначе откат
105. **MODE-MATCH3-0105** — [Механика режима] Своп (1,3)↔(0,3) [вверх]: разрешён только если даёт серию, иначе откат
106. **MODE-MATCH3-0106** — [C] [Механика режима] Своп (1,4)↔(1,5) [вправо]: разрешён только если даёт серию, иначе откат
107. **MODE-MATCH3-0107** — [Механика режима] Своп (1,4)↔(2,4) [вниз]: разрешён только если даёт серию, иначе откат
108. **MODE-MATCH3-0108** — [Механика режима] Своп (1,4)↔(1,3) [влево]: разрешён только если даёт серию, иначе откат
109. **MODE-MATCH3-0109** — [Механика режима] Своп (1,4)↔(0,4) [вверх]: разрешён только если даёт серию, иначе откат
110. **MODE-MATCH3-0110** — [Механика режима] Своп (1,5)↔(1,6) [вправо]: разрешён только если даёт серию, иначе откат
111. **MODE-MATCH3-0111** — [Механика режима] Своп (1,5)↔(2,5) [вниз]: разрешён только если даёт серию, иначе откат
112. **MODE-MATCH3-0112** — [Механика режима] Своп (1,5)↔(1,4) [влево]: разрешён только если даёт серию, иначе откат
113. **MODE-MATCH3-0113** — [C] [Механика режима] Своп (1,5)↔(0,5) [вверх]: разрешён только если даёт серию, иначе откат
114. **MODE-MATCH3-0114** — [Механика режима] Своп (1,6)↔(1,7) [вправо]: разрешён только если даёт серию, иначе откат
115. **MODE-MATCH3-0115** — [Механика режима] Своп (1,6)↔(2,6) [вниз]: разрешён только если даёт серию, иначе откат
116. **MODE-MATCH3-0116** — [Механика режима] Своп (1,6)↔(1,5) [влево]: разрешён только если даёт серию, иначе откат
117. **MODE-MATCH3-0117** — [Механика режима] Своп (1,6)↔(0,6) [вверх]: разрешён только если даёт серию, иначе откат
118. **MODE-MATCH3-0118** — [Механика режима] Своп (1,7)↔(2,7) [вниз]: разрешён только если даёт серию, иначе откат
119. **MODE-MATCH3-0119** — [Механика режима] Своп (1,7)↔(1,6) [влево]: разрешён только если даёт серию, иначе откат
120. **MODE-MATCH3-0120** — [C] [Механика режима] Своп (1,7)↔(0,7) [вверх]: разрешён только если даёт серию, иначе откат
121. **MODE-MATCH3-0121** — [Механика режима] Своп (2,0)↔(2,1) [вправо]: разрешён только если даёт серию, иначе откат
122. **MODE-MATCH3-0122** — [Механика режима] Своп (2,0)↔(3,0) [вниз]: разрешён только если даёт серию, иначе откат
123. **MODE-MATCH3-0123** — [Механика режима] Своп (2,0)↔(1,0) [вверх]: разрешён только если даёт серию, иначе откат
124. **MODE-MATCH3-0124** — [Механика режима] Своп (2,1)↔(2,2) [вправо]: разрешён только если даёт серию, иначе откат
125. **MODE-MATCH3-0125** — [Механика режима] Своп (2,1)↔(3,1) [вниз]: разрешён только если даёт серию, иначе откат
126. **MODE-MATCH3-0126** — [Механика режима] Своп (2,1)↔(2,0) [влево]: разрешён только если даёт серию, иначе откат
127. **MODE-MATCH3-0127** — [C] [Механика режима] Своп (2,1)↔(1,1) [вверх]: разрешён только если даёт серию, иначе откат
128. **MODE-MATCH3-0128** — [Механика режима] Своп (2,2)↔(2,3) [вправо]: разрешён только если даёт серию, иначе откат
129. **MODE-MATCH3-0129** — [Механика режима] Своп (2,2)↔(3,2) [вниз]: разрешён только если даёт серию, иначе откат
130. **MODE-MATCH3-0130** — [Механика режима] Своп (2,2)↔(2,1) [влево]: разрешён только если даёт серию, иначе откат
131. **MODE-MATCH3-0131** — [Механика режима] Своп (2,2)↔(1,2) [вверх]: разрешён только если даёт серию, иначе откат
132. **MODE-MATCH3-0132** — [Механика режима] Своп (2,3)↔(2,4) [вправо]: разрешён только если даёт серию, иначе откат
133. **MODE-MATCH3-0133** — [Механика режима] Своп (2,3)↔(3,3) [вниз]: разрешён только если даёт серию, иначе откат
134. **MODE-MATCH3-0134** — [C] [Механика режима] Своп (2,3)↔(2,2) [влево]: разрешён только если даёт серию, иначе откат
135. **MODE-MATCH3-0135** — [Механика режима] Своп (2,3)↔(1,3) [вверх]: разрешён только если даёт серию, иначе откат
136. **MODE-MATCH3-0136** — [Механика режима] Своп (2,4)↔(2,5) [вправо]: разрешён только если даёт серию, иначе откат
137. **MODE-MATCH3-0137** — [Механика режима] Своп (2,4)↔(3,4) [вниз]: разрешён только если даёт серию, иначе откат
138. **MODE-MATCH3-0138** — [Механика режима] Своп (2,4)↔(2,3) [влево]: разрешён только если даёт серию, иначе откат
139. **MODE-MATCH3-0139** — [Механика режима] Своп (2,4)↔(1,4) [вверх]: разрешён только если даёт серию, иначе откат
140. **MODE-MATCH3-0140** — [Механика режима] Своп (2,5)↔(2,6) [вправо]: разрешён только если даёт серию, иначе откат
141. **MODE-MATCH3-0141** — [C] [Механика режима] Своп (2,5)↔(3,5) [вниз]: разрешён только если даёт серию, иначе откат
142. **MODE-MATCH3-0142** — [Механика режима] Своп (2,5)↔(2,4) [влево]: разрешён только если даёт серию, иначе откат
143. **MODE-MATCH3-0143** — [Механика режима] Своп (2,5)↔(1,5) [вверх]: разрешён только если даёт серию, иначе откат
144. **MODE-MATCH3-0144** — [Механика режима] Своп (2,6)↔(2,7) [вправо]: разрешён только если даёт серию, иначе откат
145. **MODE-MATCH3-0145** — [Механика режима] Своп (2,6)↔(3,6) [вниз]: разрешён только если даёт серию, иначе откат
146. **MODE-MATCH3-0146** — [Механика режима] Своп (2,6)↔(2,5) [влево]: разрешён только если даёт серию, иначе откат
147. **MODE-MATCH3-0147** — [Механика режима] Своп (2,6)↔(1,6) [вверх]: разрешён только если даёт серию, иначе откат
148. **MODE-MATCH3-0148** — [C] [Механика режима] Своп (2,7)↔(3,7) [вниз]: разрешён только если даёт серию, иначе откат
149. **MODE-MATCH3-0149** — [Механика режима] Своп (2,7)↔(2,6) [влево]: разрешён только если даёт серию, иначе откат
150. **MODE-MATCH3-0150** — [Механика режима] Своп (2,7)↔(1,7) [вверх]: разрешён только если даёт серию, иначе откат
151. **MODE-MATCH3-0151** — [Механика режима] Своп (3,0)↔(3,1) [вправо]: разрешён только если даёт серию, иначе откат
152. **MODE-MATCH3-0152** — [Механика режима] Своп (3,0)↔(4,0) [вниз]: разрешён только если даёт серию, иначе откат
153. **MODE-MATCH3-0153** — [Механика режима] Своп (3,0)↔(2,0) [вверх]: разрешён только если даёт серию, иначе откат
154. **MODE-MATCH3-0154** — [Механика режима] Своп (3,1)↔(3,2) [вправо]: разрешён только если даёт серию, иначе откат
155. **MODE-MATCH3-0155** — [C] [Механика режима] Своп (3,1)↔(4,1) [вниз]: разрешён только если даёт серию, иначе откат
156. **MODE-MATCH3-0156** — [Механика режима] Своп (3,1)↔(3,0) [влево]: разрешён только если даёт серию, иначе откат
157. **MODE-MATCH3-0157** — [Механика режима] Своп (3,1)↔(2,1) [вверх]: разрешён только если даёт серию, иначе откат
158. **MODE-MATCH3-0158** — [Механика режима] Своп (3,2)↔(3,3) [вправо]: разрешён только если даёт серию, иначе откат
159. **MODE-MATCH3-0159** — [Механика режима] Своп (3,2)↔(4,2) [вниз]: разрешён только если даёт серию, иначе откат
160. **MODE-MATCH3-0160** — [Механика режима] Своп (3,2)↔(3,1) [влево]: разрешён только если даёт серию, иначе откат
161. **MODE-MATCH3-0161** — [Механика режима] Своп (3,2)↔(2,2) [вверх]: разрешён только если даёт серию, иначе откат
162. **MODE-MATCH3-0162** — [C] [Механика режима] Своп (3,3)↔(3,4) [вправо]: разрешён только если даёт серию, иначе откат
163. **MODE-MATCH3-0163** — [Механика режима] Своп (3,3)↔(4,3) [вниз]: разрешён только если даёт серию, иначе откат
164. **MODE-MATCH3-0164** — [Механика режима] Своп (3,3)↔(3,2) [влево]: разрешён только если даёт серию, иначе откат
165. **MODE-MATCH3-0165** — [Механика режима] Своп (3,3)↔(2,3) [вверх]: разрешён только если даёт серию, иначе откат
166. **MODE-MATCH3-0166** — [Механика режима] Своп (3,4)↔(3,5) [вправо]: разрешён только если даёт серию, иначе откат
167. **MODE-MATCH3-0167** — [Механика режима] Своп (3,4)↔(4,4) [вниз]: разрешён только если даёт серию, иначе откат
168. **MODE-MATCH3-0168** — [Механика режима] Своп (3,4)↔(3,3) [влево]: разрешён только если даёт серию, иначе откат
169. **MODE-MATCH3-0169** — [C] [Механика режима] Своп (3,4)↔(2,4) [вверх]: разрешён только если даёт серию, иначе откат
170. **MODE-MATCH3-0170** — [Механика режима] Своп (3,5)↔(3,6) [вправо]: разрешён только если даёт серию, иначе откат
171. **MODE-MATCH3-0171** — [Механика режима] Своп (3,5)↔(4,5) [вниз]: разрешён только если даёт серию, иначе откат
172. **MODE-MATCH3-0172** — [Механика режима] Своп (3,5)↔(3,4) [влево]: разрешён только если даёт серию, иначе откат
173. **MODE-MATCH3-0173** — [Механика режима] Своп (3,5)↔(2,5) [вверх]: разрешён только если даёт серию, иначе откат
174. **MODE-MATCH3-0174** — [Механика режима] Своп (3,6)↔(3,7) [вправо]: разрешён только если даёт серию, иначе откат
175. **MODE-MATCH3-0175** — [Механика режима] Своп (3,6)↔(4,6) [вниз]: разрешён только если даёт серию, иначе откат
176. **MODE-MATCH3-0176** — [C] [Механика режима] Своп (3,6)↔(3,5) [влево]: разрешён только если даёт серию, иначе откат
177. **MODE-MATCH3-0177** — [Механика режима] Своп (3,6)↔(2,6) [вверх]: разрешён только если даёт серию, иначе откат
178. **MODE-MATCH3-0178** — [Механика режима] Своп (3,7)↔(4,7) [вниз]: разрешён только если даёт серию, иначе откат
179. **MODE-MATCH3-0179** — [Механика режима] Своп (3,7)↔(3,6) [влево]: разрешён только если даёт серию, иначе откат
180. **MODE-MATCH3-0180** — [Механика режима] Своп (3,7)↔(2,7) [вверх]: разрешён только если даёт серию, иначе откат
181. **MODE-MATCH3-0181** — [Механика режима] Своп (4,0)↔(4,1) [вправо]: разрешён только если даёт серию, иначе откат
182. **MODE-MATCH3-0182** — [Механика режима] Своп (4,0)↔(5,0) [вниз]: разрешён только если даёт серию, иначе откат
183. **MODE-MATCH3-0183** — [C] [Механика режима] Своп (4,0)↔(3,0) [вверх]: разрешён только если даёт серию, иначе откат
184. **MODE-MATCH3-0184** — [Механика режима] Своп (4,1)↔(4,2) [вправо]: разрешён только если даёт серию, иначе откат
185. **MODE-MATCH3-0185** — [Механика режима] Своп (4,1)↔(5,1) [вниз]: разрешён только если даёт серию, иначе откат
186. **MODE-MATCH3-0186** — [Механика режима] Своп (4,1)↔(4,0) [влево]: разрешён только если даёт серию, иначе откат
187. **MODE-MATCH3-0187** — [Механика режима] Своп (4,1)↔(3,1) [вверх]: разрешён только если даёт серию, иначе откат
188. **MODE-MATCH3-0188** — [Механика режима] Своп (4,2)↔(4,3) [вправо]: разрешён только если даёт серию, иначе откат
189. **MODE-MATCH3-0189** — [Механика режима] Своп (4,2)↔(5,2) [вниз]: разрешён только если даёт серию, иначе откат
190. **MODE-MATCH3-0190** — [C] [Механика режима] Своп (4,2)↔(4,1) [влево]: разрешён только если даёт серию, иначе откат
191. **MODE-MATCH3-0191** — [Механика режима] Своп (4,2)↔(3,2) [вверх]: разрешён только если даёт серию, иначе откат
192. **MODE-MATCH3-0192** — [Механика режима] Своп (4,3)↔(4,4) [вправо]: разрешён только если даёт серию, иначе откат
193. **MODE-MATCH3-0193** — [Механика режима] Своп (4,3)↔(5,3) [вниз]: разрешён только если даёт серию, иначе откат
194. **MODE-MATCH3-0194** — [Механика режима] Своп (4,3)↔(4,2) [влево]: разрешён только если даёт серию, иначе откат
195. **MODE-MATCH3-0195** — [Механика режима] Своп (4,3)↔(3,3) [вверх]: разрешён только если даёт серию, иначе откат
196. **MODE-MATCH3-0196** — [Механика режима] Своп (4,4)↔(4,5) [вправо]: разрешён только если даёт серию, иначе откат
197. **MODE-MATCH3-0197** — [C] [Механика режима] Своп (4,4)↔(5,4) [вниз]: разрешён только если даёт серию, иначе откат
198. **MODE-MATCH3-0198** — [Механика режима] Своп (4,4)↔(4,3) [влево]: разрешён только если даёт серию, иначе откат
199. **MODE-MATCH3-0199** — [Механика режима] Своп (4,4)↔(3,4) [вверх]: разрешён только если даёт серию, иначе откат
200. **MODE-MATCH3-0200** — [Механика режима] Своп (4,5)↔(4,6) [вправо]: разрешён только если даёт серию, иначе откат
201. **MODE-MATCH3-0201** — [Механика режима] Своп (4,5)↔(5,5) [вниз]: разрешён только если даёт серию, иначе откат
202. **MODE-MATCH3-0202** — [Механика режима] Своп (4,5)↔(4,4) [влево]: разрешён только если даёт серию, иначе откат
203. **MODE-MATCH3-0203** — [Механика режима] Своп (4,5)↔(3,5) [вверх]: разрешён только если даёт серию, иначе откат
204. **MODE-MATCH3-0204** — [C] [Механика режима] Своп (4,6)↔(4,7) [вправо]: разрешён только если даёт серию, иначе откат
205. **MODE-MATCH3-0205** — [Механика режима] Своп (4,6)↔(5,6) [вниз]: разрешён только если даёт серию, иначе откат
206. **MODE-MATCH3-0206** — [Механика режима] Своп (4,6)↔(4,5) [влево]: разрешён только если даёт серию, иначе откат
207. **MODE-MATCH3-0207** — [Механика режима] Своп (4,6)↔(3,6) [вверх]: разрешён только если даёт серию, иначе откат
208. **MODE-MATCH3-0208** — [Механика режима] Своп (4,7)↔(5,7) [вниз]: разрешён только если даёт серию, иначе откат
209. **MODE-MATCH3-0209** — [Механика режима] Своп (4,7)↔(4,6) [влево]: разрешён только если даёт серию, иначе откат
210. **MODE-MATCH3-0210** — [Механика режима] Своп (4,7)↔(3,7) [вверх]: разрешён только если даёт серию, иначе откат
211. **MODE-MATCH3-0211** — [C] [Механика режима] Своп (5,0)↔(5,1) [вправо]: разрешён только если даёт серию, иначе откат
212. **MODE-MATCH3-0212** — [Механика режима] Своп (5,0)↔(6,0) [вниз]: разрешён только если даёт серию, иначе откат
213. **MODE-MATCH3-0213** — [Механика режима] Своп (5,0)↔(4,0) [вверх]: разрешён только если даёт серию, иначе откат
214. **MODE-MATCH3-0214** — [Механика режима] Своп (5,1)↔(5,2) [вправо]: разрешён только если даёт серию, иначе откат
215. **MODE-MATCH3-0215** — [Механика режима] Своп (5,1)↔(6,1) [вниз]: разрешён только если даёт серию, иначе откат
216. **MODE-MATCH3-0216** — [Механика режима] Своп (5,1)↔(5,0) [влево]: разрешён только если даёт серию, иначе откат
217. **MODE-MATCH3-0217** — [Механика режима] Своп (5,1)↔(4,1) [вверх]: разрешён только если даёт серию, иначе откат
218. **MODE-MATCH3-0218** — [C] [Механика режима] Своп (5,2)↔(5,3) [вправо]: разрешён только если даёт серию, иначе откат
219. **MODE-MATCH3-0219** — [Механика режима] Своп (5,2)↔(6,2) [вниз]: разрешён только если даёт серию, иначе откат
220. **MODE-MATCH3-0220** — [Механика режима] Своп (5,2)↔(5,1) [влево]: разрешён только если даёт серию, иначе откат
221. **MODE-MATCH3-0221** — [Механика режима] Своп (5,2)↔(4,2) [вверх]: разрешён только если даёт серию, иначе откат
222. **MODE-MATCH3-0222** — [Механика режима] Своп (5,3)↔(5,4) [вправо]: разрешён только если даёт серию, иначе откат
223. **MODE-MATCH3-0223** — [Механика режима] Своп (5,3)↔(6,3) [вниз]: разрешён только если даёт серию, иначе откат
224. **MODE-MATCH3-0224** — [Механика режима] Своп (5,3)↔(5,2) [влево]: разрешён только если даёт серию, иначе откат
225. **MODE-MATCH3-0225** — [C] [Механика режима] Своп (5,3)↔(4,3) [вверх]: разрешён только если даёт серию, иначе откат
226. **MODE-MATCH3-0226** — [Механика режима] Своп (5,4)↔(5,5) [вправо]: разрешён только если даёт серию, иначе откат
227. **MODE-MATCH3-0227** — [Механика режима] Своп (5,4)↔(6,4) [вниз]: разрешён только если даёт серию, иначе откат
228. **MODE-MATCH3-0228** — [Механика режима] Своп (5,4)↔(5,3) [влево]: разрешён только если даёт серию, иначе откат
229. **MODE-MATCH3-0229** — [Механика режима] Своп (5,4)↔(4,4) [вверх]: разрешён только если даёт серию, иначе откат
230. **MODE-MATCH3-0230** — [Механика режима] Своп (5,5)↔(5,6) [вправо]: разрешён только если даёт серию, иначе откат
231. **MODE-MATCH3-0231** — [Механика режима] Своп (5,5)↔(6,5) [вниз]: разрешён только если даёт серию, иначе откат
232. **MODE-MATCH3-0232** — [C] [Механика режима] Своп (5,5)↔(5,4) [влево]: разрешён только если даёт серию, иначе откат
233. **MODE-MATCH3-0233** — [Механика режима] Своп (5,5)↔(4,5) [вверх]: разрешён только если даёт серию, иначе откат
234. **MODE-MATCH3-0234** — [Механика режима] Своп (5,6)↔(5,7) [вправо]: разрешён только если даёт серию, иначе откат
235. **MODE-MATCH3-0235** — [Механика режима] Своп (5,6)↔(6,6) [вниз]: разрешён только если даёт серию, иначе откат
236. **MODE-MATCH3-0236** — [Механика режима] Своп (5,6)↔(5,5) [влево]: разрешён только если даёт серию, иначе откат
237. **MODE-MATCH3-0237** — [Механика режима] Своп (5,6)↔(4,6) [вверх]: разрешён только если даёт серию, иначе откат
238. **MODE-MATCH3-0238** — [Механика режима] Своп (5,7)↔(6,7) [вниз]: разрешён только если даёт серию, иначе откат
239. **MODE-MATCH3-0239** — [C] [Механика режима] Своп (5,7)↔(5,6) [влево]: разрешён только если даёт серию, иначе откат
240. **MODE-MATCH3-0240** — [Механика режима] Своп (5,7)↔(4,7) [вверх]: разрешён только если даёт серию, иначе откат
241. **MODE-MATCH3-0241** — [Механика режима] Своп (6,0)↔(6,1) [вправо]: разрешён только если даёт серию, иначе откат
242. **MODE-MATCH3-0242** — [Механика режима] Своп (6,0)↔(7,0) [вниз]: разрешён только если даёт серию, иначе откат
243. **MODE-MATCH3-0243** — [Механика режима] Своп (6,0)↔(5,0) [вверх]: разрешён только если даёт серию, иначе откат
244. **MODE-MATCH3-0244** — [Механика режима] Своп (6,1)↔(6,2) [вправо]: разрешён только если даёт серию, иначе откат
245. **MODE-MATCH3-0245** — [Механика режима] Своп (6,1)↔(7,1) [вниз]: разрешён только если даёт серию, иначе откат
246. **MODE-MATCH3-0246** — [C] [Механика режима] Своп (6,1)↔(6,0) [влево]: разрешён только если даёт серию, иначе откат
247. **MODE-MATCH3-0247** — [Механика режима] Своп (6,1)↔(5,1) [вверх]: разрешён только если даёт серию, иначе откат
248. **MODE-MATCH3-0248** — [Механика режима] Своп (6,2)↔(6,3) [вправо]: разрешён только если даёт серию, иначе откат
249. **MODE-MATCH3-0249** — [Механика режима] Своп (6,2)↔(7,2) [вниз]: разрешён только если даёт серию, иначе откат
250. **MODE-MATCH3-0250** — [Механика режима] Своп (6,2)↔(6,1) [влево]: разрешён только если даёт серию, иначе откат
251. **MODE-MATCH3-0251** — [Механика режима] Своп (6,2)↔(5,2) [вверх]: разрешён только если даёт серию, иначе откат
252. **MODE-MATCH3-0252** — [Механика режима] Своп (6,3)↔(6,4) [вправо]: разрешён только если даёт серию, иначе откат
253. **MODE-MATCH3-0253** — [C] [Механика режима] Своп (6,3)↔(7,3) [вниз]: разрешён только если даёт серию, иначе откат
254. **MODE-MATCH3-0254** — [Механика режима] Своп (6,3)↔(6,2) [влево]: разрешён только если даёт серию, иначе откат
255. **MODE-MATCH3-0255** — [Механика режима] Своп (6,3)↔(5,3) [вверх]: разрешён только если даёт серию, иначе откат
256. **MODE-MATCH3-0256** — [Механика режима] Своп (6,4)↔(6,5) [вправо]: разрешён только если даёт серию, иначе откат
257. **MODE-MATCH3-0257** — [Механика режима] Своп (6,4)↔(7,4) [вниз]: разрешён только если даёт серию, иначе откат
258. **MODE-MATCH3-0258** — [Механика режима] Своп (6,4)↔(6,3) [влево]: разрешён только если даёт серию, иначе откат
259. **MODE-MATCH3-0259** — [Механика режима] Своп (6,4)↔(5,4) [вверх]: разрешён только если даёт серию, иначе откат
260. **MODE-MATCH3-0260** — [C] [Механика режима] Своп (6,5)↔(6,6) [вправо]: разрешён только если даёт серию, иначе откат
261. **MODE-MATCH3-0261** — [Механика режима] Своп (6,5)↔(7,5) [вниз]: разрешён только если даёт серию, иначе откат
262. **MODE-MATCH3-0262** — [Механика режима] Своп (6,5)↔(6,4) [влево]: разрешён только если даёт серию, иначе откат
263. **MODE-MATCH3-0263** — [Механика режима] Своп (6,5)↔(5,5) [вверх]: разрешён только если даёт серию, иначе откат
264. **MODE-MATCH3-0264** — [Механика режима] Своп (6,6)↔(6,7) [вправо]: разрешён только если даёт серию, иначе откат
265. **MODE-MATCH3-0265** — [Механика режима] Своп (6,6)↔(7,6) [вниз]: разрешён только если даёт серию, иначе откат
266. **MODE-MATCH3-0266** — [Механика режима] Своп (6,6)↔(6,5) [влево]: разрешён только если даёт серию, иначе откат
267. **MODE-MATCH3-0267** — [C] [Механика режима] Своп (6,6)↔(5,6) [вверх]: разрешён только если даёт серию, иначе откат
268. **MODE-MATCH3-0268** — [Механика режима] Своп (6,7)↔(7,7) [вниз]: разрешён только если даёт серию, иначе откат
269. **MODE-MATCH3-0269** — [Механика режима] Своп (6,7)↔(6,6) [влево]: разрешён только если даёт серию, иначе откат
270. **MODE-MATCH3-0270** — [Механика режима] Своп (6,7)↔(5,7) [вверх]: разрешён только если даёт серию, иначе откат
271. **MODE-MATCH3-0271** — [Механика режима] Своп (7,0)↔(7,1) [вправо]: разрешён только если даёт серию, иначе откат
272. **MODE-MATCH3-0272** — [Механика режима] Своп (7,0)↔(6,0) [вверх]: разрешён только если даёт серию, иначе откат
273. **MODE-MATCH3-0273** — [Механика режима] Своп (7,1)↔(7,2) [вправо]: разрешён только если даёт серию, иначе откат
274. **MODE-MATCH3-0274** — [C] [Механика режима] Своп (7,1)↔(7,0) [влево]: разрешён только если даёт серию, иначе откат
275. **MODE-MATCH3-0275** — [Механика режима] Своп (7,1)↔(6,1) [вверх]: разрешён только если даёт серию, иначе откат
276. **MODE-MATCH3-0276** — [Механика режима] Своп (7,2)↔(7,3) [вправо]: разрешён только если даёт серию, иначе откат
277. **MODE-MATCH3-0277** — [Механика режима] Своп (7,2)↔(7,1) [влево]: разрешён только если даёт серию, иначе откат
278. **MODE-MATCH3-0278** — [Механика режима] Своп (7,2)↔(6,2) [вверх]: разрешён только если даёт серию, иначе откат
279. **MODE-MATCH3-0279** — [Механика режима] Своп (7,3)↔(7,4) [вправо]: разрешён только если даёт серию, иначе откат
280. **MODE-MATCH3-0280** — [Механика режима] Своп (7,3)↔(7,2) [влево]: разрешён только если даёт серию, иначе откат
281. **MODE-MATCH3-0281** — [C] [Механика режима] Своп (7,3)↔(6,3) [вверх]: разрешён только если даёт серию, иначе откат
282. **MODE-MATCH3-0282** — [Механика режима] Своп (7,4)↔(7,5) [вправо]: разрешён только если даёт серию, иначе откат
283. **MODE-MATCH3-0283** — [Механика режима] Своп (7,4)↔(7,3) [влево]: разрешён только если даёт серию, иначе откат
284. **MODE-MATCH3-0284** — [Механика режима] Своп (7,4)↔(6,4) [вверх]: разрешён только если даёт серию, иначе откат
285. **MODE-MATCH3-0285** — [Механика режима] Своп (7,5)↔(7,6) [вправо]: разрешён только если даёт серию, иначе откат
286. **MODE-MATCH3-0286** — [Механика режима] Своп (7,5)↔(7,4) [влево]: разрешён только если даёт серию, иначе откат
287. **MODE-MATCH3-0287** — [Механика режима] Своп (7,5)↔(6,5) [вверх]: разрешён только если даёт серию, иначе откат
288. **MODE-MATCH3-0288** — [C] [Механика режима] Своп (7,6)↔(7,7) [вправо]: разрешён только если даёт серию, иначе откат
289. **MODE-MATCH3-0289** — [Механика режима] Своп (7,6)↔(7,5) [влево]: разрешён только если даёт серию, иначе откат
290. **MODE-MATCH3-0290** — [Механика режима] Своп (7,6)↔(6,6) [вверх]: разрешён только если даёт серию, иначе откат
291. **MODE-MATCH3-0291** — [Механика режима] Своп (7,7)↔(7,6) [влево]: разрешён только если даёт серию, иначе откат
292. **MODE-MATCH3-0292** — [Механика режима] Своп (7,7)↔(6,7) [вверх]: разрешён только если даёт серию, иначе откат
293. **MODE-MATCH3-0293** — [Механика режима] Клетка (0,0): если входит в серию — очищается и даёт очки ходившему
294. **MODE-MATCH3-0294** — [Механика режима] Клетка (0,1): если входит в серию — очищается и даёт очки ходившему
295. **MODE-MATCH3-0295** — [C] [Механика режима] Клетка (0,2): если входит в серию — очищается и даёт очки ходившему
296. **MODE-MATCH3-0296** — [Механика режима] Клетка (0,3): если входит в серию — очищается и даёт очки ходившему
297. **MODE-MATCH3-0297** — [Механика режима] Клетка (0,4): если входит в серию — очищается и даёт очки ходившему
298. **MODE-MATCH3-0298** — [Механика режима] Клетка (0,5): если входит в серию — очищается и даёт очки ходившему
299. **MODE-MATCH3-0299** — [Механика режима] Клетка (0,6): если входит в серию — очищается и даёт очки ходившему
300. **MODE-MATCH3-0300** — [Механика режима] Клетка (0,7): если входит в серию — очищается и даёт очки ходившему
301. **MODE-MATCH3-0301** — [Механика режима] Клетка (1,0): если входит в серию — очищается и даёт очки ходившему
302. **MODE-MATCH3-0302** — [C] [Механика режима] Клетка (1,1): если входит в серию — очищается и даёт очки ходившему
303. **MODE-MATCH3-0303** — [Механика режима] Клетка (1,2): если входит в серию — очищается и даёт очки ходившему
304. **MODE-MATCH3-0304** — [Механика режима] Клетка (1,3): если входит в серию — очищается и даёт очки ходившему
305. **MODE-MATCH3-0305** — [Механика режима] Клетка (1,4): если входит в серию — очищается и даёт очки ходившему
306. **MODE-MATCH3-0306** — [Механика режима] Клетка (1,5): если входит в серию — очищается и даёт очки ходившему
307. **MODE-MATCH3-0307** — [Механика режима] Клетка (1,6): если входит в серию — очищается и даёт очки ходившему
308. **MODE-MATCH3-0308** — [Механика режима] Клетка (1,7): если входит в серию — очищается и даёт очки ходившему
309. **MODE-MATCH3-0309** — [C] [Механика режима] Клетка (2,0): если входит в серию — очищается и даёт очки ходившему
310. **MODE-MATCH3-0310** — [Механика режима] Клетка (2,1): если входит в серию — очищается и даёт очки ходившему
311. **MODE-MATCH3-0311** — [Механика режима] Клетка (2,2): если входит в серию — очищается и даёт очки ходившему
312. **MODE-MATCH3-0312** — [Механика режима] Клетка (2,3): если входит в серию — очищается и даёт очки ходившему
313. **MODE-MATCH3-0313** — [Механика режима] Клетка (2,4): если входит в серию — очищается и даёт очки ходившему
314. **MODE-MATCH3-0314** — [Механика режима] Клетка (2,5): если входит в серию — очищается и даёт очки ходившему
315. **MODE-MATCH3-0315** — [Механика режима] Клетка (2,6): если входит в серию — очищается и даёт очки ходившему
316. **MODE-MATCH3-0316** — [C] [Механика режима] Клетка (2,7): если входит в серию — очищается и даёт очки ходившему
317. **MODE-MATCH3-0317** — [Механика режима] Клетка (3,0): если входит в серию — очищается и даёт очки ходившему
318. **MODE-MATCH3-0318** — [Механика режима] Клетка (3,1): если входит в серию — очищается и даёт очки ходившему
319. **MODE-MATCH3-0319** — [Механика режима] Клетка (3,2): если входит в серию — очищается и даёт очки ходившему
320. **MODE-MATCH3-0320** — [Механика режима] Клетка (3,3): если входит в серию — очищается и даёт очки ходившему
321. **MODE-MATCH3-0321** — [Механика режима] Клетка (3,4): если входит в серию — очищается и даёт очки ходившему
322. **MODE-MATCH3-0322** — [Механика режима] Клетка (3,5): если входит в серию — очищается и даёт очки ходившему
323. **MODE-MATCH3-0323** — [C] [Механика режима] Клетка (3,6): если входит в серию — очищается и даёт очки ходившему
324. **MODE-MATCH3-0324** — [Механика режима] Клетка (3,7): если входит в серию — очищается и даёт очки ходившему
325. **MODE-MATCH3-0325** — [Механика режима] Клетка (4,0): если входит в серию — очищается и даёт очки ходившему
326. **MODE-MATCH3-0326** — [Механика режима] Клетка (4,1): если входит в серию — очищается и даёт очки ходившему
327. **MODE-MATCH3-0327** — [Механика режима] Клетка (4,2): если входит в серию — очищается и даёт очки ходившему
328. **MODE-MATCH3-0328** — [Механика режима] Клетка (4,3): если входит в серию — очищается и даёт очки ходившему
329. **MODE-MATCH3-0329** — [Механика режима] Клетка (4,4): если входит в серию — очищается и даёт очки ходившему
330. **MODE-MATCH3-0330** — [C] [Механика режима] Клетка (4,5): если входит в серию — очищается и даёт очки ходившему
331. **MODE-MATCH3-0331** — [Механика режима] Клетка (4,6): если входит в серию — очищается и даёт очки ходившему
332. **MODE-MATCH3-0332** — [Механика режима] Клетка (4,7): если входит в серию — очищается и даёт очки ходившему
333. **MODE-MATCH3-0333** — [Механика режима] Клетка (5,0): если входит в серию — очищается и даёт очки ходившему
334. **MODE-MATCH3-0334** — [Механика режима] Клетка (5,1): если входит в серию — очищается и даёт очки ходившему
335. **MODE-MATCH3-0335** — [Механика режима] Клетка (5,2): если входит в серию — очищается и даёт очки ходившему
336. **MODE-MATCH3-0336** — [Механика режима] Клетка (5,3): если входит в серию — очищается и даёт очки ходившему
337. **MODE-MATCH3-0337** — [C] [Механика режима] Клетка (5,4): если входит в серию — очищается и даёт очки ходившему
338. **MODE-MATCH3-0338** — [Механика режима] Клетка (5,5): если входит в серию — очищается и даёт очки ходившему
339. **MODE-MATCH3-0339** — [Механика режима] Клетка (5,6): если входит в серию — очищается и даёт очки ходившему
340. **MODE-MATCH3-0340** — [Механика режима] Клетка (5,7): если входит в серию — очищается и даёт очки ходившему
341. **MODE-MATCH3-0341** — [Механика режима] Клетка (6,0): если входит в серию — очищается и даёт очки ходившему
342. **MODE-MATCH3-0342** — [Механика режима] Клетка (6,1): если входит в серию — очищается и даёт очки ходившему
343. **MODE-MATCH3-0343** — [Механика режима] Клетка (6,2): если входит в серию — очищается и даёт очки ходившему
344. **MODE-MATCH3-0344** — [C] [Механика режима] Клетка (6,3): если входит в серию — очищается и даёт очки ходившему
345. **MODE-MATCH3-0345** — [Механика режима] Клетка (6,4): если входит в серию — очищается и даёт очки ходившему
346. **MODE-MATCH3-0346** — [Механика режима] Клетка (6,5): если входит в серию — очищается и даёт очки ходившему
347. **MODE-MATCH3-0347** — [Механика режима] Клетка (6,6): если входит в серию — очищается и даёт очки ходившему
348. **MODE-MATCH3-0348** — [Механика режима] Клетка (6,7): если входит в серию — очищается и даёт очки ходившему
349. **MODE-MATCH3-0349** — [Механика режима] Клетка (7,0): если входит в серию — очищается и даёт очки ходившему
350. **MODE-MATCH3-0350** — [Механика режима] Клетка (7,1): если входит в серию — очищается и даёт очки ходившему
351. **MODE-MATCH3-0351** — [C] [Механика режима] Клетка (7,2): если входит в серию — очищается и даёт очки ходившему
352. **MODE-MATCH3-0352** — [Механика режима] Клетка (7,3): если входит в серию — очищается и даёт очки ходившему
353. **MODE-MATCH3-0353** — [Механика режима] Клетка (7,4): если входит в серию — очищается и даёт очки ходившему
354. **MODE-MATCH3-0354** — [Механика режима] Клетка (7,5): если входит в серию — очищается и даёт очки ходившему
355. **MODE-MATCH3-0355** — [Механика режима] Клетка (7,6): если входит в серию — очищается и даёт очки ходившему
356. **MODE-MATCH3-0356** — [Механика режима] Клетка (7,7): если входит в серию — очищается и даёт очки ходившему
357. **MODE-MATCH3-0357** — [Механика режима] Строка 0, цвет 0: горизонтальная серия обнаруживается и очищается
358. **MODE-MATCH3-0358** — [C] [Механика режима] Строка 0, цвет 1: горизонтальная серия обнаруживается и очищается
359. **MODE-MATCH3-0359** — [Механика режима] Строка 0, цвет 2: горизонтальная серия обнаруживается и очищается
360. **MODE-MATCH3-0360** — [Механика режима] Строка 0, цвет 3: горизонтальная серия обнаруживается и очищается
361. **MODE-MATCH3-0361** — [Механика режима] Строка 0, цвет 4: горизонтальная серия обнаруживается и очищается
362. **MODE-MATCH3-0362** — [Механика режима] Строка 0, цвет 5: горизонтальная серия обнаруживается и очищается
363. **MODE-MATCH3-0363** — [Механика режима] Строка 1, цвет 0: горизонтальная серия обнаруживается и очищается
364. **MODE-MATCH3-0364** — [Механика режима] Строка 1, цвет 1: горизонтальная серия обнаруживается и очищается
365. **MODE-MATCH3-0365** — [C] [Механика режима] Строка 1, цвет 2: горизонтальная серия обнаруживается и очищается
366. **MODE-MATCH3-0366** — [Механика режима] Строка 1, цвет 3: горизонтальная серия обнаруживается и очищается
367. **MODE-MATCH3-0367** — [Механика режима] Строка 1, цвет 4: горизонтальная серия обнаруживается и очищается
368. **MODE-MATCH3-0368** — [Механика режима] Строка 1, цвет 5: горизонтальная серия обнаруживается и очищается
369. **MODE-MATCH3-0369** — [Механика режима] Строка 2, цвет 0: горизонтальная серия обнаруживается и очищается
370. **MODE-MATCH3-0370** — [Механика режима] Строка 2, цвет 1: горизонтальная серия обнаруживается и очищается
371. **MODE-MATCH3-0371** — [Механика режима] Строка 2, цвет 2: горизонтальная серия обнаруживается и очищается
372. **MODE-MATCH3-0372** — [C] [Механика режима] Строка 2, цвет 3: горизонтальная серия обнаруживается и очищается
373. **MODE-MATCH3-0373** — [Механика режима] Строка 2, цвет 4: горизонтальная серия обнаруживается и очищается
374. **MODE-MATCH3-0374** — [Механика режима] Строка 2, цвет 5: горизонтальная серия обнаруживается и очищается
375. **MODE-MATCH3-0375** — [Механика режима] Строка 3, цвет 0: горизонтальная серия обнаруживается и очищается
376. **MODE-MATCH3-0376** — [Механика режима] Строка 3, цвет 1: горизонтальная серия обнаруживается и очищается
377. **MODE-MATCH3-0377** — [Механика режима] Строка 3, цвет 2: горизонтальная серия обнаруживается и очищается
378. **MODE-MATCH3-0378** — [Механика режима] Строка 3, цвет 3: горизонтальная серия обнаруживается и очищается
379. **MODE-MATCH3-0379** — [C] [Механика режима] Строка 3, цвет 4: горизонтальная серия обнаруживается и очищается
380. **MODE-MATCH3-0380** — [Механика режима] Строка 3, цвет 5: горизонтальная серия обнаруживается и очищается
381. **MODE-MATCH3-0381** — [Механика режима] Строка 4, цвет 0: горизонтальная серия обнаруживается и очищается
382. **MODE-MATCH3-0382** — [Механика режима] Строка 4, цвет 1: горизонтальная серия обнаруживается и очищается
383. **MODE-MATCH3-0383** — [Механика режима] Строка 4, цвет 2: горизонтальная серия обнаруживается и очищается
384. **MODE-MATCH3-0384** — [Механика режима] Строка 4, цвет 3: горизонтальная серия обнаруживается и очищается
385. **MODE-MATCH3-0385** — [Механика режима] Строка 4, цвет 4: горизонтальная серия обнаруживается и очищается
386. **MODE-MATCH3-0386** — [C] [Механика режима] Строка 4, цвет 5: горизонтальная серия обнаруживается и очищается
387. **MODE-MATCH3-0387** — [Механика режима] Строка 5, цвет 0: горизонтальная серия обнаруживается и очищается
388. **MODE-MATCH3-0388** — [Механика режима] Строка 5, цвет 1: горизонтальная серия обнаруживается и очищается
389. **MODE-MATCH3-0389** — [Механика режима] Строка 5, цвет 2: горизонтальная серия обнаруживается и очищается
390. **MODE-MATCH3-0390** — [Механика режима] Строка 5, цвет 3: горизонтальная серия обнаруживается и очищается
391. **MODE-MATCH3-0391** — [Механика режима] Строка 5, цвет 4: горизонтальная серия обнаруживается и очищается
392. **MODE-MATCH3-0392** — [Механика режима] Строка 5, цвет 5: горизонтальная серия обнаруживается и очищается
393. **MODE-MATCH3-0393** — [C] [Механика режима] Строка 6, цвет 0: горизонтальная серия обнаруживается и очищается
394. **MODE-MATCH3-0394** — [Механика режима] Строка 6, цвет 1: горизонтальная серия обнаруживается и очищается
395. **MODE-MATCH3-0395** — [Механика режима] Строка 6, цвет 2: горизонтальная серия обнаруживается и очищается
396. **MODE-MATCH3-0396** — [Механика режима] Строка 6, цвет 3: горизонтальная серия обнаруживается и очищается
397. **MODE-MATCH3-0397** — [Механика режима] Строка 6, цвет 4: горизонтальная серия обнаруживается и очищается
398. **MODE-MATCH3-0398** — [Механика режима] Строка 6, цвет 5: горизонтальная серия обнаруживается и очищается
399. **MODE-MATCH3-0399** — [Механика режима] Строка 7, цвет 0: горизонтальная серия обнаруживается и очищается
400. **MODE-MATCH3-0400** — [C] [Механика режима] Строка 7, цвет 1: горизонтальная серия обнаруживается и очищается
401. **MODE-MATCH3-0401** — [Механика режима] Строка 7, цвет 2: горизонтальная серия обнаруживается и очищается
402. **MODE-MATCH3-0402** — [Механика режима] Строка 7, цвет 3: горизонтальная серия обнаруживается и очищается
403. **MODE-MATCH3-0403** — [Механика режима] Строка 7, цвет 4: горизонтальная серия обнаруживается и очищается
404. **MODE-MATCH3-0404** — [Механика режима] Строка 7, цвет 5: горизонтальная серия обнаруживается и очищается
405. **MODE-MATCH3-0405** — [Механика режима] Серия длины 3 цвета 0: обнаружение и очистка
406. **MODE-MATCH3-0406** — [Механика режима] Серия длины 4 цвета 0: обнаружение и очистка
407. **MODE-MATCH3-0407** — [C] [Механика режима] Серия длины 5 цвета 0: обнаружение и очистка
408. **MODE-MATCH3-0408** — [Механика режима] Серия длины 3 цвета 1: обнаружение и очистка
409. **MODE-MATCH3-0409** — [Механика режима] Серия длины 4 цвета 1: обнаружение и очистка
410. **MODE-MATCH3-0410** — [Механика режима] Серия длины 5 цвета 1: обнаружение и очистка
411. **MODE-MATCH3-0411** — [Механика режима] Серия длины 3 цвета 2: обнаружение и очистка
412. **MODE-MATCH3-0412** — [Механика режима] Серия длины 4 цвета 2: обнаружение и очистка
413. **MODE-MATCH3-0413** — [Механика режима] Серия длины 5 цвета 2: обнаружение и очистка
414. **MODE-MATCH3-0414** — [C] [Механика режима] Серия длины 3 цвета 3: обнаружение и очистка
415. **MODE-MATCH3-0415** — [Механика режима] Серия длины 4 цвета 3: обнаружение и очистка
416. **MODE-MATCH3-0416** — [Механика режима] Серия длины 5 цвета 3: обнаружение и очистка
417. **MODE-MATCH3-0417** — [Механика режима] Серия длины 3 цвета 4: обнаружение и очистка
418. **MODE-MATCH3-0418** — [Механика режима] Серия длины 4 цвета 4: обнаружение и очистка
419. **MODE-MATCH3-0419** — [Механика режима] Серия длины 5 цвета 4: обнаружение и очистка
420. **MODE-MATCH3-0420** — [Механика режима] Серия длины 3 цвета 5: обнаружение и очистка
421. **MODE-MATCH3-0421** — [C] [Механика режима] Серия длины 4 цвета 5: обнаружение и очистка
422. **MODE-MATCH3-0422** — [Механика режима] Серия длины 5 цвета 5: обнаружение и очистка
423. **MODE-MATCH3-0423** — [Механика режима] Столбец 0: гравитация роняет клетки вниз, досыпка сверху детерминирована по seed
424. **MODE-MATCH3-0424** — [Механика режима] Столбец 1: гравитация роняет клетки вниз, досыпка сверху детерминирована по seed
425. **MODE-MATCH3-0425** — [Механика режима] Столбец 2: гравитация роняет клетки вниз, досыпка сверху детерминирована по seed
426. **MODE-MATCH3-0426** — [Механика режима] Столбец 3: гравитация роняет клетки вниз, досыпка сверху детерминирована по seed
427. **MODE-MATCH3-0427** — [Механика режима] Столбец 4: гравитация роняет клетки вниз, досыпка сверху детерминирована по seed
428. **MODE-MATCH3-0428** — [C] [Механика режима] Столбец 5: гравитация роняет клетки вниз, досыпка сверху детерминирована по seed
429. **MODE-MATCH3-0429** — [Механика режима] Столбец 6: гравитация роняет клетки вниз, досыпка сверху детерминирована по seed
430. **MODE-MATCH3-0430** — [Механика режима] Столбец 7: гравитация роняет клетки вниз, досыпка сверху детерминирована по seed
431. **MODE-MATCH3-0431** — [Механика режима] Каскад глубины 1: множитель 1+0.5·(k-1), очки растут
432. **MODE-MATCH3-0432** — [Механика режима] Каскад глубины 2: множитель 1+0.5·(k-1), очки растут
433. **MODE-MATCH3-0433** — [Механика режима] Каскад глубины 3: множитель 1+0.5·(k-1), очки растут
434. **MODE-MATCH3-0434** — [Механика режима] Каскад глубины 4: множитель 1+0.5·(k-1), очки растут
435. **MODE-MATCH3-0435** — [C] [Механика режима] Каскад глубины 5: множитель 1+0.5·(k-1), очки растут
436. **MODE-MATCH3-0436** — [Механика режима] Каскад глубины 6: множитель 1+0.5·(k-1), очки растут
437. **MODE-MATCH3-0437** — [Механика режима] Каскад глубины 7: множитель 1+0.5·(k-1), очки растут
438. **MODE-MATCH3-0438** — [Механика режима] Каскад глубины 8: множитель 1+0.5·(k-1), очки растут
439. **MODE-MATCH3-0439** — [Механика режима] resolveBoard завершается даже при длинной цепочке (потолок 128)
440. **MODE-MATCH3-0440** — [Механика режима] Своп без серии откатывается, ход не тратится
441. **MODE-MATCH3-0441** — [Механика режима] Достигнут лимит ходов → партия завершается, победитель по очкам
442. **MODE-MATCH3-0442** — [C] [Механика режима] Нет доступных ходов → пересборка поля (reshuffle)
443. **MODE-MATCH3-0443** — [Механика режима] Досыпка новых цветов сверху детерминирована по seed
444. **MODE-MATCH3-0444** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · 6 цветов [match3]: счёт неотрицателен на всём протяжении
445. **MODE-MATCH3-0445** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · 6 цветов [match3]: ни одного исключения/краша за партию
446. **MODE-MATCH3-0446** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · 6 цветов [match3]: resume в середине — бит-в-бит то же продолжение
447. **MODE-MATCH3-0447** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · 6 цветов [match3]: число ходов конечно (партия сходится)
448. **MODE-MATCH3-0448** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · 6 цветов [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
449. **MODE-MATCH3-0449** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · 6 цветов [match3]: ни одной невалидной постановки не принято
450. **MODE-MATCH3-0450** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · 6 цветов [match3]: инвариант доски: заполненные клетки не «протекают» за границы
451. **MODE-MATCH3-0451** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · 6 цветов [match3]: каскады затухают (resolveBoard завершается)
452. **MODE-MATCH3-0452** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · 6 цветов [match3]: своп без серии откатывается
453. **MODE-MATCH3-0453** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · лимит ходов низкий [match3]: счёт неотрицателен на всём протяжении
454. **MODE-MATCH3-0454** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · лимит ходов низкий [match3]: ни одного исключения/краша за партию
455. **MODE-MATCH3-0455** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · лимит ходов низкий [match3]: resume в середине — бит-в-бит то же продолжение
456. **MODE-MATCH3-0456** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · лимит ходов низкий [match3]: число ходов конечно (партия сходится)
457. **MODE-MATCH3-0457** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · лимит ходов низкий [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
458. **MODE-MATCH3-0458** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · лимит ходов низкий [match3]: ни одной невалидной постановки не принято
459. **MODE-MATCH3-0459** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · лимит ходов низкий [match3]: инвариант доски: заполненные клетки не «протекают» за границы
460. **MODE-MATCH3-0460** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · лимит ходов низкий [match3]: каскады затухают (resolveBoard завершается)
461. **MODE-MATCH3-0461** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · лимит ходов низкий [match3]: своп без серии откатывается
462. **MODE-MATCH3-0462** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · лимит ходов высокий [match3]: счёт неотрицателен на всём протяжении
463. **MODE-MATCH3-0463** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · лимит ходов высокий [match3]: ни одного исключения/краша за партию
464. **MODE-MATCH3-0464** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · лимит ходов высокий [match3]: resume в середине — бит-в-бит то же продолжение
465. **MODE-MATCH3-0465** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · лимит ходов высокий [match3]: число ходов конечно (партия сходится)
466. **MODE-MATCH3-0466** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · лимит ходов высокий [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
467. **MODE-MATCH3-0467** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · лимит ходов высокий [match3]: ни одной невалидной постановки не принято
468. **MODE-MATCH3-0468** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · лимит ходов высокий [match3]: инвариант доски: заполненные клетки не «протекают» за границы
469. **MODE-MATCH3-0469** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · лимит ходов высокий [match3]: каскады затухают (resolveBoard завершается)
470. **MODE-MATCH3-0470** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · лимит ходов высокий [match3]: своп без серии откатывается
471. **MODE-MATCH3-0471** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · плотное поле [match3]: счёт неотрицателен на всём протяжении
472. **MODE-MATCH3-0472** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · плотное поле [match3]: ни одного исключения/краша за партию
473. **MODE-MATCH3-0473** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · плотное поле [match3]: resume в середине — бит-в-бит то же продолжение
474. **MODE-MATCH3-0474** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · плотное поле [match3]: число ходов конечно (партия сходится)
475. **MODE-MATCH3-0475** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · плотное поле [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
476. **MODE-MATCH3-0476** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · плотное поле [match3]: ни одной невалидной постановки не принято
477. **MODE-MATCH3-0477** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · плотное поле [match3]: инвариант доски: заполненные клетки не «протекают» за границы
478. **MODE-MATCH3-0478** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · плотное поле [match3]: каскады затухают (resolveBoard завершается)
479. **MODE-MATCH3-0479** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · плотное поле [match3]: своп без серии откатывается
480. **MODE-MATCH3-0480** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · разреженное поле [match3]: счёт неотрицателен на всём протяжении
481. **MODE-MATCH3-0481** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · разреженное поле [match3]: ни одного исключения/краша за партию
482. **MODE-MATCH3-0482** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · разреженное поле [match3]: resume в середине — бит-в-бит то же продолжение
483. **MODE-MATCH3-0483** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · разреженное поле [match3]: число ходов конечно (партия сходится)
484. **MODE-MATCH3-0484** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · разреженное поле [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
485. **MODE-MATCH3-0485** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · разреженное поле [match3]: ни одной невалидной постановки не принято
486. **MODE-MATCH3-0486** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · разреженное поле [match3]: инвариант доски: заполненные клетки не «протекают» за границы
487. **MODE-MATCH3-0487** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · разреженное поле [match3]: каскады затухают (resolveBoard завершается)
488. **MODE-MATCH3-0488** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · разреженное поле [match3]: своп без серии откатывается
489. **MODE-MATCH3-0489** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · seed без стартовых серий [match3]: счёт неотрицателен на всём протяжении
490. **MODE-MATCH3-0490** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · seed без стартовых серий [match3]: ни одного исключения/краша за партию
491. **MODE-MATCH3-0491** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · seed без стартовых серий [match3]: resume в середине — бит-в-бит то же продолжение
492. **MODE-MATCH3-0492** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · seed без стартовых серий [match3]: число ходов конечно (партия сходится)
493. **MODE-MATCH3-0493** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · seed без стартовых серий [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
494. **MODE-MATCH3-0494** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · seed без стартовых серий [match3]: ни одной невалидной постановки не принято
495. **MODE-MATCH3-0495** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · seed без стартовых серий [match3]: инвариант доски: заполненные клетки не «протекают» за границы
496. **MODE-MATCH3-0496** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · seed без стартовых серий [match3]: каскады затухают (resolveBoard завершается)
497. **MODE-MATCH3-0497** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=1 · seed без стартовых серий [match3]: своп без серии откатывается
498. **MODE-MATCH3-0498** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · 6 цветов [match3]: счёт неотрицателен на всём протяжении
499. **MODE-MATCH3-0499** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · 6 цветов [match3]: ни одного исключения/краша за партию
500. **MODE-MATCH3-0500** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · 6 цветов [match3]: resume в середине — бит-в-бит то же продолжение
501. **MODE-MATCH3-0501** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · 6 цветов [match3]: число ходов конечно (партия сходится)
502. **MODE-MATCH3-0502** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · 6 цветов [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
503. **MODE-MATCH3-0503** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · 6 цветов [match3]: ни одной невалидной постановки не принято
504. **MODE-MATCH3-0504** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · 6 цветов [match3]: инвариант доски: заполненные клетки не «протекают» за границы
505. **MODE-MATCH3-0505** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · 6 цветов [match3]: каскады затухают (resolveBoard завершается)
506. **MODE-MATCH3-0506** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · 6 цветов [match3]: своп без серии откатывается
507. **MODE-MATCH3-0507** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · лимит ходов низкий [match3]: счёт неотрицателен на всём протяжении
508. **MODE-MATCH3-0508** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · лимит ходов низкий [match3]: ни одного исключения/краша за партию
509. **MODE-MATCH3-0509** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · лимит ходов низкий [match3]: resume в середине — бит-в-бит то же продолжение
510. **MODE-MATCH3-0510** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · лимит ходов низкий [match3]: число ходов конечно (партия сходится)
511. **MODE-MATCH3-0511** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · лимит ходов низкий [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
512. **MODE-MATCH3-0512** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · лимит ходов низкий [match3]: ни одной невалидной постановки не принято
513. **MODE-MATCH3-0513** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · лимит ходов низкий [match3]: инвариант доски: заполненные клетки не «протекают» за границы
514. **MODE-MATCH3-0514** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · лимит ходов низкий [match3]: каскады затухают (resolveBoard завершается)
515. **MODE-MATCH3-0515** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · лимит ходов низкий [match3]: своп без серии откатывается
516. **MODE-MATCH3-0516** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · лимит ходов высокий [match3]: счёт неотрицателен на всём протяжении
517. **MODE-MATCH3-0517** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · лимит ходов высокий [match3]: ни одного исключения/краша за партию
518. **MODE-MATCH3-0518** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · лимит ходов высокий [match3]: resume в середине — бит-в-бит то же продолжение
519. **MODE-MATCH3-0519** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · лимит ходов высокий [match3]: число ходов конечно (партия сходится)
520. **MODE-MATCH3-0520** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · лимит ходов высокий [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
521. **MODE-MATCH3-0521** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · лимит ходов высокий [match3]: ни одной невалидной постановки не принято
522. **MODE-MATCH3-0522** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · лимит ходов высокий [match3]: инвариант доски: заполненные клетки не «протекают» за границы
523. **MODE-MATCH3-0523** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · лимит ходов высокий [match3]: каскады затухают (resolveBoard завершается)
524. **MODE-MATCH3-0524** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · лимит ходов высокий [match3]: своп без серии откатывается
525. **MODE-MATCH3-0525** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · плотное поле [match3]: счёт неотрицателен на всём протяжении
526. **MODE-MATCH3-0526** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · плотное поле [match3]: ни одного исключения/краша за партию
527. **MODE-MATCH3-0527** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · плотное поле [match3]: resume в середине — бит-в-бит то же продолжение
528. **MODE-MATCH3-0528** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · плотное поле [match3]: число ходов конечно (партия сходится)
529. **MODE-MATCH3-0529** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · плотное поле [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
530. **MODE-MATCH3-0530** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · плотное поле [match3]: ни одной невалидной постановки не принято
531. **MODE-MATCH3-0531** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · плотное поле [match3]: инвариант доски: заполненные клетки не «протекают» за границы
532. **MODE-MATCH3-0532** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · плотное поле [match3]: каскады затухают (resolveBoard завершается)
533. **MODE-MATCH3-0533** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · плотное поле [match3]: своп без серии откатывается
534. **MODE-MATCH3-0534** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · разреженное поле [match3]: счёт неотрицателен на всём протяжении
535. **MODE-MATCH3-0535** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · разреженное поле [match3]: ни одного исключения/краша за партию
536. **MODE-MATCH3-0536** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · разреженное поле [match3]: resume в середине — бит-в-бит то же продолжение
537. **MODE-MATCH3-0537** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · разреженное поле [match3]: число ходов конечно (партия сходится)
538. **MODE-MATCH3-0538** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · разреженное поле [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
539. **MODE-MATCH3-0539** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · разреженное поле [match3]: ни одной невалидной постановки не принято
540. **MODE-MATCH3-0540** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · разреженное поле [match3]: инвариант доски: заполненные клетки не «протекают» за границы
541. **MODE-MATCH3-0541** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · разреженное поле [match3]: каскады затухают (resolveBoard завершается)
542. **MODE-MATCH3-0542** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · разреженное поле [match3]: своп без серии откатывается
543. **MODE-MATCH3-0543** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · seed без стартовых серий [match3]: счёт неотрицателен на всём протяжении
544. **MODE-MATCH3-0544** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · seed без стартовых серий [match3]: ни одного исключения/краша за партию
545. **MODE-MATCH3-0545** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · seed без стартовых серий [match3]: resume в середине — бит-в-бит то же продолжение
546. **MODE-MATCH3-0546** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · seed без стартовых серий [match3]: число ходов конечно (партия сходится)
547. **MODE-MATCH3-0547** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · seed без стартовых серий [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
548. **MODE-MATCH3-0548** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · seed без стартовых серий [match3]: ни одной невалидной постановки не принято
549. **MODE-MATCH3-0549** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · seed без стартовых серий [match3]: инвариант доски: заполненные клетки не «протекают» за границы
550. **MODE-MATCH3-0550** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · seed без стартовых серий [match3]: каскады затухают (resolveBoard завершается)
551. **MODE-MATCH3-0551** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=2 · seed без стартовых серий [match3]: своп без серии откатывается
552. **MODE-MATCH3-0552** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · 6 цветов [match3]: счёт неотрицателен на всём протяжении
553. **MODE-MATCH3-0553** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · 6 цветов [match3]: ни одного исключения/краша за партию
554. **MODE-MATCH3-0554** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · 6 цветов [match3]: resume в середине — бит-в-бит то же продолжение
555. **MODE-MATCH3-0555** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · 6 цветов [match3]: число ходов конечно (партия сходится)
556. **MODE-MATCH3-0556** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · 6 цветов [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
557. **MODE-MATCH3-0557** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · 6 цветов [match3]: ни одной невалидной постановки не принято
558. **MODE-MATCH3-0558** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · 6 цветов [match3]: инвариант доски: заполненные клетки не «протекают» за границы
559. **MODE-MATCH3-0559** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · 6 цветов [match3]: каскады затухают (resolveBoard завершается)
560. **MODE-MATCH3-0560** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · 6 цветов [match3]: своп без серии откатывается
561. **MODE-MATCH3-0561** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · лимит ходов низкий [match3]: счёт неотрицателен на всём протяжении
562. **MODE-MATCH3-0562** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · лимит ходов низкий [match3]: ни одного исключения/краша за партию
563. **MODE-MATCH3-0563** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · лимит ходов низкий [match3]: resume в середине — бит-в-бит то же продолжение
564. **MODE-MATCH3-0564** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · лимит ходов низкий [match3]: число ходов конечно (партия сходится)
565. **MODE-MATCH3-0565** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · лимит ходов низкий [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
566. **MODE-MATCH3-0566** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · лимит ходов низкий [match3]: ни одной невалидной постановки не принято
567. **MODE-MATCH3-0567** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · лимит ходов низкий [match3]: инвариант доски: заполненные клетки не «протекают» за границы
568. **MODE-MATCH3-0568** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · лимит ходов низкий [match3]: каскады затухают (resolveBoard завершается)
569. **MODE-MATCH3-0569** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · лимит ходов низкий [match3]: своп без серии откатывается
570. **MODE-MATCH3-0570** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · лимит ходов высокий [match3]: счёт неотрицателен на всём протяжении
571. **MODE-MATCH3-0571** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · лимит ходов высокий [match3]: ни одного исключения/краша за партию
572. **MODE-MATCH3-0572** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · лимит ходов высокий [match3]: resume в середине — бит-в-бит то же продолжение
573. **MODE-MATCH3-0573** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · лимит ходов высокий [match3]: число ходов конечно (партия сходится)
574. **MODE-MATCH3-0574** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · лимит ходов высокий [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
575. **MODE-MATCH3-0575** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · лимит ходов высокий [match3]: ни одной невалидной постановки не принято
576. **MODE-MATCH3-0576** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · лимит ходов высокий [match3]: инвариант доски: заполненные клетки не «протекают» за границы
577. **MODE-MATCH3-0577** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · лимит ходов высокий [match3]: каскады затухают (resolveBoard завершается)
578. **MODE-MATCH3-0578** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · лимит ходов высокий [match3]: своп без серии откатывается
579. **MODE-MATCH3-0579** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · плотное поле [match3]: счёт неотрицателен на всём протяжении
580. **MODE-MATCH3-0580** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · плотное поле [match3]: ни одного исключения/краша за партию
581. **MODE-MATCH3-0581** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · плотное поле [match3]: resume в середине — бит-в-бит то же продолжение
582. **MODE-MATCH3-0582** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · плотное поле [match3]: число ходов конечно (партия сходится)
583. **MODE-MATCH3-0583** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · плотное поле [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
584. **MODE-MATCH3-0584** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · плотное поле [match3]: ни одной невалидной постановки не принято
585. **MODE-MATCH3-0585** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · плотное поле [match3]: инвариант доски: заполненные клетки не «протекают» за границы
586. **MODE-MATCH3-0586** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · плотное поле [match3]: каскады затухают (resolveBoard завершается)
587. **MODE-MATCH3-0587** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · плотное поле [match3]: своп без серии откатывается
588. **MODE-MATCH3-0588** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · разреженное поле [match3]: счёт неотрицателен на всём протяжении
589. **MODE-MATCH3-0589** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · разреженное поле [match3]: ни одного исключения/краша за партию
590. **MODE-MATCH3-0590** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · разреженное поле [match3]: resume в середине — бит-в-бит то же продолжение
591. **MODE-MATCH3-0591** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · разреженное поле [match3]: число ходов конечно (партия сходится)
592. **MODE-MATCH3-0592** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · разреженное поле [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
593. **MODE-MATCH3-0593** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · разреженное поле [match3]: ни одной невалидной постановки не принято
594. **MODE-MATCH3-0594** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · разреженное поле [match3]: инвариант доски: заполненные клетки не «протекают» за границы
595. **MODE-MATCH3-0595** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · разреженное поле [match3]: каскады затухают (resolveBoard завершается)
596. **MODE-MATCH3-0596** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · разреженное поле [match3]: своп без серии откатывается
597. **MODE-MATCH3-0597** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · seed без стартовых серий [match3]: счёт неотрицателен на всём протяжении
598. **MODE-MATCH3-0598** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · seed без стартовых серий [match3]: ни одного исключения/краша за партию
599. **MODE-MATCH3-0599** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · seed без стартовых серий [match3]: resume в середине — бит-в-бит то же продолжение
600. **MODE-MATCH3-0600** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · seed без стартовых серий [match3]: число ходов конечно (партия сходится)
601. **MODE-MATCH3-0601** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · seed без стартовых серий [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
602. **MODE-MATCH3-0602** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · seed без стартовых серий [match3]: ни одной невалидной постановки не принято
603. **MODE-MATCH3-0603** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · seed без стартовых серий [match3]: инвариант доски: заполненные клетки не «протекают» за границы
604. **MODE-MATCH3-0604** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · seed без стартовых серий [match3]: каскады затухают (resolveBoard завершается)
605. **MODE-MATCH3-0605** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=3 · seed без стартовых серий [match3]: своп без серии откатывается
606. **MODE-MATCH3-0606** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · 6 цветов [match3]: счёт неотрицателен на всём протяжении
607. **MODE-MATCH3-0607** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · 6 цветов [match3]: ни одного исключения/краша за партию
608. **MODE-MATCH3-0608** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · 6 цветов [match3]: resume в середине — бит-в-бит то же продолжение
609. **MODE-MATCH3-0609** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · 6 цветов [match3]: число ходов конечно (партия сходится)
610. **MODE-MATCH3-0610** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · 6 цветов [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
611. **MODE-MATCH3-0611** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · 6 цветов [match3]: ни одной невалидной постановки не принято
612. **MODE-MATCH3-0612** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · 6 цветов [match3]: инвариант доски: заполненные клетки не «протекают» за границы
613. **MODE-MATCH3-0613** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · 6 цветов [match3]: каскады затухают (resolveBoard завершается)
614. **MODE-MATCH3-0614** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · 6 цветов [match3]: своп без серии откатывается
615. **MODE-MATCH3-0615** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · лимит ходов низкий [match3]: счёт неотрицателен на всём протяжении
616. **MODE-MATCH3-0616** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · лимит ходов низкий [match3]: ни одного исключения/краша за партию
617. **MODE-MATCH3-0617** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · лимит ходов низкий [match3]: resume в середине — бит-в-бит то же продолжение
618. **MODE-MATCH3-0618** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · лимит ходов низкий [match3]: число ходов конечно (партия сходится)
619. **MODE-MATCH3-0619** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · лимит ходов низкий [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
620. **MODE-MATCH3-0620** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · лимит ходов низкий [match3]: ни одной невалидной постановки не принято
621. **MODE-MATCH3-0621** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · лимит ходов низкий [match3]: инвариант доски: заполненные клетки не «протекают» за границы
622. **MODE-MATCH3-0622** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · лимит ходов низкий [match3]: каскады затухают (resolveBoard завершается)
623. **MODE-MATCH3-0623** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · лимит ходов низкий [match3]: своп без серии откатывается
624. **MODE-MATCH3-0624** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · лимит ходов высокий [match3]: счёт неотрицателен на всём протяжении
625. **MODE-MATCH3-0625** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · лимит ходов высокий [match3]: ни одного исключения/краша за партию
626. **MODE-MATCH3-0626** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · лимит ходов высокий [match3]: resume в середине — бит-в-бит то же продолжение
627. **MODE-MATCH3-0627** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · лимит ходов высокий [match3]: число ходов конечно (партия сходится)
628. **MODE-MATCH3-0628** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · лимит ходов высокий [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
629. **MODE-MATCH3-0629** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · лимит ходов высокий [match3]: ни одной невалидной постановки не принято
630. **MODE-MATCH3-0630** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · лимит ходов высокий [match3]: инвариант доски: заполненные клетки не «протекают» за границы
631. **MODE-MATCH3-0631** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · лимит ходов высокий [match3]: каскады затухают (resolveBoard завершается)
632. **MODE-MATCH3-0632** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · лимит ходов высокий [match3]: своп без серии откатывается
633. **MODE-MATCH3-0633** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · плотное поле [match3]: счёт неотрицателен на всём протяжении
634. **MODE-MATCH3-0634** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · плотное поле [match3]: ни одного исключения/краша за партию
635. **MODE-MATCH3-0635** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · плотное поле [match3]: resume в середине — бит-в-бит то же продолжение
636. **MODE-MATCH3-0636** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · плотное поле [match3]: число ходов конечно (партия сходится)
637. **MODE-MATCH3-0637** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · плотное поле [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
638. **MODE-MATCH3-0638** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · плотное поле [match3]: ни одной невалидной постановки не принято
639. **MODE-MATCH3-0639** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · плотное поле [match3]: инвариант доски: заполненные клетки не «протекают» за границы
640. **MODE-MATCH3-0640** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · плотное поле [match3]: каскады затухают (resolveBoard завершается)
641. **MODE-MATCH3-0641** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · плотное поле [match3]: своп без серии откатывается
642. **MODE-MATCH3-0642** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · разреженное поле [match3]: счёт неотрицателен на всём протяжении
643. **MODE-MATCH3-0643** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · разреженное поле [match3]: ни одного исключения/краша за партию
644. **MODE-MATCH3-0644** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · разреженное поле [match3]: resume в середине — бит-в-бит то же продолжение
645. **MODE-MATCH3-0645** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · разреженное поле [match3]: число ходов конечно (партия сходится)
646. **MODE-MATCH3-0646** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · разреженное поле [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
647. **MODE-MATCH3-0647** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · разреженное поле [match3]: ни одной невалидной постановки не принято
648. **MODE-MATCH3-0648** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · разреженное поле [match3]: инвариант доски: заполненные клетки не «протекают» за границы
649. **MODE-MATCH3-0649** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · разреженное поле [match3]: каскады затухают (resolveBoard завершается)
650. **MODE-MATCH3-0650** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · разреженное поле [match3]: своп без серии откатывается
651. **MODE-MATCH3-0651** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · seed без стартовых серий [match3]: счёт неотрицателен на всём протяжении
652. **MODE-MATCH3-0652** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · seed без стартовых серий [match3]: ни одного исключения/краша за партию
653. **MODE-MATCH3-0653** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · seed без стартовых серий [match3]: resume в середине — бит-в-бит то же продолжение
654. **MODE-MATCH3-0654** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · seed без стартовых серий [match3]: число ходов конечно (партия сходится)
655. **MODE-MATCH3-0655** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · seed без стартовых серий [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
656. **MODE-MATCH3-0656** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · seed без стартовых серий [match3]: ни одной невалидной постановки не принято
657. **MODE-MATCH3-0657** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · seed без стартовых серий [match3]: инвариант доски: заполненные клетки не «протекают» за границы
658. **MODE-MATCH3-0658** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · seed без стартовых серий [match3]: каскады затухают (resolveBoard завершается)
659. **MODE-MATCH3-0659** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=4 · seed без стартовых серий [match3]: своп без серии откатывается
660. **MODE-MATCH3-0660** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · 6 цветов [match3]: счёт неотрицателен на всём протяжении
661. **MODE-MATCH3-0661** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · 6 цветов [match3]: ни одного исключения/краша за партию
662. **MODE-MATCH3-0662** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · 6 цветов [match3]: resume в середине — бит-в-бит то же продолжение
663. **MODE-MATCH3-0663** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · 6 цветов [match3]: число ходов конечно (партия сходится)
664. **MODE-MATCH3-0664** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · 6 цветов [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
665. **MODE-MATCH3-0665** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · 6 цветов [match3]: ни одной невалидной постановки не принято
666. **MODE-MATCH3-0666** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · 6 цветов [match3]: инвариант доски: заполненные клетки не «протекают» за границы
667. **MODE-MATCH3-0667** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · 6 цветов [match3]: каскады затухают (resolveBoard завершается)
668. **MODE-MATCH3-0668** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · 6 цветов [match3]: своп без серии откатывается
669. **MODE-MATCH3-0669** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · лимит ходов низкий [match3]: счёт неотрицателен на всём протяжении
670. **MODE-MATCH3-0670** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · лимит ходов низкий [match3]: ни одного исключения/краша за партию
671. **MODE-MATCH3-0671** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · лимит ходов низкий [match3]: resume в середине — бит-в-бит то же продолжение
672. **MODE-MATCH3-0672** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · лимит ходов низкий [match3]: число ходов конечно (партия сходится)
673. **MODE-MATCH3-0673** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · лимит ходов низкий [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
674. **MODE-MATCH3-0674** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · лимит ходов низкий [match3]: ни одной невалидной постановки не принято
675. **MODE-MATCH3-0675** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · лимит ходов низкий [match3]: инвариант доски: заполненные клетки не «протекают» за границы
676. **MODE-MATCH3-0676** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · лимит ходов низкий [match3]: каскады затухают (resolveBoard завершается)
677. **MODE-MATCH3-0677** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · лимит ходов низкий [match3]: своп без серии откатывается
678. **MODE-MATCH3-0678** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · лимит ходов высокий [match3]: счёт неотрицателен на всём протяжении
679. **MODE-MATCH3-0679** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · лимит ходов высокий [match3]: ни одного исключения/краша за партию
680. **MODE-MATCH3-0680** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · лимит ходов высокий [match3]: resume в середине — бит-в-бит то же продолжение
681. **MODE-MATCH3-0681** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · лимит ходов высокий [match3]: число ходов конечно (партия сходится)
682. **MODE-MATCH3-0682** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · лимит ходов высокий [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
683. **MODE-MATCH3-0683** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · лимит ходов высокий [match3]: ни одной невалидной постановки не принято
684. **MODE-MATCH3-0684** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · лимит ходов высокий [match3]: инвариант доски: заполненные клетки не «протекают» за границы
685. **MODE-MATCH3-0685** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · лимит ходов высокий [match3]: каскады затухают (resolveBoard завершается)
686. **MODE-MATCH3-0686** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · лимит ходов высокий [match3]: своп без серии откатывается
687. **MODE-MATCH3-0687** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · плотное поле [match3]: счёт неотрицателен на всём протяжении
688. **MODE-MATCH3-0688** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · плотное поле [match3]: ни одного исключения/краша за партию
689. **MODE-MATCH3-0689** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · плотное поле [match3]: resume в середине — бит-в-бит то же продолжение
690. **MODE-MATCH3-0690** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · плотное поле [match3]: число ходов конечно (партия сходится)
691. **MODE-MATCH3-0691** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · плотное поле [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
692. **MODE-MATCH3-0692** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · плотное поле [match3]: ни одной невалидной постановки не принято
693. **MODE-MATCH3-0693** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · плотное поле [match3]: инвариант доски: заполненные клетки не «протекают» за границы
694. **MODE-MATCH3-0694** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · плотное поле [match3]: каскады затухают (resolveBoard завершается)
695. **MODE-MATCH3-0695** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · плотное поле [match3]: своп без серии откатывается
696. **MODE-MATCH3-0696** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · разреженное поле [match3]: счёт неотрицателен на всём протяжении
697. **MODE-MATCH3-0697** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · разреженное поле [match3]: ни одного исключения/краша за партию
698. **MODE-MATCH3-0698** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · разреженное поле [match3]: resume в середине — бит-в-бит то же продолжение
699. **MODE-MATCH3-0699** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · разреженное поле [match3]: число ходов конечно (партия сходится)
700. **MODE-MATCH3-0700** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · разреженное поле [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
701. **MODE-MATCH3-0701** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · разреженное поле [match3]: ни одной невалидной постановки не принято
702. **MODE-MATCH3-0702** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · разреженное поле [match3]: инвариант доски: заполненные клетки не «протекают» за границы
703. **MODE-MATCH3-0703** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · разреженное поле [match3]: каскады затухают (resolveBoard завершается)
704. **MODE-MATCH3-0704** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · разреженное поле [match3]: своп без серии откатывается
705. **MODE-MATCH3-0705** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · seed без стартовых серий [match3]: счёт неотрицателен на всём протяжении
706. **MODE-MATCH3-0706** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · seed без стартовых серий [match3]: ни одного исключения/краша за партию
707. **MODE-MATCH3-0707** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · seed без стартовых серий [match3]: resume в середине — бит-в-бит то же продолжение
708. **MODE-MATCH3-0708** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · seed без стартовых серий [match3]: число ходов конечно (партия сходится)
709. **MODE-MATCH3-0709** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · seed без стартовых серий [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
710. **MODE-MATCH3-0710** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · seed без стартовых серий [match3]: ни одной невалидной постановки не принято
711. **MODE-MATCH3-0711** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · seed без стартовых серий [match3]: инвариант доски: заполненные клетки не «протекают» за границы
712. **MODE-MATCH3-0712** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · seed без стартовых серий [match3]: каскады затухают (resolveBoard завершается)
713. **MODE-MATCH3-0713** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=5 · seed без стартовых серий [match3]: своп без серии откатывается
714. **MODE-MATCH3-0714** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · 6 цветов [match3]: счёт неотрицателен на всём протяжении
715. **MODE-MATCH3-0715** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · 6 цветов [match3]: ни одного исключения/краша за партию
716. **MODE-MATCH3-0716** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · 6 цветов [match3]: resume в середине — бит-в-бит то же продолжение
717. **MODE-MATCH3-0717** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · 6 цветов [match3]: число ходов конечно (партия сходится)
718. **MODE-MATCH3-0718** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · 6 цветов [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
719. **MODE-MATCH3-0719** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · 6 цветов [match3]: ни одной невалидной постановки не принято
720. **MODE-MATCH3-0720** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · 6 цветов [match3]: инвариант доски: заполненные клетки не «протекают» за границы
721. **MODE-MATCH3-0721** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · 6 цветов [match3]: каскады затухают (resolveBoard завершается)
722. **MODE-MATCH3-0722** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · 6 цветов [match3]: своп без серии откатывается
723. **MODE-MATCH3-0723** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · лимит ходов низкий [match3]: счёт неотрицателен на всём протяжении
724. **MODE-MATCH3-0724** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · лимит ходов низкий [match3]: ни одного исключения/краша за партию
725. **MODE-MATCH3-0725** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · лимит ходов низкий [match3]: resume в середине — бит-в-бит то же продолжение
726. **MODE-MATCH3-0726** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · лимит ходов низкий [match3]: число ходов конечно (партия сходится)
727. **MODE-MATCH3-0727** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · лимит ходов низкий [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
728. **MODE-MATCH3-0728** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · лимит ходов низкий [match3]: ни одной невалидной постановки не принято
729. **MODE-MATCH3-0729** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · лимит ходов низкий [match3]: инвариант доски: заполненные клетки не «протекают» за границы
730. **MODE-MATCH3-0730** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · лимит ходов низкий [match3]: каскады затухают (resolveBoard завершается)
731. **MODE-MATCH3-0731** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · лимит ходов низкий [match3]: своп без серии откатывается
732. **MODE-MATCH3-0732** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · лимит ходов высокий [match3]: счёт неотрицателен на всём протяжении
733. **MODE-MATCH3-0733** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · лимит ходов высокий [match3]: ни одного исключения/краша за партию
734. **MODE-MATCH3-0734** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · лимит ходов высокий [match3]: resume в середине — бит-в-бит то же продолжение
735. **MODE-MATCH3-0735** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · лимит ходов высокий [match3]: число ходов конечно (партия сходится)
736. **MODE-MATCH3-0736** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · лимит ходов высокий [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
737. **MODE-MATCH3-0737** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · лимит ходов высокий [match3]: ни одной невалидной постановки не принято
738. **MODE-MATCH3-0738** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · лимит ходов высокий [match3]: инвариант доски: заполненные клетки не «протекают» за границы
739. **MODE-MATCH3-0739** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · лимит ходов высокий [match3]: каскады затухают (resolveBoard завершается)
740. **MODE-MATCH3-0740** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · лимит ходов высокий [match3]: своп без серии откатывается
741. **MODE-MATCH3-0741** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · плотное поле [match3]: счёт неотрицателен на всём протяжении
742. **MODE-MATCH3-0742** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · плотное поле [match3]: ни одного исключения/краша за партию
743. **MODE-MATCH3-0743** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · плотное поле [match3]: resume в середине — бит-в-бит то же продолжение
744. **MODE-MATCH3-0744** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · плотное поле [match3]: число ходов конечно (партия сходится)
745. **MODE-MATCH3-0745** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · плотное поле [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
746. **MODE-MATCH3-0746** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · плотное поле [match3]: ни одной невалидной постановки не принято
747. **MODE-MATCH3-0747** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · плотное поле [match3]: инвариант доски: заполненные клетки не «протекают» за границы
748. **MODE-MATCH3-0748** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · плотное поле [match3]: каскады затухают (resolveBoard завершается)
749. **MODE-MATCH3-0749** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · плотное поле [match3]: своп без серии откатывается
750. **MODE-MATCH3-0750** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · разреженное поле [match3]: счёт неотрицателен на всём протяжении
751. **MODE-MATCH3-0751** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · разреженное поле [match3]: ни одного исключения/краша за партию
752. **MODE-MATCH3-0752** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · разреженное поле [match3]: resume в середине — бит-в-бит то же продолжение
753. **MODE-MATCH3-0753** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · разреженное поле [match3]: число ходов конечно (партия сходится)
754. **MODE-MATCH3-0754** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · разреженное поле [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
755. **MODE-MATCH3-0755** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · разреженное поле [match3]: ни одной невалидной постановки не принято
756. **MODE-MATCH3-0756** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · разреженное поле [match3]: инвариант доски: заполненные клетки не «протекают» за границы
757. **MODE-MATCH3-0757** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · разреженное поле [match3]: каскады затухают (resolveBoard завершается)
758. **MODE-MATCH3-0758** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · разреженное поле [match3]: своп без серии откатывается
759. **MODE-MATCH3-0759** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · seed без стартовых серий [match3]: счёт неотрицателен на всём протяжении
760. **MODE-MATCH3-0760** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · seed без стартовых серий [match3]: ни одного исключения/краша за партию
761. **MODE-MATCH3-0761** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · seed без стартовых серий [match3]: resume в середине — бит-в-бит то же продолжение
762. **MODE-MATCH3-0762** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · seed без стартовых серий [match3]: число ходов конечно (партия сходится)
763. **MODE-MATCH3-0763** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · seed без стартовых серий [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
764. **MODE-MATCH3-0764** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · seed без стартовых серий [match3]: ни одной невалидной постановки не принято
765. **MODE-MATCH3-0765** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · seed без стартовых серий [match3]: инвариант доски: заполненные клетки не «протекают» за границы
766. **MODE-MATCH3-0766** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · seed без стартовых серий [match3]: каскады затухают (resolveBoard завершается)
767. **MODE-MATCH3-0767** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=6 · seed без стартовых серий [match3]: своп без серии откатывается
768. **MODE-MATCH3-0768** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · 6 цветов [match3]: счёт неотрицателен на всём протяжении
769. **MODE-MATCH3-0769** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · 6 цветов [match3]: ни одного исключения/краша за партию
770. **MODE-MATCH3-0770** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · 6 цветов [match3]: resume в середине — бит-в-бит то же продолжение
771. **MODE-MATCH3-0771** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · 6 цветов [match3]: число ходов конечно (партия сходится)
772. **MODE-MATCH3-0772** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · 6 цветов [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
773. **MODE-MATCH3-0773** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · 6 цветов [match3]: ни одной невалидной постановки не принято
774. **MODE-MATCH3-0774** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · 6 цветов [match3]: инвариант доски: заполненные клетки не «протекают» за границы
775. **MODE-MATCH3-0775** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · 6 цветов [match3]: каскады затухают (resolveBoard завершается)
776. **MODE-MATCH3-0776** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · 6 цветов [match3]: своп без серии откатывается
777. **MODE-MATCH3-0777** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · лимит ходов низкий [match3]: счёт неотрицателен на всём протяжении
778. **MODE-MATCH3-0778** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · лимит ходов низкий [match3]: ни одного исключения/краша за партию
779. **MODE-MATCH3-0779** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · лимит ходов низкий [match3]: resume в середине — бит-в-бит то же продолжение
780. **MODE-MATCH3-0780** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · лимит ходов низкий [match3]: число ходов конечно (партия сходится)
781. **MODE-MATCH3-0781** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · лимит ходов низкий [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
782. **MODE-MATCH3-0782** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · лимит ходов низкий [match3]: ни одной невалидной постановки не принято
783. **MODE-MATCH3-0783** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · лимит ходов низкий [match3]: инвариант доски: заполненные клетки не «протекают» за границы
784. **MODE-MATCH3-0784** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · лимит ходов низкий [match3]: каскады затухают (resolveBoard завершается)
785. **MODE-MATCH3-0785** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · лимит ходов низкий [match3]: своп без серии откатывается
786. **MODE-MATCH3-0786** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · лимит ходов высокий [match3]: счёт неотрицателен на всём протяжении
787. **MODE-MATCH3-0787** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · лимит ходов высокий [match3]: ни одного исключения/краша за партию
788. **MODE-MATCH3-0788** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · лимит ходов высокий [match3]: resume в середине — бит-в-бит то же продолжение
789. **MODE-MATCH3-0789** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · лимит ходов высокий [match3]: число ходов конечно (партия сходится)
790. **MODE-MATCH3-0790** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · лимит ходов высокий [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
791. **MODE-MATCH3-0791** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · лимит ходов высокий [match3]: ни одной невалидной постановки не принято
792. **MODE-MATCH3-0792** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · лимит ходов высокий [match3]: инвариант доски: заполненные клетки не «протекают» за границы
793. **MODE-MATCH3-0793** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · лимит ходов высокий [match3]: каскады затухают (resolveBoard завершается)
794. **MODE-MATCH3-0794** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · лимит ходов высокий [match3]: своп без серии откатывается
795. **MODE-MATCH3-0795** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · плотное поле [match3]: счёт неотрицателен на всём протяжении
796. **MODE-MATCH3-0796** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · плотное поле [match3]: ни одного исключения/краша за партию
797. **MODE-MATCH3-0797** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · плотное поле [match3]: resume в середине — бит-в-бит то же продолжение
798. **MODE-MATCH3-0798** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · плотное поле [match3]: число ходов конечно (партия сходится)
799. **MODE-MATCH3-0799** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · плотное поле [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
800. **MODE-MATCH3-0800** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · плотное поле [match3]: ни одной невалидной постановки не принято
801. **MODE-MATCH3-0801** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · плотное поле [match3]: инвариант доски: заполненные клетки не «протекают» за границы
802. **MODE-MATCH3-0802** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · плотное поле [match3]: каскады затухают (resolveBoard завершается)
803. **MODE-MATCH3-0803** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · плотное поле [match3]: своп без серии откатывается
804. **MODE-MATCH3-0804** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · разреженное поле [match3]: счёт неотрицателен на всём протяжении
805. **MODE-MATCH3-0805** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · разреженное поле [match3]: ни одного исключения/краша за партию
806. **MODE-MATCH3-0806** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · разреженное поле [match3]: resume в середине — бит-в-бит то же продолжение
807. **MODE-MATCH3-0807** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · разреженное поле [match3]: число ходов конечно (партия сходится)
808. **MODE-MATCH3-0808** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · разреженное поле [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
809. **MODE-MATCH3-0809** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · разреженное поле [match3]: ни одной невалидной постановки не принято
810. **MODE-MATCH3-0810** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · разреженное поле [match3]: инвариант доски: заполненные клетки не «протекают» за границы
811. **MODE-MATCH3-0811** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · разреженное поле [match3]: каскады затухают (resolveBoard завершается)
812. **MODE-MATCH3-0812** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · разреженное поле [match3]: своп без серии откатывается
813. **MODE-MATCH3-0813** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · seed без стартовых серий [match3]: счёт неотрицателен на всём протяжении
814. **MODE-MATCH3-0814** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · seed без стартовых серий [match3]: ни одного исключения/краша за партию
815. **MODE-MATCH3-0815** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · seed без стартовых серий [match3]: resume в середине — бит-в-бит то же продолжение
816. **MODE-MATCH3-0816** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · seed без стартовых серий [match3]: число ходов конечно (партия сходится)
817. **MODE-MATCH3-0817** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · seed без стартовых серий [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
818. **MODE-MATCH3-0818** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · seed без стартовых серий [match3]: ни одной невалидной постановки не принято
819. **MODE-MATCH3-0819** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · seed без стартовых серий [match3]: инвариант доски: заполненные клетки не «протекают» за границы
820. **MODE-MATCH3-0820** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · seed без стартовых серий [match3]: каскады затухают (resolveBoard завершается)
821. **MODE-MATCH3-0821** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=7 · seed без стартовых серий [match3]: своп без серии откатывается
822. **MODE-MATCH3-0822** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · 6 цветов [match3]: счёт неотрицателен на всём протяжении
823. **MODE-MATCH3-0823** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · 6 цветов [match3]: ни одного исключения/краша за партию
824. **MODE-MATCH3-0824** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · 6 цветов [match3]: resume в середине — бит-в-бит то же продолжение
825. **MODE-MATCH3-0825** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · 6 цветов [match3]: число ходов конечно (партия сходится)
826. **MODE-MATCH3-0826** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · 6 цветов [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
827. **MODE-MATCH3-0827** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · 6 цветов [match3]: ни одной невалидной постановки не принято
828. **MODE-MATCH3-0828** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · 6 цветов [match3]: инвариант доски: заполненные клетки не «протекают» за границы
829. **MODE-MATCH3-0829** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · 6 цветов [match3]: каскады затухают (resolveBoard завершается)
830. **MODE-MATCH3-0830** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · 6 цветов [match3]: своп без серии откатывается
831. **MODE-MATCH3-0831** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · лимит ходов низкий [match3]: счёт неотрицателен на всём протяжении
832. **MODE-MATCH3-0832** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · лимит ходов низкий [match3]: ни одного исключения/краша за партию
833. **MODE-MATCH3-0833** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · лимит ходов низкий [match3]: resume в середине — бит-в-бит то же продолжение
834. **MODE-MATCH3-0834** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · лимит ходов низкий [match3]: число ходов конечно (партия сходится)
835. **MODE-MATCH3-0835** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · лимит ходов низкий [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
836. **MODE-MATCH3-0836** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · лимит ходов низкий [match3]: ни одной невалидной постановки не принято
837. **MODE-MATCH3-0837** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · лимит ходов низкий [match3]: инвариант доски: заполненные клетки не «протекают» за границы
838. **MODE-MATCH3-0838** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · лимит ходов низкий [match3]: каскады затухают (resolveBoard завершается)
839. **MODE-MATCH3-0839** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · лимит ходов низкий [match3]: своп без серии откатывается
840. **MODE-MATCH3-0840** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · лимит ходов высокий [match3]: счёт неотрицателен на всём протяжении
841. **MODE-MATCH3-0841** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · лимит ходов высокий [match3]: ни одного исключения/краша за партию
842. **MODE-MATCH3-0842** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · лимит ходов высокий [match3]: resume в середине — бит-в-бит то же продолжение
843. **MODE-MATCH3-0843** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · лимит ходов высокий [match3]: число ходов конечно (партия сходится)
844. **MODE-MATCH3-0844** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · лимит ходов высокий [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
845. **MODE-MATCH3-0845** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · лимит ходов высокий [match3]: ни одной невалидной постановки не принято
846. **MODE-MATCH3-0846** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · лимит ходов высокий [match3]: инвариант доски: заполненные клетки не «протекают» за границы
847. **MODE-MATCH3-0847** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · лимит ходов высокий [match3]: каскады затухают (resolveBoard завершается)
848. **MODE-MATCH3-0848** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · лимит ходов высокий [match3]: своп без серии откатывается
849. **MODE-MATCH3-0849** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · плотное поле [match3]: счёт неотрицателен на всём протяжении
850. **MODE-MATCH3-0850** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · плотное поле [match3]: ни одного исключения/краша за партию
851. **MODE-MATCH3-0851** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · плотное поле [match3]: resume в середине — бит-в-бит то же продолжение
852. **MODE-MATCH3-0852** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · плотное поле [match3]: число ходов конечно (партия сходится)
853. **MODE-MATCH3-0853** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · плотное поле [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
854. **MODE-MATCH3-0854** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · плотное поле [match3]: ни одной невалидной постановки не принято
855. **MODE-MATCH3-0855** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · плотное поле [match3]: инвариант доски: заполненные клетки не «протекают» за границы
856. **MODE-MATCH3-0856** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · плотное поле [match3]: каскады затухают (resolveBoard завершается)
857. **MODE-MATCH3-0857** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · плотное поле [match3]: своп без серии откатывается
858. **MODE-MATCH3-0858** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · разреженное поле [match3]: счёт неотрицателен на всём протяжении
859. **MODE-MATCH3-0859** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · разреженное поле [match3]: ни одного исключения/краша за партию
860. **MODE-MATCH3-0860** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · разреженное поле [match3]: resume в середине — бит-в-бит то же продолжение
861. **MODE-MATCH3-0861** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · разреженное поле [match3]: число ходов конечно (партия сходится)
862. **MODE-MATCH3-0862** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · разреженное поле [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
863. **MODE-MATCH3-0863** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · разреженное поле [match3]: ни одной невалидной постановки не принято
864. **MODE-MATCH3-0864** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · разреженное поле [match3]: инвариант доски: заполненные клетки не «протекают» за границы
865. **MODE-MATCH3-0865** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · разреженное поле [match3]: каскады затухают (resolveBoard завершается)
866. **MODE-MATCH3-0866** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · разреженное поле [match3]: своп без серии откатывается
867. **MODE-MATCH3-0867** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · seed без стартовых серий [match3]: счёт неотрицателен на всём протяжении
868. **MODE-MATCH3-0868** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · seed без стартовых серий [match3]: ни одного исключения/краша за партию
869. **MODE-MATCH3-0869** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · seed без стартовых серий [match3]: resume в середине — бит-в-бит то же продолжение
870. **MODE-MATCH3-0870** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · seed без стартовых серий [match3]: число ходов конечно (партия сходится)
871. **MODE-MATCH3-0871** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · seed без стартовых серий [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
872. **MODE-MATCH3-0872** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · seed без стартовых серий [match3]: ни одной невалидной постановки не принято
873. **MODE-MATCH3-0873** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · seed без стартовых серий [match3]: инвариант доски: заполненные клетки не «протекают» за границы
874. **MODE-MATCH3-0874** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · seed без стартовых серий [match3]: каскады затухают (resolveBoard завершается)
875. **MODE-MATCH3-0875** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=8 · seed без стартовых серий [match3]: своп без серии откатывается
876. **MODE-MATCH3-0876** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · 6 цветов [match3]: счёт неотрицателен на всём протяжении
877. **MODE-MATCH3-0877** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · 6 цветов [match3]: ни одного исключения/краша за партию
878. **MODE-MATCH3-0878** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · 6 цветов [match3]: resume в середине — бит-в-бит то же продолжение
879. **MODE-MATCH3-0879** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · 6 цветов [match3]: число ходов конечно (партия сходится)
880. **MODE-MATCH3-0880** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · 6 цветов [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
881. **MODE-MATCH3-0881** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · 6 цветов [match3]: ни одной невалидной постановки не принято
882. **MODE-MATCH3-0882** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · 6 цветов [match3]: инвариант доски: заполненные клетки не «протекают» за границы
883. **MODE-MATCH3-0883** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · 6 цветов [match3]: каскады затухают (resolveBoard завершается)
884. **MODE-MATCH3-0884** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · 6 цветов [match3]: своп без серии откатывается
885. **MODE-MATCH3-0885** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · лимит ходов низкий [match3]: счёт неотрицателен на всём протяжении
886. **MODE-MATCH3-0886** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · лимит ходов низкий [match3]: ни одного исключения/краша за партию
887. **MODE-MATCH3-0887** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · лимит ходов низкий [match3]: resume в середине — бит-в-бит то же продолжение
888. **MODE-MATCH3-0888** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · лимит ходов низкий [match3]: число ходов конечно (партия сходится)
889. **MODE-MATCH3-0889** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · лимит ходов низкий [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
890. **MODE-MATCH3-0890** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · лимит ходов низкий [match3]: ни одной невалидной постановки не принято
891. **MODE-MATCH3-0891** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · лимит ходов низкий [match3]: инвариант доски: заполненные клетки не «протекают» за границы
892. **MODE-MATCH3-0892** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · лимит ходов низкий [match3]: каскады затухают (resolveBoard завершается)
893. **MODE-MATCH3-0893** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · лимит ходов низкий [match3]: своп без серии откатывается
894. **MODE-MATCH3-0894** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · лимит ходов высокий [match3]: счёт неотрицателен на всём протяжении
895. **MODE-MATCH3-0895** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · лимит ходов высокий [match3]: ни одного исключения/краша за партию
896. **MODE-MATCH3-0896** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · лимит ходов высокий [match3]: resume в середине — бит-в-бит то же продолжение
897. **MODE-MATCH3-0897** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · лимит ходов высокий [match3]: число ходов конечно (партия сходится)
898. **MODE-MATCH3-0898** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · лимит ходов высокий [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
899. **MODE-MATCH3-0899** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · лимит ходов высокий [match3]: ни одной невалидной постановки не принято
900. **MODE-MATCH3-0900** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · лимит ходов высокий [match3]: инвариант доски: заполненные клетки не «протекают» за границы
901. **MODE-MATCH3-0901** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · лимит ходов высокий [match3]: каскады затухают (resolveBoard завершается)
902. **MODE-MATCH3-0902** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · лимит ходов высокий [match3]: своп без серии откатывается
903. **MODE-MATCH3-0903** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · плотное поле [match3]: счёт неотрицателен на всём протяжении
904. **MODE-MATCH3-0904** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · плотное поле [match3]: ни одного исключения/краша за партию
905. **MODE-MATCH3-0905** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · плотное поле [match3]: resume в середине — бит-в-бит то же продолжение
906. **MODE-MATCH3-0906** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · плотное поле [match3]: число ходов конечно (партия сходится)
907. **MODE-MATCH3-0907** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · плотное поле [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
908. **MODE-MATCH3-0908** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · плотное поле [match3]: ни одной невалидной постановки не принято
909. **MODE-MATCH3-0909** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · плотное поле [match3]: инвариант доски: заполненные клетки не «протекают» за границы
910. **MODE-MATCH3-0910** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · плотное поле [match3]: каскады затухают (resolveBoard завершается)
911. **MODE-MATCH3-0911** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · плотное поле [match3]: своп без серии откатывается
912. **MODE-MATCH3-0912** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · разреженное поле [match3]: счёт неотрицателен на всём протяжении
913. **MODE-MATCH3-0913** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · разреженное поле [match3]: ни одного исключения/краша за партию
914. **MODE-MATCH3-0914** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · разреженное поле [match3]: resume в середине — бит-в-бит то же продолжение
915. **MODE-MATCH3-0915** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · разреженное поле [match3]: число ходов конечно (партия сходится)
916. **MODE-MATCH3-0916** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · разреженное поле [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
917. **MODE-MATCH3-0917** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · разреженное поле [match3]: ни одной невалидной постановки не принято
918. **MODE-MATCH3-0918** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · разреженное поле [match3]: инвариант доски: заполненные клетки не «протекают» за границы
919. **MODE-MATCH3-0919** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · разреженное поле [match3]: каскады затухают (resolveBoard завершается)
920. **MODE-MATCH3-0920** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · разреженное поле [match3]: своп без серии откатывается
921. **MODE-MATCH3-0921** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · seed без стартовых серий [match3]: счёт неотрицателен на всём протяжении
922. **MODE-MATCH3-0922** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · seed без стартовых серий [match3]: ни одного исключения/краша за партию
923. **MODE-MATCH3-0923** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · seed без стартовых серий [match3]: resume в середине — бит-в-бит то же продолжение
924. **MODE-MATCH3-0924** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · seed без стартовых серий [match3]: число ходов конечно (партия сходится)
925. **MODE-MATCH3-0925** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · seed без стартовых серий [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
926. **MODE-MATCH3-0926** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · seed без стартовых серий [match3]: ни одной невалидной постановки не принято
927. **MODE-MATCH3-0927** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · seed без стартовых серий [match3]: инвариант доски: заполненные клетки не «протекают» за границы
928. **MODE-MATCH3-0928** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · seed без стартовых серий [match3]: каскады затухают (resolveBoard завершается)
929. **MODE-MATCH3-0929** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=9 · seed без стартовых серий [match3]: своп без серии откатывается
930. **MODE-MATCH3-0930** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · 6 цветов [match3]: счёт неотрицателен на всём протяжении
931. **MODE-MATCH3-0931** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · 6 цветов [match3]: ни одного исключения/краша за партию
932. **MODE-MATCH3-0932** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · 6 цветов [match3]: resume в середине — бит-в-бит то же продолжение
933. **MODE-MATCH3-0933** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · 6 цветов [match3]: число ходов конечно (партия сходится)
934. **MODE-MATCH3-0934** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · 6 цветов [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
935. **MODE-MATCH3-0935** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · 6 цветов [match3]: ни одной невалидной постановки не принято
936. **MODE-MATCH3-0936** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · 6 цветов [match3]: инвариант доски: заполненные клетки не «протекают» за границы
937. **MODE-MATCH3-0937** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · 6 цветов [match3]: каскады затухают (resolveBoard завершается)
938. **MODE-MATCH3-0938** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · 6 цветов [match3]: своп без серии откатывается
939. **MODE-MATCH3-0939** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · лимит ходов низкий [match3]: счёт неотрицателен на всём протяжении
940. **MODE-MATCH3-0940** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · лимит ходов низкий [match3]: ни одного исключения/краша за партию
941. **MODE-MATCH3-0941** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · лимит ходов низкий [match3]: resume в середине — бит-в-бит то же продолжение
942. **MODE-MATCH3-0942** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · лимит ходов низкий [match3]: число ходов конечно (партия сходится)
943. **MODE-MATCH3-0943** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · лимит ходов низкий [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
944. **MODE-MATCH3-0944** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · лимит ходов низкий [match3]: ни одной невалидной постановки не принято
945. **MODE-MATCH3-0945** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · лимит ходов низкий [match3]: инвариант доски: заполненные клетки не «протекают» за границы
946. **MODE-MATCH3-0946** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · лимит ходов низкий [match3]: каскады затухают (resolveBoard завершается)
947. **MODE-MATCH3-0947** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · лимит ходов низкий [match3]: своп без серии откатывается
948. **MODE-MATCH3-0948** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · лимит ходов высокий [match3]: счёт неотрицателен на всём протяжении
949. **MODE-MATCH3-0949** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · лимит ходов высокий [match3]: ни одного исключения/краша за партию
950. **MODE-MATCH3-0950** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · лимит ходов высокий [match3]: resume в середине — бит-в-бит то же продолжение
951. **MODE-MATCH3-0951** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · лимит ходов высокий [match3]: число ходов конечно (партия сходится)
952. **MODE-MATCH3-0952** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · лимит ходов высокий [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
953. **MODE-MATCH3-0953** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · лимит ходов высокий [match3]: ни одной невалидной постановки не принято
954. **MODE-MATCH3-0954** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · лимит ходов высокий [match3]: инвариант доски: заполненные клетки не «протекают» за границы
955. **MODE-MATCH3-0955** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · лимит ходов высокий [match3]: каскады затухают (resolveBoard завершается)
956. **MODE-MATCH3-0956** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · лимит ходов высокий [match3]: своп без серии откатывается
957. **MODE-MATCH3-0957** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · плотное поле [match3]: счёт неотрицателен на всём протяжении
958. **MODE-MATCH3-0958** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · плотное поле [match3]: ни одного исключения/краша за партию
959. **MODE-MATCH3-0959** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · плотное поле [match3]: resume в середине — бит-в-бит то же продолжение
960. **MODE-MATCH3-0960** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · плотное поле [match3]: число ходов конечно (партия сходится)
961. **MODE-MATCH3-0961** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · плотное поле [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
962. **MODE-MATCH3-0962** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · плотное поле [match3]: ни одной невалидной постановки не принято
963. **MODE-MATCH3-0963** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · плотное поле [match3]: инвариант доски: заполненные клетки не «протекают» за границы
964. **MODE-MATCH3-0964** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · плотное поле [match3]: каскады затухают (resolveBoard завершается)
965. **MODE-MATCH3-0965** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · плотное поле [match3]: своп без серии откатывается
966. **MODE-MATCH3-0966** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · разреженное поле [match3]: счёт неотрицателен на всём протяжении
967. **MODE-MATCH3-0967** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · разреженное поле [match3]: ни одного исключения/краша за партию
968. **MODE-MATCH3-0968** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · разреженное поле [match3]: resume в середине — бит-в-бит то же продолжение
969. **MODE-MATCH3-0969** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · разреженное поле [match3]: число ходов конечно (партия сходится)
970. **MODE-MATCH3-0970** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · разреженное поле [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
971. **MODE-MATCH3-0971** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · разреженное поле [match3]: ни одной невалидной постановки не принято
972. **MODE-MATCH3-0972** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · разреженное поле [match3]: инвариант доски: заполненные клетки не «протекают» за границы
973. **MODE-MATCH3-0973** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · разреженное поле [match3]: каскады затухают (resolveBoard завершается)
974. **MODE-MATCH3-0974** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · разреженное поле [match3]: своп без серии откатывается
975. **MODE-MATCH3-0975** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · seed без стартовых серий [match3]: счёт неотрицателен на всём протяжении
976. **MODE-MATCH3-0976** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · seed без стартовых серий [match3]: ни одного исключения/краша за партию
977. **MODE-MATCH3-0977** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · seed без стартовых серий [match3]: resume в середине — бит-в-бит то же продолжение
978. **MODE-MATCH3-0978** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · seed без стартовых серий [match3]: число ходов конечно (партия сходится)
979. **MODE-MATCH3-0979** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · seed без стартовых серий [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
980. **MODE-MATCH3-0980** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · seed без стартовых серий [match3]: ни одной невалидной постановки не принято
981. **MODE-MATCH3-0981** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · seed без стартовых серий [match3]: инвариант доски: заполненные клетки не «протекают» за границы
982. **MODE-MATCH3-0982** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · seed без стартовых серий [match3]: каскады затухают (resolveBoard завершается)
983. **MODE-MATCH3-0983** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=10 · seed без стартовых серий [match3]: своп без серии откатывается
984. **MODE-MATCH3-0984** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · 6 цветов [match3]: счёт неотрицателен на всём протяжении
985. **MODE-MATCH3-0985** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · 6 цветов [match3]: ни одного исключения/краша за партию
986. **MODE-MATCH3-0986** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · 6 цветов [match3]: resume в середине — бит-в-бит то же продолжение
987. **MODE-MATCH3-0987** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · 6 цветов [match3]: число ходов конечно (партия сходится)
988. **MODE-MATCH3-0988** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · 6 цветов [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
989. **MODE-MATCH3-0989** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · 6 цветов [match3]: ни одной невалидной постановки не принято
990. **MODE-MATCH3-0990** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · 6 цветов [match3]: инвариант доски: заполненные клетки не «протекают» за границы
991. **MODE-MATCH3-0991** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · 6 цветов [match3]: каскады затухают (resolveBoard завершается)
992. **MODE-MATCH3-0992** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · 6 цветов [match3]: своп без серии откатывается
993. **MODE-MATCH3-0993** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · лимит ходов низкий [match3]: счёт неотрицателен на всём протяжении
994. **MODE-MATCH3-0994** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · лимит ходов низкий [match3]: ни одного исключения/краша за партию
995. **MODE-MATCH3-0995** — [C] [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · лимит ходов низкий [match3]: resume в середине — бит-в-бит то же продолжение
996. **MODE-MATCH3-0996** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · лимит ходов низкий [match3]: число ходов конечно (партия сходится)
997. **MODE-MATCH3-0997** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · лимит ходов низкий [match3]: детерминизм: повтор на той же паре (seed,cfg) даёт тот же исход
998. **MODE-MATCH3-0998** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · лимит ходов низкий [match3]: ни одной невалидной постановки не принято
999. **MODE-MATCH3-0999** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · лимит ходов низкий [match3]: инвариант доски: заполненные клетки не «протекают» за границы
1000. **MODE-MATCH3-1000** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · лимит ходов низкий [match3]: каскады затухают (resolveBoard завершается)
1001. **MODE-MATCH3-1001** — [Property-фаззинг (seed×конфиг)] Фаззинг seed=11 · лимит ходов низкий [match3]: своп без серии откатывается
