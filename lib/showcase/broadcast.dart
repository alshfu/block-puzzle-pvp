/// broadcast.dart — фасад захвата трансляции (выбор реализации по платформе).
///
/// За что отвечает файл:
///   Единая точка для «В эфир» в режиме Авто-шоу. На Web — реальный захват
///   экрана/вкладки (`getDisplayMedia`) с записью вертикального клипа для
///   YouTube Shorts (`MediaRecorder` → скачивание WebM): рабочий клиентский слой
///   стриминг-пайплайна. На остальных платформах — заглушка (`supported=false`).
///
///   ⚠️ Прямой live-эфир на YouTube (RTMP) из браузера невозможен без серверного
///   медиа-релея + YouTube Live API (OAuth) — это серверная надстройка (🔒,
///   завязана на VPS). Запись клипа — достижимая часть здесь и сейчас.
library;

export 'broadcast_stub.dart'
    if (dart.library.html) 'broadcast_web.dart';
