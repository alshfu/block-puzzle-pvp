# BlockDuel 9×9 — спецификация спрайтов и ассетов

Документ описывает **все визуальные и звуковые ассеты, которые нужны игре**:
что уже есть, что генерируется кодом и что предстоит нарисовать/записать, если
мы захотим заменить процедурную графику на художественную.

> **Важно про текущее состояние.** Сегодня игра **почти полностью процедурная**:
> доска, фигуры и эффекты рисуются кодом (`CustomPaint` / Flame), маскоты — это
> inline-SVG прямо в Dart-коде, а звук **синтезируется в рантайме** (WAV
> генерируется из нот, аудиофайлов нет). Из «настоящих» бинарных ассетов в репо
> только **6 шрифтов** (`assets/fonts/`) и **иконки приложения** по платформам.
> Поэтому ниже два пласта: **(A) что уже бандлится** и **(B) что нужно, если
> переходим на художественные ассеты**.

Источник правды по правилам/балансу — [TZ_BlockDuel_9x9.md](TZ_BlockDuel_9x9.md);
по фичам и фазам — [ROADMAP.md](ROADMAP.md). Этот файл — про **арт-/аудио-
производство**, а не про логику.

---

## Легенда статусов

- ✅ **есть** — файл лежит в репо и подключён.
- 🎨 **процедурно** — рисуется кодом (спрайт не нужен, но при желании заменяем
  на художественный).
- 🔉 **синтез** — звук генерируется кодом (аудиофайл не нужен).
- ⬜ **нужно** — ассета нет; для художественной версии его надо создать.
- 🟨 **частично/плейсхолдер** — есть, но черновой/требует доработки.

## Темы (определяют палитры и наборы ассетов)

Три темы — везде, где ассет «по теме», нужен вариант под каждую:

| id | Характер | Где задаётся |
|----|----------|--------------|
| `neutral` | тёмный synthwave / кибер | `lib/ui/design_tokens.dart` (`ThemeId`) |
| `candy` | светлый «леденцовый», пони | то же |
| `night` | глубокий тёмный, «тень» | то же |

---

## 1. Игровое поле и фигуры

Доска 9×9 (основной режим), 10×20 (Co-op, Tetris), 8×8 (Match-3). Все клетки
сейчас рисуются глянцевым градиентом (`lib/ui/decor/cell_fx.dart`
→ `paintGlossyCell`).

| Ассет | Назначение | Где используется | Формат / размер | Статус |
|-------|-----------|------------------|-----------------|--------|
| Клетка доски (пустая) | фон ячейки + сетка | `cell_fx.dart`, `board_game.dart` | — | 🎨 процедурно |
| Заполненная клетка | глянцевый блок игрока/фигуры | те же | — | 🎨 процедурно |
| «Призрак» постановки | подсветка цели/приземления | `board_view.dart`, `tetris_board_view.dart` | — | 🎨 процедурно |
| 7 тетромино (I,O,T,S,Z,J,L) | мини-превью в руке/NEXT/HOLD | `mini_piece.dart`, `tetris_screen.dart` | — | 🎨 процедурно |
| **Скин `gem`** | блок-самоцвет | `shop/skins.dart` (`SkinStyle.gem`) | спрайт 128×128 @1x/@2x/@3x | 🟨 процедурно → ⬜ если арт |
| **Скин `bullet`** | блок-патрон | `SkinStyle.bullet` | то же | 🟨 → ⬜ |
| **Скин `neon`** | неоновый блок | `SkinStyle.neon` | то же | 🟨 → ⬜ |
| **Скин `pixel`** | пиксельный блок | `SkinStyle.pixel` | то же | 🟨 → ⬜ |
| **Скин `candy`** | леденцовый блок | `SkinStyle.candy` | то же | 🟨 → ⬜ |

**Если делаем художественные скины блоков:** на каждый из 6 стилей —
single-cell спрайт (тайл), способный собираться в фигуру без швов. Палитра
Tetris-фигур (классическая): I cyan, O yellow, T purple, S green, Z red, J blue,
L orange (см. `tetris_colors` в `tetris_board_view.dart`) — арт-вариант должен
поддерживать ту же 7-цветную раскраску по типу фигуры.

- Формат: **PNG** (RGBA, без потерь) либо **SVG** (предпочтительно для масштаба).
- Базовый тайл: **128×128 px** (@1x), плюс @2x (256) и @3x (384) для PNG.
- Именование: `assets/sprites/cells/<skin>_<colorOrPlayer>.png`.

---

## 2. Фоны и окружение

| Ассет | Назначение | Где используется | Формат / размер | Статус |
|-------|-----------|------------------|-----------------|--------|
| Фон экрана (3 темы) | задник меню/игры | `lib/ui/decor/theme_backdrop.dart` | — | 🎨 процедурно |
| Парящие фигуры-декор | анимированный фон меню | `floating_theme.dart` | — | 🎨 процедурно |
| Рамка/градиент доски | обрамление поля | board views | — | 🎨 процедурно |

**Если делаем художественные фоны:** 3 фоновых изображения (по теме), бесшовные
или с безопасными полями под все аспект-отношения (9:16 … 16:9).
- Формат: PNG/WebP; ориентир **1920×1920** (квадрат с запасом) или набор под
  portrait/landscape.
- Именование: `assets/backgrounds/<theme>.webp`.

---

## 3. Маскоты и персонажи

Сейчас — inline-SVG прямо в коде (`lib/ui/decor/mascot.dart`,
`cartoon_pony.dart`), без файлов.

| Маскот | Тема | Где | Формат / размер | Статус |
|--------|------|-----|-----------------|--------|
| Пони (candy) | `candy` | `cartoon_pony.dart` (viewBox 160×160) | SVG | 🎨 inline-SVG |
| «Тень в капюшоне» (night) | `night` | `mascot.dart` (viewBox 120×120) | SVG | 🎨 inline-SVG |
| Кибер-робот (neutral) | `neutral` | `mascot.dart` (viewBox 120×120) | SVG | 🎨 inline-SVG |

**Если выносим в художественные ассеты:** по маскоту на тему, желательно с
несколькими позами/эмоциями (idle / победа / поражение / «думает»).
- Формат: **SVG** (вектор, чтобы совпадал силуэт) или PNG 512×512 @2x.
- Именование: `assets/mascots/<theme>_<pose>.svg`.

---

## 4. Иконки UI и контент

| Ассет | Назначение | Где | Сейчас | Статус |
|-------|-----------|-----|--------|--------|
| Иконки режимов меню | bot/hotseat/arcade/… (12) | `menu_screen.dart` ← `ui/icons/game_icons.dart` (`modeIcon`) | **Material-иконки** | ✅ векторные (emoji заменены) |
| Аватары профиля | 🙂😎🦊… (12) | `profile_screen.dart` | **emoji** | 🟨 emoji-плейсхолдер |
| Power-ups (6) | hint / swap_hand / stick_row / stick_col / bomb_3x3 / auto_play | `powerups_panel.dart`, `shop_screen.dart` ← `game_icons.dart` (`powerUpIcon`) | **Material-иконки** | ✅ векторные (emoji заменены) |
| Награды/валюта | 🪙 монеты, 💎 кристаллы, 🎁 награда | shop/profile/quests | **emoji** | 🟨 emoji-плейсхолдер |
| Достижения (~120) | бейджи ачивок | `achievements_*` | текст/emoji | ⬜ если бейджи-арт |
| Логотип игры | заставка меню | `logo.dart` | 🎨 процедурно | 🎨 |

**Сделано (2026-06-18):** иконки **режимов меню** и **power-ups** переведены с
emoji на векторные **Material-иконки** — единый источник `lib/ui/icons/
game_icons.dart` (`modeIcon(id)` / `powerUpIcon(id)`). В меню иконка режима
рисуется в тонированном чипе; power-ups — в панели матча и в магазине. Нулевой
вес ассетов, чёткость на любом DPI. Художественный набор (если понадобится)
заменяет только эти мапы/отрисовку.

**Если делаем художественные иконки (следующий шаг):** единый набор в одном
стиле, заменяющий Material-иконки.
- Формат: **SVG** (масштаб) или PNG **96×96 @1x/@2x/@3x**.
- Именование: `assets/icons/<group>/<id>.svg` (например `icons/powerups/bomb_3x3.svg`,
  `icons/modes/tetris.svg`, `icons/avatars/fox.svg`).
- Остаток: **аватары профиля** ещё emoji; **бейджи достижений** — текст/emoji.

---

## 5. Иконки приложения и сторов (по платформам)

| Платформа | Где | Что есть | Статус |
|-----------|-----|----------|--------|
| Web PWA | `web/icons/` + `web/favicon.png` | Icon-192/512 + maskable 192/512, favicon | ✅ есть (проверить, не дефолтный ли Flutter) |
| Android | `android/app/src/main/res/mipmap-*/ic_launcher.png` | launcher по плотностям | 🟨 проверить (возможно дефолт) |
| iOS | `ios/Runner/Assets.xcassets/AppIcon.appiconset/` | полный набор размеров | 🟨 проверить |
| macOS | `macos/Runner/Assets.xcassets/AppIcon.appiconset/` | 16…1024 | 🟨 проверить |

**Нужно для релиза:** мастер-иконка **1024×1024** PNG (без альфы для iOS/store),
из неё генерируются все размеры (рекомендую `flutter_launcher_icons`). Плюс
**maskable**-версия с safe-zone для Android/PWA, и **splash/launch screen**
(`flutter_native_splash`): лого по центру на фоне темы.
- Мастер: `assets/branding/app_icon_1024.png`, `assets/branding/splash.png`.

---

## 6. Аудио

Сейчас весь звук **синтезируется** (`lib/audio/synth.dart` + `sfx.dart` +
`music.dart`) — аудиофайлов нет. Воспроизведение — через `audioplayers`
(сгенерированный WAV).

### Звуковые эффекты (`enum Sfx`)

| Sfx | Событие | Статус |
|-----|---------|--------|
| `place` | постановка фигуры | 🔉 синтез |
| `invalid` | недопустимый ход | 🔉 синтез |
| `perfect` | perfect clear | 🔉 синтез |
| `win` / `lose` / `draw` | исход партии | 🔉 синтез |
| `tick` | блиц-таймер в danger-зоне | 🔉 синтез |
| `click` | клик по UI | 🔉 синтез |
| `clear_N` | очистка N линий (по числу) | 🔉 синтез |

### Музыка (`tracks`, по теме)

| Трек | Тема | Статус |
|------|------|--------|
| synthwave-ambient | `neutral` | 🔉 синтез |
| «леденцовый» | `candy` | 🔉 синтез |
| тёмный ambient | `night` | 🔉 синтез |

**Если переходим на записанный звук:**
- SFX: короткие **WAV/OGG**, моно, 44.1 кГц, ≤ 1 с; `assets/audio/sfx/<name>.ogg`.
- Музыка: зацикливаемые **OGG/MP3**, ~60–120 с луп; `assets/audio/music/<theme>.ogg`.
- ⚠️ Не забыть объявить каталоги в `pubspec.yaml` (`flutter: assets:`) — сейчас
  там объявлены только шрифты.

---

## 7. Шрифты (есть)

| Family | Файл | Назначение | Статус |
|--------|------|-----------|--------|
| BricolageGrotesque | `assets/fonts/BricolageGrotesque.ttf` | дисплейный (заголовки) | ✅ |
| Fredoka | `assets/fonts/Fredoka.ttf` | дисплейный (candy) | ✅ |
| Baloo2 | `assets/fonts/Baloo2.ttf` | дисплейный | ✅ |
| Oswald | `assets/fonts/Oswald.ttf` | дисплейный/акцент | ✅ |
| DMMono | `assets/fonts/DMMono-Regular.ttf` | моно (счёт/таймер) | ✅ |
| ShareTechMono | `assets/fonts/ShareTechMono-Regular.ttf` | моно (кибер) | ✅ |

Лицензия OFL, family-имена должны 1:1 совпадать с `fontDisplay`/`fontMono` в
`BlockDuelTheme`.

---

## Соглашения

- **Каталог:** новый арт класть под `assets/<group>/…`; каждый новый каталог
  **обязательно** объявлять в `pubspec.yaml` → `flutter: assets:`.
- **Именование:** `snake_case`, без пробелов; вариант темы — суффиксом
  (`_neutral`/`_candy`/`_night`), плотность PNG — `@2x`/`@3x`.
- **Формат по умолчанию:** SVG для иконок/масштабируемого; WebP/PNG для растра;
  OGG для звука.
- **Темы:** любой «по-теме» ассет = 3 варианта.
- **Детерминизм/паритет:** ассеты — чистый View-слой; не влияют на ядро
  `lib/core/` и golden-паритет с TS.

## Приоритеты производства

1. **Брендинг релиза:** мастер-иконка 1024² → иконки всех платформ + splash.
2. ~~**Иконки power-ups (6) и режимов меню**~~ ✅ переведены на Material-иконки
   (`game_icons.dart`, 2026-06-18); художественный арт-набор — опционально позже.
3. **Скины блоков (6 стилей)** — монетизация магазина.
4. **Маскоты** в SVG/арте (3 темы + позы).
5. **Записанный звук** (если синтез решат заменить) — SFX, затем музыка.
6. **Фоны тем** и **бейджи достижений** — по остаточному принципу.
