# week22.py — Неделя 22 «Сервер и WebSocket» (дни 146–152). Спутница — лама Лу.
# Идея недели: сервер живёт в интернете и передаёт сообщения; WebSocket —
# провод в обе стороны; JSON — конверт; эхо-сервер на TypeScript (server/).
from . import M, W

WEEK = dict(
    num=22, folder="week22_server", companion="Лама Лу", badge="llama", title="Сервер и WebSocket",
    lead="Игра работает на одном компьютере. Чтобы играть с другом через океан, нужен посредник в интернете — сервер. "
         "Лу протягивает первую нить.",
    days=[
        dict(day=146, theme="что такое сервер", h3="Домик в облаках",
             seen="дружелюбный домик-сервер в облаках интернета",
             story=[
                 "Лу показала Марьям на небо: там, в облаках, стоял маленький домик с антенной. «Это сервер, — сказала она. — Компьютер, который никогда не спит и ждёт, когда к нему постучатся. Ты стучишь — он отвечает. Твой друг стучит — он и ему отвечает, и может передать привет от тебя».",
                 f"«У нашей игры есть такой домик, — добавила Лу. — Он написан не на Dart, а на TypeScript — это другой язык, но идеи те же: переменные, функции, классы. Живёт в папке {M('server/')} и ждёт игроков по адресу {M('pvp.alshfu.com')}». Марьям удивилась, что вторая половина игры говорит на другом языке — и что она поймёт его.",
                 f"Сегодня откройте {M('server/index.ts')} и просто прочитайте первые строки: как сервер создаётся и на каком порту слушает. Найдите знакомое: {M('const')} — как {M('final')}, {M('function')} — как функция Лу, {M('=&gt;')} — стрелка. Языки разные, идеи одни.",
             ],
             steps=[f"Открой {M('server/index.ts')}.",
                    "Найди, где сервер создаётся и какой порт слушает.",
                    "Выпиши три знакомых слова из другого языка."],
             real=(f"«Нить к другу» на самом деле — {W('WebSocket')}: постоянное двустороннее "
                   f"соединение по сети (поверх протокола TCP). Обычная веб-страница работает иначе: "
                   f"спросила сервер один раз — получила ответ — и «забыла». WebSocket же держит провод "
                   f"открытым в обе стороны, поэтому сервер может <b>сам</b> прислать тебе сообщение в ту "
                   f"секунду, когда соперник сделал ход, — ждать и переспрашивать не нужно. А «конверт», в "
                   f"котором едут данные (какой ход, чей, сколько очков), — это {W('JSON')}: текстовый формат, "
                   f"понятный и Dart-клиенту, и TypeScript-серверу. Домик в облаке — красивая картинка, но "
                   f"под ней настоящий инженерный механизм, и ты уже знаешь его имя.")),
        dict(day=147, theme="провод в обе стороны", h3="WebSocket — провод, а не письмо",
             seen="светящийся провод несёт «привет» в обе стороны",
             story=[
                 "Лу протянула от домика к Марьям светящуюся нить и сказала в неё «привет». Нить донесла слово до домика, а домик тут же ответил «привет» обратно — по той же нити, без задержки. «Это WebSocket, — сказала Лу. — Не письмо, которое надо каждый раз отправлять, а провод, который остаётся включённым».",
                 f"«Обычный сайт работает как письма: попросил — получил — связь закрылась, — объяснила она. — А игре нужно, чтобы ход соперника прилетел сразу, без просьбы. Поэтому {W('WebSocket')}: открыли провод, и оба конца шлют друг другу, когда хотят».",
                 f"Сегодня найдите в {M('server/index.ts')} слово {M('ws')} — библиотеку WebSocket — и место, где сервер говорит «кто-то подключился» ({M('connection')}). Потом найдите, где он слушает сообщения ({M('message')}). Это уши сервера — как {M('onTap')} у виджета.",
             ],
             steps=[f"Найди {M('ws')} и обработчик {M('connection')}.",
                    f"Найди обработчик {M('message')}.",
                    "Чем провод отличается от письма? Скажи вслух."]),
        dict(day=148, theme="эхо-сервер", h3="Сервер, который повторяет",
             seen="крошечный эхо-сервер отбивает «привет» обратно, уютно",
             story=[
                 f"Марьям написала свой первый сервер — крошечный, на десять строк: создать, слушать порт, при подключении — ждать сообщение, при сообщении — отправить его же обратно. Запустила через {M('npx tsx')} — и домик ожил на её компьютере. Лу постучалась и получила своё «привет» назад.",
                 "«Эхо-сервер — «здравствуй, мир» серверного мира, — сказала Лу. — Он ничего не умеет, кроме как повторять, но в нём уже есть всё: подключение, приём, отправка». Марьям заметила, что сервер — это тоже цикл: ждать, принять, ответить, ждать.",
                 f"Сегодня напишите {M('course/week22_server/day148.ts')}: {M('new WebSocketServer({ port: 1999 })')}, {M('on(&#39;connection&#39;)')}, внутри {M('on(&#39;message&#39;, (m) =&gt; ws.send(m))')}. Запустите и проверьте любым WebSocket-клиентом (папа подскажет, например из браузера). Если ответ вернулся — ваш первый сервер работает.",
             ],
             steps=["Эхо-сервер на ws: connection → message → send обратно.",
                    f"Запусти: {M('npx tsx course/week22_server/day148.ts')}.",
                    "Отправь «привет» — вернулось?"]),
        dict(day=149, theme="конверт JSON", h3="Письмо в конверте JSON",
             seen="лама читает конверт с надписью JSON",
             story=[
                 "Лу показала конверт с надписью JSON. Внутри было не просто слово, а аккуратная запись: тип сообщения, имя, число. «Сервер и игра говорят конвертами, — сказала она. — Снаружи текст, внутри — поля, как у класса. Отправитель складывает, получатель разворачивает».",
                 f"«{M('JSON.stringify')} складывает объект в текст, {M('JSON.parse')} разворачивает обратно, — объяснила Лу. — И обязательно проверяй, что внутри: провод может принести мусор». Марьям вспомнила, что на сервере игры есть проверка «а объект ли это вообще» — после одного неприятного аудита.",
                 f"Сегодня научите эхо-сервер разворачивать конверт: {M('JSON.parse(m)')}, проверить, что это объект и есть поле {M('type')}, и отвечать конвертом {M('{ type: &#39;echo&#39;, got: ... }')}. Пришлите мусор вместо JSON — сервер не должен упасть. Оберните разбор в {M('try/catch')}.",
             ],
             steps=["Разбирай JSON.parse в try/catch.",
                    "Проверь: объект и есть поле type.",
                    "Пришли мусор — сервер жив?"]),
        dict(day=150, theme="сообщение «ход»", h3="Ход летит по проводу",
             seen="сообщение с ходом летит по светящемуся проводу",
             story=[
                 f"Марьям придумала конверт для хода: {M('{ type: &#39;move&#39;, piece: &#39;T&#39;, rot: 0, r: 4, c: 4 }')}. Отправила серверу — сервер развернул, проверил поля и ответил {M('{ type: &#39;ok&#39; }')}. Отправила без поля {M('r')} — ответил {M('{ type: &#39;error&#39; }')}. Лу довольно кивнула: сервер не верит на слово.",
                 f"«В настоящем сервере ход проверяется по-настоящему, — сказала Лу. — Он держит свою доску и зовёт тот же закон {M('canPlace')} — из TS-ядра, которое совпадает с Dart бит-в-бит. Игрок не может прислать невозможный ход». Марьям вспомнила два ядра и golden-тесты — вот зачем они.",
                 f"Сегодня добавьте в сервер обработку {M('type: &#39;move&#39;')}: проверьте, что {M('r')} и {M('c')} — числа от 0 до 8, ответьте {M('ok')} или {M('error')}. Потом найдите в {M('server/room.ts')}, как настоящий сервер проверяет ход — там зовётся ядро из {M('legacy-ts/core')}. Судья, который не проверяет, — не судья.",
             ],
             steps=["Обработай type: 'move' — проверь r и c 0…8.",
                    "Ответь ok / error.",
                    f"Найди в {M('room.ts')} проверку хода ядром."]),
        dict(day=151, theme="сервер не падает", h3="Крепость, которая не падает",
             seen="домик-сервер стоит крепко, вокруг шторм из странных сообщений",
             story=[
                 "Котики решили проверить сервер Марьям на прочность: слали пустые строки, огромные строки, JSON без полей, JSON с полями не того типа, сто сообщений подряд. Первая версия упала на втором сообщении. Марьям чинила, котики слали снова — и к вечеру сервер стоял, как крепость.",
                 "«Сервер — общий для всех, — сказала Лу. — Если он упал из-за одного шутника, партии всех игроков прерываются. Поэтому сервер проверяет всё и никогда не доверяет проводу». Марьям вспомнила бонусную главу про серверные дыры — именно так их и закрывали.",
                 f"Сегодня устройте своему серверу шторм: мусор, пустоту, гигантские строки, неправильные типы. Каждый раз, когда падает, — {M('try/catch')} и проверка. Потом прочитайте бонусную главу «Крепость-сервер» и найдите там те же приёмы: проверка типа, лимит размера, защита от исключений.",
             ],
             steps=["Шли серверу мусор пяти видов.",
                    "Чини каждое падение проверкой.",
                    "Прочитай главу «Крепость-сервер» — найди свои приёмы."]),
        dict(day=152, theme="мини-проект недели", h3="🎯 Крошечный эхо-сервер",
             seen="уютный эхо-сервер на WebSocket отбивает привет",
             story=[
                 "К концу недели у Марьям был свой сервер: слушал порт, принимал конверты JSON, проверял их, отвечал на «привет» и на «ход», не падал от мусора. Лу подключилась с одного конца, По — с другого, и оба получили свои ответы. Домик Марьям стоял в её собственных облаках.",
                 "«Неделю сервера закрыли, — сказала Лу. — Сервер — это ухо, проверка и ответ. На следующей неделе он научится главному: передавать ход от одного игрока другому». Марьям подумала, что серверная половина оказалась не страшнее клиентской — те же коробочки и развилки.",
                 f"Мини-проект недели: эхо-сервер на WebSocket с JSON-конвертами, обработкой {M('hello')} и {M('move')}, проверкой полей и {M('try/catch')}. Проверьте двумя подключениями. Сравните с {M('server/index.ts')}: найдите там heartbeat — «ты живой?» — и подумайте, зачем он.",
             ],
             steps=["Эхо-сервер: JSON, hello, move, проверки, try/catch.",
                    "Два подключения одновременно.",
                    f"Найди heartbeat в {M('server/index.ts')} — зачем?"]),
    ])
