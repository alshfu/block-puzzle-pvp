.DS_Store
/tmp
/out-tsc
/dist
/coverage
*.tsbuildinfo

/node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
/.pnp
.pnp.js

.vscode/*
.idea/
.claude/

# Vite — локальные env переопределения (не для коммита)
.env.local
.env.*.local

# Lighthouse / отладочные отчёты — лежат локально, не для коммита
test-raport.json
test-report.json
lighthouse-*.json
*.lighthouse.json

# E2E скриншоты пилотного скрипта (генерируются локально, тяжёлые)
tools/screens/
tools/debug-page.ts

# Рантайм-данные локального PvP-сервера (лидерборд из server:dev / E2E-прогонов)
/data/

# Flutter — сборочные артефакты вне flutter/ (на случай запуска из корня)
/build/
firebase-debug.log
firebase-debug.*.log
# Firebase эмулятор (логи/экспорт/кэш) — генерятся при `firebase emulators:*`
firestore-debug.log
ui-debug.log
database-debug.log
pubsub-debug.log
.firebase/
firebase-export-*/
# Нативные Firebase-конфиги (содержат ключи приложения) — не коммитим
**/GoogleService-Info.plist
**/google-services.json

# Flutter/Dart (проект в корне после реструктуризации)
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
**/doc/api/
**/ios/Flutter/.last_build_id
*.iml
*.ipr
*.iws
.swiftpm/
migrate_working_dir/
app.*.symbols
app.*.map.json
/android/app/debug
/android/app/profile
/android/app/release
